---
phase: 12-design-polish-media
plan: 02
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/components/landing/Hero.tsx
  - src/pages/LandingPage.tsx
  - src/components/common/CTAButton.tsx
  - src/index.css
autonomous: true
requirements:
  - REQ-HERO-3D
  - REQ-SCROLL-MICRO

must_haves:
  truths:
    - 'Hero renders SplineHero on desktop when Spline scene URL is configured'
    - 'Hero falls back to OrbitVisual when Spline is loading or on mobile'
    - 'Homepage below-fold sections animate in on scroll via ScrollReveal'
    - 'Service cards on homepage have subtle tilt effect on hover (desktop only)'
    - 'CTAButton arrow icon shifts right on hover'
  artifacts:
    - path: 'src/components/landing/Hero.tsx'
      provides: 'Hero with SplineHero integration and OrbitVisual fallback'
      contains: 'SplineHero'
    - path: 'src/components/common/CTAButton.tsx'
      provides: 'CTAButton with arrow hover micro-animation'
      contains: 'whileHover'
  key_links:
    - from: 'src/components/landing/Hero.tsx'
      to: 'src/components/common/SplineHero.tsx'
      via: 'conditional render'
      pattern: 'SplineHero|OrbitVisual'
    - from: 'src/pages/LandingPage.tsx'
      to: 'src/components/common/ScrollReveal.tsx'
      via: 'wrapper around below-fold sections'
      pattern: 'ScrollReveal'
---

<objective>
Wire SplineHero into the homepage hero (replacing OrbitVisual on desktop when a Spline scene URL is provided), add ScrollReveal to homepage below-fold sections, apply card tilt to service cards, and add arrow hover micro-animation to CTAButton.

Purpose: The homepage is the primary impression — this plan makes it feel premium with interactive 3D, scroll reveals, and micro-interactions.
Output: Updated Hero.tsx, LandingPage.tsx, CTAButton.tsx, index.css.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-design-polish-media/12-RESEARCH.md
@.planning/phases/12-design-polish-media/12-01-SUMMARY.md

@src/components/landing/Hero.tsx
@src/pages/LandingPage.tsx
@src/components/common/CTAButton.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SplineHero into Hero.tsx with OrbitVisual fallback + service card tilt</name>
  <files>src/components/landing/Hero.tsx</files>
  <action>
1. Import SplineHero from '../common/SplineHero'
2. Import useTilt from '../../hooks/useTilt'

3. **Spline integration in hero right column:**
   - Add a constant at top of file: `const SPLINE_SCENE_URL = ''` with comment "// Set to Spline scene URL when 3D asset is ready. Empty string = OrbitVisual fallback."
   - In the hero right-side column (where OrbitVisual currently renders):
     - If `SPLINE_SCENE_URL` is non-empty, render `<SplineHero sceneUrl={SPLINE_SCENE_URL} className="w-full h-full" />`
     - Otherwise keep existing `<OrbitVisual />` as-is
   - Both are wrapped in the existing `hidden lg:block` container (desktop-only per CLAUDE.md)
   - This means OrbitVisual stays active until a Spline scene is designed and the URL is populated

4. **Card tilt on service cards:**
   - The hero has 2x2 service cards (01-04). Each card is currently a Link with card-gradient-border.
   - Add a `useIsMobile` check (import from existing hooks/useIsMobile or use `window.matchMedia('(hover: hover) and (pointer: fine)')`)
   - For each service card, if NOT mobile: attach the useTilt hook's ref, style, handleMouseMove, handleMouseLeave
   - Since there are 4 cards and useTilt returns one ref, create 4 separate useTilt instances OR use a single shared approach:
     - Simplest: Don't use useTilt hook (which holds state per instance). Instead, add inline onMouseMove/onMouseLeave handlers with a lightweight CSS-only approach:
     - Add a CSS class `card-tilt` to each service card
     - In index.css, add: `.card-tilt { transition: transform 0.15s ease-out; } .card-tilt:hover { transform: perspective(800px) rotateX(2deg) rotateY(2deg); }` (subtle fixed tilt, not mouse-tracking, to avoid 4 hook instances)
   - Actually, the useTilt hook is per-card. For 4 cards, this creates 4 state objects which is fine in React. But to keep the hero clean, prefer the CSS-only subtle tilt approach and save useTilt for service PAGES where there are fewer cards.
   - Decision: Use CSS `card-tilt` class on hero service cards for subtle hover depth. Save useTilt for service page cards in Plan 03.

5. Keep all existing content, i18n, animations, and layout structure intact.
   </action>
   <verify>
   <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit src/components/landing/Hero.tsx 2>&1 | head -10</automated>
   </verify>
   <done>Hero.tsx imports SplineHero with conditional render (empty URL = OrbitVisual fallback), service cards have card-tilt class</done>
   </task>

<task type="auto">
  <name>Task 2: Add ScrollReveal to homepage sections + CTAButton arrow animation + CSS tilt class</name>
  <files>src/pages/LandingPage.tsx, src/components/common/CTAButton.tsx, src/index.css</files>
  <action>
1. **LandingPage.tsx — ScrollReveal wrappers:**
   - Import ScrollReveal from '../components/common/ScrollReveal'
   - Wrap each below-fold section in a ScrollReveal:
     - VisionTimeline section (if still in LandingPage — check; it may have moved to /marketing-machine in Phase 10. If not present, skip)
     - The service cards grid section — wrap the outer container in `<ScrollReveal direction="up" delay={0}>`
     - Any remaining sections below the hero fold
   - Do NOT wrap the Hero itself (it has CSS fadeIn already and is above fold)
   - Do NOT wrap each individual card — wrap at section level to avoid "popcorn" effect (per research anti-pattern)
   - Stagger: if multiple sections, use delay={0}, delay={0.1}, delay={0.2}

2. **CTAButton.tsx — Arrow hover micro-animation:**
   - Import motion from 'framer-motion'
   - The existing ArrowRight icon is rendered when `arrow` prop is true
   - Wrap the ArrowRight icon in a `<motion.span>` with: `whileHover={{ x: 4 }}` and `transition={{ type: 'spring', stiffness: 400, damping: 20 }}`
   - BUT: whileHover on a child span won't fire (hover is on parent button). Instead:
     - Add a `group` class to the button element
     - Use Tailwind `group-hover:translate-x-1` on the ArrowRight wrapper span (simpler than Framer Motion for this)
     - Add `transition-transform duration-200` to the icon wrapper
   - This avoids importing motion into CTAButton (which is used everywhere) and keeps it lightweight

3. **index.css — card-tilt class:**
   - Add after the existing card-gradient-border block:
   ```css
   /* Card tilt — subtle depth on hover (desktop only) */
   @media (hover: hover) and (pointer: fine) {
     .card-tilt {
       transition: transform 0.2s ease-out;
     }
     .card-tilt:hover {
       transform: perspective(800px) rotateX(-1deg) rotateY(1deg) translateY(-2px);
     }
   }
   ```

   - The @media query ensures it only applies on desktop (pointer: fine), preventing touch device issues
     </action>
     <verify>
     <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit src/pages/LandingPage.tsx src/components/common/CTAButton.tsx 2>&1 | head -10</automated>
     </verify>
     <done>LandingPage sections wrapped in ScrollReveal, CTAButton arrow shifts on hover via Tailwind group-hover, card-tilt CSS class added with desktop-only media query</done>
     </task>

</tasks>

<verification>
- Hero.tsx conditionally renders SplineHero or OrbitVisual based on SPLINE_SCENE_URL constant
- LandingPage below-fold sections use ScrollReveal wrapper
- CTAButton arrow icon animates on hover (group-hover:translate-x-1)
- card-tilt CSS class exists with @media (hover: hover) guard
- `npm run build` completes without errors
</verification>

<success_criteria>

- Homepage hero ready for Spline scene (placeholder constant, OrbitVisual fallback active)
- Scroll reveal animations visible on homepage below-fold sections
- CTAButton arrow icon shifts smoothly on button hover
- Service cards have subtle tilt depth on desktop hover
  </success_criteria>

<output>
After completion, create `.planning/phases/12-design-polish-media/12-02-SUMMARY.md`
</output>
