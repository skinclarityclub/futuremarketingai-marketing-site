---
phase: 01-foundation-and-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/es.json
  - fmai-nextjs/src/i18n/routing.ts
  - fmai-nextjs/src/i18n/request.ts
  - fmai-nextjs/src/i18n/navigation.ts
  - fmai-nextjs/proxy.ts
autonomous: true
requirements:
  - SEO-03
  - INT-07

must_haves:
  truths:
    - "All 23 translation namespaces from the Vite site are accessible via useTranslations('namespace') in the Next.js app"
    - '/en/, /nl/, /es/ URL prefixes route correctly and load the right locale translations'
    - 'No translation keys are lost during the merge (all 3 locales have identical key structures)'
    - 'next-intl Link component preserves locale prefix in navigation'
    - 'i18next-specific interpolation syntax (double braces) is converted to next-intl format (single braces)'
  artifacts:
    - path: 'fmai-nextjs/messages/en.json'
      provides: 'All English translations merged from 23 namespace files'
      min_lines: 500
    - path: 'fmai-nextjs/messages/nl.json'
      provides: 'All Dutch translations merged from 23 namespace files'
      min_lines: 500
    - path: 'fmai-nextjs/messages/es.json'
      provides: 'All Spanish translations merged from 23 namespace files'
      min_lines: 500
    - path: 'fmai-nextjs/src/i18n/routing.ts'
      provides: 'next-intl routing config with 3 locales'
      contains: 'defineRouting'
    - path: 'fmai-nextjs/src/i18n/request.ts'
      provides: 'next-intl request config for server-side message loading'
      contains: 'getRequestConfig'
    - path: 'fmai-nextjs/src/i18n/navigation.ts'
      provides: 'Locale-aware Link, redirect, usePathname, useRouter'
      exports: ['Link', 'redirect', 'usePathname', 'useRouter']
    - path: 'fmai-nextjs/proxy.ts'
      provides: 'next-intl middleware for locale detection and routing'
      contains: 'createMiddleware'
  key_links:
    - from: 'fmai-nextjs/proxy.ts'
      to: 'fmai-nextjs/src/i18n/routing.ts'
      via: 'imports routing config for middleware'
      pattern: 'import.*routing.*from'
    - from: 'fmai-nextjs/src/i18n/request.ts'
      to: 'fmai-nextjs/messages/{locale}.json'
      via: 'dynamic import of locale messages'
      pattern: "import.*messages.*\\.json"
    - from: 'fmai-nextjs/src/app/[locale]/layout.tsx'
      to: 'next-intl'
      via: 'NextIntlClientProvider wrapping children'
      pattern: 'NextIntlClientProvider'
---

<objective>
Migrate all 23 translation namespace files (x3 locales = 69 files) from i18next format to next-intl merged JSON, configure next-intl routing with locale-prefixed URLs, and set up locale-aware navigation helpers. This replaces the entire i18next + react-i18next + browser-languagedetector stack.

Purpose: Enable all future pages to use server-side and client-side translations via next-intl. Every page needs translations -- this is a critical dependency.
Output: 3 merged translation files (en.json, nl.json, es.json), complete i18n routing config, and locale-aware navigation exports.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md

Source files for translation migration:
@public/locales/en/ (23 namespace JSON files)
@public/locales/nl/ (23 namespace JSON files)
@public/locales/es/ (23 namespace JSON files)
@src/i18n/config.ts (existing i18next configuration)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge 69 translation files into 3 locale JSONs with syntax conversion</name>
  <files>
    fmai-nextjs/messages/en.json
    fmai-nextjs/messages/nl.json
    fmai-nextjs/messages/es.json
  </files>
  <action>
    Write and execute a Node.js migration script that:

    1. Reads all 23 namespace files from C:\Users\daley\Desktop\Futuremarketingai\public\locales\{en,nl,es}\*.json
    2. For each locale, creates a merged JSON where each namespace filename (without .json) becomes a top-level key:
       ```json
       {
         "about": { ...contents of about.json },
         "adbuilder": { ...contents of adbuilder.json },
         "analytics": { ...contents of analytics.json },
         "automations": { ...contents of automations.json },
         "calculator": { ...contents of calculator.json },
         "calendly": { ...contents of calendly.json },
         "chatbots": { ...contents of chatbots.json },
         "common": { ...contents of common.json },
         "contact": { ...contents of contact.json },
         "dashboard": { ...contents of dashboard.json },
         "errors": { ...contents of errors.json },
         "explorer": { ...contents of explorer.json },
         "features": { ...contents of features.json },
         "forms": { ...contents of forms.json },
         "hero": { ...contents of hero.json },
         "how-it-works": { ...contents of how-it-works.json },
         "login": { ...contents of login.json },
         "navigation": { ...contents of navigation.json },
         "pricing": { ...contents of pricing.json },
         "pricing_comparison": { ...contents of pricing_comparison.json },
         "profiling": { ...contents of profiling.json },
         "seo": { ...contents of seo.json },
         "voice-agents": { ...contents of voice-agents.json }
       }
       ```

    3. Scans ALL values for i18next-specific interpolation syntax and converts:
       - `{{variable}}` (double braces) -> `{variable}` (single braces for next-intl ICU format)
       - `$t(key)` references -> leave as-is with a warning (may need manual review)
       - `{{count}}` pluralization -> `{count}` (next-intl uses ICU plurals)

    4. Validates:
       - All 3 locales have identical key structures (same namespace names, same nested key paths)
       - No key collisions (namespace names are unique -- they should be since they're filenames)
       - Total key count per locale matches

    5. Writes merged JSON to C:\Users\daley\Desktop\fmai-nextjs\messages\{en,nl,es}.json with 2-space indentation

    6. Prints a migration report:
       - Files processed per locale
       - Total keys per locale
       - Syntax conversions made (count of {{}} -> {} replacements)
       - Any warnings ($t() references, missing files, key mismatches)

    Run the script from the Futuremarketingai directory. Delete the script after successful execution.

    IMPORTANT: next-intl uses hyphens in namespace access -- `useTranslations('how-it-works')` works fine. Do NOT rename namespace keys.

  </action>
  <verify>
    Verify merged files exist and have correct structure:
    - `node -e "const j=require('./messages/en.json'); console.log(Object.keys(j).length, 'namespaces')"` outputs "23 namespaces"
    - Same check for nl.json and es.json
    - `node -e "const en=require('./messages/en.json'); const nl=require('./messages/nl.json'); console.log(JSON.stringify(Object.keys(en))===JSON.stringify(Object.keys(nl)) ? 'MATCH' : 'MISMATCH')"` outputs "MATCH"
    - Spot-check: `node -e "const j=require('./messages/en.json'); console.log(j.common.cta)"` shows CTA translations
    - Grep for remaining `{{` in all 3 files -- should find zero occurrences
  </verify>
  <done>3 merged translation files exist with all 23 namespaces each. Key structures match across locales. No i18next-specific syntax remains.</done>
</task>

<task type="auto">
  <name>Task 2: Configure next-intl routing, navigation, and verify locale routing</name>
  <files>
    fmai-nextjs/src/i18n/routing.ts
    fmai-nextjs/src/i18n/request.ts
    fmai-nextjs/src/i18n/navigation.ts
    fmai-nextjs/proxy.ts
    fmai-nextjs/src/app/[locale]/page.tsx
  </files>
  <action>
    The i18n stub files from Plan 01 need to be finalized (routing.ts and request.ts may already be correct -- verify and update if needed).

    **1. Finalize src/i18n/routing.ts** (may already exist from Plan 01 stub):
    ```typescript
    import { defineRouting } from 'next-intl/routing';
    export const routing = defineRouting({
      locales: ['en', 'nl', 'es'],
      defaultLocale: 'en',
      localePrefix: 'always',
    });
    ```

    **2. Create src/i18n/navigation.ts:**
    ```typescript
    import { createNavigation } from 'next-intl/navigation';
    import { routing } from './routing';
    export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
    ```

    **3. Finalize src/i18n/request.ts** (may already exist from Plan 01 stub):
    Ensure it imports from the correct relative path to messages/ directory and handles locale validation.

    **4. Finalize proxy.ts** (may already exist from Plan 01 stub):
    Ensure it uses createMiddleware from next-intl/middleware with the routing config. Matcher should exclude api, _next, _vercel, and static files.

    **5. Update homepage to demonstrate full translation usage:**

    Update src/app/[locale]/page.tsx to:
    - Import and use translations from multiple namespaces (hero + common at minimum)
    - Show both server-rendered translated content and demonstrate that the translation system works end-to-end
    - Use generateMetadata with translated meta title/description from hero namespace
    - Include a link using the locale-aware Link from @/i18n/navigation to verify navigation preserves locale prefix

    **6. Verify locale switching works:**
    - Visiting /en/ shows English content
    - Visiting /nl/ shows Dutch content
    - Visiting /es/ shows Spanish content
    - Visiting / (no locale) redirects to /en/
    - Visiting /de/ (unsupported locale) returns 404 or redirects to /en/

  </action>
  <verify>
    Run `cd C:\Users\daley\Desktop\fmai-nextjs && npm run dev` and test:
    1. http://localhost:3000/ redirects to /en/
    2. http://localhost:3000/en/ shows English translations
    3. http://localhost:3000/nl/ shows Dutch translations
    4. http://localhost:3000/es/ shows Spanish translations
    5. View source of /en/ contains translated text in HTML (SSR confirmed)
    6. `npm run build` completes without errors -- all 3 locales generate static pages
  </verify>
  <done>next-intl routing is fully configured. All 3 locales route correctly with locale-prefixed URLs. Translations load server-side and render in HTML source. Navigation helpers preserve locale prefix. The full i18next-to-next-intl migration is complete.</done>
</task>

</tasks>

<verification>
1. All 69 translation files merged into 3 locale JSONs with zero key loss
2. No i18next-specific syntax (double braces) remains in merged files
3. /en/, /nl/, /es/ all render with correct locale-specific translations
4. / redirects to /en/ (default locale)
5. View-source shows translated content in HTML (server-rendered, not client-hydrated)
6. `npm run build` generates static pages for all 3 locales
7. Locale-aware Link preserves prefix when navigating
</verification>

<success_criteria>
All 23 translation namespaces are accessible via useTranslations() in the Next.js app. Three locales route correctly via URL prefix. Translations render server-side in HTML source. The entire i18next stack is replaced by next-intl with zero translation loss.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-02-SUMMARY.md`
</output>
