---
phase: 09-living-system-page-conversion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/SocialProof.tsx
  - src/components/landing/FeaturesSection.tsx
  - src/components/landing/FeatureShowcase.tsx
autonomous: true
requirements:
  - REQ-PAGE-CONVERSION
must_haves:
  truths:
    - 'SocialProof section uses SystemPanel for testimonial cards and teal/amber accents'
    - 'FeaturesSection uses Living System tokens for all feature cards and icons'
    - 'FeatureShowcase displays features with accent-system icons and bg-bg-surface cards'
    - 'No indigo/violet/blue/purple glassmorphism classes remain in any of the 3 files'
  artifacts:
    - path: src/components/landing/SocialProof.tsx
      provides: 'Social proof section with Living System cards'
      contains: 'accent-system'
    - path: src/components/landing/FeaturesSection.tsx
      provides: 'Features section with Living System tokens'
      contains: 'bg-bg-deep'
    - path: src/components/landing/FeatureShowcase.tsx
      provides: 'Feature showcase with teal/amber accents'
      contains: 'accent-system'
  key_links:
    - from: src/components/landing/SocialProof.tsx
      to: src/components/common/SystemPanel.tsx
      via: 'SystemPanel import for testimonial cards'
      pattern: 'SystemPanel'
---

<objective>
Convert the homepage content sections (SocialProof, FeaturesSection, FeatureShowcase) from glassmorphism to Living System tokens.

Purpose: These sections make up the body of the landing page below the hero. They contain feature cards, testimonials, and showcases that currently use old blue/purple glass cards.
Output: 3 converted landing section components using Living System tokens and shared components.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-living-system-page-conversion/09-RESEARCH.md

@src/components/common/SystemPanel.tsx
@src/components/common/SectionContainer.tsx
@src/components/common/StatusIndicator.tsx
@src/components/common/MetricDisplay.tsx

<interfaces>
From src/components/common/SystemPanel.tsx:
```tsx
interface SystemPanelProps {
  children: React.ReactNode;
  className?: string;
  hoverAccent?: boolean;
  as?: React.ElementType;
  onClick?: () => void;
}
// Base: bg-bg-surface border border-border-primary rounded-sm
export const SystemPanel = React.forwardRef<HTMLDivElement, SystemPanelProps>(...)
```

From src/components/common/SectionContainer.tsx:

```tsx
// Wraps content with consistent max-width and padding
// Props: maxWidth, padding, className, children
```

From src/components/common/MetricDisplay.tsx:

```tsx
// Large monospace numbers with labels — use for stat counters
// Props: value, label, accent
```

From src/components/common/StatusIndicator.tsx:

```tsx
// Pulsing status dots — use for live/active indicators
// Props: variant, pulse, label
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert SocialProof to Living System tokens</name>
  <files>src/components/landing/SocialProof.tsx</files>
  <action>
**SocialProof.tsx (~315 lines):**

This component likely contains testimonial cards, client logos, and social proof metrics.

**Card conversion:**

- Replace `bg-white/5 backdrop-blur-sm border border-white/10 rounded-xl` card patterns with `<SystemPanel>` import from `'../common'`. Use `<SystemPanel className="p-6">` or `<SystemPanel hoverAccent className="p-6">` for interactive cards.
- If cards are wrapped in `motion.div` with animation props, keep `motion.div` and update className instead: `className="bg-bg-surface border border-border-primary rounded-sm p-6"`. Do NOT replace animated `motion.div` with SystemPanel unless animation is not needed.

**Color mapping:**

- `text-white` → `text-text-primary`
- `text-blue-100` / `text-slate-400` → `text-text-secondary`
- `text-blue-100/80` → `text-text-muted`
- `text-blue-400` / `text-cyan-400` → `text-accent-system`
- `text-purple-400` → `text-accent-human`
- `bg-blue-500/10` → `bg-accent-system/10`
- `border-blue-500/20` → `border-accent-system/20`
- Star/rating colors if using yellow → keep yellow (semantic color). If using blue → `text-accent-human` (amber)
- `rounded-xl` / `rounded-2xl` on cards → `rounded-sm`
- `rounded-full` on badges/pills → `rounded-sm`
- Remove all `backdrop-blur-*`

**Section wrapper:**

- If section has inline `max-w-6xl mx-auto px-6` padding, consider wrapping with `<SectionContainer>` from `'../common'`.
- Section background: any gradient → `bg-bg-deep`

**Metric numbers (if present):**

- If there are stat counters (e.g., "500+ clients", "99% satisfaction"), consider using `<MetricDisplay>` from `'../common'` for large numbers.

**CRITICAL:** Preserve all Framer Motion animation props. Only change className values and component wrappers.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep -cE "(from-indigo|bg-indigo|text-indigo|from-violet|bg-violet|text-violet|from-purple|bg-purple|text-purple|from-blue|bg-blue|text-blue|border-blue|text-cyan|from-cyan|text-slate|bg-slate|border-white|backdrop-blur)" src/components/landing/SocialProof.tsx</automated>
</verify>
<done>SocialProof.tsx has zero old palette classes. Cards use SystemPanel or Living System bg-bg-surface tokens. All text uses text-text-primary/secondary/muted hierarchy. Icons use text-accent-system. Animations preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Convert FeaturesSection and FeatureShowcase to Living System tokens</name>
  <files>src/components/landing/FeaturesSection.tsx, src/components/landing/FeatureShowcase.tsx</files>
  <action>
**FeaturesSection.tsx (~209 lines):**

Feature grid/list component with icon + title + description cards.

- Section wrapper gradient → `bg-bg-deep`
- Feature cards: `bg-white/5 backdrop-blur border-white/10 rounded-xl` → `<SystemPanel hoverAccent className="p-6">` or `bg-bg-surface border border-border-primary rounded-sm`
- Icon colors: `text-blue-400` / `text-cyan-400` → `text-accent-system`
- Secondary icon colors (if using purple for variety): `text-purple-400` → `text-accent-human`
- Heading gradient text: `from-blue-400 to-purple-400 bg-clip-text text-transparent` → `bg-gradient-flow bg-clip-text text-transparent`
- Section headings: `text-white` → `text-text-primary`, add `font-display` for Space Grotesk
- Body text: `text-blue-100` → `text-text-secondary`, `text-blue-100/80` → `text-text-muted`
- `rounded-xl` → `rounded-sm` on all cards
- Remove `backdrop-blur-*`

**FeatureShowcase.tsx (~142 lines):**

Smaller component, likely a focused feature highlight or interactive demo area.

Same conversion patterns:

- Gradients → `bg-bg-deep` or `bg-bg-surface`
- Glass cards → SystemPanel or `bg-bg-surface border border-border-primary rounded-sm`
- All blue/indigo/violet/purple/cyan → Living System tokens
- `text-white` → `text-text-primary`
- Icon/accent colors → `text-accent-system`
- `rounded-xl/2xl` → `rounded-sm`
- Remove `backdrop-blur`

**CRITICAL:** Preserve all Framer Motion animations. Keep motion.div wrappers, only update className values.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep -cE "(from-indigo|bg-indigo|text-indigo|from-violet|bg-violet|text-violet|from-purple|bg-purple|text-purple|from-blue|bg-blue|text-blue|border-blue|text-cyan|from-cyan|text-slate|bg-slate|border-white|backdrop-blur)" src/components/landing/FeaturesSection.tsx src/components/landing/FeatureShowcase.tsx</automated>
</verify>
<done>FeaturesSection and FeatureShowcase have zero old palette classes. Feature cards use SystemPanel or Living System tokens. Icons are teal. Gradient text uses bg-gradient-flow. All animations preserved.</done>
</task>

</tasks>

<verification>
```bash
# Zero old classes in all 3 files
grep -cE "(from-indigo|bg-indigo|text-indigo|from-violet|bg-violet|text-violet|from-purple|bg-purple|text-purple|from-blue|bg-blue|text-blue|border-blue|text-cyan|from-cyan|text-slate|bg-slate|border-white|backdrop-blur)" src/components/landing/SocialProof.tsx src/components/landing/FeaturesSection.tsx src/components/landing/FeatureShowcase.tsx

# TypeScript compiles

npx tsc --noEmit

# Living System tokens present

grep -cE "(bg-bg-deep|bg-bg-surface|text-text-primary|text-accent-system|border-border-primary|SystemPanel|SectionContainer)" src/components/landing/SocialProof.tsx src/components/landing/FeaturesSection.tsx src/components/landing/FeatureShowcase.tsx

```
</verification>

<success_criteria>
- All 3 files compile without TypeScript errors
- Zero old palette class matches in all 3 files
- Living System tokens and/or shared components used throughout
- All Framer Motion animations preserved
</success_criteria>

<output>
After completion, create `.planning/phases/09-living-system-page-conversion/09-02-SUMMARY.md`
</output>
```
