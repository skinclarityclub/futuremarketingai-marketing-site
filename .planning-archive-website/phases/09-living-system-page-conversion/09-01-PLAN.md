---
phase: 09-living-system-page-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/SimpleHeader.tsx
  - src/components/landing/Hero.tsx
  - src/components/landing/SimplifiedHeroMobile.tsx
  - src/components/landing/Footer.tsx
autonomous: true
requirements:
  - REQ-PAGE-CONVERSION
must_haves:
  truths:
    - 'Header shows teal accent colors, sharp corners, no glassmorphism blur'
    - 'Hero section uses bg-bg-deep background, teal/amber accents, no indigo/violet/blue gradients'
    - 'Footer uses text-accent-system for icons and text-text-muted for links'
    - 'Mobile hero uses Living System tokens throughout'
  artifacts:
    - path: src/components/landing/SimpleHeader.tsx
      provides: 'Living System header with teal accents and rounded-sm'
      contains: 'bg-bg-surface'
    - path: src/components/landing/Hero.tsx
      provides: 'Living System hero with bg-bg-deep and gradient-flow'
      contains: 'bg-bg-deep'
    - path: src/components/landing/SimplifiedHeroMobile.tsx
      provides: 'Mobile hero with Living System tokens'
      contains: 'accent-system'
    - path: src/components/landing/Footer.tsx
      provides: 'Footer with teal accents and muted text'
      contains: 'text-accent-system'
  key_links:
    - from: src/components/landing/SimpleHeader.tsx
      to: src/components/common/CTAButton.tsx
      via: 'CTAButton import for header CTA'
      pattern: 'CTAButton'
---

<objective>
Convert the homepage header, hero, mobile hero, and footer from indigo/violet/blue glassmorphism to Living System teal/amber tokens.

Purpose: These are the highest-visibility components — every visitor sees them immediately. Converting them first ensures the brand transition is visible on first load.
Output: 4 converted landing components using Living System design tokens exclusively.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-living-system-page-conversion/09-RESEARCH.md

@prototype-2-living-system.html
@src/components/common/SystemPanel.tsx
@src/components/common/CTAButton.tsx
@src/components/common/SectionContainer.tsx

<interfaces>
<!-- Living System shared components (from Phase 3) -->

From src/components/common/SystemPanel.tsx:

```tsx
interface SystemPanelProps {
  children: React.ReactNode;
  className?: string;
  hoverAccent?: boolean;  // adds hover:border-accent-system/30 hover:bg-bg-elevated
  as?: React.ElementType;
  onClick?: () => void;
}
// Base classes: bg-bg-surface border border-border-primary rounded-sm transition-all duration-200
export const SystemPanel = React.forwardRef<HTMLDivElement, SystemPanelProps>(...)
```

From src/components/common/CTAButton.tsx:

```tsx
// Self-manages Calendly modal state internally
// Props: size="sm"|"md"|"lg", calendly, arrow, href, calendlyUrl, className, children
```

From src/components/common/SectionContainer.tsx:

```tsx
// Props: maxWidth="xl"|"2xl"|..., padding="sm"|"md"|"lg", className, children
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert SimpleHeader and Footer to Living System tokens</name>
  <files>src/components/landing/SimpleHeader.tsx, src/components/landing/Footer.tsx</files>
  <action>
**SimpleHeader.tsx (~587 lines):**

Replace ALL old color classes using this mapping (from 09-RESEARCH.md Pattern 7):

- `bg-slate-950/90 backdrop-blur-2xl` → `bg-bg-surface/95` (remove backdrop-blur entirely)
- `border-white/10` → `border-border-primary`
- `rounded-2xl` → `rounded-sm` (on all panel/container elements)
- `bg-gradient-to-r from-blue-400 via-cyan-400 to-blue-500 bg-clip-text text-transparent` (brand text) → `text-accent-system`
- `bg-gradient-to-r from-blue-500 to-cyan-500` (CTA button) → replace with `<CTAButton size="sm" calendly>` import from `'../common'`
- `text-blue-400` (active nav links) → `text-accent-system`
- `bg-blue-500/10` (active state bg) → `bg-accent-system/10`
- `text-slate-400` (inactive links) → `text-text-secondary`
- `hover:text-white` → `hover:text-text-primary`
- `bg-slate-900/95 backdrop-blur-xl` (dropdowns) → `bg-bg-elevated border border-border-primary` (remove backdrop-blur)
- `shadow-2xl shadow-black/40` → `shadow-glow-sm`
- `rounded-xl` on any remaining elements → `rounded-sm`
- Any `from-blue-*`, `to-cyan-*`, `text-cyan-*` → appropriate accent-system or text-text-\* token

If SimpleHeader has a hand-rolled CTA `<a>` or `<button>` linking to Calendly, replace with `<CTAButton size="sm" calendly>`. Import CTAButton from `'../common'`. Remove any local Calendly state or CalendlyModal JSX if present.

**Footer.tsx (~202 lines):**

Replace using 09-RESEARCH.md Footer pattern:

- `text-blue-400` (icon colors) → `text-accent-system`
- `text-slate-400` (link text) → `text-text-muted`
- `hover:text-white` → `hover:text-text-primary`
- `border-white/5` or `border-white/10` → `border-border-primary`
- Any `bg-gradient-*` page wrappers → `bg-bg-deep`
- `text-white` for headings → `text-text-primary`
- Any remaining indigo/blue/violet/purple/cyan references → appropriate Living System token

**CRITICAL rules:**

- Preserve ALL Framer Motion animation props (initial, animate, whileInView, transition, viewport). Only change className values.
- Do NOT add `rounded-xl`, `rounded-2xl`, or `rounded-full` on card/panel elements. Living System = `rounded-sm` only.
- Do NOT keep any `backdrop-blur` classes.
- Do NOT replace `rounded-full` on truly circular elements like avatar dots or status indicators — only on cards/panels/badges.
  </action>
  <verify>
  <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>SimpleHeader and Footer contain zero indigo/violet/blue/purple/cyan/slate color classes. All backgrounds use bg-bg-deep/bg-bg-surface/bg-bg-elevated. All text uses text-text-primary/secondary/muted or text-accent-system/human. All borders use border-border-primary. Sharp corners (rounded-sm) on all panels. No backdrop-blur.</done>
  </task>

<task type="auto">
  <name>Task 2: Convert Hero and SimplifiedHeroMobile to Living System tokens</name>
  <files>src/components/landing/Hero.tsx, src/components/landing/SimplifiedHeroMobile.tsx</files>
  <action>
**Hero.tsx (~553 lines):**

This is the largest and most visually complex homepage component. Replace ALL old colors:

- Page/section wrapper `bg-gradient-to-br from-slate-950 via-blue-950 to-slate-900` or similar → `bg-bg-deep`
- `from-indigo-*`, `bg-indigo-*`, `text-indigo-*` → `text-accent-system` or `bg-accent-system/10`
- Neural node/graph colors (likely `text-blue-400`, `bg-blue-500`, `stroke-blue-*`) → `text-accent-system`, `bg-accent-system`, `stroke-accent-system`
- Gradient text headings (`from-blue-400 to-purple-400 bg-clip-text text-transparent`) → `bg-gradient-flow bg-clip-text text-transparent`
- Body text `text-blue-100` / `text-blue-100/80` → `text-text-secondary` / `text-text-muted`
- `text-white` → `text-text-primary`
- Badge/pill elements: `rounded-full` → `rounded-sm`, color classes → `bg-accent-system/10 border-accent-system/20 text-accent-system`
- CTA buttons: replace any hand-rolled gradient buttons with `<CTAButton size="lg" calendly arrow>` or `<CTAButton href="/..." size="md" arrow>`. Remove local Calendly state + CalendlyModal JSX if switching to CTAButton.
- `bg-white/5 backdrop-blur` card patterns → use SystemPanel or `bg-bg-surface border border-border-primary rounded-sm`
- `shadow-blue-500/25` → `shadow-glow-sm`
- Any SVG/canvas/pseudo-element colors that use blue/indigo/purple → teal equivalents (`#00D4AA` or `rgba(0,212,170,*)`)

**SimplifiedHeroMobile.tsx (~213 lines):**

Same conversion pattern as Hero.tsx:

- All gradient backgrounds → `bg-bg-deep`
- All blue/indigo/violet text → `text-accent-system` or `text-text-secondary`
- All glass cards → SystemPanel or `bg-bg-surface border border-border-primary rounded-sm`
- CTA buttons → CTAButton component
- Gradient text → `bg-gradient-flow bg-clip-text text-transparent`

**CRITICAL:**

- Hero likely has complex Framer Motion animations (parallax, staggered reveals, neural background). Keep ALL motion.\* wrappers and animation props intact. Only swap className color/bg/border values.
- If Hero renders inline SVGs or canvas elements with hardcoded hex colors (#4F46E5 = indigo, #6366F1, #818CF8, #7C3AED = violet), replace with teal (#00D4AA) and amber (#F5A623) equivalents.
- Check for `style={{ ... }}` inline styles with old colors and update those too.
  </action>
  <verify>
  <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Hero and SimplifiedHeroMobile contain zero old palette classes. Background is bg-bg-deep. Accent colors are teal/amber. Gradient text uses bg-gradient-flow. All animations preserved. No glassmorphism patterns remain.</done>
  </task>

</tasks>

<verification>
Run these checks after both tasks:

```bash
# Zero old color classes in converted files
grep -cE "(from-indigo|bg-indigo|text-indigo|from-violet|bg-violet|text-violet|from-purple|bg-purple|text-purple|from-blue|bg-blue|text-blue|border-blue|text-cyan|from-cyan|text-slate|bg-slate|border-white|backdrop-blur)" src/components/landing/SimpleHeader.tsx src/components/landing/Hero.tsx src/components/landing/SimplifiedHeroMobile.tsx src/components/landing/Footer.tsx

# TypeScript compiles
npx tsc --noEmit

# Living System tokens present
grep -cE "(bg-bg-deep|bg-bg-surface|bg-bg-elevated|text-text-primary|text-text-secondary|text-text-muted|text-accent-system|text-accent-human|border-border-primary|accent-system|accent-human|gradient-flow)" src/components/landing/SimpleHeader.tsx src/components/landing/Hero.tsx src/components/landing/SimplifiedHeroMobile.tsx src/components/landing/Footer.tsx
```

</verification>

<success_criteria>

- All 4 files compile without TypeScript errors
- Zero matches for old palette classes (indigo, violet, purple, blue, cyan, slate) in converted files
- Living System tokens present in all 4 files
- All Framer Motion animations still intact (motion.\* wrappers preserved)
- Header CTA uses CTAButton component (not hand-rolled gradient button)
  </success_criteria>

<output>
After completion, create `.planning/phases/09-living-system-page-conversion/09-01-SUMMARY.md`
</output>
