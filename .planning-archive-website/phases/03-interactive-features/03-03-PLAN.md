---
phase: 03-interactive-features
plan: 03
type: execute
wave: 3
depends_on:
  - '03-02'
files_modified:
  - fmai-nextjs/src/components/chatbot/DemoPlayground.tsx
  - fmai-nextjs/src/components/chatbot/PersonaSelector.tsx
  - fmai-nextjs/src/components/chatbot/DemoContextCard.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoOrchestrator.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoProgress.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoScenarioCard.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoCheckpoint.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoCompletionCard.tsx
  - fmai-nextjs/src/components/chatbot/demo/scenarios.ts
  - fmai-nextjs/src/hooks/useCalendlyBooking.ts
  - fmai-nextjs/src/config/calendlyConfig.ts
  - fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx
autonomous: true
requirements:
  - INT-03
  - INT-04
  - INT-08
  - INT-09

must_haves:
  truths:
    - '3-persona demo playground on Chatbots page lets users switch between ecommerce, support, and leadgen personas'
    - 'Each persona in the playground responds with persona-specific behavior and tools'
    - 'Guided demo mode walks through 3 scenarios with progression controls and checkpoints'
    - 'Calendly modal opens from CTA buttons across the site with prefill support'
    - 'Cookie consent banner appears on first visit and persists user preference across sessions'
  artifacts:
    - path: 'fmai-nextjs/src/components/chatbot/DemoPlayground.tsx'
      provides: '3-persona demo playground with persona selector and embedded chat'
      contains: 'use client'
    - path: 'fmai-nextjs/src/components/chatbot/demo/DemoOrchestrator.tsx'
      provides: 'State machine orchestrator for guided demo mode'
      contains: 'use client'
    - path: 'fmai-nextjs/src/components/chatbot/demo/scenarios.ts'
      provides: 'Demo scenario definitions (pure data, no directive)'
    - path: 'fmai-nextjs/src/hooks/useCalendlyBooking.ts'
      provides: 'Hook to open Calendly modal with prefill'
      contains: 'use client'
    - path: 'fmai-nextjs/src/config/calendlyConfig.ts'
      provides: 'Calendly URL and default config'
    - path: 'fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx'
      provides: 'Chatbots page with DemoPlayground island embedded'
      contains: 'DemoPlayground'
  key_links:
    - from: 'fmai-nextjs/src/components/chatbot/DemoPlayground.tsx'
      to: 'fmai-nextjs/src/components/chatbot/PersonaSelector.tsx'
      via: 'import and render'
      pattern: 'PersonaSelector'
    - from: 'fmai-nextjs/src/components/chatbot/DemoPlayground.tsx'
      to: 'fmai-nextjs/src/components/chatbot/ChatWidget.tsx'
      via: 'embedded mode ChatWidget per persona'
      pattern: 'ChatWidget.*mode.*embedded'
    - from: 'fmai-nextjs/src/components/chatbot/demo/DemoOrchestrator.tsx'
      to: 'fmai-nextjs/src/stores/chatbotStore.ts'
      via: 'demoMode, demoScenario, demoStep state'
      pattern: 'useChatbotStore'
    - from: 'fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx'
      to: 'fmai-nextjs/src/components/chatbot/DemoPlayground.tsx'
      via: 'import client island into server page'
      pattern: 'import.*DemoPlayground'
---

<objective>
Migrate the demo playground (3 personas), guided demo mode (state machine with 3 scenarios), Calendly booking hook, and embed the DemoPlayground into the Chatbots page. Verify cookie consent (created in Plan 01) works end-to-end.

Purpose: Completes all remaining interactive features -- the persona playground showcases chatbot versatility, demo mode provides a guided walkthrough for prospects, and Calendly enables booking from any CTA.
Output: Working demo playground on Chatbots page, guided demo mode, Calendly booking integration, cookie consent verified.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactive-features/03-RESEARCH.md
@.planning/phases/03-interactive-features/03-01-SUMMARY.md
@.planning/phases/03-interactive-features/03-02-SUMMARY.md

Source references (Vite codebase -- migrate from these):
@src/components/chatbot/DemoPlayground.tsx
@src/components/chatbot/PersonaSelector.tsx
@src/components/chatbot/DemoContextCard.tsx
@src/components/chatbot/demo/DemoOrchestrator.tsx
@src/components/chatbot/demo/DemoProgress.tsx
@src/components/chatbot/demo/DemoScenarioCard.tsx
@src/components/chatbot/demo/DemoCheckpoint.tsx
@src/components/chatbot/demo/DemoCompletionCard.tsx
@src/components/common/CalendlyModal.tsx

Target page to embed playground:
@fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx

Chatbot components from Plan 02:
@fmai-nextjs/src/components/chatbot/ChatWidget.tsx
@fmai-nextjs/src/stores/chatbotStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate demo playground, persona selector, and all demo mode components</name>
  <files>
    fmai-nextjs/src/components/chatbot/DemoPlayground.tsx
    fmai-nextjs/src/components/chatbot/PersonaSelector.tsx
    fmai-nextjs/src/components/chatbot/DemoContextCard.tsx
    fmai-nextjs/src/components/chatbot/demo/DemoOrchestrator.tsx
    fmai-nextjs/src/components/chatbot/demo/DemoProgress.tsx
    fmai-nextjs/src/components/chatbot/demo/DemoScenarioCard.tsx
    fmai-nextjs/src/components/chatbot/demo/DemoCheckpoint.tsx
    fmai-nextjs/src/components/chatbot/demo/DemoCompletionCard.tsx
    fmai-nextjs/src/components/chatbot/demo/scenarios.ts
  </files>
  <action>
    Migrate all demo/playground components from the Vite codebase. Same systematic replacements as Plan 02:

    1. Migrate `src/components/chatbot/DemoPlayground.tsx`:
       - "use client" directive
       - The playground renders PersonaSelector (3 personas: ecommerce, support, leadgen) and an embedded ChatWidget for the selected persona
       - Replace framer-motion --> motion/react
       - Replace useTranslation --> useTranslations from next-intl
       - Replace store imports with @/stores/ paths
       - Replace useNavigate with useRouter from next/navigation if used
       - The DemoPlayground receives no props -- it manages its own state for selected persona

    2. Migrate `src/components/chatbot/PersonaSelector.tsx`:
       - "use client" directive
       - Renders 3 persona cards (ecommerce, support, leadgen) with selection state
       - Each card shows persona name, description, and capabilities
       - Apply same import replacements

    3. Migrate `src/components/chatbot/DemoContextCard.tsx`:
       - "use client" directive
       - Shows context information for the current demo persona
       - Apply same import replacements

    4. Migrate all demo mode components:
       - `demo/DemoOrchestrator.tsx` ("use client"): State machine that controls demo progression. Uses chatbotStore for demoMode, demoScenario, demoStep. Renders DemoScenarioCard, DemoProgress, DemoCheckpoint, DemoCompletionCard based on state.
       - `demo/DemoProgress.tsx` ("use client"): Progress bar/steps indicator for current demo scenario.
       - `demo/DemoScenarioCard.tsx` ("use client"): Card showing scenario details with start button.
       - `demo/DemoCheckpoint.tsx` ("use client"): Checkpoint between demo steps with continue/skip options.
       - `demo/DemoCompletionCard.tsx` ("use client"): Completion card with CTA to book a call (triggers Calendly).
       - `demo/scenarios.ts` (NO "use client" -- pure data): Scenario definitions. This is just data (names, descriptions, steps). No directive needed.

    5. Apply to ALL components:
       - framer-motion --> motion/react
       - useTranslation --> useTranslations (next-intl)
       - Relative imports --> @/ absolute imports
       - Verify no `useNavigate` or `<Link to=...>` from react-router remain

  </action>
  <verify>
    Run `cd fmai-nextjs && npm run build` -- build succeeds. Verify all demo components have "use client" except scenarios.ts. Verify no framer-motion or react-router imports remain.
  </verify>
  <done>
    DemoPlayground with 3-persona selector, DemoOrchestrator state machine, and all demo progression components migrated. scenarios.ts contains pure data definitions. All use next-intl and motion/react.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Calendly booking hook, embed DemoPlayground in Chatbots page, and verify cookie consent</name>
  <files>
    fmai-nextjs/src/hooks/useCalendlyBooking.ts
    fmai-nextjs/src/config/calendlyConfig.ts
    fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx
  </files>
  <action>
    1. Create `src/config/calendlyConfig.ts` (NO "use client" -- pure config data):
       - Migrate from Vite codebase if exists, otherwise create:
       - Export calendlyConfig object with: url (string -- the Calendly booking URL), defaultPrefill (optional name/email), utm parameters
       - The actual Calendly URL should come from an env var NEXT_PUBLIC_CALENDLY_URL with a fallback to a placeholder
       - This is shared between CalendlyModal (from Plan 01) and useCalendlyBooking

    2. Create `src/hooks/useCalendlyBooking.ts`:
       - "use client" directive
       - Hook that manages Calendly modal state: isOpen, open(prefill?), close()
       - Accepts optional prefill data (name, email from lead qualification)
       - Returns { isOpen, openCalendly, closeCalendly, CalendlyModalComponent }
       - CalendlyModalComponent is a pre-configured CalendlyModal (from Plan 01) with the config URL
       - This hook is used by DemoCompletionCard, ProgressiveCTA, and any CTA button that triggers booking

    3. Update `src/app/[locale]/(services)/chatbots/page.tsx`:
       - This is an existing Server Component page from Phase 2
       - Add import of DemoPlayground from '@/components/chatbot/DemoPlayground'
       - Add a new section AFTER the existing page content (below the hero and feature sections) for the demo playground
       - Wrap DemoPlayground in a semantic section with an id="demo" for deep linking
       - Add a heading (h2) for the demo section using getTranslations('chatbots') for the heading text
       - The page itself stays a Server Component -- DemoPlayground is a client island within it
       - Do NOT remove or modify existing page content -- only ADD the demo section

    4. Verify cookie consent end-to-end:
       - The CookieConsentBanner was added to layout.tsx in Plan 01
       - Verify it renders correctly by checking the build passes and the component is in the layout
       - INT-09 (cookie consent) is satisfied by: CookieConsentBanner in layout, mounted guard for SSR safety, accept/decline buttons, cookie persistence for 365 days
       - No additional work needed for INT-09 beyond what Plan 01 created -- just verify it builds

    5. Wire up DemoCompletionCard to use Calendly:
       - If DemoCompletionCard has a "Book a call" CTA, ensure it triggers the CalendlyModal
       - Import useCalendlyBooking in DemoCompletionCard
       - On CTA click, call openCalendly() which opens the CalendlyModal

  </action>
  <verify>
    Run `cd fmai-nextjs && npm run build` -- build succeeds. Verify chatbots/page.tsx imports DemoPlayground. Verify useCalendlyBooking.ts exists with "use client". Verify calendlyConfig.ts exports config. Verify CookieConsentBanner is still in layout.tsx.
  </verify>
  <done>
    DemoPlayground embedded in Chatbots page as a client island within the server-rendered page. Calendly booking hook created with modal integration. Cookie consent verified in layout. All Phase 3 interactive features are complete.
  </done>
</task>

</tasks>

<verification>
1. `cd fmai-nextjs && npm run build` passes with zero errors
2. Chatbots page.tsx imports and renders DemoPlayground
3. DemoPlayground renders PersonaSelector with 3 personas
4. DemoOrchestrator uses chatbotStore for demo state
5. useCalendlyBooking hook manages modal state
6. CalendlyModal uses next/dynamic with ssr: false (from Plan 01)
7. CookieConsentBanner is in layout.tsx (from Plan 01)
8. No framer-motion or react-router imports in any migrated file
9. scenarios.ts has no "use client" directive (pure data)
</verification>

<success_criteria>

- DemoPlayground with 3-persona selector renders on the Chatbots page
- Guided demo mode with DemoOrchestrator state machine, 3 scenarios, progression controls
- Calendly modal opens from DemoCompletionCard CTA and any booking button
- Cookie consent banner renders on first visit with accept/decline
- All migrated components use motion/react and next-intl (not framer-motion/react-i18next)
- `npm run build` passes
  </success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-features/03-03-SUMMARY.md`
</output>
