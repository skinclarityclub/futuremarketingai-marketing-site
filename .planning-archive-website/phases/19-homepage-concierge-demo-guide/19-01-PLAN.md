---
phase: 19-homepage-concierge-demo-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/chatbot/ChatWidget.tsx
  - src/hooks/usePersonaChat.ts
autonomous: true
requirements:
  - REQ-CHATBOT-CONCIERGE

must_haves:
  truths:
    - 'Concierge floating chatbot appears on all marketing pages (/, /pricing, /about, /chatbots, etc.)'
    - 'Demo-guide floating chatbot appears on demo pages (/explorer, /calculator, /dashboard)'
    - 'Chatbot persona switches automatically when navigating between marketing and demo routes'
    - 'Page context (pathname) is passed through to the chatbot API for context-aware responses'
  artifacts:
    - path: 'src/App.tsx'
      provides: 'ChatWidget floating mode replacing AIJourneyAssistant'
      contains: 'ChatWidget'
    - path: 'src/components/chatbot/ChatWidget.tsx'
      provides: 'pageContext prop support'
      contains: 'pageContext'
    - path: 'src/hooks/usePersonaChat.ts'
      provides: 'pageContext forwarding to API body'
      contains: 'context'
  key_links:
    - from: 'src/App.tsx'
      to: 'src/components/chatbot/ChatWidget.tsx'
      via: "ChatWidget mode='floating' with personaId and pageContext props"
      pattern: 'ChatWidget.*mode.*floating'
    - from: 'src/components/chatbot/ChatWidget.tsx'
      to: 'src/hooks/usePersonaChat.ts'
      via: 'pageContext prop forwarded to usePersonaChat'
      pattern: 'usePersonaChat.*pageContext'
    - from: 'src/hooks/usePersonaChat.ts'
      to: 'api/chatbot.ts'
      via: 'context field in DefaultChatTransport body'
      pattern: 'context.*pageContext'
---

<objective>
Wire the ChatWidget in floating mode into App.tsx with route-based persona switching. Marketing pages get the concierge persona, demo pages get the demo-guide persona. Add pageContext prop plumbing so the chatbot engine receives the current pathname for context-aware greetings and behavior.

Purpose: Replace the AIJourneyAssistant with the new persona-driven chatbot system built in Phases 15-17.
Output: Floating chatbot visible on all pages, switching persona based on route.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.tsx
@src/components/chatbot/ChatWidget.tsx
@src/hooks/usePersonaChat.ts
@src/stores/chatbotStore.ts
@src/components/chatbot/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pageContext prop to ChatWidget and usePersonaChat</name>
  <files>src/components/chatbot/ChatWidget.tsx, src/hooks/usePersonaChat.ts</files>
  <action>
1. In `src/hooks/usePersonaChat.ts`:
   - Add optional second parameter: `pageContext?: { pathname: string }`
   - Include `context: pageContext` in the `body` object passed to `DefaultChatTransport` (alongside existing `personaId` and `sessionId`)
   - This sends the page context to the server-side prompt builder

2. In `src/components/chatbot/ChatWidget.tsx`:
   - Add `pageContext?: { pathname: string }` to `ChatWidgetProps` interface
   - Pass `pageContext` to `usePersonaChat(personaId, pageContext)` call inside the component
   - No other changes needed -- the component already handles floating mode

Both changes are additive -- existing consumers (DemoPlayground in Phase 18) continue working without passing pageContext.
</action>
<verify>Run `npx tsc --noEmit` -- no type errors. Existing ChatbotsPage demo playground still renders correctly.</verify>
<done>ChatWidget accepts optional pageContext prop and forwards it through usePersonaChat to the API transport body.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ChatWidget into App.tsx with route-based persona switching</name>
  <files>src/App.tsx</files>
  <action>
1. Replace imports:
   - Remove: `AIJourneyAssistant` from the destructured import of `'./components'`
   - Remove: `import NudgeToast from './components/ai-assistant/NudgeToast'`
   - Add: `import { ChatWidget } from './components/chatbot'`

2. Add route-based persona detection after the existing `isDemoRoute` line (~line 120):

   ```
   const isDemoPage = ['/explorer', '/calculator', '/dashboard', '/demo'].some(
     p => location.pathname.startsWith(p)
   )
   ```

3. Replace `<AIJourneyAssistant />` (line 182) with:

   ```
   <ChatWidget
     mode="floating"
     personaId={isDemoPage ? 'demo-guide' : 'concierge'}
     pageContext={{ pathname: location.pathname }}
   />
   ```

4. Remove `<NudgeToast />` (line 249) entirely -- the new chatbot system does not use nudge toasts.

5. Add one-time localStorage cleanup in the existing `useEffect` that runs on mount (the dark mode one), or as a new mount effect:
   ```
   localStorage.removeItem('fmai-chat-state')
   ```
   This cleans up the old ARIA chatStore localStorage key from returning visitors.

NOTE: Keep `FloatingElementProvider` and `AIJourneyAssistant` import from components barrel for NOW -- Plan 02 cleans up the barrel exports and removes FloatingElementContext. The barrel export will just be unused after this change.

Actually, the `AIJourneyAssistant` import must be removed from App.tsx since we are no longer rendering it. The barrel export cleanup happens in Plan 02. Just remove the import line and the NudgeToast import. The build may warn about unused barrel exports but will not error.
</action>
<verify>

1. `npx tsc --noEmit` -- no type errors
2. `npx vite build` -- build succeeds
3. Start dev server and verify:
   - Navigate to `/` -- floating chat button appears (concierge)
   - Navigate to `/explorer` -- floating chat button appears (demo-guide)
   - Open chat on `/pricing` -- chat works with concierge persona
   - No console errors about missing ARIA imports
     </verify>
     <done>AIJourneyAssistant and NudgeToast replaced by ChatWidget with route-based persona switching. Floating chatbot visible on all pages. Old localStorage key cleaned up.</done>
     </task>

</tasks>

<verification>
- Floating chatbot button visible on homepage
- Floating chatbot button visible on /explorer
- Chat opens and streams responses on marketing pages (concierge persona)
- Chat opens and streams responses on demo pages (demo-guide persona)
- Persona switches when navigating from marketing to demo route
- Build succeeds with no errors
- No console errors about missing modules
</verification>

<success_criteria>
ChatWidget floating mode replaces AIJourneyAssistant in App.tsx. Route-based persona switching works: marketing pages use concierge, demo pages use demo-guide. Page context (pathname) flows through to the chatbot API. Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/19-homepage-concierge-demo-guide/19-01-SUMMARY.md`
</output>
