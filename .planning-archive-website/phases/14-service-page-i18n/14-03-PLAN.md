---
phase: 14-service-page-i18n
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/VoiceAgentsPage.tsx
  - public/locales/en/voice-agents.json
  - public/locales/nl/voice-agents.json
  - public/locales/es/voice-agents.json
autonomous: true
requirements: [REQ-SERVICE-I18N]

must_haves:
  truths:
    - 'VoiceAgentsPage displays correct content in English when EN selected'
    - 'VoiceAgentsPage displays Dutch content when NL selected'
    - 'VoiceAgentsPage displays Spanish content when ES selected'
    - 'All hardcoded English strings are replaced with t() calls'
    - 'Const arrays (useCases, pricingTiers, faqs) use t() inside component'
    - 'Partnership note section text is translated'
  artifacts:
    - path: 'public/locales/en/voice-agents.json'
      provides: 'English translation strings for VoiceAgentsPage'
      contains: 'hero'
    - path: 'public/locales/nl/voice-agents.json'
      provides: 'Dutch translation strings for VoiceAgentsPage'
      contains: 'hero'
    - path: 'public/locales/es/voice-agents.json'
      provides: 'Spanish translation strings for VoiceAgentsPage'
      contains: 'hero'
    - path: 'src/pages/VoiceAgentsPage.tsx'
      provides: 'Internationalized VoiceAgentsPage component'
      contains: 'useTranslation'
  key_links:
    - from: 'src/pages/VoiceAgentsPage.tsx'
      to: 'public/locales/en/voice-agents.json'
      via: "useTranslation(['voice-agents', 'common'])"
      pattern: "useTranslation\\(\\['voice-agents'"
---

<objective>
Internationalize VoiceAgentsPage -- extract all ~59 hardcoded English strings into dedicated voice-agents.json namespace files (EN/NL/ES) and refactor the component to use useTranslation.

Purpose: Language switcher currently does nothing on /voice-agents. This makes the page fully multilingual.
Output: 3 locale JSON files + refactored VoiceAgentsPage.tsx using t() for all visible text.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-service-page-i18n/14-RESEARCH.md
@src/pages/VoiceAgentsPage.tsx
@src/pages/AboutPage.tsx
@public/locales/en/about.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create voice-agents.json locale files (EN/NL/ES)</name>
  <files>public/locales/en/voice-agents.json, public/locales/nl/voice-agents.json, public/locales/es/voice-agents.json</files>
  <action>
Create `public/locales/en/voice-agents.json` with all hardcoded English strings from VoiceAgentsPage.tsx, structured as:
- `hero.badge`, `hero.title`, `hero.description`, `hero.cta_primary`, `hero.cta_secondary`
- `use_cases.title`, `use_cases.items.outbound_leads.title/description`, `.appointment_setting.title/description`, `.inbound_service.title/description`, `.post_sale.title/description`
- `pricing.title`, `pricing.subtitle`, `pricing.most_popular`, `pricing.cta` ("Get Started"), `pricing.tiers.basic.name/price/description/features[]`, `.standard.name/price/description/features[]`, `.ongoing.name/price/description/features[]`
- `partnership.title`, `partnership.description`
- `trust_metrics.availability.value/label`, `.calls.value/label`, `.accuracy.value/label`
- `faq.title`, `faq.items.voice_quality.q/a`, `.objections.q/a`, `.languages.q/a`, `.crm.q/a`
- `final_cta.title`, `final_cta.description`, `final_cta.button`

Copy EN structure exactly for NL with professional Dutch translations. Key translations: "AI Voice Agents for Lead Qualification & Booking" = "AI Voice Agents voor Leadkwalificatie & Boekingen", "AI That Handles Your Calls" = "AI Die Uw Gesprekken Afhandelt", "What Voice Agents Can Do" = "Wat Voice Agents Kunnen Doen", "Built With Specialized Partners" = "Gebouwd Met Gespecialiseerde Partners", "Book a Demo Call" = "Demo Gesprek Boeken", "See Use Cases" = "Bekijk Toepassingen", "Book Demo Call" = "Demo Gesprek Boeken", "Always Available" = "Altijd Beschikbaar", "Calls Per Day" = "Gesprekken Per Dag", "Accuracy Rate" = "Nauwkeurigheidsgraad".

Copy EN structure exactly for ES with professional Spanish translations. Key translations: "AI Voice Agents for Lead Qualification & Booking" = "Agentes de Voz IA para Cualificacion de Leads y Reservas", "Book a Demo Call" = "Reservar una Llamada Demo".

Keep price formatting identical across all 3 languages. SEO meta tags stay hardcoded EN per project decision [11-06].
</action>
<verify>All 3 JSON files are valid JSON: `node -e "require('./public/locales/en/voice-agents.json'); require('./public/locales/nl/voice-agents.json'); require('./public/locales/es/voice-agents.json'); console.log('All valid')"`. All 3 files have identical key structure.</verify>
<done>EN/NL/ES voice-agents.json files exist with all ~59 strings each, valid JSON, identical key structure.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor VoiceAgentsPage.tsx to use useTranslation</name>
  <files>src/pages/VoiceAgentsPage.tsx</files>
  <action>
Refactor VoiceAgentsPage.tsx to use i18n:

1. Add `import { useTranslation } from 'react-i18next'` and `import type { LucideIcon } from 'lucide-react'`.

2. Inside the component function, add: `const { t } = useTranslation(['voice-agents', 'common'])`

3. Refactor const arrays. Keep icon/config mappings at module level:

```typescript
const USE_CASE_KEYS = [
  'outbound_leads',
  'appointment_setting',
  'inbound_service',
  'post_sale',
] as const
const USE_CASE_ICONS: Record<string, LucideIcon> = {
  outbound_leads: Phone,
  appointment_setting: Calendar,
  inbound_service: Headphones,
  post_sale: RefreshCw,
}

const PRICING_TIER_KEYS = ['basic', 'standard', 'ongoing'] as const
const PRICING_TIER_CONFIG: Record<string, { highlighted: boolean }> = {
  basic: { highlighted: false },
  standard: { highlighted: true },
  ongoing: { highlighted: false },
}
const FAQ_KEYS = ['voice_quality', 'objections', 'languages', 'crm'] as const
```

4. Inside the component, construct arrays using t():

```typescript
const useCases = USE_CASE_KEYS.map((key) => ({
  icon: USE_CASE_ICONS[key],
  title: t(`voice-agents:use_cases.items.${key}.title`),
  description: t(`voice-agents:use_cases.items.${key}.description`),
}))
```

Same pattern for pricingTiers (use PRICING_TIER_CONFIG for highlighted), faqs.

5. Replace ALL hardcoded English strings in JSX with t() calls:

- Hero badge, title, description, CTA buttons
- Section headings ("What Voice Agents Can Do", "Pricing", "Frequently Asked Questions")
- Pricing subtitle ("Transparent pricing for voice AI solutions")
- "Most Popular" badge: `t('voice-agents:pricing.most_popular')`
- "Get Started" button text: `t('voice-agents:pricing.cta')`
- Partnership note title and description: `t('voice-agents:partnership.title')`, `t('voice-agents:partnership.description')`
- Trust metric values and labels (24/7, 500+, 98%)
- Final CTA section title, description, button text

6. Do NOT change: SEOHead props (stay hardcoded EN), component structure, CSS classes, Framer Motion animations, Lucide icon imports, ProductMedia props.

Note: VoiceAgentsPage has no process steps section (unlike Automations/Chatbots), but has a unique Partnership Note section. Make sure that section is fully translated.
</action>
<verify>Run `npx tsc --noEmit` -- no TypeScript errors. Run `npx vite build` -- build succeeds.</verify>
<done>VoiceAgentsPage.tsx uses useTranslation for all visible text. Zero hardcoded English strings remain (except SEO). Build passes.</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds with no errors
- `node -e "const en = require('./public/locales/en/voice-agents.json'); const nl = require('./public/locales/nl/voice-agents.json'); const es = require('./public/locales/es/voice-agents.json'); const check = (a,b,p='') => { for(const k of Object.keys(a)) { if(!(k in b)) console.log('MISSING:',p+k); else if(typeof a[k]==='object' && !Array.isArray(a[k])) check(a[k],b[k],p+k+'.'); } }; check(en,nl); check(en,es); console.log('Key check done')"` -- no MISSING keys
- `grep -c "useTranslation" src/pages/VoiceAgentsPage.tsx` returns 1
</verification>

<success_criteria>

- VoiceAgentsPage renders correctly in EN (identical to before)
- All ~59 strings extracted to voice-agents.json namespace
- NL and ES JSON files have identical structure with translated values
- No hardcoded English visible text remains in the component (SEO excepted)
- Partnership note section fully translated in all languages
- Build passes without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/14-service-page-i18n/14-03-SUMMARY.md`
</output>
