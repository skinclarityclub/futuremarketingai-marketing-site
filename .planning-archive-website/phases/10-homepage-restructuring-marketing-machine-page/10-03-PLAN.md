---
phase: 10-homepage-restructuring-marketing-machine-page
plan: 03
type: execute
wave: 2
depends_on:
  - '10-01'
  - '10-02'
files_modified:
  - public/locales/nl/common.json
  - public/locales/es/common.json
autonomous: true
requirements:
  - REQ-HOMEPAGE-RESTRUCTURE

must_haves:
  truths:
    - 'NL hero_landing keys match EN structure (no stale founding-member/wachtlijst copy)'
    - 'ES hero_landing keys match EN structure (no stale founding-member copy)'
    - 'ES common.json has no duplicate pricing or actions blocks'
    - 'NL common.json has new services.* keys (translated)'
    - 'ES common.json has new services.* keys (translated)'
  artifacts:
    - path: 'public/locales/nl/common.json'
      provides: 'Synced NL translations with new homepage service cards + fixed hero_landing keys'
    - path: 'public/locales/es/common.json'
      provides: 'Synced ES translations with new homepage service cards + fixed structural issues'
  key_links:
    - from: 'src/components/landing/Hero.tsx'
      to: 'public/locales/nl/common.json'
      via: "useTranslation('common') with NL language selected"
      pattern: "landing\\.hero_landing"
    - from: 'src/components/landing/Hero.tsx'
      to: 'public/locales/es/common.json'
      via: "useTranslation('common') with ES language selected"
      pattern: "landing\\.hero_landing"
---

<objective>
Sync NL and ES translation files with the EN changes made in Plan 01. Fix stale hero_landing keys (NL still has "wachtlijst"/founding member copy, ES has same stale copy). Fix ES common.json structural issues (duplicate blocks). Add new service card translation keys in NL and ES.

Purpose: Without this sync, switching to NL or ES on the homepage shows stale marketing-focused copy instead of the new general FutureAI hub content. The ES file also has structural JSON issues that may cause parsing errors.

Output: Synced NL and ES locale files with all new keys translated and structural issues fixed.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-homepage-restructuring-marketing-machine-page/10-CONTEXT.md
@.planning/phases/10-homepage-restructuring-marketing-machine-page/10-01-SUMMARY.md
@public/locales/en/common.json
@public/locales/nl/common.json
@public/locales/es/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync NL common.json hero_landing keys + add service card keys</name>
  <files>public/locales/nl/common.json</files>
  <action>
  Read the current NL common.json and EN common.json (post Plan 01 changes). Update NL to match EN structure.

**1. Update stale hero_landing keys (from CONTEXT.md i18n table):**

Current NL (stale):

```json
"badge": "Next-Gen AI Marketing",
"main_headline": "Turn content into growth.",
"sub_headline": "On autopilot.",
"description": "[old autonomous marketing copy]",
"cta": {
  "primary": "Probeer Interactieve Demo",
  "secondary": "Sluit aan bij Wachtlijst"
}
```

Update to match EN meaning (translated to Dutch):

```json
"badge": "AI Implementatie Partner",
"main_headline": "AI Systemen Die Jouw",
"sub_headline": "Bedrijf Runnen.",
"description": "[translate EN description to Dutch — gateway services pitch about AI automation, chatbots, voice agents]",
"trust_anchor": "[translate EN trust_anchor to Dutch]",
"cta": {
  "primary": "Ontdek Onze Services",
  "secondary": "Plan een Strategiegesprek"
}
```

**2. Add new services.\* keys (Dutch translations):**

```json
"services": {
  "title": "AI Platforms Gebouwd voor Groei",
  "subtitle": "Drie gespecialiseerde AI platforms. Kies je startpunt.",
  "cta_label": "Meer info",
  "marketing": {
    "name": "FutureMarketingAI",
    "pitch": "Autonome contentcreatie, planning en campagne-automatisering"
  },
  "chat": {
    "name": "FutureChatAI",
    "pitch": "AI chatagenten die leads kwalificeren en klanten 24/7 ondersteunen"
  },
  "voice": {
    "name": "FutureVoiceAI",
    "pitch": "Spraakagenten die inkomende en uitgaande gesprekken afhandelen"
  },
  "automations": {
    "name": "AI Automatisering",
    "pitch": "Maatwerk workflow-automatisering die jouw tools en processen verbindt"
  }
}
```

**3. Update final_cta keys** to match EN changes (if Plan 01 changed them):

```json
"final_cta": {
  "title": "Niet Zeker Waar te Beginnen?",
  "subtitle": "Plan een gratis 30 minuten AI audit. We brengen jouw kansen in kaart.",
  "button": "Plan een Gratis Audit"
}
```

**4. Verify all other hero_landing sub-keys** (solution_section, etc.) are present and not stale. If any keys exist in EN but not NL, add them with Dutch translations.

Keep all NON-hero_landing keys unchanged — do not modify other sections of the NL file.
</action>
<verify>
<automated>cd /c/Users/daley/Desktop/Futuremarketingai && node -e "const nl=require('./public/locales/nl/common.json'); const en=require('./public/locales/en/common.json'); const nlKeys=Object.keys(nl.landing?.hero_landing?.services || {}); const enKeys=Object.keys(en.landing?.hero_landing?.services || {}); console.log('NL services keys:', nlKeys); console.log('EN services keys:', enKeys); console.log('Match:', JSON.stringify(nlKeys.sort()) === JSON.stringify(enKeys.sort())); console.log('Badge:', nl.landing?.hero_landing?.badge); console.log('No wachtlijst:', !JSON.stringify(nl).includes('Wachtlijst'));"</automated>
</verify>
<done>

- NL hero_landing.badge = "AI Implementatie Partner" (not "Next-Gen AI Marketing")
- NL hero_landing.cta.secondary does NOT contain "Wachtlijst"
- NL hero_landing.services.\* keys present with Dutch translations
- NL hero_landing.services key structure matches EN
- NL JSON parses without errors
  </done>
  </task>

<task type="auto">
  <name>Task 2: Fix ES common.json structural issues + sync hero_landing + add service card keys</name>
  <files>public/locales/es/common.json</files>
  <action>
  Read the current ES common.json. Fix structural issues AND sync with EN.

**1. Fix structural issues (from CONTEXT.md):**

- **Duplicate `"pricing"` block** (lines ~78-92 and ~217-221): Remove the duplicate. Keep the more complete one. Ensure only one `"pricing"` key exists at its correct nesting level.
- **Duplicate `"actions"` block** (lines ~28-53 and ~229-236): Remove the duplicate. Keep the more complete one.
- **Missing `cookie_consent` block**: Add the `cookie_consent` block from EN, translated to Spanish:
  ```json
  "cookie_consent": {
    "message": "Usamos cookies para mejorar tu experiencia.",
    "accept": "Aceptar",
    "decline": "Rechazar"
  }
  ```
  (Check exact EN structure first and mirror it.)
- **Extra top-level keys not in EN/NL**: Identify any keys present in ES but not in EN. If they are clearly orphaned/wrong, remove them. If unclear, leave them.

**2. Update stale hero_landing keys (same issue as NL):**

Update to match EN meaning (translated to Spanish):

```json
"badge": "Socio de Implementacion AI",
"main_headline": "Sistemas AI Que Gestionan",
"sub_headline": "Tu Negocio.",
"description": "[translate EN description to Spanish — gateway services pitch]",
"trust_anchor": "[translate EN trust_anchor to Spanish]",
"cta": {
  "primary": "Explora Nuestros Servicios",
  "secondary": "Reserva una Llamada Estrategica"
}
```

**3. Add new services.\* keys (Spanish translations):**

```json
"services": {
  "title": "Plataformas AI Construidas para el Crecimiento",
  "subtitle": "Tres plataformas AI especializadas. Elige tu punto de partida.",
  "cta_label": "Saber mas",
  "marketing": {
    "name": "FutureMarketingAI",
    "pitch": "Creacion autonoma de contenido, programacion y automatizacion de campanas"
  },
  "chat": {
    "name": "FutureChatAI",
    "pitch": "Agentes de chat AI que cualifican leads y atienden clientes 24/7"
  },
  "voice": {
    "name": "FutureVoiceAI",
    "pitch": "Agentes de voz que gestionan llamadas entrantes y salientes"
  },
  "automations": {
    "name": "Automatizaciones AI",
    "pitch": "Automatizacion de flujos de trabajo personalizados conectando tus herramientas"
  }
}
```

**4. Update final_cta keys** to match EN changes:

```json
"final_cta": {
  "title": "No Sabes Por Donde Empezar?",
  "subtitle": "Reserva una auditoria AI gratuita de 30 minutos. Identificaremos tus oportunidades.",
  "button": "Reserva una Auditoria Gratis"
}
```

**5. Validate JSON** after all edits. Run `node -e "JSON.parse(require('fs').readFileSync('public/locales/es/common.json', 'utf8'))"` to confirm valid JSON.

**CRITICAL:** After fixing duplicates, ensure no other keys were accidentally removed. Compare the key count with EN to spot missing sections.
</action>
<verify>
<automated>cd /c/Users/daley/Desktop/Futuremarketingai && node -e "const es=require('./public/locales/es/common.json'); const en=require('./public/locales/en/common.json'); const esKeys=Object.keys(es.landing?.hero_landing?.services || {}); const enKeys=Object.keys(en.landing?.hero_landing?.services || {}); console.log('ES services keys:', esKeys); console.log('Match:', JSON.stringify(esKeys.sort()) === JSON.stringify(enKeys.sort())); console.log('Badge:', es.landing?.hero_landing?.badge); console.log('JSON valid: true'); const str=JSON.stringify(es); const pricingCount=(str.match(/\"pricing\"/g)||[]).length; console.log('pricing occurrences:', pricingCount, '(expect 1-2, not 3+)');"</automated>
</verify>
<done>

- ES hero_landing.badge = Spanish translation (not "Next-Gen AI Marketing")
- ES hero_landing.services.\* keys present with Spanish translations
- ES JSON parses without errors
- No duplicate pricing blocks (count reduced)
- No duplicate actions blocks
- cookie_consent block present in ES
- ES hero_landing key structure matches EN
  </done>
  </task>

</tasks>

<verification>
1. `node -e "JSON.parse(require('fs').readFileSync('public/locales/nl/common.json','utf8')); console.log('NL OK')"` — valid JSON
2. `node -e "JSON.parse(require('fs').readFileSync('public/locales/es/common.json','utf8')); console.log('ES OK')"` — valid JSON
3. Dev server: Switch to NL → homepage hero shows Dutch general FutureAI copy, service cards in Dutch
4. Dev server: Switch to ES → homepage hero shows Spanish general FutureAI copy, service cards in Spanish
5. Dev server: No "Wachtlijst" or "Lista de Espera" visible anywhere on homepage in any language
6. `npm run build` — Vite build succeeds
</verification>

<success_criteria>

- NL and ES hero_landing keys synced with EN (no stale founding-member copy)
- NL and ES have services.\* keys with proper translations
- ES structural issues fixed (no duplicate blocks)
- All three locale files parse as valid JSON
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/10-homepage-restructuring-marketing-machine-page/10-03-SUMMARY.md`
</output>
