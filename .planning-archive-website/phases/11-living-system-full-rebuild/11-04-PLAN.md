---
phase: 11-living-system-full-rebuild
plan: 04
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/pages/AutomationsPage.tsx
  - src/pages/ChatbotsPage.tsx
  - src/pages/VoiceAgentsPage.tsx
autonomous: true
requirements:
  - REQ-LIVING-SYSTEM-REBUILD

must_haves:
  truths:
    - 'All 3 service pages use card-gradient-border + rounded-card on feature/benefit cards'
    - 'Service page hero sections use fadeIn/fadeInUp CSS animations instead of heavy Framer Motion wrappers'
    - 'Service page backgrounds inherit from global GradientMesh (no local heavy backgrounds)'
    - 'All CTAs on service pages automatically show warm gradient (inherited from CTAButton Plan 02)'
  artifacts:
    - path: 'src/pages/AutomationsPage.tsx'
      provides: 'Rebuilt Automations page with prototype card/layout patterns'
    - path: 'src/pages/ChatbotsPage.tsx'
      provides: 'Rebuilt Chatbots page with prototype card/layout patterns'
    - path: 'src/pages/VoiceAgentsPage.tsx'
      provides: 'Rebuilt Voice Agents page with prototype card/layout patterns'
  key_links:
    - from: 'src/pages/AutomationsPage.tsx'
      to: 'src/index.css'
      via: 'Uses card-gradient-border utility class'
      pattern: 'card-gradient-border'
    - from: 'src/pages/ChatbotsPage.tsx'
      to: 'src/index.css'
      via: 'Uses card-gradient-border utility class'
      pattern: 'card-gradient-border'
---

<objective>
Structural rebuild of all 3 service pages (Automations, Chatbots, VoiceAgents) to match prototype aesthetic — card-gradient-border pattern, rounded-card corners, CSS fadeIn animations, remove heavy backgrounds.

Purpose: Service pages are the primary conversion pages. They need the same structural quality as the homepage.
Output: All 3 service pages rebuilt with prototype card patterns, CSS animations, and consistent layout.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-living-system-full-rebuild/11-RESEARCH.md
@.planning/phases/11-living-system-full-rebuild/11-01-SUMMARY.md
@.planning/phases/11-living-system-full-rebuild/11-02-SUMMARY.md
@src/pages/AutomationsPage.tsx
@src/pages/ChatbotsPage.tsx
@src/pages/VoiceAgentsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild AutomationsPage structural layout</name>
  <files>src/pages/AutomationsPage.tsx</files>
  <action>
Rebuild the AutomationsPage visual structure while preserving ALL content, i18n translations, Calendly CTAs, and page structure. Apply these changes:

1. **Cards/panels:** Find all feature cards, benefit cards, use-case cards, etc. Apply:
   - Add `card-gradient-border` class
   - Change border-radius to `rounded-card` (20px)
   - Background: `bg-white/[0.02]` with `border border-border-primary`
   - Hover: `hover:bg-white/[0.03] hover:-translate-y-1 hover:shadow-[0_20px_60px_rgba(0,0,0,0.3)]`
   - Add `transition-all duration-500` for smooth hover

2. **Hero section animations:** Replace heavy Framer Motion `motion.div` wrappers on hero elements with CSS animation styles:
   - Badge/tag: `style={{ animation: 'fadeIn 0.8s ease-out' }}`
   - Headline: `style={{ animation: 'fadeInUp 0.8s ease-out 0.2s both' }}`
   - Description: `style={{ animation: 'fadeInUp 0.8s ease-out 0.4s both' }}`
   - KEEP `whileInView` animations on scroll-revealed sections lower on the page — only replace the initial hero load animations

3. **Remove local background layers:** If the page has any local gradient orbs, particle systems, or neural network backgrounds, remove them (GradientMesh handles background globally).

4. **Section containers:** Ensure sections use consistent `max-w-7xl mx-auto px-12` padding.

5. **Typography:** Ensure headings use `font-display` (Space Grotesk). Body text inherits DM Sans from global config.

Do NOT change: page content, i18n keys, CTAButton usage (already updated globally), route behavior, Calendly URLs.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep "card-gradient-border" src/pages/AutomationsPage.tsx && grep "rounded-card" src/pages/AutomationsPage.tsx</automated>
</verify>
<done>AutomationsPage uses card-gradient-border + rounded-card on all cards, CSS fadeIn on hero, no local background layers</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild ChatbotsPage + VoiceAgentsPage structural layout</name>
  <files>src/pages/ChatbotsPage.tsx, src/pages/VoiceAgentsPage.tsx</files>
  <action>
Apply the EXACT same structural changes as Task 1 to both ChatbotsPage and VoiceAgentsPage:

1. **Cards:** Add `card-gradient-border rounded-card bg-white/[0.02] border border-border-primary` + hover effects
2. **Hero animations:** Replace Framer Motion hero wrappers with CSS fadeIn/fadeInUp styles (keep whileInView on scroll sections)
3. **Remove local backgrounds:** Delete any local gradient/particle/orb backgrounds
4. **Section padding:** `max-w-7xl mx-auto px-12`
5. **Typography:** `font-display` on headings

Keep the same anti-pattern rules as Task 1: preserve ALL content, i18n, CTAs, routes, Calendly.

Both pages should end up with the same structural patterns as AutomationsPage for visual consistency across all service pages.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep "card-gradient-border" src/pages/ChatbotsPage.tsx && grep "card-gradient-border" src/pages/VoiceAgentsPage.tsx && grep "rounded-card" src/pages/ChatbotsPage.tsx && grep "rounded-card" src/pages/VoiceAgentsPage.tsx</automated>
</verify>
<done>Both ChatbotsPage and VoiceAgentsPage use card-gradient-border + rounded-card on all cards, CSS fadeIn on hero, consistent with AutomationsPage</done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- All 3 service pages: cards have gradient border hover effect
- All 3 service pages: rounded-card (20px) on cards
- All 3 service pages: hero uses CSS fadeIn animations
- All i18n translations render correctly
- All Calendly CTAs still open modals
</verification>

<success_criteria>

- All feature/benefit/use-case cards across 3 service pages use card-gradient-border + rounded-card
- Hero sections use CSS fadeIn/fadeInUp (not heavy Framer Motion for load animations)
- No local background layers — pages use global GradientMesh
- Consistent max-w-7xl px-12 section padding
- All existing content and functionality preserved
- Build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/11-living-system-full-rebuild/11-04-SUMMARY.md`
</output>
