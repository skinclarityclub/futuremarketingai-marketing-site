---
phase: 11-living-system-full-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/common/CTAButton.tsx
  - src/components/landing/SimpleHeader.tsx
  - src/components/common/Footer.tsx
autonomous: true
requirements:
  - REQ-LIVING-SYSTEM-REBUILD

must_haves:
  truths:
    - 'Primary CTA buttons use warm amber gradient with rounded-14px corners'
    - 'Secondary CTA buttons use glass backdrop-blur effect with rounded-14px corners'
    - 'Navigation has full-width backdrop-blur with gradient underline on link hover'
    - 'Navigation logo shows FM with gradient ai suffix (no Sparkles icon)'
    - 'Footer has zero hardcoded slate/indigo classes'
  artifacts:
    - path: 'src/components/common/CTAButton.tsx'
      provides: 'Warm gradient primary + glass secondary CTA variants'
      exports: ['CTAButton']
    - path: 'src/components/landing/SimpleHeader.tsx'
      provides: 'Rebuilt navigation with backdrop-blur, gradient underline, FM logo'
      exports: ['SimpleHeader']
    - path: 'src/components/common/Footer.tsx'
      provides: 'Footer with all Living System tokens'
      exports: ['Footer']
  key_links:
    - from: 'src/components/common/CTAButton.tsx'
      to: 'All pages using CTAButton'
      via: 'Global component — variant classes cascade to all consumers'
      pattern: 'CTAButton'
    - from: 'src/components/landing/SimpleHeader.tsx'
      to: 'App.tsx layout'
      via: 'Header rendered on every page'
      pattern: 'SimpleHeader'
---

<objective>
Rebuild the three global chrome components: CTAButton (warm gradient + glass variants), SimpleHeader (backdrop-blur nav with gradient underline), and Footer (Living System token cleanup).

Purpose: These three components appear on every page. Updating them first means all pages inherit the new button/nav/footer design automatically.
Output: CTAButton with warm gradient primary + glass secondary, SimpleHeader with backdrop-blur + gradient underline, Footer with zero hardcoded slate/indigo.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-living-system-full-rebuild/11-RESEARCH.md
@src/components/common/CTAButton.tsx
@src/components/landing/SimpleHeader.tsx
@src/components/common/Footer.tsx
</context>

<interfaces>
<!-- Current CTAButton interface — preserve props, change styling only -->
interface CTAButtonProps {
  children: React.ReactNode
  variant?: 'primary' | 'secondary'
  size?: 'sm' | 'md' | 'lg'
  arrow?: boolean
  calendly?: boolean
  calendlyUrl?: string
  href?: string
  onClick?: () => void
  className?: string
  ariaLabel?: string
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild CTAButton with warm gradient + glass variants</name>
  <files>src/components/common/CTAButton.tsx</files>
  <action>
Update the CTAButton component styling. Keep ALL existing props, logic, and Calendly integration unchanged. Only modify the CSS classes:

1. **variantClasses** — Replace:

   ```ts
   primary: 'bg-accent-system text-bg-deep font-semibold hover:bg-accent-system/90 active:bg-accent-system/80',
   secondary: 'bg-transparent border border-border-primary text-text-primary hover:bg-bg-elevated active:bg-bg-elevated/80',
   ```

   With:

   ```ts
   primary: 'bg-gradient-to-br from-accent-human to-[#E8941A] text-bg-deep font-semibold hover:-translate-y-0.5 hover:shadow-[0_12px_40px_rgba(245,166,35,0.3)] active:translate-y-0',
   secondary: 'bg-white/[0.04] backdrop-blur-[12px] border border-white/[0.08] text-text-primary hover:bg-white/[0.07] hover:border-white/[0.12] hover:-translate-y-0.5',
   ```

2. **baseClasses** — Replace `'rounded-sm'` with `'rounded-btn'` (uses the 14px token from Plan 01).

3. **JSDoc comment** — Update the component docstring to say:
   "Primary: warm amber gradient, rounded-btn. Secondary: glass backdrop-blur, rounded-btn. Can open Calendly modal directly."

Do NOT change: props interface, Calendly logic, size classes, arrow icon, href/button rendering, or the CalendlyModal import.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep "from-accent-human" src/components/common/CTAButton.tsx && grep "backdrop-blur" src/components/common/CTAButton.tsx && grep "rounded-btn" src/components/common/CTAButton.tsx</automated>
</verify>
<done>CTAButton primary uses warm amber gradient (from-accent-human to #E8941A), secondary uses glass backdrop-blur, both use rounded-btn (14px). All existing props and Calendly logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild SimpleHeader navigation + fix Footer tokens</name>
  <files>src/components/landing/SimpleHeader.tsx, src/components/common/Footer.tsx</files>
  <action>
**SimpleHeader.tsx — Full navigation rebuild:**

Keep ALL existing functionality: brandMiddle logic, services dropdown, mobile menu, language switcher, i18n. Change only the visual structure:

1. **Outer container:** Replace the floating rounded container with full-width backdrop-blur nav:
   - Remove: `rounded-sm`, `mx-4`, max-width constraints
   - Add: `bg-[rgba(12,12,20,0.5)] backdrop-blur-[24px] border-b border-border-primary`
   - Keep: `fixed top-0 left-0 right-0 z-50`
   - Padding: `px-12 py-5` (48px horizontal, 20px vertical)

2. **Logo:** Replace the Sparkles icon + "Future{brandMiddle}AI" text with:
   - Text-only: "FM" in white + "ai" with gradient text (bg-gradient-to-r from-accent-human to-accent-system bg-clip-text text-transparent)
   - Use `font-display font-bold text-xl tracking-tight`
   - Keep the `brandMiddle` logic for route-specific branding ONLY in the middle portion — on homepage show nothing extra, on /marketing-machine show "Marketing" between FM and ai
   - Remove the Sparkles icon import if it becomes unused

3. **Nav links:** Add gradient underline on hover using a CSS approach:
   - Each nav link gets `relative` positioning
   - Add an `after:` pseudo-element: `after:absolute after:bottom-[-4px] after:left-0 after:w-0 after:h-px after:bg-gradient-to-r after:from-accent-human after:to-accent-system after:transition-all after:duration-300 hover:after:w-full`
   - Remove any existing hover background color changes on nav links

4. **Nav CTA button:** The header CTA should use CTAButton with `variant="primary" size="sm"` (which now automatically gets warm gradient styling from Task 1).

5. **Services dropdown:** Keep existing dropdown logic and content. Update dropdown container styling to match: `bg-[rgba(12,12,20,0.8)] backdrop-blur-[24px] border border-border-primary rounded-card`.

**Footer.tsx — Token cleanup:**

Replace all hardcoded slate/indigo classes (research found 12 instances):

- `text-slate-300` → `text-text-secondary`
- `text-slate-500` → `text-text-muted`
- `text-slate-600` → `text-text-muted`
- `border-slate-600` → `border-border-primary`
- `border-slate-700` → `border-border-primary`
- `hover:text-indigo-400` → `hover:text-accent-system`
- `text-gray-400` → `text-text-secondary`
- `text-gray-500` → `text-text-muted`

Search the entire Footer.tsx for ANY remaining raw color classes (slate-, indigo-, gray-, blue-, violet-, purple-) and replace with the appropriate Living System token.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep "backdrop-blur" src/components/landing/SimpleHeader.tsx && grep -c "slate\|indigo\|gray-[0-9]" src/components/common/Footer.tsx | grep "^0$"</automated>
</verify>
<done>SimpleHeader has full-width backdrop-blur nav with gradient underline hovers and FM+gradient-ai logo. Footer has zero hardcoded slate/indigo/gray classes.</done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- All CTAButton consumers automatically show warm gradient styling
- SimpleHeader renders full-width with backdrop-blur background
- Footer grep for slate/indigo/gray returns 0 matches
</verification>

<success_criteria>

- CTAButton primary: warm amber gradient, not flat teal
- CTAButton secondary: glass backdrop-blur, not transparent border
- Both CTA variants: rounded-btn (14px), not rounded-sm (4px)
- SimpleHeader: backdrop-blur nav, gradient underline on link hover, FM+ai logo
- Footer: zero hardcoded slate/indigo/gray color classes
- All existing functionality preserved (Calendly, dropdown, mobile menu, i18n)
- Build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/11-living-system-full-rebuild/11-02-SUMMARY.md`
</output>
