---
phase: 3
plan: 3
name: UX & Accessibility Fixes
wave: 2
autonomous: true
requirement_ids: [REQ-UX-FIXES]
files_modified:
  [
    src/pages/VoiceAgentsPage.tsx,
    src/hooks/useReducedMotion.ts,
    src/components/landing/SimpleHeader.tsx,
    src/pages/AutomationsPage.tsx,
    src/pages/ChatbotsPage.tsx,
    src/components/common/Footer.tsx,
    src/components/common/CalendlyModal.tsx,
  ]
---

## Objective

Fix all critical UX and accessibility issues identified in the UI/UX audit: add missing VoiceAgents secondary CTA and trust metrics, add prefers-reduced-motion support, fix mobile menu focus trap, standardize all Calendly CTAs to open as modal (not new tab), replace footer emoji with Lucide icon, and update CalendlyModal with dark theme colors.

## Tasks

### Task 1: Fix VoiceAgents Page — Missing Secondary CTA + Trust Metrics

**Files:** Modify `src/pages/VoiceAgentsPage.tsx`

1. Add secondary CTA "See Use Cases" linking to `#use-cases` in hero section (alongside existing "Book a Demo Call")
2. Add `id="use-cases"` to the use cases section
3. Add trust metrics row (24/7, 500+, 98%) before FAQ section

**Commit:** `fix: add missing secondary CTA and trust metrics to VoiceAgents page`

### Task 2: Fix Framer Motion Accessibility — prefers-reduced-motion

**Files:** Create `src/hooks/useReducedMotion.ts`, Modify `src/hooks/index.ts` (if exists)

1. Create `useMotionSafe` hook that wraps `useReducedMotion` from framer-motion
2. Returns empty object when reduced motion preferred, otherwise passes through animation props
3. Export from hooks barrel if exists

**Commit:** `feat: add useMotionSafe hook — respects prefers-reduced-motion for Framer Motion`

### Task 3: Fix Mobile Menu Focus Trap

**Files:** Modify `src/components/landing/SimpleHeader.tsx`

1. Add useEffect that traps focus within mobile menu when open
2. Query focusable elements (a, button, [tabindex])
3. Trap Tab/Shift+Tab cycling within menu
4. Focus first element on open
5. Add `id="mobile-menu"` to mobile menu container

**Commit:** `fix: add focus trap to mobile menu for WCAG keyboard navigation`

### Task 4: Standardize CTA Destinations — Calendly Modal

**Files:** Modify `src/pages/VoiceAgentsPage.tsx`, `src/pages/AutomationsPage.tsx`, `src/pages/ChatbotsPage.tsx`

1. In each page: add `useState` for CalendlyModal, import CalendlyModal
2. Replace all `<a href="calendly.com/...">` tags with `<button onClick={() => setIsCalendlyOpen(true)}>`
3. Add CalendlyModal component with dark theme URL params
4. Verify all CTAs open modal, not new tab

**Commit:** `fix: all Calendly CTAs now open as modal with dark theme, not new tab`

### Task 5: Fix Footer Emoji — Replace with Lucide Icon

**Files:** Modify `src/components/common/Footer.tsx`

1. Import `Trash2` from lucide-react
2. Replace 🗑️ emoji with `<Trash2 className="w-4 h-4 inline-block mr-1" aria-hidden="true" />`

**Commit:** `fix: replace emoji icon with Lucide Trash2 in footer`

### Task 6: Fix Calendly Dark Theme Colors

**Files:** Modify `src/components/common/CalendlyModal.tsx`

1. Update DEFAULT_CALENDLY_URL with dark theme params: `background_color=111520&text_color=e8ecf4&primary_color=00D4AA`
2. Update loading spinner: `border-accent-primary` → `border-accent-system`
3. Update error fallback buttons: `bg-blue-600` → `bg-accent-system text-bg-deep rounded-sm`

**Commit:** `fix: update CalendlyModal with dark theme colors and Living System tokens`

### Task 7: Full Build Verification

**Files:** None (verification only)

1. Run `npx tsc --noEmit` — no type errors
2. Run `npm run lint` — no new lint errors
3. Run `npm run build` — build succeeds

**Commit:** Only if fixes needed: `chore: Wave 1 foundation complete — design tokens, shared components, critical fixes`
