---
phase: 18-chatbotspage-demo-playground
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - src/pages/ChatbotsPage.tsx
  - src/components/chatbot/index.ts
  - public/locales/en/chatbots.json
  - public/locales/nl/chatbots.json
  - public/locales/es/chatbots.json
autonomous: false
requirements:
  - REQ-CHATBOT-PLAYGROUND
must_haves:
  truths:
    - 'ChatbotsPage renders Hero -> DemoPlayground -> MultiPlatformShowcase -> UseCases -> Process -> Pricing -> FAQ -> FinalCTA'
    - 'Hero CTA scrolls smoothly to demo playground section'
    - 'Use case cards scroll to playground and activate the matching demo tab'
    - 'All demo text displays correctly in EN, NL, and ES'
    - 'Product Demo Media section is removed and replaced by DemoPlayground'
    - 'Barrel exports include all new chatbot components'
  artifacts:
    - path: 'src/pages/ChatbotsPage.tsx'
      provides: 'Restructured page with DemoPlayground + MultiPlatformShowcase wired in'
    - path: 'src/components/chatbot/index.ts'
      provides: 'Barrel exports for DemoPlayground, PersonaSelector, DemoContextCard, ProgressiveCTA, MultiPlatformShowcase'
    - path: 'public/locales/en/chatbots.json'
      provides: 'EN demo playground i18n keys'
    - path: 'public/locales/nl/chatbots.json'
      provides: 'NL demo playground i18n keys'
    - path: 'public/locales/es/chatbots.json'
      provides: 'ES demo playground i18n keys'
  key_links:
    - from: 'src/pages/ChatbotsPage.tsx'
      to: 'src/components/chatbot/DemoPlayground.tsx'
      via: 'import and render with lifted activeTab state'
      pattern: 'DemoPlayground'
    - from: 'src/pages/ChatbotsPage.tsx'
      to: 'src/components/chatbot/MultiPlatformShowcase.tsx'
      via: 'import and render as standalone section'
      pattern: 'MultiPlatformShowcase'
    - from: 'src/pages/ChatbotsPage.tsx'
      to: '#demo-playground'
      via: 'scrollIntoView from hero CTA and use case cards'
      pattern: 'scrollIntoView'
---

<objective>
Wire DemoPlayground and MultiPlatformShowcase into ChatbotsPage, update hero and use case sections, add all i18n keys for EN/NL/ES, and update barrel exports.

Purpose: Complete the page transformation from static marketing to interactive demo experience.
Output: Restructured ChatbotsPage with live demos, updated i18n files, updated barrel exports.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-chatbotspage-demo-playground/18-CONTEXT.md
@.planning/phases/18-chatbotspage-demo-playground/18-RESEARCH.md
@.planning/phases/18-chatbotspage-demo-playground/18-01-SUMMARY.md
@.planning/phases/18-chatbotspage-demo-playground/18-02-SUMMARY.md
@src/pages/ChatbotsPage.tsx
@src/components/chatbot/index.ts
@public/locales/en/chatbots.json
@public/locales/nl/chatbots.json
@public/locales/es/chatbots.json

<interfaces>
<!-- Key contracts from Plan 01 and Plan 02 -->

From Plan 01 - DemoPlayground.tsx:

```typescript
interface DemoPlaygroundProps {
  activeTab: DemoPersonaId
  onTabChange: (id: DemoPersonaId) => void
}
// DemoPersonaId = 'ecommerce' | 'leadgen' | 'support'
```

From Plan 01 - PersonaSelector.tsx:

```typescript
export type DemoPersonaId = 'ecommerce' | 'leadgen' | 'support'
export const DEMO_PERSONAS: readonly ['ecommerce', 'leadgen', 'support']
```

From Plan 02 - ProgressiveCTA.tsx:

```typescript
interface ProgressiveCTAProps {
  messageCount: number
}
```

From Plan 02 - MultiPlatformShowcase.tsx:

```typescript
// Standalone section, no props required
export function MultiPlatformShowcase(): JSX.Element
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys for demo playground to EN/NL/ES chatbots.json</name>
  <files>public/locales/en/chatbots.json, public/locales/nl/chatbots.json, public/locales/es/chatbots.json</files>
  <action>
Add a `demo` section and `multi_platform` section to each locale file. Also update `hero` keys for new copy.

**EN chatbots.json — Add these new keys (merge into existing JSON):**

Update hero:

```json
"hero": {
  "badge": "Interactive Demo",
  "title": "Experience AI Chatbots in Action",
  "description": "Don't take our word for it — try them yourself. Three live demos showing what AI chatbots can do for your business.",
  "cta_primary": "Try a Demo Below",
  "cta_secondary": "Get a Free Strategy Session"
}
```

Add demo section:

```json
"demo": {
  "title": "Live Demo Playground",
  "subtitle": "Choose a scenario and start chatting. Each demo runs on the same AI engine we build for clients.",
  "capabilities_label": "This bot can:",
  "tabs": {
    "ecommerce": {
      "label": "E-commerce Advisor",
      "scenario_title": "Skincare Webshop",
      "scenario": "You're browsing a skincare webshop. The AI advisor helps you find the right products, build a routine, and answers ingredient questions.",
      "capabilities": [
        "Recommend products for your skin type",
        "Build personalized routines",
        "Explain ingredients",
        "Suggest add-to-cart items"
      ]
    },
    "leadgen": {
      "label": "Lead Qualifier",
      "scenario_title": "B2B SaaS Demo",
      "scenario": "You're exploring a B2B software platform. The AI qualifies your needs, scores your fit, and helps schedule a demo with the sales team.",
      "capabilities": [
        "Qualify your business needs",
        "Calculate ROI estimates",
        "Score lead fit",
        "Schedule product demos"
      ]
    },
    "support": {
      "label": "Support Agent",
      "scenario_title": "Helpdesk Portal",
      "scenario": "You're a customer with a question. The AI searches the knowledge base, creates tickets, and escalates to a human when needed.",
      "capabilities": [
        "Search knowledge base articles",
        "Create support tickets",
        "Check ticket status",
        "Escalate to human agents"
      ]
    }
  },
  "cta_subtle": "Like what you see? We build custom chatbots for your business.",
  "cta_subtle_link": "Learn more",
  "cta_strong": "Ready to get your own? Book a free strategy session.",
  "cta_strong_button": "Book Free Session",
  "cta_gate_title": "Demo Limit Reached",
  "cta_gate_description": "You've seen what our chatbots can do. Book a call to experience the full version built for your business.",
  "cta_gate_button": "Book a Call"
}
```

Add multi_platform section:

```json
"multi_platform": {
  "title": "One Brain, Every Channel",
  "subtitle": "Build once, deploy everywhere. The same AI serves your website, Shopify store, and WhatsApp — each with behavior tailored to the platform.",
  "platforms": {
    "website": {
      "name": "Website Widget",
      "behavior": "Education-focused, books consultations, teaches about products"
    },
    "shopify": {
      "name": "Shopify Store",
      "behavior": "Sales-focused, recommends products, builds routines, adds to cart"
    },
    "whatsapp": {
      "name": "WhatsApp Business",
      "behavior": "Quick support, order status, appointment reminders"
    }
  },
  "case_study": "Real case: SkinClarity Club uses one AI assistant across their website AND Shopify store. Same knowledge, different behavior per platform.",
  "stats": {
    "handled": "70% handled",
    "availability": "24/7",
    "platforms": "2 platforms"
  }
}
```

**NL chatbots.json — Add equivalent keys with Dutch translations:**

Update hero:

```json
"hero": {
  "badge": "Interactieve Demo",
  "title": "Ervaar AI Chatbots in Actie",
  "description": "Geloof ons niet op ons woord — probeer ze zelf. Drie live demo's die laten zien wat AI chatbots voor jouw bedrijf kunnen doen.",
  "cta_primary": "Probeer een Demo",
  "cta_secondary": "Gratis Strategiegesprek"
}
```

Add demo section with NL translations of all keys above (scenario descriptions, capabilities, CTA text).

Add multi_platform section with NL translations.

**ES chatbots.json — Add equivalent keys with Spanish translations:**

Update hero with ES translations.
Add demo section with ES translations.
Add multi_platform section with ES translations.

**Important:** Preserve ALL existing keys in the JSON files. Only add/update the listed keys. Do NOT remove existing process, pricing, use_cases, trust_metrics, faq, or final_cta keys.
</action>
<verify>
<automated>cd /c/Users/daley/Desktop/Futuremarketingai && node -e "['en','nl','es'].forEach(l => { const j = require('./public/locales/'+l+'/chatbots.json'); console.log(l+': demo='+!!j.demo+' multi_platform='+!!j.multi_platform+' hero='+!!j.hero+' use_cases='+!!j.use_cases) })"</automated>
</verify>
<done>All 3 locale files have demo, multi_platform, and updated hero keys while preserving all existing keys</done>
</task>

<task type="auto">
  <name>Task 2: Restructure ChatbotsPage and update barrel exports</name>
  <files>src/pages/ChatbotsPage.tsx, src/components/chatbot/index.ts</files>
  <action>
**ChatbotsPage.tsx — Restructure with new sections:**

1. Add imports: `DemoPlayground`, `MultiPlatformShowcase`, `ProgressiveCTA` from `../components/chatbot`. Import `DemoPersonaId` type from `../components/chatbot/PersonaSelector`.
2. Remove `ProductMedia` and `ScrollReveal` imports (Product Demo Media section replaced).
3. Add state: `const [activeTab, setActiveTab] = useState<DemoPersonaId>('ecommerce')`.
4. Import `useChatbotStore` from `../stores/chatbotStore` to read `messageCounts[activeTab]` for ProgressiveCTA.

**Hero section changes:**

- Hero badge text already reads from i18n — `t('chatbots:hero.badge')` will pick up "Interactive Demo".
- Title and description already from i18n — will pick up new copy automatically.
- Change primary CTA from `<CTAButton href="/demo">` to:
  ```tsx
  <CTAButton
    size="lg"
    arrow
    onClick={() => {
      document.getElementById('demo-playground')?.scrollIntoView({ behavior: 'smooth' })
    }}
  >
    {t('chatbots:hero.cta_primary')}
  </CTAButton>
  ```
- Keep secondary CTA with `calendly` prop as-is.

**Page section order (per CONTEXT.md):**

1. Hero (updated CTA)
2. DemoPlayground (NEW) + ProgressiveCTA below it
3. MultiPlatformShowcase (NEW)
4. Use Cases (existing, updated with scroll-to-tab)
5. Process (existing, preserved)
6. Pricing (existing, preserved)
7. Trust Metrics (existing, preserved)
8. FAQ (existing, preserved)
9. Final CTA (existing, preserved — add id="final-cta")

**Insert DemoPlayground after hero:**

```tsx
<DemoPlayground activeTab={activeTab} onTabChange={setActiveTab} />
<div className="max-w-7xl mx-auto px-12">
  <ProgressiveCTA messageCount={messageCounts[activeTab] || 0} />
</div>
```

Read messageCounts from store: `const messageCounts = useChatbotStore((s) => s.messageCounts)`.

**Insert MultiPlatformShowcase after DemoPlayground:**

```tsx
<MultiPlatformShowcase />
```

**Update use case cards to scroll to specific demo tabs:**

Create a helper function:

```typescript
const scrollToDemo = (personaId: DemoPersonaId) => {
  setActiveTab(personaId)
  // Use requestAnimationFrame to ensure React has rendered the tab change
  requestAnimationFrame(() => {
    document.getElementById('demo-playground')?.scrollIntoView({ behavior: 'smooth' })
  })
}
```

Map use case keys to demo persona IDs:

```typescript
const USE_CASE_TO_PERSONA: Record<string, DemoPersonaId | null> = {
  customer_service: 'support',
  lead_qualification: 'leadgen',
  appointment_booking: null, // No direct demo for this
  faq_automation: 'support',
}
```

Update use case card rendering: if `USE_CASE_TO_PERSONA[key]` is not null, add an `onClick` handler and `cursor-pointer` class that calls `scrollToDemo(USE_CASE_TO_PERSONA[key])`. Add a small "Try it" link text at the bottom of cards that have a matching demo.

**Remove Product Demo Media section entirely:**
Delete the ScrollReveal + ProductMedia block (lines ~340-351 in current file).

**Update Final CTA section:**
Add `id="final-cta"` to the final CTA section element (for subtle CTA link target).

**index.ts barrel exports — Add new components:**

```typescript
export { DemoPlayground } from './DemoPlayground'
export { PersonaSelector } from './PersonaSelector'
export type { DemoPersonaId } from './PersonaSelector'
export { DemoContextCard } from './DemoContextCard'
export { ProgressiveCTA } from './ProgressiveCTA'
export { MultiPlatformShowcase } from './MultiPlatformShowcase'
```

  </action>
  <verify>
    <automated>cd /c/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ChatbotsPage renders new section order, hero CTA scrolls to demo, use case cards link to demo tabs, Product Demo Media removed, barrel exports updated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete demo playground</name>
  <files>src/pages/ChatbotsPage.tsx</files>
  <action>
What was built: Complete ChatbotsPage demo playground with 3 switchable persona demos, progressive CTAs, multi-platform showcase, and full i18n (EN/NL/ES).

How to verify:

1. Run `npm run dev` and navigate to /chatbots
2. Verify hero says "Experience AI Chatbots in Action" with "Try a Demo Below" button
3. Click "Try a Demo Below" — should smooth-scroll to demo playground
4. Verify 3 tabs: E-commerce Advisor, Lead Qualifier, Support — with Lucide icons (no emojis)
5. Click each tab — verify context card updates with scenario + capabilities
6. Type a message in E-commerce tab, then switch to Lead Qualifier, then back — E-commerce conversation should be preserved
7. Scroll down to see Multi-Platform Showcase with brain node and 3 platform nodes
8. Verify SKC case study card below diagram
9. Verify use case cards (Customer Service, Lead Qualification) have "Try it" links that scroll up to the correct demo tab
10. Switch language to NL — verify all demo text translates
11. Switch language to ES — verify all demo text translates
12. Check mobile viewport — context card should stack above chat widget, tabs should wrap
    </action>
    <verify>
    <automated>cd /c/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit 2>&1 | head -10</automated>
    </verify>
    <done>User confirms demo playground renders correctly with tab switching, conversation preservation, i18n, and responsive layout</done>
    </task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Page renders in correct section order per CONTEXT.md
- Hero CTA scrolls to #demo-playground
- Use case cards trigger tab switch + scroll
- All 3 locale files have complete demo and multi_platform keys
- Barrel exports include all 5 new components
- Product Demo Media section removed
- No emojis in rendered output
</verification>

<success_criteria>

- Interactive demo playground functional with tab switching and conversation preservation
- Progressive CTAs appear at correct message thresholds
- Multi-platform diagram animates with CSS-only
- Full i18n coverage for EN, NL, ES
- Hero and use case sections link to demo playground
- Page section order matches CONTEXT.md spec exactly
  </success_criteria>

<output>
After completion, create `.planning/phases/18-chatbotspage-demo-playground/18-03-SUMMARY.md`
</output>
