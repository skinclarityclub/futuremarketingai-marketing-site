'use client'

/**
 * Re-export useReducedMotion from motion/react for convenience.
 *
 * Allows other client components to check the user's
 * prefers-reduced-motion preference.
 */
export { useReducedMotion } from 'motion/react'
