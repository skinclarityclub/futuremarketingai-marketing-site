---
phase: 01-kennisbank-infrastructuur-afronden
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/blog/geo-generative-engine-optimization.mdx
autonomous: false
requirements: [KB-01, KB-02, KB-03]

must_haves:
  truths:
    - "An MDX article exists that uses the full SKC-grade frontmatter (heroImage, readTime, keyTakeaways, faqs, citations, tableOfContents, pillar)"
    - "Visiting /[locale]/blog/{slug} renders TOC, key takeaways, FAQ and citations blocks"
    - "The article page emits valid Article JSON-LD (pillar -> Article) and FAQPage JSON-LD"
    - "npm run build succeeds with the new article included"
  artifacts:
    - path: "content/blog/geo-generative-engine-optimization.mdx"
      provides: "SKC-grade demo/seed pillar article with complete frontmatter — proves KB-03 renderer + JSON-LD"
      contains: "pillar: true"
  key_links:
    - from: "content/blog/geo-generative-engine-optimization.mdx"
      to: "src/lib/blog.ts toMeta mapper"
      via: "frontmatter fields consumed by toMeta"
      pattern: "keyTakeaways|faqs|citations|tableOfContents"
    - from: "src/app/[locale]/(blog)/blog/[slug]/page.tsx"
      to: "ArticleJsonLd + FaqJsonLd"
      via: "renders for posts with pillar/faqs"
      pattern: "ArticleJsonLd|FaqJsonLd"
---

<objective>
Prove KB-01/KB-02/KB-03 end-to-end by shipping one SKC-grade pillar article with the complete new frontmatter schema, so the rich renderer (TOC/takeaways/FAQ/citations) and the Article + FAQPage JSON-LD are demonstrably emitted. This also seeds the `/resources` hub (Plan 03) with a real pillar so it is not empty.

Purpose: The only existing MDX (`ai-marketing-automation-guide.mdx`) uses the OLD frontmatter (no keyTakeaways/faqs/citations/tableOfContents/pillar), so success criterion 3 cannot be proven with it (research Pitfall 3). This article is an infra proof plus a template for Phase 3 cornerstone content, NOT a money-page (real cornerstone = Phase 3).
Output: `content/blog/geo-generative-engine-optimization.mdx` with full frontmatter and a body that uses MDX components, plus a verified build + JSON-LD validation checkpoint.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-kennisbank-infrastructuur-afronden/01-RESEARCH.md
@src/lib/blog.ts
@content/blog/ai-marketing-automation-guide.mdx

<interfaces>
<!-- Frontmatter fields consumed by toMeta (src/lib/blog.ts). Use these exactly. -->
```typescript
interface BlogPostMeta {
  slug, title, description, author, publishedAt, updatedAt, category, tags[], locale  // base
  heroImage?: string
  readTime?: number
  keyTakeaways?: string[]
  faqs?: { question: string; answer: string }[]
  citations?: { title: string; url?: string; source?: string; year?: number }[]
  tableOfContents?: { id: string; title: string; level: 2 | 3 }[]
  pillar?: boolean          // true -> renderer emits Article (not BlogPosting) JSON-LD
  clusterOf?: string
  relatedSlugs?: string[]
  schemaType?: 'Article' | 'BlogPosting'
}
```
The renderer at src/app/[locale]/(blog)/blog/[slug]/page.tsx:
- renders TableOfContents when `tableOfContents` present (must match heading anchor ids in body)
- renders KeyTakeaways when `keyTakeaways` present
- renders BlogFaq plus emits FaqJsonLd when `faqs` present
- renders Citations when `citations` present
- emits ArticleJsonLd; `pillar: true` or `schemaType: 'Article'` produces Article type
MDX components available in body (from mdx-components.tsx): Callout, ComparisonTable,
KeyTakeaways, TableOfContents, CtaBlock, Citations, BlogFaq. The frontmatter-driven
blocks are rendered by the page, so the body should focus on prose plus Callout/CtaBlock.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SKC-grade GEO pillar article (full frontmatter)</name>
  <files>content/blog/geo-generative-engine-optimization.mdx</files>
  <action>
Create `content/blog/geo-generative-engine-optimization.mdx`. This is the infra-proof pillar article. Write in NL (NL authoritative) with `locale: "nl"`.

Frontmatter MUST include the full schema:
```yaml
---
title: "Wat is GEO? Generative Engine Optimization uitgelegd"
description: "GEO laat AI-engines zoals ChatGPT, Perplexity en Google AI Overviews jouw merk citeren. Lees wat GEO is, hoe het verschilt van SEO en hoe je begint."
author: "Daley Maat"
publishedAt: "2026-06-02"
updatedAt: "2026-06-02"
category: "geo"
tags: ["GEO", "generative engine optimization", "AI Overviews", "AI marketing"]
locale: "nl"
heroImage: "/og-image.png"
readTime: 8
pillar: true
schemaType: "Article"
relatedSlugs: []
keyTakeaways:
  - "GEO optimaliseert content zodat AI-engines jouw merk citeren, niet alleen ranken in blauwe links."
  - "Waar SEO mikt op posities, mikt GEO op vermelding in het gegenereerde antwoord."
  - "Gestructureerde data, heldere definities en citeerbare bronnen vergroten je citatiekans."
tableOfContents:
  - { id: "wat-is-geo", title: "Wat is GEO", level: 2 }
  - { id: "geo-vs-seo", title: "GEO versus SEO", level: 2 }
  - { id: "hoe-begin-je", title: "Hoe begin je met GEO", level: 2 }
faqs:
  - question: "Wat betekent GEO?"
    answer: "GEO staat voor Generative Engine Optimization: het optimaliseren van content zodat generatieve AI-engines jouw merk als bron citeren in hun antwoorden."
  - question: "Is GEO hetzelfde als SEO?"
    answer: "Nee. SEO mikt op posities in de klassieke zoekresultaten, GEO mikt op vermelding en citatie binnen het door AI gegenereerde antwoord."
citations:
  - title: "Schema.org DefinedTerm specification"
    url: "https://schema.org/DefinedTerm"
    source: "Schema.org"
    year: 2026
---
```

Body (NL, no em-dashes): use `##` headings whose anchor slugs EXACTLY match the `tableOfContents` ids (`wat-is-geo`, `geo-vs-seo`, `hoe-begin-je`). Write 600-900 words of genuine GEO explanation across the three sections. Use at least one `<Callout>` and end with one `<CtaBlock>` pointing to `/apply`. Honor key-phrase glossary ("AI Marketing Medewerker"/"Clyde", "vaardigheden", "merken"). Do NOT duplicate the frontmatter-driven blocks (KeyTakeaways/FAQ/Citations/TOC) in the body, the page renders those from frontmatter.

NOTE: the heading-anchor id generation must match the renderer's slugging. If the renderer uses rehype-slug-style lowercased-hyphenated ids, write headings like `## Wat is GEO` and confirm the produced id is `wat-is-geo` during verification; adjust either the heading text or the TOC id to match.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter'); const fs=require('fs'); const f=m(fs.readFileSync('content/blog/geo-generative-engine-optimization.mdx','utf8')); const d=f.data; ['keyTakeaways','faqs','citations','tableOfContents'].forEach(k=>{if(!d[k]||!d[k].length) throw new Error('missing '+k)}); if(d.pillar!==true) throw new Error('not pillar'); if(/—/.test(f.content)||/—/.test(JSON.stringify(d))) throw new Error('em-dash'); console.log('frontmatter OK')"</automated>
  </verify>
  <done>The MDX file has complete frontmatter (all SKC fields), pillar:true, TOC ids matching body heading anchors, no em-dashes, and a `/apply` CtaBlock.</done>
</task>

<task type="auto">
  <name>Task 2: Build + confirm renderer and JSON-LD emit</name>
  <files>content/blog/geo-generative-engine-optimization.mdx</files>
  <action>
Run `npm run build`. The new article must appear under `/nl/blog/geo-generative-engine-optimization` (generateStaticParams reads locale from frontmatter). If the build fails on the new MDX (e.g. component import, frontmatter parse), fix the article. Do NOT modify the renderer or blog.ts (KB-01/02/03 are shipped and out of scope to change).

After a green build, confirm the rendered HTML contains the rich blocks and JSON-LD. Start the production server or inspect the built output. Verify via grep on the rendered/static HTML (or by reading the page component output) that:
- an `ArticleJsonLd` script with `"@type":"Article"` is present (pillar produces Article),
- a `FaqJsonLd` script with `"@type":"FAQPage"` is present,
- the TOC, KeyTakeaways, FAQ and Citations sections render.

If `pillar:true` does not yield `"@type":"Article"`, check whether the renderer keys off `schemaType` instead. The frontmatter already sets `schemaType: "Article"` as a belt-and-braces measure, so Article should emit either way.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "Compiled successfully|✓ Compiled|geo-generative-engine-optimization|error" | Select-Object -First 20; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>`npm run build` exits 0 with the new article statically generated; rendered output contains Article + FAQPage JSON-LD and all four rich blocks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify rich render + validate JSON-LD via correct 2026 tooling</name>
  <action>Claude has shipped a SKC-grade GEO pillar article exercising the full KB-01/02/03 pipeline (complete frontmatter, rich renderer, Article + FAQPage JSON-LD) with a green build. User verifies the rendered page and validates the JSON-LD using the 2026-correct validator split below.</action>
  <what-built>
A SKC-grade GEO pillar article that exercises the full KB-01/02/03 pipeline: complete frontmatter, rich renderer (TOC/takeaways/FAQ/citations), and Article + FAQPage JSON-LD. Build is green.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000/nl/blog/geo-generative-engine-optimization
2. Confirm visually: table of contents, key takeaways box, FAQ accordion, citations list all render.
3. Copy the page's JSON-LD scripts (View Source, find `application/ld+json`).
4. Validate the Article JSON-LD via Rich Results Test (https://search.google.com/test/rich-results), expect Article eligible.
5. Validate the FAQPage JSON-LD via Schema Markup Validator (https://validator.schema.org), expect zero errors. NOTE per research: do NOT expect a FAQ rich-result in the Rich Results Test; Google deprecated FAQ rich results in 2026. FAQPage validity is checked on schema.org's validator only.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify checkpoint; automated proof is Task 2 build + JSON-LD presence grep</automated>
  </verify>
  <done>User confirms all four rich blocks render and both JSON-LD scripts validate (Article via Rich Results Test, FAQPage via Schema Markup Validator).</done>
  <resume-signal>Type "approved" or describe what failed (broken block, JSON-LD error)</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with the article statically generated for `nl`.
- Article JSON-LD validates as Article (Rich Results Test).
- FAQPage JSON-LD validates with zero errors (Schema Markup Validator, NOT Rich Results Test FAQ appearance).
- All four rich blocks render on the page.
- No em-dashes in copy.
</verification>

<success_criteria>
- KB-01/KB-02/KB-03 proven end-to-end via a real article using the full schema.
- Success criterion 3 of the phase is demonstrably met (rich render + valid Article + FAQPage JSON-LD, validated per the 2026-correct tooling split).
- `/resources` hub (Plan 03) has at least one pillar post to display.
</success_criteria>

<output>
After completion, create `.planning/phases/01-kennisbank-infrastructuur-afronden/01-02-SUMMARY.md`
</output>
