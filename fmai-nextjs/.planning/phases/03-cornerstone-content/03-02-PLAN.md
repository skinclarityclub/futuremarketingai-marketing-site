---
phase: 03-cornerstone-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/blog/ai-marketing-automation-voor-bureaus.mdx
autonomous: true
requirements: [CONT-05]
must_haves:
  truths:
    - "De pillar-gids 'AI marketing automation voor bureaus' is 1500-3000 woorden, NL, category ai-marketing-automation, pillar:true"
    - "De pagina rendert TOC, KeyTakeaways, FAQ en Citations en emit valide Article + FAQPage JSON-LD"
    - "Elke tableOfContents.id resolveert naar een <h2 id> in de body"
    - "De pillar bevat precies één CtaBlock naar /apply"
    - "Positionering volgt de glossary: AI Marketing Medewerker / Clyde / vaardigheden / merken (nooit AI tool / features / klanten)"
  artifacts:
    - path: "content/blog/ai-marketing-automation-voor-bureaus.mdx"
      provides: "Verse NL pillar-gids voor het ai-marketing-automation keyword"
      contains: "pillar: true"
  key_links:
    - from: "content/blog/ai-marketing-automation-voor-bureaus.mdx"
      to: "tableOfContents <h2 id> anchors"
      via: "explicit <h2 id> headings matching tableOfContents ids"
      pattern: "<h2 id="
---

<objective>
Schrijf een verse NL pillar-gids `content/blog/ai-marketing-automation-voor-bureaus.mdx` op het non-branded keyword "AI marketing automation voor bureaus" (CONT-05). Het bestaande `ai-marketing-automation-guide.mdx` is oud EN-schema (locale en, category ai-marketing, geen pillar) en wordt NIET hergebruikt of aangepast — vers schrijven op het exacte target-keyword voorkomt duplicate-intent (03-RESEARCH Pitfall 3). Laat het oude bestand ongemoeid.

Purpose: pillar-autoriteit op het ai-marketing-automation cluster, anker voor de Clyde-cluster (CONT-06) en de comparison money-page (CONT-07), beide hangen aan deze pillar-slug.
Output: één verse SKC-grade NL MDX-pillar met volledige frontmatter + body, gegrond in de huidige FMai-positionering.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cornerstone-content/03-RESEARCH.md
@content/blog/geo-generative-engine-optimization.mdx
@src/lib/blog.ts
@src/lib/pricing-data.ts
@src/lib/glossary.ts

<interfaces>
<!-- Mirror the canonical template's shape verbatim. -->
Callout / CtaBlock / ComparisonTable props — see 03-01-PLAN interfaces (do NOT drift; 03-RESEARCH Pitfall 4).
Frontmatter object-arrays = block-YAML only. Headings = explicit `<h2 id>`. Renderer emits TOC/takeaways/FAQ/citations from frontmatter — never duplicate in body.
Slug = filename = URL. This pillar's slug `ai-marketing-automation-voor-bureaus` is the clusterOf target for CONT-06 and CONT-07 — keep it stable.
Glossary "AI Marketing Medewerker" definition lives in src/lib/glossary.ts — reuse its framing, don't contradict.
Pricing: any tier/price reference must match src/lib/pricing-data.ts (Founding 997 fixed, Growth 499/ws, Pro 399/ws, Ent 299/ws). Do NOT hardcode divergent numbers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author frontmatter for the AI-automation pillar</name>
  <files>content/blog/ai-marketing-automation-voor-bureaus.mdx</files>
  <action>
Create the file with full block-YAML frontmatter mirroring the GEO pillar shape:
- `title`: target keyword, NL, no em-dash (e.g. "AI marketing automation voor bureaus: de complete gids").
- `description`: 1-2 zinnen, keyword-rijk, no em-dash.
- `author: "Daley Maat"`, `publishedAt: "2026-06-02"`, `updatedAt: "2026-06-02"`.
- `category: "ai-marketing-automation"` (hub-bucket id — required for hub visibility).
- `tags`: ["AI marketing automation", "bureaus", "AI Marketing Medewerker", "Clyde"].
- `locale: "nl"`, `heroImage: "/og-image.png"`, `readTime: 12`.
- `pillar: true`, `schemaType: "Article"`, `relatedSlugs: ["wat-is-een-ai-marketing-medewerker", "clyde-vs-jasper-chatgpt-semrush"]`.
- `keyTakeaways`: 4-5 quotable claims framing automation als een AI Marketing Medewerker (Clyde) die per merk/klantportfolio werkt, vaardigheden levert, en zonder plafond voor zover infra reikt schaalt.
- `tableOfContents`: 5-6 block-YAML entries; suggested ids: `wat-is-ai-marketing-automation`, `waarom-bureaus`, `ai-marketing-medewerker-vs-tools`, `vaardigheden`, `aan-de-slag`, `wat-levert-het-op`.
- `faqs`: 4-5 Q&A (drives FAQPage). Bv. "Wat is het verschil tussen een AI Marketing Medewerker en een AI-tool?", "Werkt dit per merk of per account?", "Hoe begin ik als bureau?".
- `citations`: 3-5 bronnen. Mag mix van FMai-positionering + branche-context; gebruik echte URLs (geen verzonnen). Indien geen sterke externe bron beschikbaar, houd citations bescheiden (3) maar valide.
NO em-dashes (scan U+2014). Glossary terms enforced.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/ai-marketing-automation-voor-bureaus.mdx','utf8'));if(d.data.pillar!==true)throw'not pillar';if(d.data.category!=='ai-marketing-automation')throw'bad category';if(d.data.locale!=='nl')throw'bad locale';if((d.data.faqs||[]).length<4)throw'faqs<4';if((d.data.tableOfContents||[]).length<5)throw'toc<5';if(d.content.includes(String.fromCharCode(8212)))throw'em-dash';console.log('OK')"</automated>
  </verify>
  <done>File exists with pillar:true, category ai-marketing-automation, locale nl, 4-5 takeaways, 5-6 TOC, 4-5 FAQs, valid citations, no em-dash.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 1500-3000 word body</name>
  <files>content/blog/ai-marketing-automation-voor-bureaus.mdx</files>
  <action>
Write the MDX body, 1500-3000 words, each section an explicit `<h2 id>` matching the TOC ids:
- `wat-is-ai-marketing-automation`: definieer voor bureaus; antwoord-eerst in de eerste 2 zinnen (GEO-tactiek).
- `waarom-bureaus`: schaal over meerdere merken/klantportfolio's, consistente toon, geen evenredige headcount-groei.
- `ai-marketing-medewerker-vs-tools`: positioneer Clyde als AI Marketing Medewerker (geen losse tool); leer/train-framing, hosted dashboard waar bureaus op inloggen (NIET on-site install). Link in-body met locale-prefix naar de Clyde-cluster `/nl/blog/wat-is-een-ai-marketing-medewerker` en naar de comparison `/nl/blog/clyde-vs-jasper-chatgpt-semrush`.
- `vaardigheden`: noem de vaardigheden-categorieën (content, GEO/SEO, lead-kwalificatie, voice, reporting) zonder pricing-claims te verzinnen; verwijs naar de tiers conceptueel, exacte prijzen alleen indien uit pricing-data.ts.
- `aan-de-slag`: concrete eerste stappen voor een bureau.
- `wat-levert-het-op`: ROI/uitkomst-framing, zacht onderbouwd.
Add at most one `<Callout>`. Exactly ONE `<CtaBlock title=... body=... label="Plan een gesprek" href="/apply" />` at the end. No duplicated frontmatter components in the body. In-body cross-links locale-prefixed raw `/nl/blog/<slug>`.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/ai-marketing-automation-voor-bureaus.mdx','utf8'));const w=d.content.replace(/<[^>]+>/g,' ').trim().split(/\s+/).length;if(w<1500)throw'words '+w+' <1500';const cta=(d.content.match(/<CtaBlock/g)||[]).length;if(cta!==1)throw'CtaBlock '+cta;const ids=(d.data.tableOfContents||[]).map(t=>t.id);for(const id of ids){if(!d.content.includes('id=\"'+id+'\"'))throw'missing anchor '+id}console.log('OK words='+w)"</automated>
  </verify>
  <done>Body 1500-3000 words, every TOC id has matching `<h2 id>`, exactly one /apply CtaBlock, in-body links to Clyde-cluster + comparison present, glossary terms honored.</done>
</task>

<task type="auto">
  <name>Task 3: Build + JSON-LD + mobile proof</name>
  <files>content/blog/ai-marketing-automation-voor-bureaus.mdx</files>
  <action>
Run `npm run build`; confirm the new article is statically generated for `nl`. Run the testing-playbook mobile flow on `/nl/blog/ai-marketing-automation-voor-bureaus`: TOC anchors scroll, takeaways/FAQ/citations render, single CTA routes to `/nl/apply`. Capture the emitted Article + FAQPage JSON-LD for external validation (Rich Results Test for Article, Schema Markup Validator for FAQPage). Confirm no CWV regression (no heavy assets).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "compiled successfully|✓ Compiled|generating static pages|error" | tail -20</automated>
  </verify>
  <done>Build passes; page renders on mobile with one /apply CTA; Article + FAQPage JSON-LD present and recorded.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; `/nl/blog/ai-marketing-automation-voor-bureaus` generated.
- Body 1500-3000 words; TOC ids match `<h2 id>`; one /apply CTA; no em-dashes; glossary terms.
- Article + FAQPage JSON-LD emitted.
- category ai-marketing-automation + pillar:true → will appear in the ai-marketing-automation hub bucket (verified in 03-07).
</verification>

<success_criteria>
"AI marketing automation voor bureaus" pillar live, 1500-3000w, full schema, one /apply CTA, on-brand positioning (Phase Success Criterion 1).
</success_criteria>

<output>
After completion, create `.planning/phases/03-cornerstone-content/03-02-SUMMARY.md`
</output>
