---
phase: 03-cornerstone-content
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx
autonomous: true
requirements: [CONT-04]
must_haves:
  truths:
    - "De cluster 'GEO monitoring tools voor ChatGPT en Perplexity' is live, NL, category geo, clusterOf de GEO-pillar"
    - "De cluster bevat een ComparisonTable met de tool-matrix (kolommen = tools, rijen = capabilities)"
    - "De pagina rendert TOC/takeaways/FAQ/citations en emit valide Article + FAQPage JSON-LD"
    - "De cluster linkt in-body terug naar de GEO-pillar en heeft precies één CtaBlock naar /apply"
    - "Tool-namen en capabilities komen uit 03-RESEARCH §3, met gedateerde caption op de tabel"
  artifacts:
    - path: "content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx"
      provides: "GEO monitoring tools cluster met ComparisonTable"
      contains: "<ComparisonTable"
  key_links:
    - from: "content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx"
      to: "content/blog/geo-generative-engine-optimization.mdx"
      via: "in-body locale-prefixed prose link"
      pattern: "/nl/blog/geo-generative-engine-optimization"
---

<objective>
Schrijf de NL cluster `content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx` (CONT-04) onder de GEO-pillar, met een `ComparisonTable` die het GEO-monitoring-tool-landschap vergelijkt. Cluster-post: `category: geo`, `clusterOf: "geo-generative-engine-optimization"`, `schemaType: "BlogPosting"`, ~900-1500 woorden.

Purpose: een citeerbare, gedateerde tool-overzichtspagina die op de "GEO monitoring tools"-query mikt en de meet-cluster aanvult.
Output: één verse SKC-grade NL MDX-cluster met ComparisonTable, gegrond in 03-RESEARCH §3.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cornerstone-content/03-RESEARCH.md
@content/blog/geo-generative-engine-optimization.mdx
@src/components/blog/ComparisonTable.tsx
@src/lib/blog.ts

<interfaces>
ComparisonTable: columns: string[]  rows: {label: string, values: (string|boolean)[]}[]  highlightColumn?: number  caption?: string
  - First conceptual column is the row label (no header). Booleans → check/dash. Values per row align to columns.
  - MDX-body inline-object arrays are JSX expressions (parse fine) — block-YAML rule applies ONLY to frontmatter.
Callout / CtaBlock — see 03-01-PLAN. Renderer emits TOC/takeaways/FAQ/citations from frontmatter — never duplicate in body. Headings = explicit `<h2 id>`. In-body links locale-prefixed `/nl/blog/<slug>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author frontmatter + body sections</name>
  <files>content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx</files>
  <action>
Create with full block-YAML frontmatter: title "GEO monitoring tools voor ChatGPT en Perplexity" (no em-dash), description, author "Daley Maat", dates "2026-06-02", `category: "geo"`, tags ["GEO tools","ChatGPT","Perplexity","monitoring"], `locale: "nl"`, heroImage "/og-image.png", readTime ~8, `clusterOf: "geo-generative-engine-optimization"`, `schemaType: "BlogPosting"`, relatedSlugs ["geo-generative-engine-optimization","zichtbaarheid-meten-ai-overviews"], 3-4 keyTakeaways, 4-5 TOC (ids match body), 4-5 FAQs, 4-6 citations from §3 (Profound tryprofound.com, Peec peec.ai, AthenaHQ athenahq.ai, Otterly otterly.ai, SE Ranking seranking.com, Conductor conductor.com, Semrush).
Body 900-1500 words, explicit `<h2 id>` sections, e.g.: `waarom-geo-monitoring`, `categorieen-tools` (enterprise/independent vs mid-market vs SEO-suites), `de-tools-vergeleken` (houses the ComparisonTable), `welke-tool-kiezen`. Ground from §3 prose. Link in-body to the pillar `/nl/blog/geo-generative-engine-optimization` and to the meet-cluster `/nl/blog/zichtbaarheid-meten-ai-overviews`. NO em-dashes, glossary terms.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx','utf8'));if(d.data.clusterOf!=='geo-generative-engine-optimization')throw'clusterOf';if(d.data.category!=='geo')throw'cat';if((d.data.faqs||[]).length<4)throw'faqs';if(d.content.includes(String.fromCharCode(8212)))throw'em-dash';const ids=(d.data.tableOfContents||[]).map(t=>t.id);for(const id of ids){if(!d.content.includes('id=\"'+id+'\"'))throw'anchor '+id}console.log('OK')"</automated>
  </verify>
  <done>File exists with clusterOf GEO pillar, category geo, full schema, TOC ids resolve, no em-dash.</done>
</task>

<task type="auto">
  <name>Task 2: Add the tool-comparison ComparisonTable + CTA</name>
  <files>content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx</files>
  <action>
In the `de-tools-vergeleken` section add ONE `<ComparisonTable>` (MDX-body JSX, inline objects are fine here):
- `columns`: pick 4-5 tools from §3, e.g. ["Profound", "Peec AI", "Otterly", "Semrush"].
- `rows`: capabilities from §3 — "ChatGPT tracking", "Perplexity tracking", "Google AI Overviews tracking", "Citatiegraad", "AI share of voice", "Prompt-volume data", "Multi-language". Use booleans where binary, strings where nuanced. Only assert what §3 supports; where uncertain use a string like "Deels" rather than a false boolean claim (defensible-claims rule, 03-RESEARCH Pitfall 5).
- `caption`: "Op basis van publieke productinformatie, juni 2026." (gedateerd).
- No `highlightColumn` (Clyde is not a GEO-monitoring tool; this is a neutral comparison).
Confirm exactly ONE `<CtaBlock title=... body=... label="Plan een gesprek" href="/apply" />` at the end. Body total stays 900-1500 words. No duplicated frontmatter components.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx','utf8'));if((d.content.match(/<ComparisonTable/g)||[]).length!==1)throw'table count';if((d.content.match(/<CtaBlock/g)||[]).length!==1)throw'cta';if(!/caption=/.test(d.content))throw'no caption';if(!d.content.includes('/nl/blog/geo-generative-engine-optimization'))throw'no pillar link';const w=d.content.replace(/<[^>]+>/g,' ').trim().split(/\s+/).length;if(w<900)throw'words '+w;console.log('OK '+w)"</automated>
  </verify>
  <done>Exactly one ComparisonTable with dated caption + neutral tool matrix, one /apply CTA, in-body pillar link, 900-1500w.</done>
</task>

<task type="auto">
  <name>Task 3: Build + mobile + JSON-LD proof</name>
  <files>content/blog/geo-monitoring-tools-chatgpt-perplexity.mdx</files>
  <action>
Run `npm run build`; confirm the article generates for `nl`. Run testing-playbook mobile on `/nl/blog/geo-monitoring-tools-chatgpt-perplexity`: ComparisonTable renders + horizontally scrolls on mobile, TOC/takeaways/FAQ/citations render, one /apply CTA. Capture Article + FAQPage JSON-LD for external validation. Confirm CWV neutral.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "compiled successfully|✓ Compiled|generating static pages|error" | tail -20</automated>
  </verify>
  <done>Build passes; page renders on mobile with table + one /apply CTA; JSON-LD recorded.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; cluster URL generated for nl.
- clusterOf GEO pillar, category geo, one ComparisonTable (dated caption), one /apply CTA, in-body pillar link, TOC ids resolve, grounded tool data, no em-dash.
- Appears under GEO bucket in /resources (verified 03-07).
</verification>

<success_criteria>
"GEO monitoring tools voor ChatGPT en Perplexity" cluster live with ComparisonTable, linked to the pillar (Phase Success Criterion 2).
</success_criteria>

<output>
After completion, create `.planning/phases/03-cornerstone-content/03-04-SUMMARY.md`
</output>
