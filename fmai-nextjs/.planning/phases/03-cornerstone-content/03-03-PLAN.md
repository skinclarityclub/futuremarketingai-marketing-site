---
phase: 03-cornerstone-content
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - content/blog/geo-vs-seo-waar-investeren-2026.mdx
  - content/blog/zichtbaarheid-meten-ai-overviews.mdx
autonomous: true
requirements: [CONT-02, CONT-03]
must_haves:
  truths:
    - "De cluster 'GEO vs SEO: waar investeren in 2026' is live, NL, category geo, clusterOf de GEO-pillar"
    - "De cluster 'Hoe meet je zichtbaarheid in AI Overviews' is live, NL, category geo, clusterOf de GEO-pillar"
    - "Beide clusters renderen TOC/takeaways/FAQ/citations, emit valide Article + FAQPage JSON-LD"
    - "Beide clusters linken in-body terug naar de GEO-pillar (CONT-08 wiring deels)"
    - "Elke cluster heeft precies één CtaBlock naar /apply; GEO-stats zijn gegrond + geciteerd"
  artifacts:
    - path: "content/blog/geo-vs-seo-waar-investeren-2026.mdx"
      provides: "GEO vs SEO cluster"
      contains: "clusterOf: \"geo-generative-engine-optimization\""
    - path: "content/blog/zichtbaarheid-meten-ai-overviews.mdx"
      provides: "AI Overviews meet-cluster"
      contains: "clusterOf: \"geo-generative-engine-optimization\""
  key_links:
    - from: "content/blog/geo-vs-seo-waar-investeren-2026.mdx"
      to: "content/blog/geo-generative-engine-optimization.mdx"
      via: "in-body locale-prefixed prose link /nl/blog/geo-generative-engine-optimization"
      pattern: "/nl/blog/geo-generative-engine-optimization"
    - from: "content/blog/zichtbaarheid-meten-ai-overviews.mdx"
      to: "content/blog/geo-generative-engine-optimization.mdx"
      via: "in-body locale-prefixed prose link"
      pattern: "/nl/blog/geo-generative-engine-optimization"
---

<objective>
Schrijf twee NL GEO-clusters die onder de GEO-pillar hangen (CONT-02 + CONT-03). Beide zijn cluster-posts: `category: geo`, `clusterOf: "geo-generative-engine-optimization"`, `schemaType: "BlogPosting"`. Korter dan een pillar mag, maar substantieel (richt op 900-1500 woorden). Beide linken in-body terug naar de pillar (CONT-08 wiring half).

Purpose: de GEO-pillar verdiepen met question-level clusters die AI-engines kunnen citeren op concrete vragen.
Output: twee verse SKC-grade NL MDX cluster-bestanden, gegrond in 03-RESEARCH §1 (vs SEO) en §2 (meten).
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cornerstone-content/03-RESEARCH.md
@content/blog/geo-generative-engine-optimization.mdx
@src/lib/blog.ts

<interfaces>
Callout / CtaBlock props — see 03-01-PLAN (no drift). Frontmatter = block-YAML; headings = explicit `<h2 id>`; renderer emits TOC/takeaways/FAQ/citations — never duplicate in body.
Cluster identity: `clusterOf: "geo-generative-engine-optimization"` makes it group under the GEO pillar in the /resources hub. `category: "geo"` required (hub bucket).
In-body cross-links are raw MDX links → must be locale-prefixed `/nl/blog/<slug>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CONT-02 — GEO vs SEO: waar investeren in 2026</name>
  <files>content/blog/geo-vs-seo-waar-investeren-2026.mdx</files>
  <action>
Create with full block-YAML frontmatter: title "GEO vs SEO: waar moet je in 2026 in investeren?" (no em-dash), keyword-rich description, author "Daley Maat", publishedAt/updatedAt "2026-06-02", `category: "geo"`, tags ["GEO","SEO","2026"], `locale: "nl"`, heroImage "/og-image.png", readTime ~8, `clusterOf: "geo-generative-engine-optimization"`, `schemaType: "BlogPosting"`, relatedSlugs ["geo-generative-engine-optimization"], 3-4 keyTakeaways, 4-5 TOC entries (ids matching body `<h2 id>`), 4-5 FAQs, 4-5 citations from 03-RESEARCH §1/§3 (Aggarwal GEO paper, Search Engine Land GEO-metrics 476642, Gartner ~25% decline, zero-click ~64.8%, Mabbly GEO-vs-SEO).
Body 900-1500 words, explicit `<h2 id>` sections. Ground from §1: SEO=rankings/clicks vs GEO=citations/mentions; GEO extends not replaces SEO; metric-shift; behavioral shift (zero-click, AIO CTR-daling). Frame the investment answer: blijf SEO-fundament behouden, voeg GEO toe. Link in-body to the pillar `/nl/blog/geo-generative-engine-optimization` and to the meet-cluster `/nl/blog/zichtbaarheid-meten-ai-overviews`. Exactly ONE `<CtaBlock ... href="/apply" label="Plan een gesprek" />`. No em-dashes, glossary terms.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/geo-vs-seo-waar-investeren-2026.mdx','utf8'));if(d.data.clusterOf!=='geo-generative-engine-optimization')throw'clusterOf';if(d.data.category!=='geo')throw'cat';const w=d.content.replace(/<[^>]+>/g,' ').trim().split(/\s+/).length;if(w<900)throw'words '+w;if((d.content.match(/<CtaBlock/g)||[]).length!==1)throw'cta';if(!d.content.includes('/nl/blog/geo-generative-engine-optimization'))throw'no pillar link';if(d.content.includes(String.fromCharCode(8212)))throw'em-dash';const ids=(d.data.tableOfContents||[]).map(t=>t.id);for(const id of ids){if(!d.content.includes('id=\"'+id+'\"'))throw'anchor '+id}console.log('OK '+w)"</automated>
  </verify>
  <done>CONT-02 cluster file exists, clusterOf GEO pillar, 900-1500w, one /apply CTA, in-body pillar link, TOC ids resolve, grounded citations, no em-dash.</done>
</task>

<task type="auto">
  <name>Task 2: Author CONT-03 — Hoe meet je zichtbaarheid in AI Overviews</name>
  <files>content/blog/zichtbaarheid-meten-ai-overviews.mdx</files>
  <action>
Create with full block-YAML frontmatter: title "Hoe meet je zichtbaarheid in AI Overviews?" (no em-dash), description, author "Daley Maat", dates "2026-06-02", `category: "geo"`, tags ["AI Overviews","GEO meten","citatiegraad"], `locale: "nl"`, heroImage "/og-image.png", readTime ~8, `clusterOf: "geo-generative-engine-optimization"`, `schemaType: "BlogPosting"`, relatedSlugs ["geo-generative-engine-optimization","geo-monitoring-tools-chatgpt-perplexity"], 3-4 keyTakeaways, 4-5 TOC (ids match body), 4-5 FAQs, 4-5 citations from §2 (Conductor brand-mentions vs citations, Siftly/Profound/Peec, Semrush Position Tracking, Waikay non-determinisme run 5-10x, CJR citatie-accuratesse).
Body 900-1500 words, explicit `<h2 id>` sections. Ground from §2: brand mentions vs website citations; kern-metrics (citatiegraad, AI share of voice, AI-impressies, AI-referred verkeer via GA4 referrer-filtering); per-engine aanpak (AIO 3-5 bronnen + GSC+GA4-triangulatie, ChatGPT inconsistent → brand mentions + simulatie, Perplexity rijk → beste voor experimenten); method-caveats (run prompts 5-10x, continu monitoren). Link in-body to the pillar `/nl/blog/geo-generative-engine-optimization` and to the tools-cluster `/nl/blog/geo-monitoring-tools-chatgpt-perplexity`. Exactly ONE `<CtaBlock ... href="/apply" />`. No em-dashes, glossary terms.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/zichtbaarheid-meten-ai-overviews.mdx','utf8'));if(d.data.clusterOf!=='geo-generative-engine-optimization')throw'clusterOf';if(d.data.category!=='geo')throw'cat';const w=d.content.replace(/<[^>]+>/g,' ').trim().split(/\s+/).length;if(w<900)throw'words '+w;if((d.content.match(/<CtaBlock/g)||[]).length!==1)throw'cta';if(!d.content.includes('/nl/blog/geo-generative-engine-optimization'))throw'no pillar link';if(d.content.includes(String.fromCharCode(8212)))throw'em-dash';const ids=(d.data.tableOfContents||[]).map(t=>t.id);for(const id of ids){if(!d.content.includes('id=\"'+id+'\"'))throw'anchor '+id}console.log('OK '+w)"</automated>
  </verify>
  <done>CONT-03 cluster file exists, clusterOf GEO pillar, 900-1500w, one /apply CTA, in-body pillar + tools-cluster links, TOC ids resolve, grounded citations, no em-dash.</done>
</task>

<task type="auto">
  <name>Task 3: Build + mobile + JSON-LD proof (both clusters)</name>
  <files>content/blog/geo-vs-seo-waar-investeren-2026.mdx, content/blog/zichtbaarheid-meten-ai-overviews.mdx</files>
  <action>
Run `npm run build`; confirm both new articles statically generate for `nl`. Run testing-playbook mobile on both URLs (`/nl/blog/geo-vs-seo-waar-investeren-2026`, `/nl/blog/zichtbaarheid-meten-ai-overviews`): TOC scroll, takeaways/FAQ/citations render, one /apply CTA each. Capture Article + FAQPage JSON-LD for external validation. Confirm CWV neutral.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "compiled successfully|✓ Compiled|generating static pages|error" | tail -20</automated>
  </verify>
  <done>Build passes; both clusters render on mobile with one /apply CTA each; JSON-LD recorded.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; both cluster URLs generated for nl.
- Both: clusterOf GEO pillar, category geo, one /apply CTA, in-body back-link to pillar, TOC ids resolve, grounded citations, no em-dash.
- Both will appear under the GEO bucket in /resources (verified 03-07).
</verification>

<success_criteria>
Two GEO clusters (GEO vs SEO, meten in AI Overviews) live, linked to the pillar (Phase Success Criterion 2).
</success_criteria>

<output>
After completion, create `.planning/phases/03-cornerstone-content/03-03-SUMMARY.md`
</output>
