---
phase: 03-cornerstone-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/blog/geo-generative-engine-optimization.mdx
autonomous: true
requirements: [CONT-01]
must_haves:
  truths:
    - "De GEO-pillargids is 1500-3000 woorden en beantwoordt wat GEO is, GEO vs SEO, meten, tactieken en hoe te beginnen"
    - "De pagina rendert TOC, KeyTakeaways, FAQ-accordion en Citations, en emit valide Article + FAQPage JSON-LD"
    - "Elke tableOfContents.id resolveert naar een <h2 id> in de body"
    - "De pillar bevat precies één CtaBlock naar /apply"
    - "GEO-statistieken en claims komen uit de gegronde bronnen in 03-RESEARCH.md, geciteerd in citations"
  artifacts:
    - path: "content/blog/geo-generative-engine-optimization.mdx"
      provides: "Verdiepte GEO pillar-gids, pillar:true, category geo"
      contains: "pillar: true"
  key_links:
    - from: "content/blog/geo-generative-engine-optimization.mdx"
      to: "tableOfContents <h2 id> anchors"
      via: "explicit <h2 id> headings matching tableOfContents ids"
      pattern: "<h2 id="
---

<objective>
Verdiep de bestaande GEO pillar-gids (`content/blog/geo-generative-engine-optimization.mdx`) van ~3 secties naar 1500-3000 woorden zodat hij de pillar-bar haalt en als hub van de GEO-cluster fungeert (CONT-01). Het bestand bestaat al met volledige frontmatter en is de canonieke template; dit plan herschrijft het NIET, maar breidt het uit.

Purpose: directe topical authority op het non-branded kern-keyword "wat is GEO / generative engine optimization", NL authoritative, SKC-grade, met citeerbare passages die AI-engines kunnen aanhalen.
Output: één verdiepte MDX-pillar met uitgebreide TOC, takeaways, FAQ en citations, gegrond in de live-research.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cornerstone-content/03-RESEARCH.md
@content/blog/geo-generative-engine-optimization.mdx
@src/lib/blog.ts

<interfaces>
<!-- MDX componentmap props — verified from source, do NOT drift (03-RESEARCH Pitfall 4). -->
Callout:        variant?: 'info'|'warning'|'tip'|'success'  title?: string  children
CtaBlock:       title: string  body?: string  href?: string (default /apply)  label: string
ComparisonTable: columns: string[]  rows: {label, values:(string|boolean)[]}[]  highlightColumn?: number  caption?: string

Frontmatter object-arrays MUST be block-YAML (never inline flow `[{..}]`) — @next/mdx breaks otherwise (03-RESEARCH anti-pattern).
Headings need explicit `<h2 id="...">` — no rehype-slug. tableOfContents.id must match verbatim.
The page renderer emits TOC/KeyTakeaways/BlogFaq/Citations from frontmatter — NEVER duplicate those components in the body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand GEO pillar frontmatter (TOC, takeaways, FAQ, citations)</name>
  <files>content/blog/geo-generative-engine-optimization.mdx</files>
  <action>
Update the frontmatter (keep title/description/author/category/locale/pillar/schemaType as-is; bump `updatedAt` to today's date "2026-06-02"):
- `readTime`: raise to a realistic value for 1500-3000w (e.g. 11-13).
- `keyTakeaways`: keep 3, add 1-2 more so there are 4-5 concise, quotable claims (AI-citation anchors). Use grounded framing from 03-RESEARCH §1 (e.g. front-load answers, GEO favors earned media + citeerbare content, GEO extends SEO).
- `tableOfContents`: expand to 5-6 entries matching the new body sections below. Block-YAML, each `id` matching an `<h2 id>`. Suggested ids: `wat-is-geo`, `geo-vs-seo`, `hoe-meet-je-geo`, `geo-tactieken`, `geo-tools-landschap`, `hoe-begin-je`.
- `faqs`: expand from 2 to 4-5 Q&A (drives FAQPage JSON-LD). Grounded: "Vervangt GEO mijn SEO?", "Welke AI-engines tellen voor GEO?", "Hoe meet je of GEO werkt?", "Hoeveel levert GEO op?" (use ~35-40% visibility-lift soft phrasing from §1).
- `citations`: replace the single Schema.org entry with 4-6 grounded sources from 03-RESEARCH Sources: Aggarwal et al. GEO paper (arxiv 2311.09735, 2023), Search Engine Land GEO 2026 guide, CXL front-loading citation data, BrightEdge AI Overview presence, an earned-media-bias source (arxiv 2509.08919). Use {title, url, source, year} block-YAML.
NO em-dashes (scan U+2014). Use "AI Marketing Medewerker"/"Clyde"/"vaardigheden"/"merken" per glossary.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/geo-generative-engine-optimization.mdx','utf8'));if(!d.data.pillar)throw'not pillar';if((d.data.faqs||[]).length<4)throw'faqs<4';if((d.data.tableOfContents||[]).length<5)throw'toc<5';if((d.data.citations||[]).length<4)throw'citations<4';if(d.content.includes(String.fromCharCode(8212)))throw'em-dash';console.log('OK')"</automated>
  </verify>
  <done>Frontmatter has 4-5 takeaways, 5-6 TOC entries, 4-5 FAQs, 4-6 grounded citations; no em-dash; pillar:true preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen the body to 1500-3000 words with grounded sections</name>
  <files>content/blog/geo-generative-engine-optimization.mdx</files>
  <action>
Expand the MDX body so the article totals 1500-3000 words. Keep the existing 3 sections and ADD 2-3 sections, each as an explicit `<h2 id="...">` whose id matches the frontmatter TOC. Use the grounded facts from 03-RESEARCH §1-§3 (phrase stats softly: "ongeveer", "rond"):
- `hoe-meet-je-geo`: brand mentions vs website citations, citatiegraad, AI share of voice, AI-referred verkeer (GA4 referrer-filtering perplexity.ai / chat.openai.com). Link in-body naar de meet-cluster met locale-prefix: `/nl/blog/zichtbaarheid-meten-ai-overviews`.
- `geo-tactieken`: front-load answers (≈55% AIO-citaties uit top 30% van de pagina; ≈44% ChatGPT-citaties uit eerste 30%), standalone FAQ-blokken, gestructureerde data/entiteiten, earned media (~84% van AI-citaties uit earned media → digital PR, niet alleen on-site).
- `geo-tools-landschap`: kort overzicht van het tool-landschap (Profound, Peec AI, Otterly, Semrush) en link in-body naar de tools-cluster `/nl/blog/geo-monitoring-tools-chatgpt-perplexity` en naar de GEO-vs-SEO-cluster `/nl/blog/geo-vs-seo-waar-investeren-2026`.
Add at most ONE extra `<Callout>` if it strengthens a section (the file already has one). Keep exactly ONE `<CtaBlock href="/apply">` at the end (already present — do not add a second). Do NOT add `<KeyTakeaways>`/`<TableOfContents>`/`<BlogFaq>`/`<Citations>` in the body (renderer emits them). In-body cross-links must be locale-prefixed raw links `/nl/blog/<slug>` (they are not next-intl Links).
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/geo-generative-engine-optimization.mdx','utf8'));const w=d.content.replace(/<[^>]+>/g,' ').trim().split(/\s+/).length;if(w<1500)throw'words '+w+' <1500';const cta=(d.content.match(/<CtaBlock/g)||[]).length;if(cta!==1)throw'CtaBlock count '+cta;const ids=(d.data.tableOfContents||[]).map(t=>t.id);for(const id of ids){if(!d.content.includes('id=\"'+id+'\"'))throw'missing anchor '+id}console.log('OK words='+w)"</automated>
  </verify>
  <done>Body is 1500-3000 words, every TOC id has a matching `<h2 id>`, exactly one CtaBlock, in-body links to the 3 GEO clusters present, no duplicated frontmatter components.</done>
</task>

<task type="auto">
  <name>Task 3: Build + JSON-LD + mobile proof</name>
  <files>content/blog/geo-generative-engine-optimization.mdx</files>
  <action>
Run `npm run build` and confirm the article is statically generated for `nl` with no errors. Open the page in the testing-playbook mobile flow (`/nl/blog/geo-generative-engine-optimization`) to confirm TOC anchors scroll, KeyTakeaways/FAQ/Citations render, and the single CTA routes to `/nl/apply`. Inspect the page source for the emitted Article + FAQPage JSON-LD blocks; record the JSON-LD so it can be pasted into Google Rich Results Test (Article) and Schema Markup Validator (FAQPage) — FAQ rich result is deprecated 2026, validate syntax not appearance. Confirm no heavy hero asset was added (CWV neutral).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "compiled successfully|✓ Compiled|generating static pages|error" | tail -20</automated>
  </verify>
  <done>Build passes; page renders with TOC/takeaways/FAQ/citations + one /apply CTA on mobile; Article + FAQPage JSON-LD present in source and recorded for external validation.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; `/nl/blog/geo-generative-engine-optimization` statically generated.
- Body 1500-3000 words; 5-6 `<h2 id>` sections matching TOC ids.
- Exactly one CtaBlock → /apply. No em-dashes. Glossary terms honored.
- Article + FAQPage JSON-LD emitted (validate externally per 03-RESEARCH Validation Architecture).
- In-body links to the 3 GEO clusters present (back-links wired in 03-07).
</verification>

<success_criteria>
GEO pillar live at 1500-3000w with full TOC/takeaways/FAQ/citations, valid JSON-LD, one /apply CTA, grounded + cited GEO facts (Phase Success Criterion 1).
</success_criteria>

<output>
After completion, create `.planning/phases/03-cornerstone-content/03-01-SUMMARY.md`
</output>
