---
phase: 03-cornerstone-content
plan: 06
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - content/blog/clyde-vs-jasper-chatgpt-semrush.mdx
autonomous: true
requirements: [CONT-07]
must_haves:
  truths:
    - "De comparison money-page 'Clyde vs Jasper vs ChatGPT vs Semrush' is live, NL"
    - "De pagina bevat een ComparisonTable met highlightColumn op Clyde en een gedateerde, gegronde caption"
    - "De pagina heeft precies één CtaBlock naar /apply en emit valide Article + FAQPage JSON-LD"
    - "De pagina verschijnt in de /resources-hub onder de ai-marketing-automation pillar (CONT-07 hub-fix, Option A)"
    - "Competitor-claims zijn gegrond via live web-research op authoring-tijd; Clyde-data uit de pricing-mirror"
  artifacts:
    - path: "content/blog/clyde-vs-jasper-chatgpt-semrush.mdx"
      provides: "Clyde comparison money-page"
      contains: "highlightColumn={0}"
  key_links:
    - from: "content/blog/clyde-vs-jasper-chatgpt-semrush.mdx"
      to: "content/blog/ai-marketing-automation-voor-bureaus.mdx"
      via: "clusterOf + in-body locale-prefixed prose link (hub-visibility Option A)"
      pattern: "/nl/blog/ai-marketing-automation-voor-bureaus"
---

<objective>
Schrijf de NL comparison money-page `content/blog/clyde-vs-jasper-chatgpt-semrush.mdx` (CONT-07) met een `ComparisonTable` (Clyde gehighlight) en exact één `/apply`-CTA.

LOCKED hub-fix (03-RESEARCH Open Q1, Option A): zet `category: "ai-marketing-automation"` + `clusterOf: "ai-marketing-automation-voor-bureaus"` zodat de pagina als cluster onder de ai-marketing-automation pillar in de /resources-hub verschijnt. De product-clyde pillar-associatie blijft conceptueel (Phase 2 spine), maar de BLOG-taxonomie plaatst de pagina onder ai-marketing-automation. Gebruik NIET `category: "comparisons"` (geen hub-bucket → onzichtbaar, faalt Success Criterion 4).

Purpose: een conversie-gerichte, citeerbare vergelijkingspagina die Clyde als categorie (AI Marketing Medewerker) onderscheidt van losse tools, met defensieve claims.
Output: één verse SKC-grade NL MDX comparison-page, hub-zichtbaar, met gegronde competitor-data.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cornerstone-content/03-RESEARCH.md
@content/blog/geo-generative-engine-optimization.mdx
@src/components/blog/ComparisonTable.tsx
@src/lib/pricing-data.ts
@src/lib/blog.ts

<interfaces>
ComparisonTable: columns: string[]  rows: {label, values:(string|boolean)[]}[]  highlightColumn?: number  caption?: string
  - highlightColumn={0} highlights Clyde (first column). Booleans → check/dash; strings for nuance.
  - MDX-body inline-object arrays are JSX (parse fine).
CtaBlock: title, body?, href? (default /apply), label. Exactly ONE per page.
Pricing SSoT: src/lib/pricing-data.ts — Founding 997 fixed, Growth 499/ws, Pro 399/ws, Ent 299/ws. Do NOT invent or diverge.
Hub: category must be one of geo / ai-marketing-automation / agency-ops AND clusterOf must point at a pillar in that same bucket → use ai-marketing-automation + clusterOf the CONT-05 pillar slug.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-ground competitor claims (Jasper, ChatGPT, Semrush)</name>
  <files>content/blog/clyde-vs-jasper-chatgpt-semrush.mdx</files>
  <action>
03-RESEARCH covered GEO facts but NOT Jasper/ChatGPT/Semrush feature matrices (Pitfall 5, Open Q4 — LOW confidence). Before authoring, run GROUNDED web research (per project rule: Gemini grounded `node ~/.claude/scripts/gemini-research.mjs "..."` or Perplexity) on the current (juni 2026) public product positioning of: Jasper (AI content/marketing copy platform), ChatGPT (general assistant, Team/Enterprise), Semrush (SEO suite + AI Overview/citation module). Extract only defensible, category-level facts (what category each occupies, broad capability presence) — NOT precise internal feature assertions. Record the sources to cite. Phrase competitor rows as category-level differentiators, not feature accusations.
Then create the file with full block-YAML frontmatter: title "Clyde vs Jasper vs ChatGPT vs Semrush: welke kies je?" (no em-dash), description, author "Daley Maat", dates "2026-06-02", `category: "ai-marketing-automation"`, tags ["Clyde","Jasper","ChatGPT","Semrush","vergelijking"], `locale: "nl"`, heroImage "/og-image.png", readTime ~9, `clusterOf: "ai-marketing-automation-voor-bureaus"`, `schemaType: "BlogPosting"`, relatedSlugs ["ai-marketing-automation-voor-bureaus","wat-is-een-ai-marketing-medewerker"], 4-5 keyTakeaways, 5-6 TOC (ids match body), 4-5 FAQs, 4-6 citations (the grounded competitor sources + FMai positioning). NO em-dashes.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/clyde-vs-jasper-chatgpt-semrush.mdx','utf8'));if(d.data.category!=='ai-marketing-automation')throw'cat (hub-fix)';if(d.data.clusterOf!=='ai-marketing-automation-voor-bureaus')throw'clusterOf (hub-fix)';if((d.data.citations||[]).length<4)throw'citations<4';if(d.content.includes(String.fromCharCode(8212)))throw'em-dash';console.log('OK')"</automated>
  </verify>
  <done>File exists with hub-fix frontmatter (category ai-marketing-automation + clusterOf CONT-05 pillar), 4-6 grounded citations, no em-dash; competitor claims grounded in dated public sources.</done>
</task>

<task type="auto">
  <name>Task 2: Write body + ComparisonTable (Clyde highlighted) + CTA</name>
  <files>content/blog/clyde-vs-jasper-chatgpt-semrush.mdx</files>
  <action>
Write ~1000-1600 words, explicit `<h2 id>` sections matching TOC, e.g.: `de-kern-keuze` (answer-first), `clyde-vs-jasper`, `clyde-vs-chatgpt`, `clyde-vs-semrush`, `de-vergelijking` (houses the table), `welke-kies-je`. In `de-vergelijking` add ONE `<ComparisonTable>`:
- `columns={["Clyde","Jasper","ChatGPT","Semrush"]}`, `highlightColumn={0}`.
- `caption="Vergelijking op basis van publieke productinformatie, juni 2026."`.
- `rows`: category-level differentiators (defensible per Task 1 grounding), e.g.: "AI Marketing Medewerker (geen losse tool)" [true,false,false,false]; "Werkt per merk / klantportfolio" [true,false,false,false]; "Content genereren" [true,true,true,false]; "GEO/SEO-tracking" ["Ingebouwd",false,false,true]; "Hosted dashboard voor bureaus" [true,...]; etc. Where a competitor claim is uncertain, use a string ("Deels") or omit the row rather than assert a false boolean (Pitfall 5).
Clyde's own tier/price references (if any) must match src/lib/pricing-data.ts exactly. Link in-body to the pillar `/nl/blog/ai-marketing-automation-voor-bureaus` and to the Clyde-cluster `/nl/blog/wat-is-een-ai-marketing-medewerker`. Exactly ONE `<CtaBlock title=... body=... label="Plan een gesprek" href="/apply" />`. No duplicated frontmatter components.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/clyde-vs-jasper-chatgpt-semrush.mdx','utf8'));if((d.content.match(/<ComparisonTable/g)||[]).length!==1)throw'table';if(!/highlightColumn=\{0\}/.test(d.content))throw'no highlight';if(!/caption=/.test(d.content))throw'no caption';if((d.content.match(/<CtaBlock/g)||[]).length!==1)throw'cta';if(!d.content.includes('/nl/blog/ai-marketing-automation-voor-bureaus'))throw'no pillar link';const w=d.content.replace(/<[^>]+>/g,' ').trim().split(/\s+/).length;if(w<1000)throw'words '+w;const ids=(d.data.tableOfContents||[]).map(t=>t.id);for(const id of ids){if(!d.content.includes('id=\"'+id+'\"'))throw'anchor '+id}console.log('OK '+w)"</automated>
  </verify>
  <done>One ComparisonTable highlightColumn={0} + dated caption + defensible rows, one /apply CTA, in-body pillar + Clyde-cluster links, TOC ids resolve, Clyde prices match mirror.</done>
</task>

<task type="auto">
  <name>Task 3: Build + hub-visibility + mobile + JSON-LD proof</name>
  <files>content/blog/clyde-vs-jasper-chatgpt-semrush.mdx</files>
  <action>
Run `npm run build`; confirm the article generates for `nl`. CRITICAL hub check: load `/nl/resources` and confirm this page appears under the ai-marketing-automation bucket (as a cluster of the CONT-05 pillar) — this validates the Option A hub-fix and Success Criterion 4. Run testing-playbook mobile on `/nl/blog/clyde-vs-jasper-chatgpt-semrush`: table renders/scrolls, one /apply CTA. Capture Article + FAQPage JSON-LD for external validation. Confirm CWV neutral.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "compiled successfully|✓ Compiled|generating static pages|error" | tail -20</automated>
  </verify>
  <done>Build passes; page appears in /resources under ai-marketing-automation; mobile render OK with one /apply CTA; JSON-LD recorded.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; comparison URL generated for nl.
- ComparisonTable with highlightColumn={0} + dated caption; one /apply CTA; in-body pillar link.
- category ai-marketing-automation + clusterOf CONT-05 pillar → appears in /resources hub (Success Criterion 4).
- Competitor claims grounded + dated; Clyde data from pricing mirror; no em-dash.
</verification>

<success_criteria>
Comparison money-page live with ComparisonTable + one /apply CTA, hub-visible under the ai-marketing-automation pillar (Phase Success Criteria 3 + 4).
</success_criteria>

<output>
After completion, create `.planning/phases/03-cornerstone-content/03-06-SUMMARY.md`
</output>
