---
phase: 02-content-pillar-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/daley/Desktop/fma-app/scripts/fmai-pillars-import.mjs
autonomous: true
requirements: [PILR-01]

must_haves:
  truths:
    - "Exactly 4 active fma_content_pillars rows exist under FMai client_id b7681463-cd1f-4212-87e0-c5174b3631d9"
    - "Each pillar row carries the canonical organization_id ae336ca4-41ec-442d-b8cf-0a30bb53de1c (NOT NULL FK)"
    - "Pillar slugs are exactly [geo, ai-marketing-automation, agency-ops, product-clyde], mapping 1:1 onto the Phase-1 blog taxonomy + Phase-3 cornerstone clusters"
    - "Each pillar has funnel_stage and weight set (table columns), and a non-empty prompt_context.topics[] array (PillarTopic shape)"
    - "The same fma-app PillarPromptContext reader contract is satisfied so WF7 (Phase 6) can load these pillars with zero rework"
    - "Pillar name/description/topic copy is em-dash-free NL and respects the key-phrase glossary (AI Marketing Medewerker/Clyde, vaardigheden, merken)"
    - "The seed script is idempotent (upsert onConflict client_id,slug) and safe to re-run"
  artifacts:
    - path: "C:/Users/daley/Desktop/fma-app/scripts/fmai-pillars-import.mjs"
      provides: "Idempotent service-role seed of 4 FMai content pillars + read-back verification"
      min_lines: 120
      contains: "onConflict"
  key_links:
    - from: "scripts/fmai-pillars-import.mjs"
      to: "fma_clients (FMai row)"
      via: "service-role lookup resolving client_id + organization_id, asserting exactly one active row"
      pattern: "fma_clients"
    - from: "scripts/fmai-pillars-import.mjs"
      to: "fma_content_pillars"
      via: "upsert onConflict client_id,slug then SELECT read-back"
      pattern: "fma_content_pillars"
    - from: "pillar prompt_context.topics[]"
      to: "PillarPromptContext / PillarTopic contract in fma-app/src/types/database.ts"
      via: "topic objects with slug/title/hook shape the live readers consume"
      pattern: "topics"
---

<objective>
Seed FutureMarketingAI's four content pillars (GEO; AI marketing automation voor bureaus; agency-ops/proof; product/Clyde) as `fma_content_pillars` rows in the shared **fma-app** Supabase project, under FMai's already-existing client. This is the single strategy-spine that both the Phase-3 cornerstone blog and the future social pipeline (and WF7 in Phase 6) generate from.

Purpose: One source-of-truth for FMai's content strategy. With the pillars seeded, Phase-3 cornerstone content maps 1:1 onto real pillar rows (no orphan cluster), and Phase-6 wiring (`pillar_id` + WF7 read) inherits a ready, contract-correct payload.

Output: An idempotent Node seed script `fma-app/scripts/fmai-pillars-import.mjs` that upserts 4 pillar rows and reads them back to prove they exist with the correct FK pair, slugs, funnel_stage, weight, and non-empty topics.

**CROSS-REPO — READ THIS FIRST.** All work in this plan happens in the **fma-app** repo (`C:/Users/daley/Desktop/fma-app`), NOT in this `fmai-nextjs` repo. The Supabase DB, env (`fma-app/.env.local`), migration history, the `PillarPromptContext` reader contract, and the seed precedent all live in fma-app. Create the script there, run it there, commit it there.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-pillar-spine/02-RESEARCH.md

# Cross-repo reference files (in fma-app) — read these in the executor session:
# C:/Users/daley/Desktop/fma-app/scripts/skc-pillars-import.mjs  (the EXACT precedent to mirror)
# C:/Users/daley/Desktop/fma-app/src/types/database.ts           (PillarTopic / PillarPromptContext / FmaContentPillar contract, ~lines 3087-3125)
# C:/Users/daley/Desktop/fma-app/src/lib/clyde/pillars.ts        (runtime reader — query shape + fields consumed)
# C:/Users/daley/Desktop/fma-app/src/lib/narrative/pillars.ts    (runtime reader — weight/account_weights selection)

<facts>
<!-- RESOLVED in 02-RESEARCH.md — do NOT re-derive. Use directly. -->

REPO (all work here): C:/Users/daley/Desktop/fma-app
SCRIPT PATH: C:/Users/daley/Desktop/fma-app/scripts/fmai-pillars-import.mjs
ENV FILE: C:/Users/daley/Desktop/fma-app/.env.local  (load explicitly via dotenv — it loads .env by default, NOT .env.local)
ENV VARS: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY  (service-role bypasses RLS; NEVER print these values)
SUPABASE PROJECT REF: nurdldgqxseunotmygzn

FMai canonical client_id:       b7681463-cd1f-4212-87e0-c5174b3631d9   (is_active=true; smoke dup ae5e04cd-... is is_active=false — avoid it)
FMai canonical organization_id: ae336ca4-41ec-442d-b8cf-0a30bb53de1c   (NOT NULL FK, easy to forget, required on EVERY insert)
FMai pipeline_client_id: 'fmai'

TABLE: fma_content_pillars
  Columns set per insert: client_id, organization_id, name, slug, description, weight (REAL), funnel_stage (TEXT), color (TEXT), account_weights (JSONB), prompt_context (JSONB), is_active (BOOLEAN)
  DO NOT set id — let gen_random_uuid() fill it.
  UNIQUE(client_id, slug)  → upsert conflict target is 'client_id,slug'
  RLS enabled (is_org_member) → service-role key is required to insert.

THE FOUR PILLARS (slugs are FOREVER-STABLE — Phase 6 references them by slug/id):
  | slug                     | name (NL)                                  | blog category   | funnel_stage  | weight |
  | geo                      | GEO                                        | geo             | awareness     | 0.35   |
  | ai-marketing-automation  | AI marketing automation voor bureaus       | ai-marketing-automation | consideration | 0.30 |
  | agency-ops               | Agency-ops en proof                        | agency-ops      | consideration | 0.20   |
  | product-clyde            | Clyde, jouw AI Marketing Medewerker        | comparisons     | conversion    | 0.15   |
  Weights sum to 1.0. (geo/ai-marketing-automation/agency-ops match Phase-1 BLOG_CATEGORIES ids exactly;
  product-clyde's Phase-3 cornerstone CONT-07 "Clyde vs Jasper vs ChatGPT vs Semrush" uses the existing `comparisons` blog category — pillar still exists 1:1, no orphan cluster. This is research Option A.)

prompt_context SHAPE (PillarPromptContext) — match exactly:
  {
    doel?: string,
    instagram_share?: string[],
    platform_gating?: string[],
    topics: PillarTopic[],            // REQUIRED, non-empty
    carousel_formats?: { slug, name, structure[] }[]
  }
  PillarTopic = { slug, title, hook, slide_lines[], platform_gated[], open_lus_cta, suggested_format }
  Minimum per topic to satisfy the typed reader: slug, title, hook. Include slide_lines/open_lus_cta where it adds real value; empty arrays are tolerated by the defensive readers.

account_weights: set to {} (empty object). FMai's account_key array is not confirmed; the readers fall back to the top-level `weight` column, so {} is the safe, correct default. Do NOT invent FMai account_key strings.
</facts>

<glossary_rules>
<!-- CLAUDE.md key-phrase glossary + no-em-dash rule — copy MUST honor these -->
- "AI Marketing Medewerker" / "Clyde" — never "AI tool" / "platform"
- "vaardigheden" — never "features"
- "merken" / "klantportfolio" — never "klanten" (for an agency's end-clients)
- NO em-dashes (—) in ANY string. Use comma, period, colon. (SKC's existing rows contain em-dashes — do NOT copy that habit.)
- NL is source-of-truth for all name/description/topic copy.
- Canonical domain: future-marketing.ai. CTAs point at /apply.
</glossary_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author fmai-pillars-import.mjs (4 FMai pillars, contract-correct, em-dash-free)</name>
  <files>C:/Users/daley/Desktop/fma-app/scripts/fmai-pillars-import.mjs</files>
  <action>
First read the precedent and the contract in the fma-app repo:
- `C:/Users/daley/Desktop/fma-app/scripts/skc-pillars-import.mjs` — copy its structure (dotenv `.env.local` load, service-role createClient, client lookup, upsert onConflict).
- `C:/Users/daley/Desktop/fma-app/src/types/database.ts` (~lines 3087-3125) — confirm `PillarTopic` / `PillarPromptContext` field names.

Create `C:/Users/daley/Desktop/fma-app/scripts/fmai-pillars-import.mjs` as an idempotent ESM seed script:

1. Header/setup (mirror precedent verbatim):
   - `import { createClient } from '@supabase/supabase-js'`, `import { config } from 'dotenv'`, `path`/`url` helpers.
   - `config({ path: join(__dirname, '..', '.env.local') })` — explicit `.env.local`.
   - Read `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`; exit(1) with a clear error if either is missing. NEVER console.log the key values.
   - `createClient(url, serviceKey, { auth: { persistSession: false } })`.

2. Resolve the FMai client FK pair by lookup (do not hardcode-only):
   - SELECT `id, organization_id, account_keys` FROM `fma_clients` WHERE `id = 'b7681463-cd1f-4212-87e0-c5174b3631d9'` AND `is_active = true`.
   - Assert exactly one active row resolves. If missing/ambiguous, console.error and `process.exit(1)` (do NOT create a client row — that is Phase 4 territory).
   - Read `organization_id` from the resolved row (assert it equals `ae336ca4-41ec-442d-b8cf-0a30bb53de1c`); use that value on every pillar, never a separately-hardcoded constant. Log `Found FMai client: <id> (org: <org>)`.

3. Build a `pillars` array of exactly 4 objects, each:
   `{ client_id, organization_id, name, slug, description, weight, funnel_stage, color, account_weights: {}, is_active: true, prompt_context: {...} }`
   using the slug/name/funnel_stage/weight table in <facts>. Pick a distinct `color` hex per pillar (e.g. geo `#00d4aa`, ai-marketing-automation `#f5a623`, agency-ops `#3B82F6`, product-clyde `#8B5CF6`).
   `prompt_context` per pillar must include a `doel` (NL one-liner) and a non-empty `topics[]` of 3-6 PillarTopic objects. Topics SHOULD be the real Phase-3 cornerstone candidates so the spine drives Phase 3 1:1:
     - geo topics: "wat is GEO" (CONT-01), "GEO vs SEO in 2026" (CONT-02), "zichtbaarheid meten in AI Overviews" (CONT-03), "GEO monitoring tools voor ChatGPT en Perplexity" (CONT-04).
     - ai-marketing-automation topics: "AI marketing automation voor bureaus" (CONT-05), "Wat is een AI Marketing Medewerker (Clyde)" (CONT-06).
     - agency-ops topics: proof/SKC-case + tier-caps/klantportfolio-schaal themes (2-3 topics).
     - product-clyde topics: "Clyde vs Jasper vs ChatGPT vs Semrush" comparison (CONT-07) + 1-2 supporting Clyde-capability topics.
   Each topic: real NL `slug`, `title`, `hook`; add 3-5 `slide_lines` and an `open_lus_cta` that ends at /apply ("Plan een gesprek"). Set `platform_gated: []`, `suggested_format` to a short string (e.g. `'pillar_gids'`, `'cluster_artikel'`, `'comparison'`). Omit `carousel_formats` or set `[]` (blog-first spine, not carousels).

4. Upsert: `await supabase.from('fma_content_pillars').upsert(pillars, { onConflict: 'client_id,slug' })`; on error, console.error the error and exit(1). On success log `Upserted 4 FMai pillars`.

5. Append the read-back verification block from Task 2 in the SAME script (so the script both seeds and self-verifies on every run).

HARD RULES (from CLAUDE.md + research):
- NEVER `ALTER TABLE` / modify any schema; INSERT/UPSERT rows only. `account_weights` already exists — do NOT add it.
- NEVER run `supabase db push`.
- NEVER hardcode `id` (PK). Only the FK `client_id`/`organization_id` are known values.
- ALL copy em-dash-free NL, honoring the glossary in <glossary_rules>.
  </action>
  <verify>
    <automated>node -c "C:/Users/daley/Desktop/fma-app/scripts/fmai-pillars-import.mjs" && node -e "const s=require('fs').readFileSync('C:/Users/daley/Desktop/fma-app/scripts/fmai-pillars-import.mjs','utf8'); if(s.includes('—')) throw new Error('em-dash found in script copy'); if(!s.includes(\"onConflict\")) throw new Error('missing upsert onConflict'); if(!/geo|ai-marketing-automation|agency-ops|product-clyde/.test(s)) throw new Error('missing pillar slugs'); console.log('script syntax OK, no em-dash, slugs+upsert present')"</automated>
  </verify>
  <done>Script exists at the fma-app path, parses cleanly, contains the 4 pillar slugs, an `upsert(..., { onConflict: 'client_id,slug' })` call, resolves the FMai FK pair via lookup-with-assertion, and contains zero em-dash characters. No schema-altering statements, no `db push`.</done>
</task>

<task type="auto">
  <name>Task 2: Run the seed against live Supabase + read-back verify 4 rows</name>
  <files>C:/Users/daley/Desktop/fma-app/scripts/fmai-pillars-import.mjs</files>
  <action>
Add (or finalize) the read-back verification block at the end of `fmai-pillars-import.mjs`, then run the script against the live shared DB.

Read-back block (uses the SAME query shape the live `clyde/pillars.ts` reader uses, so passing it proves the readers will see the rows):
```
const { data, error } = await supabase
  .from('fma_content_pillars')
  .select('slug, name, weight, funnel_stage, is_active, prompt_context, organization_id, client_id')
  .eq('client_id', FMAI_CLIENT_ID)
  .eq('is_active', true)
  .order('weight', { ascending: false })
```
Assert (and console.log a PASS/FAIL summary, exit(1) on any failure):
- exactly 4 active rows returned;
- slug set === { geo, ai-marketing-automation, agency-ops, product-clyde };
- every row.organization_id === 'ae336ca4-41ec-442d-b8cf-0a30bb53de1c';
- every row.client_id === FMAI_CLIENT_ID;
- every row has a truthy funnel_stage and a numeric weight;
- every row.prompt_context.topics is a non-empty array;
- the four weights sum to within 0.001 of 1.0.
Print a compact table (slug, funnel_stage, weight, topics.length) — do NOT print client/org secret env values (the UUIDs are fine to print, the service-role key is not).

Then RUN it from the fma-app repo root (per CLAUDE.md cross-repo rule: live run + DB-output check on the shared DB):
`node scripts/fmai-pillars-import.mjs`  (cwd = C:/Users/daley/Desktop/fma-app)

Confirm stdout shows "Found FMai client", "Upserted 4 FMai pillars", and a PASS summary listing the 4 slugs. Re-run the script once more to prove idempotency (second run must NOT create duplicates — read-back must STILL report exactly 4 rows).

If the FMai client row does not resolve, or the read-back finds != 4 rows or a missing organization_id, STOP and report the failure (do not paper over it). A missing/needed column would be a BLOCKER to report, never a schema change to make.
  </action>
  <verify>
    <automated>cd /c/Users/daley/Desktop/fma-app && node scripts/fmai-pillars-import.mjs</automated>
  </verify>
  <done>Running `node scripts/fmai-pillars-import.mjs` (cwd fma-app) prints "Found FMai client", "Upserted 4 FMai pillars", and a PASS summary: exactly 4 active rows under client_id b7681463-..., slugs [geo, ai-marketing-automation, agency-ops, product-clyde], each with organization_id ae336ca4-..., a set funnel_stage + numeric weight (summing to 1.0), and a non-empty prompt_context.topics array. A second consecutive run still reports exactly 4 rows (idempotent).</done>
</task>

</tasks>

<verification>
Phase-level checks (all runnable against the live fma-app DB via the seed script):
1. `node scripts/fmai-pillars-import.mjs` (cwd `C:/Users/daley/Desktop/fma-app`) exits 0 with a PASS summary of 4 rows. (Success Criterion 1 + 2.)
2. Read-back asserts each row has a non-empty `prompt_context.topics[]`, a set `funnel_stage`, and a numeric `weight`. (Success Criterion 2.)
3. Slugs are exactly [geo, ai-marketing-automation, agency-ops, product-clyde]; the first three equal Phase-1 BLOG_CATEGORIES ids and product-clyde's Phase-3 cornerstone (CONT-07) maps to the existing `comparisons` category, so every Phase-3 cluster has a parent pillar — no orphan cluster. (Success Criterion 3.)
4. Re-running the script keeps the row count at exactly 4 (idempotent upsert on client_id,slug).
5. Script contains zero em-dash characters and uses glossary-correct copy.
6. OPTIONAL stronger smoke (if quick): import and call `getPillarContext(FMAI_ORG_ID, FMAI_CLIENT_ID)` from a throwaway node snippet and assert the returned `formattedSection` contains all 4 pillar names — proves the real reader (Phase-6 WF7 contract) sees them.
</verification>

<success_criteria>
- 4 active `fma_content_pillars` rows exist under FMai client_id `b7681463-cd1f-4212-87e0-c5174b3631d9`, each with organization_id `ae336ca4-41ec-442d-b8cf-0a30bb53de1c`.
- Slugs exactly [geo, ai-marketing-automation, agency-ops, product-clyde]; each maps 1:1 onto a Phase-3 cornerstone cluster (no orphan).
- Each row has funnel_stage + weight (columns) and a non-empty prompt_context.topics[] matching the PillarPromptContext/PillarTopic contract the fma-app readers (and Phase-6 WF7) consume.
- Seed script is idempotent and re-runnable; copy is em-dash-free, glossary-correct NL.
- No schema change, no `supabase db push`. All artifacts committed in the fma-app repo.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pillar-spine/02-01-SUMMARY.md` (in THIS fmai-nextjs repo's .planning) recording: the 4 pillar slugs/names/weights/funnel_stages seeded, the resolved FMai client_id/org_id, the script path in fma-app, the read-back PASS evidence (row count + slugs), idempotency confirmation, and a note that the fma-app script was committed in that repo. Flag for Phase 3: the product-clyde pillar's cornerstone uses the `comparisons` blog category (pillar slug != blog category by design).
</output>
