---
phase: 08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/blog/marketingbureau-schalen-met-ai.mdx
autonomous: true
requirements: [CONT-09]
must_haves:
  truths:
    - "De agency-ops pillargids 'marketingbureau schalen met AI' is 1500-3000 woorden en beantwoordt: waarom de lineaire omzet-headcount-relatie het probleem is, welk werk AI overneemt, hoe je resultaat meet, hoe je begint"
    - "category is agency-ops (= hub-bucket), pillar:true, schemaType Article"
    - "De pagina rendert TOC/KeyTakeaways/FAQ/Citations en emit valide Article + FAQPage JSON-LD"
    - "Elke tableOfContents.id resolveert naar een <h2 id> in de body"
    - "De pillar bevat precies één CtaBlock naar /apply en in-body forward-links naar de 3 agency-ops clusters"
    - "Statistieken zijn zacht gefraseerd en geciteerd als marktcontext, nooit als FMai/SKC eigen resultaat; geen verzonnen cijfers"
  artifacts:
    - path: "content/blog/marketingbureau-schalen-met-ai.mdx"
      provides: "Agency-ops pillargids, pillar:true, category agency-ops"
      contains: "pillar: true"
  key_links:
    - from: "content/blog/marketingbureau-schalen-met-ai.mdx"
      to: "tableOfContents <h2 id> anchors"
      via: "explicit <h2 id> headings matching tableOfContents ids"
      pattern: "<h2 id="
---

<objective>
Schrijf de verse NL agency-ops pillargids `content/blog/marketingbureau-schalen-met-ai.mdx` (CONT-09), SKC-grade, 1500-3000 woorden, gemodelleerd verbatim op de shape van `content/blog/ai-marketing-automation-voor-bureaus.mdx`. `category: "agency-ops"` (valt direct in de bestaande hub-bucket), `pillar: true`, `schemaType: "Article"`.

Purpose: directe topical authority op het non-branded keyword "marketingbureau schalen met AI". De winnende invalshoek (08-RESEARCH) is een operating-model framing (AI-first ops + human-in-the-loop), geen tool-lijst. Dit wordt de hub van de 3 agency-ops clusters (CONT-10/11/12).
Output: één diepe MDX-pillar met TOC/takeaways/FAQ/citations, gegrond + zacht-geciteerd, één /apply-CTA, forward-links naar de 3 clusters.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters/08-RESEARCH.md
@content/blog/ai-marketing-automation-voor-bureaus.mdx
@src/lib/blog.ts
@src/lib/constants.ts

<interfaces>
<!-- MDX componentmap props — verified, do NOT drift (08-RESEARCH Pitfall 6). -->
Callout:   variant?: 'info'|'warning'|'tip'|'success'  title?: string  children   (prop is variant, NOT type)
CtaBlock:  title: string  body?: string  href?: string (default /apply)  label: string   (props are title/body/label/href)
ComparisonTable: columns: string[]  rows: {label, values:(string|boolean)[]}[]  highlightColumn?: number  caption?: string

Frontmatter object-arrays MUST be block-YAML (never inline flow `[{..}]`) — @next/mdx build-breaker.
Headings need explicit `<h2 id="...">` — no rehype-slug. tableOfContents.id must match verbatim.
Renderer emits TOC/KeyTakeaways/BlogFaq/Citations from frontmatter — NEVER duplicate those components in the body.
In-body cross-links are raw locale-prefixed `/nl/blog/<slug>` (NOT next-intl Links, NOT auto-prefixed).
author: "Daley Maat" (byline only — unifies Person @id). schemaType pillars = Article.
PROOF RULE: SKC case via Sindy as operator only; NEVER mention Daley's SKC co-ownership; no invented result numbers. constants.ts only has FOUNDING_SPOTS_TAKEN=1 / FOUNDING_SPOTS_TOTAL=10 / MAX_PARTNERS_PER_YEAR=10.
PRICING: conceptueel "werkruimte-geprijsd" + deeplink /nl/pricing, geen hardcoded tarieven (pricing-data.ts SSoT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author full block-YAML frontmatter</name>
  <files>content/blog/marketingbureau-schalen-met-ai.mdx</files>
  <action>
Create the file with complete block-YAML frontmatter (mirror the ai-marketing-automation-voor-bureaus.mdx pillar exactly in shape):
- title: "Marketingbureau schalen met AI: het operating model" (no em-dash), description: answer-first, < ~155 chars.
- author: "Daley Maat"; publishedAt/updatedAt: "2026-06-02"; `category: "agency-ops"`; tags: ["marketingbureau schalen","AI marketing","agency operations","AI-first"]; locale: "nl"; heroImage: "/og-image.png"; readTime ~12; `pillar: true`; `schemaType: "Article"`.
- relatedSlugs: ["ai-efficientie-marketingbureau","meetbare-ai-marketing-resultaten","ai-marketing-resultaat-in-de-praktijk"].
- keyTakeaways: 4-5 quotable claims (operating-model framing: lineaire omzet-headcount-relatie breken, AI draagt uitvoering + mens houdt regie, schalen zonder evenredige headcount, meet resultaat met een framework).
- tableOfContents: 5-6 block-YAML entries, ids match the body `<h2 id>` below. Suggested ids: `het-schaalprobleem`, `operating-model`, `welk-werk-ai-overneemt`, `human-in-the-loop`, `resultaat-meten`, `hoe-begin-je`.
- faqs: 5 Q&A (drives FAQPage). Grounded: "Vervangt AI mijn team?", "Welk bureauwerk neemt AI als eerste over?", "Hoe meet ik of AI bij ons rendeert?", "Schaalt dit zonder evenredig meer mensen aan te nemen?", "Hoe begin ik met AI-first werken?".
- citations: 4-6 grounded sources from 08-RESEARCH (NL AI-adoption ~72% softly; "lack of knowledge" barrier ~54%; AI-first ops efficiency claims as market context). Use searchlab.nl / chapmanbright.com / basis.com / martech.org / growthloop.com style sources with {title,url,source,year}. NEVER present any stat as FMai/SKC's own.
NO em-dashes (scan U+2014). Glossary: "AI Marketing Medewerker"/"Clyde", "vaardigheden", "merken"/"klantportfolio" (never "klanten" for end-clients).
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/marketingbureau-schalen-met-ai.mdx','utf8'));if(d.data.pillar!==true)throw'not pillar';if(d.data.category!=='agency-ops')throw'cat';if(d.data.schemaType!=='Article')throw'schemaType';if((d.data.faqs||[]).length<5)throw'faqs<5';if((d.data.tableOfContents||[]).length<5)throw'toc<5';if((d.data.citations||[]).length<4)throw'citations<4';if(d.content.includes(String.fromCharCode(8212)))throw'em-dash';console.log('OK')"</automated>
  </verify>
  <done>Frontmatter: pillar:true, category agency-ops, schemaType Article, 4-5 takeaways, 5-6 TOC, 5 FAQs, 4-6 grounded citations; no em-dash.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 1500-3000 word body with grounded sections + cluster forward-links</name>
  <files>content/blog/marketingbureau-schalen-met-ai.mdx</files>
  <action>
Write the MDX body totaling 1500-3000 words. Each section is an explicit `<h2 id="...">` matching a TOC id. Answer-first opening sentence. Use 08-RESEARCH agency-ops facts (phrase stats softly with "ongeveer"/"rond", attribute as market context):
- `het-schaalprobleem`: the linear revenue-headcount relation as the core problem (each new merk = more hours, margin erodes).
- `operating-model`: AI-first operating model (agents for autonomous tasks, human-in-the-loop), NOT a tool list. Tie to the AI Marketing Medewerker work-model; in-body link to `/nl/blog/ai-marketing-automation-voor-bureaus` (the sibling automation pillar).
- `welk-werk-ai-overneemt`: which agency work AI takes over (content, reporting, lead-qualification, data analysis). In-body forward-link to `/nl/blog/ai-efficientie-marketingbureau` (CONT-10).
- `human-in-the-loop`: where humans stay essential (strategy, brand voice, final edit) — the regie stays with the bureau.
- `resultaat-meten`: why you measure with a framework not vanity metrics; in-body forward-link to `/nl/blog/meetbare-ai-marketing-resultaten` (CONT-11).
- `hoe-begin-je`: practical starting steps; reference the working example via Sindy as operator at SkinClarity Club (NO Daley co-ownership, no metrics brag) and in-body forward-link to `/nl/blog/ai-marketing-resultaat-in-de-praktijk` (CONT-12). Conceptual werkruimte-geprijsd model + deeplink `/nl/pricing`.
Add at most ONE `<Callout variant="info">` if it strengthens a section. Exactly ONE `<CtaBlock title=... body=... label="Plan een gesprek" href="/apply" />` at the end. Do NOT add KeyTakeaways/TableOfContents/BlogFaq/Citations in the body. All 3 cluster forward-links present and locale-prefixed.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter');const d=m(require('fs').readFileSync('content/blog/marketingbureau-schalen-met-ai.mdx','utf8'));const w=d.content.replace(/<[^>]+>/g,' ').trim().split(/\s+/).length;if(w<1500)throw'words '+w+' <1500';if((d.content.match(/<CtaBlock/g)||[]).length!==1)throw'CtaBlock!=1';if((d.content.match(/href=\"\/apply\"/g)||[]).length!==1)throw'/apply!=1';const ids=(d.data.tableOfContents||[]).map(t=>t.id);for(const id of ids){if(!d.content.includes('id=\"'+id+'\"'))throw'anchor '+id}for(const s of ['ai-efficientie-marketingbureau','meetbare-ai-marketing-resultaten','ai-marketing-resultaat-in-de-praktijk']){if(!d.content.includes('/nl/blog/'+s))throw'missing cluster link '+s}console.log('OK words='+w)"</automated>
  </verify>
  <done>Body 1500-3000w, every TOC id has a matching <h2 id>, exactly one CtaBlock + one /apply, all 3 cluster forward-links present, no duplicated frontmatter components.</done>
</task>

<task type="auto">
  <name>Task 3: Build + JSON-LD + mobile proof</name>
  <files>content/blog/marketingbureau-schalen-met-ai.mdx</files>
  <action>
Run `npm run build` and confirm the article statically generates for `nl` with no errors. Run the testing-playbook mobile flow on `/nl/blog/marketingbureau-schalen-met-ai`: TOC anchors scroll, KeyTakeaways/FAQ/Citations render, single CTA routes to `/nl/apply`. Inspect generated `.next` HTML for the emitted Article + FAQPage JSON-LD and record it for external validation (Google Rich Results Test for Article; Schema Markup Validator for FAQPage — FAQ rich result deprecated 2026, validate syntax not appearance). Confirm no heavy hero asset added (CWV neutral).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "compiled successfully|✓ Compiled|generating static pages|error" | tail -20</automated>
  </verify>
  <done>Build passes; page renders TOC/takeaways/FAQ/citations + one /apply CTA on mobile; Article + FAQPage JSON-LD recorded for external validation.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; `/nl/blog/marketingbureau-schalen-met-ai` statically generated.
- Body 1500-3000 words; 5-6 `<h2 id>` sections matching TOC ids.
- Exactly one CtaBlock -> /apply. No em-dashes. Glossary terms honored. No invented proof numbers; Sindy-as-operator only.
- Article + FAQPage JSON-LD emitted (validate externally per 08-RESEARCH Validation Architecture).
- In-body forward-links to the 3 agency-ops clusters present (back-links wired in 08-07).
- Appears under agency-ops bucket in /resources (verified 08-07).
</verification>

<success_criteria>
Agency-ops pillargids live at 1500-3000w with full TOC/takeaways/FAQ/citations, valid JSON-LD, one /apply CTA, grounded + softly-cited agency-scaling facts (Success Criterion 1).
</success_criteria>

<output>
After completion, create `.planning/phases/08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters/08-02-SUMMARY.md`
</output>
