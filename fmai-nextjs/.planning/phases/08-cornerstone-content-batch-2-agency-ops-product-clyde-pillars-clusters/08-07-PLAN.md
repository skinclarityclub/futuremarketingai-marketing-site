---
phase: 08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters
plan: 07
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06]
files_modified:
  - content/blog/marketingbureau-schalen-met-ai.mdx
  - content/blog/ai-marketing-medewerker.mdx
autonomous: true
requirements: [CONT-16]
must_haves:
  truths:
    - "Het pillar<->cluster link-netwerk is bidirectioneel dichtgetimmerd: beide pillars linken OUT naar al hun clusters, en elke cluster linkt BACK naar zijn pillar, bewezen tegen gerenderde .next-HTML (niet alleen MDX-source)"
    - "Alle 7 nieuwe cornerstones verschijnen in /nl/resources onder de juiste bucket (agency-ops + product-clyde), en de 2 nieuwe pillars in het homepage KennisbankTeaser-blok"
    - "Geen wees-cluster: elke cluster.clusterOf wijst naar een bestaande pillar-slug die in een gerenderde bucket leeft"
    - "Elke nieuwe cornerstone: precies één /apply-CTA, valide Article(pillar)/BlogPosting(cluster) + FAQPage JSON-LD, geen U+2014 em-dash, canoniek domein future-marketing.ai"
    - "npm run build slaagt; npm run check:palette slaagt; CWV neutraal"
  artifacts:
    - path: "content/blog/marketingbureau-schalen-met-ai.mdx"
      provides: "Agency-ops pillar met out-links naar alle 3 agency-ops clusters (verified/finalized)"
      contains: "/nl/blog/ai-efficientie-marketingbureau"
    - path: "content/blog/ai-marketing-medewerker.mdx"
      provides: "Product/Clyde pillar met out-links naar CONT-06 + beide product/Clyde clusters (verified/finalized)"
      contains: "/nl/blog/ai-agent-vs-ai-tool-marketing"
  key_links:
    - from: "content/blog/marketingbureau-schalen-met-ai.mdx"
      to: "3 agency-ops clusters (CONT-10/11/12)"
      via: "in-body locale-prefixed prose forward-links"
      pattern: "/nl/blog/(ai-efficientie-marketingbureau|meetbare-ai-marketing-resultaten|ai-marketing-resultaat-in-de-praktijk)"
    - from: "content/blog/ai-marketing-medewerker.mdx"
      to: "CONT-06 + 2 product/Clyde clusters (CONT-14/15)"
      via: "in-body locale-prefixed prose forward-links"
      pattern: "/nl/blog/(wat-is-een-ai-marketing-medewerker|ai-agent-vs-ai-tool-marketing|ai-marketing-agent-geheugen-en-leren)"
---

<objective>
Sluitsteen-plan (CONT-16, mirror van Phase-3 03-07): timmer het interne-link-netwerk van batch 2 bidirectioneel dicht en bewijs cross-page dat de hele batch correct staat. Verifieer/finaliseer dat beide pillars OUT-linken naar al hun clusters en dat elke cluster BACK-linkt naar zijn pillar; bewijs hub-verschijning (4 buckets), homepage-teaser-verschijning (2 nieuwe pillars), één /apply-CTA per page, valide JSON-LD, geen em-dash, geen wees-cluster, build + palette groen, CWV neutraal.

Purpose: Success Criteria 3 + 4 hard afsluiten over alle 7 nieuwe cornerstones + de 2 wiring-edits heen, exact zoals 03-07 dat voor Phase 3 deed.
Output: een dichtgetimmerd, bewezen link-netwerk + cross-page proof-sweep; eventuele ontbrekende out-/back-links gefixt in de 2 pillar-files (verwacht: meestal verificatie-only, want de pillar-plans schreven de out-links al en de cluster-plans de back-links).
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters/08-RESEARCH.md
@content/blog/marketingbureau-schalen-met-ai.mdx
@content/blog/ai-marketing-medewerker.mdx
@src/app/[locale]/(marketing)/resources/page.tsx
@src/app/[locale]/page.tsx
@src/lib/blog.ts

<interfaces>
The 7 new cornerstones (slugs): pillars marketingbureau-schalen-met-ai (agency-ops), ai-marketing-medewerker (product-clyde); clusters ai-efficientie-marketingbureau, meetbare-ai-marketing-resultaten, ai-marketing-resultaat-in-de-praktijk (clusterOf agency-ops pillar), ai-agent-vs-ai-tool-marketing, ai-marketing-agent-geheugen-en-leren (clusterOf product pillar). Plus the existing CONT-06 cluster wat-is-een-ai-marketing-medewerker (product pillar links DOWN to it).
Bidirectionality is proven against rendered HTML in `.next`, not just MDX source (Phase-3 03-07 standard). In-body links are raw locale-prefixed `/nl/blog/<slug>`.
Hub buckets (after 08-01): geo, ai-marketing-automation, agency-ops, product-clyde. Teaser feed (after 08-01): geo+ai+agency+product pillars + comparison.
No em-dash (U+2014). Canonical domain future-marketing.ai only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify + finalize bidirectional pillar<->cluster in-body links</name>
  <files>content/blog/marketingbureau-schalen-met-ai.mdx, content/blog/ai-marketing-medewerker.mdx</files>
  <action>
Verify each pillar links OUT to all of its clusters and each cluster links BACK to its pillar. Expected from upstream plans: agency-ops pillar (08-02) already forward-links to its 3 clusters; product pillar (08-03) already forward-links to CONT-06 + its 2 clusters; every cluster (08-04/05/06) already back-links to its pillar. This task is verification-first; ONLY edit the 2 pillar files if a forward-link is missing (do not touch cluster files — they own their back-links). Confirm:
- marketingbureau-schalen-met-ai.mdx contains `/nl/blog/ai-efficientie-marketingbureau`, `/nl/blog/meetbare-ai-marketing-resultaten`, `/nl/blog/ai-marketing-resultaat-in-de-praktijk`.
- ai-marketing-medewerker.mdx contains `/nl/blog/wat-is-een-ai-marketing-medewerker`, `/nl/blog/ai-agent-vs-ai-tool-marketing`, `/nl/blog/ai-marketing-agent-geheugen-en-leren`.
If any is missing, add a natural in-body prose link in the relevant pillar section. Do not introduce a second CtaBlock or a second /apply link.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('content/blog/marketingbureau-schalen-met-ai.mdx','utf8');for(const s of ['ai-efficientie-marketingbureau','meetbare-ai-marketing-resultaten','ai-marketing-resultaat-in-de-praktijk']){if(!a.includes('/nl/blog/'+s))throw'agency pillar missing '+s}const b=fs.readFileSync('content/blog/ai-marketing-medewerker.mdx','utf8');for(const s of ['wat-is-een-ai-marketing-medewerker','ai-agent-vs-ai-tool-marketing','ai-marketing-agent-geheugen-en-leren']){if(!b.includes('/nl/blog/'+s))throw'product pillar missing '+s}for(const f of ['ai-efficientie-marketingbureau','meetbare-ai-marketing-resultaten','ai-marketing-resultaat-in-de-praktijk']){if(!fs.readFileSync('content/blog/'+f+'.mdx','utf8').includes('/nl/blog/marketingbureau-schalen-met-ai'))throw f+' missing pillar back-link'}for(const f of ['ai-agent-vs-ai-tool-marketing','ai-marketing-agent-geheugen-en-leren']){if(!fs.readFileSync('content/blog/'+f+'.mdx','utf8').includes('/nl/blog/ai-marketing-medewerker'))throw f+' missing pillar back-link'}console.log('OK bidirectional')"</automated>
  </verify>
  <done>Both pillars link OUT to all their clusters; every cluster links BACK to its pillar; no second CtaBlock/apply introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-page invariant sweep (CTA, em-dash, no-orphan, canonical domain)</name>
  <files>content/blog/marketingbureau-schalen-met-ai.mdx, content/blog/ai-marketing-medewerker.mdx</files>
  <action>
Across all 7 new cornerstone MDX files, assert the page-level invariants in one sweep (read-only over the 7 files; only the 2 pillar files are owned/editable here, fix only if a pillar violates):
- Exactly one `<CtaBlock>` and exactly one `/apply` href per file.
- Zero U+2014 em-dash in every file.
- Canonical domain only: no `futuremarketingai.com` anywhere.
- No-orphan: every cluster's `clusterOf` resolves to an existing pillar slug whose pillar file has `pillar: true` and a bucket-id category (agency-ops or product-clyde).
- pillars have schemaType Article + category in a hub bucket; clusters have schemaType BlogPosting.
If a violation is found in a pillar file, fix it there; if in a cluster file, this is a blocker to report (cluster files are owned by 08-04/05/06 — flag for a gap-closure rather than edit cross-plan).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=require('gray-matter');const files=['marketingbureau-schalen-met-ai','ai-marketing-medewerker','ai-efficientie-marketingbureau','meetbare-ai-marketing-resultaten','ai-marketing-resultaat-in-de-praktijk','ai-agent-vs-ai-tool-marketing','ai-marketing-agent-geheugen-en-leren'];const pillars=['marketingbureau-schalen-met-ai','ai-marketing-medewerker'];const buckets=['agency-ops','product-clyde'];const slugs=new Set(files);for(const f of files){const raw=fs.readFileSync('content/blog/'+f+'.mdx','utf8');const d=m(raw);if((d.content.match(/<CtaBlock/g)||[]).length!==1)throw f+' CtaBlock!=1';if((d.content.match(/href=\"\/apply\"/g)||[]).length!==1)throw f+' /apply!=1';if(raw.includes(String.fromCharCode(8212)))throw f+' em-dash';if(/futuremarketingai\.com/.test(raw))throw f+' legacy domain';if(pillars.includes(f)){if(d.data.pillar!==true)throw f+' not pillar';if(d.data.schemaType!=='Article')throw f+' not Article';if(!buckets.includes(d.data.category))throw f+' cat not bucket';}else{if(d.data.schemaType!=='BlogPosting')throw f+' not BlogPosting';if(!d.data.clusterOf)throw f+' no clusterOf';if(!fs.existsSync('content/blog/'+d.data.clusterOf+'.mdx'))throw f+' orphan clusterOf '+d.data.clusterOf;}}console.log('OK invariants 7/7')"</automated>
  </verify>
  <done>All 7 new cornerstones: one CtaBlock + one /apply, zero em-dash, canonical domain, no orphan cluster, correct schemaType + bucket category.</done>
</task>

<task type="auto">
  <name>Task 3: Final build + hub/teaser appearance + JSON-LD proof against rendered HTML</name>
  <files>content/blog/marketingbureau-schalen-met-ai.mdx</files>
  <action>
Run `npm run build` (expect ~129 static pages: prior 122 + 7) and confirm it exits 0. Run `npm run check:palette` (passes). Against the generated `.next` HTML (not MDX source), prove:
- /nl/resources renders all 7 cornerstones under the correct bucket: the agency-ops pillar + its 3 clusters in the agency-ops bucket; the product/Clyde pillar + its 2 clusters (and the CONT-06 down-link target) in the product-clyde bucket.
- /nl home renders the 2 new pillars in the KennisbankTeaser block.
- Each new article's rendered HTML contains its Article/BlogPosting + FAQPage JSON-LD blocks; record them for external validation (Google Rich Results Test for Article; Schema Markup Validator for FAQPage). Confirm bidirectional pillar<->cluster anchors are present in the rendered blog HTML (not just MDX). CWV argued-neutral (static-prerendered MDX, identical renderer, inline critical CSS, no new client-JS; the 2 small tsx edits add no client-JS).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "compiled successfully|✓ Compiled|generating static pages|MISSING_MESSAGE|error" | tail -20</automated>
  </verify>
  <done>Build passes (~129 pages); palette passes; all 7 cornerstones hub-visible under correct bucket; 2 new pillars in homepage teaser; Article/BlogPosting + FAQPage JSON-LD present in rendered HTML and recorded; bidirectional links proven in rendered HTML; CWV neutral.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (~129 static pages), no MISSING_MESSAGE; `npm run check:palette` passes.
- Bidirectional pillar<->cluster link-netwerk proven in rendered .next HTML (both pillars out-link all clusters; every cluster back-links its pillar).
- All 7 new cornerstones appear in /nl/resources under the correct bucket (agency-ops + product-clyde); the 2 new pillars appear in the homepage KennisbankTeaser.
- Every new cornerstone: exactly one /apply CTA, valid Article/BlogPosting + FAQPage JSON-LD, zero em-dash, canonical domain, no orphan cluster.
- CWV neutral (static MDX + no new client-JS).
</verification>

<success_criteria>
Phase 8 closed: all 7 batch-2 cornerstones live with a bidirectional pillar<->cluster network, hub + homepage-teaser visibility under 4 buckets, one /apply CTA each, valid JSON-LD, no em-dash, no orphan cluster (Success Criteria 3 + 4).
</success_criteria>

<output>
After completion, create `.planning/phases/08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters/08-07-SUMMARY.md`
</output>
