---
phase: 08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/(marketing)/resources/page.tsx
  - messages/nl.json
  - messages/en.json
  - messages/es.json
  - src/app/[locale]/page.tsx
autonomous: true
requirements: [CONT-16]
must_haves:
  truths:
    - "De /resources-hub heeft een vierde bucket 'product-clyde' die in nl/en/es zonder MISSING_MESSAGE-crash rendert"
    - "De homepage KennisbankTeaser bevat de agency-ops pillar en de product/Clyde pillar zodra die MDX-bestanden bestaan"
    - "npm run build slaagt en npm run check:palette slaagt na de tsx/json-edits"
    - "Geen any-types, geen @ts-ignore, geen NEXT_PUBLIC-secret in de gewijzigde tsx"
  artifacts:
    - path: "src/app/[locale]/(marketing)/resources/page.tsx"
      provides: "PILLAR_BUCKETS uitgebreid met 'product-clyde'"
      contains: "product-clyde"
    - path: "messages/nl.json"
      provides: "resources.pillars.product-clyde i18n-key (NL bron)"
      contains: "product-clyde"
    - path: "src/app/[locale]/page.tsx"
      provides: "kennisbankItems uitgebreid met agency-ops + product/Clyde pillars"
      contains: "agency-ops"
  key_links:
    - from: "src/app/[locale]/(marketing)/resources/page.tsx"
      to: "messages/{nl,en,es}.json resources.pillars.product-clyde"
      via: "t(`pillars.${bucketId}.title`) lookup per locale"
      pattern: "product-clyde"
    - from: "src/app/[locale]/page.tsx"
      to: "getPillarPosts(locale) by category agency-ops + product-clyde"
      via: "pillars.find((p) => p.category === ...)"
      pattern: "category === 'agency-ops'"
---

<objective>
Leg de twee wiring-gaps van Phase 8 vast (CONT-16 infra-deel) zodat de nog-te-schrijven cornerstones automatisch oppoppen in de hub én de homepage-teaser. Twee kleine, gerichte code-edits, geen content:
1. Voeg een vierde hub-bucket `product-clyde` toe aan `/resources` (Open Q1 = Option B, locked), inclusief de `resources.pillars.product-clyde.{title,description}` i18n-key in nl.json, en.json EN es.json (anders crasht next-intl per locale, 08-RESEARCH Pitfall 5).
2. Bedraad de hardcoded homepage `kennisbankItems` (Open Q2 = hardcoded-add, locked) met de agency-ops pillar en de product/Clyde pillar.

Purpose: Success Criterion 4 vereist dat elke nieuwe cornerstone "automatisch" in hub + teaser verschijnt; de huidige code rendert maar 3 hardcoded buckets/cards. Dit plan dicht beide gaps één keer, zodat de pillar/cluster-plannen (Wave 1/2) puur authoring blijven.
Output: hub met 4 buckets, teaser die de 2 nieuwe pillars toont zodra hun MDX bestaat, build + palette groen.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters/08-RESEARCH.md
@src/app/[locale]/(marketing)/resources/page.tsx
@src/app/[locale]/page.tsx
@src/components/home/KennisbankTeaser.tsx
@src/lib/blog.ts

<interfaces>
<!-- Verified from source 2026-06-02. -->
resources/page.tsx:29  const PILLAR_BUCKETS = ['geo', 'ai-marketing-automation', 'agency-ops'] as const
  - Per bucket: t(`pillars.${bucketId}.title`) + t(`pillars.${bucketId}.description`). A missing key throws MISSING_MESSAGE in EVERY locale (next-intl strict).
  - Buckets degrade gracefully to empty state (a pillar with 0 clusters still renders), so adding the bucket BEFORE the pillar MDX exists is safe.

messages/{nl,en,es}.json  resources.pillars.{geo,ai-marketing-automation,agency-ops} all exist in all 3 locales (verified ~line 4001-4013). Add a 4th sibling key product-clyde with the SAME shape.

page.tsx:76-86  hardcoded teaser feed:
  const pillars = getPillarPosts(locale)
  const geoPillar = pillars.find((p) => p.category === 'geo')
  const aiPillar = pillars.find((p) => p.category === 'ai-marketing-automation')
  const comparison = getClusterPosts('ai-marketing-automation-voor-bureaus', locale).find(p => p.slug === 'clyde-vs-jasper-chatgpt-semrush')
  const kennisbankItems: KennisbankCard[] = [ geoPillar&&{...,kind:'pillar'}, aiPillar&&{...,kind:'pillar'}, comparison&&{...,kind:'comparison'} ].filter(...).map(...)

KennisbankTeaser items: KennisbankCard[] = { slug, title, description, readTime, kind: 'pillar'|'comparison' }. Component self-hides when items empty; grid is md:grid-cols-3 (5 cards wrap to 3+2, acceptable — curated teaser).
getPillarPosts(locale) returns BlogPostMeta[] filtered to pillar:true for that locale. The agency-ops pillar (category 'agency-ops') + product/Clyde pillar (category 'product-clyde') only resolve once their MDX exists — .find() returns undefined until then, the && guard + filter handle it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add product-clyde hub bucket + i18n in all 3 locales</name>
  <files>src/app/[locale]/(marketing)/resources/page.tsx, messages/nl.json, messages/en.json, messages/es.json</files>
  <action>
1. In `resources/page.tsx` extend the hardcoded array on line 29 to:
   `const PILLAR_BUCKETS = ['geo', 'ai-marketing-automation', 'agency-ops', 'product-clyde'] as const`
   No other change to the page — the bucket-mapping loop and graceful empty-state already handle any id.
2. Add a `product-clyde` sibling key under `resources.pillars` in ALL THREE locale files (same shape/position as the existing `agency-ops` key, immediately after it). Use these values (no em-dash, glossary-conform: "AI Marketing Medewerker"/"Clyde", "merken"):
   - nl.json: `"product-clyde": { "title": "AI Marketing Medewerker en Clyde", "description": "Wat een AI Marketing Medewerker is en hoe Clyde per merk werkt." }`
   - en.json: `"product-clyde": { "title": "AI Marketing Employee and Clyde", "description": "What an AI Marketing Employee is and how Clyde works per brand." }`
   - es.json: `"product-clyde": { "title": "Empleado de Marketing con IA y Clyde", "description": "Qué es un Empleado de Marketing con IA y cómo Clyde trabaja por marca." }`
   Keep valid JSON (comma after the agency-ops block, no trailing comma after product-clyde). Do NOT touch any other key.
  </action>
  <verify>
    <automated>node -e "for(const l of ['nl','en','es']){const j=require('./messages/'+l+'.json');const p=j.resources.pillars['product-clyde'];if(!p||!p.title||!p.description)throw l+' product-clyde key missing';if((p.title+p.description).includes(String.fromCharCode(8212)))throw l+' em-dash';}const s=require('fs').readFileSync('src/app/[locale]/(marketing)/resources/page.tsx','utf8');if(!s.includes(\"'product-clyde'\"))throw'bucket not added';console.log('OK')"</automated>
  </verify>
  <done>PILLAR_BUCKETS has 4 ids incl product-clyde; resources.pillars.product-clyde exists with title+description in nl/en/es; valid JSON; no em-dash.</done>
</task>

<task type="auto">
  <name>Task 2: Wire agency-ops + product/Clyde pillars into the homepage teaser</name>
  <files>src/app/[locale]/page.tsx</files>
  <action>
Edit the teaser feed block (page.tsx:76-86). After the existing `aiPillar` line add:
  `const agencyPillar = pillars.find((p) => p.category === 'agency-ops')`
  `const productPillar = pillars.find((p) => p.category === 'product-clyde')`
Then extend `kennisbankItems` to include them as `kind: 'pillar'`. Recommended order (curated): geo, ai-automation, agency-ops, product/Clyde pillars + keep the existing comparison card (5 cards total; the md:grid-cols-3 grid wraps 3+2 cleanly per 08-RESEARCH Pitfall 2 guidance). Each new entry follows the exact same `agencyPillar && { ...agencyPillar, kind: 'pillar' as const }` pattern so the `.filter`/`.map` stay unchanged. The `&&` guard + `.filter(Boolean)` mean cards self-omit until the MDX exists, so this is safe to ship before the pillar files land.
No `any`, no `@ts-ignore`. Do not change `KennisbankTeaser.tsx` (generic, already handles N items + self-hides).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/[locale]/page.tsx','utf8');if(!s.includes(\"category === 'agency-ops'\"))throw'agency pillar not wired';if(!s.includes(\"category === 'product-clyde'\"))throw'product pillar not wired';if(s.includes('@ts-ignore'))throw'ts-ignore';console.log('OK')"</automated>
  </verify>
  <done>page.tsx computes agencyPillar + productPillar from getPillarPosts and pushes both into kennisbankItems as kind:'pillar'; no any/ts-ignore.</done>
</task>

<task type="auto">
  <name>Task 3: Build + palette proof for the wiring edits</name>
  <files>src/app/[locale]/(marketing)/resources/page.tsx</files>
  <action>
Run `npm run build` and confirm it exits 0 (the hub renders the 4th bucket in all locales with no MISSING_MESSAGE; the teaser block compiles even though the 2 new pillar MDX files do not exist yet, because the && guards omit them). Run `npm run check:palette` and confirm it passes (no deprecated colors introduced). Note: the new bucket renders as an empty-state card on /resources until the product/Clyde pillar MDX lands in 08-03 — that is expected and correct (graceful degradation).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -iE "compiled successfully|✓ Compiled|generating static pages|MISSING_MESSAGE|error" | tail -20</automated>
  </verify>
  <done>Build passes with no MISSING_MESSAGE; check:palette passes; hub shows 4 buckets (product-clyde as graceful empty state for now).</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; no MISSING_MESSAGE for resources.pillars.product-clyde in any locale.
- `npm run check:palette` passes.
- PILLAR_BUCKETS = 4 ids; product-clyde i18n present in nl/en/es; no em-dash; valid JSON.
- page.tsx teaser feed includes agency-ops + product/Clyde pillar lookups (cards appear once MDX lands).
- No any-types / @ts-ignore / NEXT_PUBLIC secrets introduced.
</verification>

<success_criteria>
The hub + teaser infrastructure is ready so every Phase-8 cornerstone auto-surfaces in /resources (4 buckets) and on the homepage teaser the moment its MDX exists (Success Criterion 4, infra half of CONT-16).
</success_criteria>

<output>
After completion, create `.planning/phases/08-cornerstone-content-batch-2-agency-ops-product-clyde-pillars-clusters/08-01-SUMMARY.md`
</output>
