---
phase: 12-design-polish-media
plan: 03
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - src/pages/AutomationsPage.tsx
  - src/pages/ChatbotsPage.tsx
  - src/pages/VoiceAgentsPage.tsx
  - src/pages/MarketingMachinePage.tsx
  - src/pages/AboutPage.tsx
  - src/pages/PricingPage.tsx
  - src/pages/HowItWorksPage.tsx
  - src/index.css
autonomous: true
requirements:
  - REQ-SCROLL-MICRO
  - REQ-PRODUCT-MEDIA
  - REQ-TYPOGRAPHY-POLISH

must_haves:
  truths:
    - 'Service page below-fold sections animate in on scroll'
    - 'Service page feature cards have tilt effect on desktop hover'
    - 'Product media placeholder structure exists on each service page'
    - 'Typography spacing is consistent across all pages at 1280px and 1440px'
    - 'Supporting pages have scroll reveal on below-fold sections'
  artifacts:
    - path: 'src/pages/AutomationsPage.tsx'
      provides: 'Automations page with ScrollReveal + card-tilt + ProductMedia placeholder'
      contains: 'ScrollReveal'
    - path: 'src/index.css'
      provides: 'Typography polish utilities'
      contains: 'tracking-tight'
  key_links:
    - from: 'src/pages/AutomationsPage.tsx'
      to: 'src/components/common/ScrollReveal.tsx'
      via: 'section wrapper'
      pattern: 'ScrollReveal'
    - from: 'src/pages/AutomationsPage.tsx'
      to: 'src/components/common/ProductMedia.tsx'
      via: 'demo media section'
      pattern: 'ProductMedia'
---

<objective>
Apply ScrollReveal, card-tilt, and ProductMedia placeholders across all service pages and supporting pages. Fine-tune typography spacing for premium feel.

Purpose: Extend the premium micro-interactions from homepage to every page. Add product media placeholders so real assets can be dropped in later. Polish typography for consistent rhythm.
Output: Updated service pages, supporting pages, and index.css typography utilities.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-design-polish-media/12-RESEARCH.md
@.planning/phases/12-design-polish-media/12-01-SUMMARY.md

@src/pages/AutomationsPage.tsx
@src/pages/ChatbotsPage.tsx
@src/pages/VoiceAgentsPage.tsx
@src/pages/MarketingMachinePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ScrollReveal + card-tilt + ProductMedia placeholder to service pages</name>
  <files>src/pages/AutomationsPage.tsx, src/pages/ChatbotsPage.tsx, src/pages/VoiceAgentsPage.tsx, src/pages/MarketingMachinePage.tsx</files>
  <action>
For each of the 4 service pages (AutomationsPage, ChatbotsPage, VoiceAgentsPage, MarketingMachinePage):

1. **Import ScrollReveal** from '../components/common/ScrollReveal'
2. **Import ProductMedia** from '../components/common/ProductMedia'

3. **ScrollReveal on below-fold sections:**
   - Wrap each section BELOW the hero in a `<ScrollReveal>` component
   - Hero sections already have CSS fadeIn — do NOT wrap heroes
   - Use section-level wrapping (one ScrollReveal per section, not per card)
   - Stagger delays: first section delay={0}, second delay={0.1}, third delay={0.15}
   - Maximum 4-5 ScrollReveal wrappers per page

4. **card-tilt class on feature/capability cards:**
   - Find the main feature/capability cards on each page (the ones with card-gradient-border)
   - Add `card-tilt` to their className (alongside existing card-gradient-border)
   - This gives them the subtle CSS tilt defined in Plan 02's index.css addition
   - Do NOT add card-tilt to small UI elements, badges, or non-card containers

5. **ProductMedia placeholder section:**
   - On each service page, add a ProductMedia component in a logical location (e.g., after features, before final CTA):

   ```tsx
   <ScrollReveal>
     <div className="max-w-7xl mx-auto px-12 py-16">
       <ProductMedia
         videoSrc="/media/placeholder-{service}.mp4"
         posterSrc="/media/placeholder-{service}-poster.webp"
         alt="{Service name} demo"
         className="max-w-4xl mx-auto"
       />
     </div>
   </ScrollReveal>
   ```

   - Replace `{service}` with: automations, chatbots, voice-agents, marketing-machine
   - The actual media files don't exist yet — ProductMedia handles missing sources gracefully (video shows poster, poster shows alt text)
   - Add a `{/* TODO: Replace with real demo video/screenshot */}` comment above each ProductMedia
   - For MarketingMachinePage specifically: place ProductMedia after the features section and before pricing teaser

6. **Preserve all existing content, i18n, routes, and functionality.** Only ADD ScrollReveal wrappers, card-tilt classes, and ProductMedia sections. Do not restructure layouts.

IMPORTANT: If any service page already uses Framer Motion whileInView on individual elements, leave those as-is. Only add ScrollReveal to sections that DON'T already have scroll-triggered animations. Avoid double-animating.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit src/pages/AutomationsPage.tsx src/pages/ChatbotsPage.tsx src/pages/VoiceAgentsPage.tsx src/pages/MarketingMachinePage.tsx 2>&1 | head -15</automated>
</verify>
<done>All 4 service pages have ScrollReveal on below-fold sections, card-tilt on feature cards, and ProductMedia placeholder with TODO comment</done>
</task>

<task type="auto">
  <name>Task 2: Add ScrollReveal to supporting pages + typography polish in index.css</name>
  <files>src/pages/AboutPage.tsx, src/pages/PricingPage.tsx, src/pages/HowItWorksPage.tsx, src/index.css</files>
  <action>
1. **Supporting pages — ScrollReveal:**
   For AboutPage, PricingPage, HowItWorksPage:
   - Import ScrollReveal from '../components/common/ScrollReveal'
   - Wrap below-fold sections in ScrollReveal (same pattern as service pages)
   - Do NOT wrap heroes (already have CSS fadeIn)
   - Do NOT double-animate sections that already have Framer Motion whileInView
   - Add card-tilt to pricing cards on PricingPage (the tier cards with card-gradient-border)
   - Add card-tilt to team/value cards on AboutPage
   - Skip ContactPage and LegalPage (text-heavy, no card-based sections that benefit from tilt/reveal)

2. **Typography polish in index.css:**
   Add a typography polish section after existing animation keyframes:

   ```css
   /* ── Typography Polish ── */

   /* Tighter tracking on large display headings */
   .font-display {
     letter-spacing: -0.02em;
   }

   /* Consistent section spacing rhythm */
   .section-gap {
     padding-top: 5rem;
     padding-bottom: 5rem;
   }

   @media (min-width: 1280px) {
     .section-gap {
       padding-top: 6rem;
       padding-bottom: 6rem;
     }

     /* Slightly larger headings at xl */
     .hero-heading-xl {
       font-size: 4rem;
       line-height: 1.1;
     }
   }

   @media (min-width: 1440px) {
     .section-gap {
       padding-top: 7rem;
       padding-bottom: 7rem;
     }

     .hero-heading-xl {
       font-size: 4.5rem;
     }
   }
   ```

   - These are opt-in utility classes, NOT global overrides. Plans/future work can apply them incrementally.
   - The font-display letter-spacing of -0.02em tightens Space Grotesk headings for a more premium feel at large sizes.

3. **Do NOT modify any existing Tailwind config** — these are CSS utilities that layer on top of the existing system.
   </action>
   <verify>
   <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit src/pages/AboutPage.tsx src/pages/PricingPage.tsx src/pages/HowItWorksPage.tsx 2>&1 | head -10</automated>
   </verify>
   <done>Supporting pages have ScrollReveal on below-fold sections, pricing/about cards have card-tilt, typography polish utilities added to index.css</done>
   </task>

</tasks>

<verification>
- All service pages (4) and supporting pages (3) have ScrollReveal wrappers on below-fold sections
- Feature cards across service pages have card-tilt class
- ProductMedia placeholders exist on all 4 service pages with TODO comments
- Typography polish utilities (font-display tracking, section-gap, hero-heading-xl) in index.css
- `npm run build` completes without errors
</verification>

<success_criteria>

- Scroll reveal animations work across all service and supporting pages
- Card tilt hover effect visible on desktop across feature cards
- ProductMedia placeholder structure ready for real assets on 4 service pages
- Typography spacing utilities available for premium heading treatment
  </success_criteria>

<output>
After completion, create `.planning/phases/12-design-polish-media/12-03-SUMMARY.md`
</output>
