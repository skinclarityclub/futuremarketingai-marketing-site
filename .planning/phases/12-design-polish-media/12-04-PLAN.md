---
phase: 12-design-polish-media
plan: 04
type: execute
wave: 3
depends_on: ['12-02', '12-03']
files_modified:
  - src/components/common/index.ts
autonomous: false
requirements:
  - REQ-HERO-3D
  - REQ-SCROLL-MICRO
  - REQ-PRODUCT-MEDIA
  - REQ-TYPOGRAPHY-POLISH

must_haves:
  truths:
    - 'New components exported from common/index.ts barrel'
    - 'Full site builds without errors'
    - 'Scroll reveals, card tilt, and CTAButton arrow animation all function correctly on desktop'
    - 'All animations disabled when prefers-reduced-motion is set'
  artifacts:
    - path: 'src/components/common/index.ts'
      provides: 'Updated barrel export including SplineHero, ScrollReveal, ProductMedia'
      contains: 'ScrollReveal'
  key_links:
    - from: 'src/components/common/index.ts'
      to: 'src/components/common/ScrollReveal.tsx'
      via: 'barrel export'
      pattern: 'export.*ScrollReveal'
---

<objective>
Export new components from barrel file and verify the complete Phase 12 integration across all pages.

Purpose: Final wiring and quality gate — ensure all micro-interactions, scroll reveals, and media placeholders work cohesively.
Output: Updated index.ts barrel, verified build.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-design-polish-media/12-02-SUMMARY.md
@.planning/phases/12-design-polish-media/12-03-SUMMARY.md
@src/components/common/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add barrel exports and run full build verification</name>
  <files>src/components/common/index.ts</files>
  <action>
1. Add exports to `src/components/common/index.ts`:
   ```typescript
   export { SplineHero } from './SplineHero'
   export { ScrollReveal } from './ScrollReveal'
   export { ProductMedia } from './ProductMedia'
   ```

2. Run full build:

   ```bash
   npm run build
   ```

   Fix any type errors or import issues.

3. Verify no regressions:
   - Check that build output size is reasonable (Spline runtime adds ~200KB as a lazy chunk, not to main bundle)
   - Confirm no console warnings about unused imports
     </action>
     <verify>
     <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npm run build 2>&1 | tail -20</automated>
     </verify>
     <done>All new components exported from barrel, full build passes, Spline runtime isolated in lazy chunk</done>
     </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 12 micro-interactions</name>
  <files>N/A</files>
  <action>
Human verifies the complete Phase 12 integration across all pages.

What was built:

- SplineHero component ready (OrbitVisual fallback active until Spline scene URL set)
- ScrollReveal wrappers on all below-fold sections across homepage, service pages, and supporting pages
- Card tilt hover effect on feature cards (desktop only)
- CTAButton arrow icon shifts on hover
- ProductMedia placeholder sections on 4 service pages (with TODO comments for real assets)
- Typography polish utilities (letter-spacing, section-gap, hero-heading-xl)

How to verify:

1. Run `npm run dev` and open http://localhost:5173
2. **Homepage:** Scroll down — sections should fade in smoothly. Hover service cards — subtle tilt. Hover CTA buttons with arrows — arrow shifts right.
3. **Service pages** (/automations, /chatbots, /voice-agents, /marketing-machine): Below-fold sections fade in on scroll. Feature cards have tilt. ProductMedia placeholder visible.
4. **Supporting pages** (/about, /pricing, /how-it-works): Scroll reveals on below-fold sections. Pricing/about cards have tilt hover.
5. **Reduced motion:** Enable "Reduce motion" in OS settings, reload — all animations disabled, elements visible immediately.
6. **Mobile (resize below 1024px):** Card tilt NOT active. OrbitVisual/SplineHero hidden.
   </action>
   <verify>
   <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npm run build 2>&1 | tail -5</automated>
   </verify>
   <done>Human approves visual quality of micro-interactions, scroll reveals work across all pages, reduced motion respected, mobile experience unchanged</done>
   </task>

</tasks>

<verification>
- Full `npm run build` succeeds
- All new components in barrel export
- Human verification of scroll reveals, card tilt, arrow animation, reduced motion respect
</verification>

<success_criteria>

- Human approves visual quality of micro-interactions across all pages
- Build passes with no errors
- Reduced motion fully respected
- Mobile experience unchanged (no new animations on touch devices)
  </success_criteria>

<output>
After completion, create `.planning/phases/12-design-polish-media/12-04-SUMMARY.md`
</output>
