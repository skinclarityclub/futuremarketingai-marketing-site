---
phase: 16-chatbot-personas-knowledge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chatbot/knowledge/leadgen-kb.ts
  - src/lib/chatbot/tools/leadgen-tools.ts
  - src/lib/chatbot/personas/leadgen.ts
  - src/lib/chatbot/knowledge/support-kb.ts
  - src/lib/chatbot/tools/support-tools.ts
  - src/lib/chatbot/personas/support.ts
autonomous: true
requirements:
  - REQ-CHATBOT-PERSONAS

must_haves:
  truths:
    - 'Lead-gen persona registered with B2B SaaS qualification framework, ICP criteria, pricing, and ROI topics'
    - 'Support persona registered with mock helpdesk FAQ articles covering billing, technical, account, and getting started'
    - 'Both personas have working tool definitions with AI SDK v6 tool() + Zod 4 inputSchema'
    - 'Both personas have i18n conversation starters in EN/NL/ES'
  artifacts:
    - path: 'src/lib/chatbot/knowledge/leadgen-kb.ts'
      provides: 'TopicDefinition[] for qualification framework, ICP, pricing tiers, ROI benchmarks'
      exports: ['LEADGEN_TOPICS']
    - path: 'src/lib/chatbot/tools/leadgen-tools.ts'
      provides: 'AI SDK tool() definitions for qualify_lead, get_pricing_info, schedule_demo, get_roi_estimate'
      exports: ['leadgenTools']
    - path: 'src/lib/chatbot/personas/leadgen.ts'
      provides: 'PersonaConfig + registerPersona() side-effect + i18n starters'
      exports: ['leadgenPersona', 'LEADGEN_STARTERS']
    - path: 'src/lib/chatbot/knowledge/support-kb.ts'
      provides: 'TopicDefinition[] for billing, technical, account, getting started FAQ articles'
      exports: ['SUPPORT_TOPICS']
    - path: 'src/lib/chatbot/tools/support-tools.ts'
      provides: 'AI SDK tool() definitions for search_knowledge_base, create_ticket, check_status, escalate_to_human'
      exports: ['supportTools']
    - path: 'src/lib/chatbot/personas/support.ts'
      provides: 'PersonaConfig + registerPersona() side-effect + i18n starters'
      exports: ['supportPersona', 'SUPPORT_STARTERS']
  key_links:
    - from: 'src/lib/chatbot/personas/leadgen.ts'
      to: 'src/lib/chatbot/persona-router.ts'
      via: 'registerPersona(leadgenPersona) side-effect call'
      pattern: 'registerPersona'
    - from: 'src/lib/chatbot/personas/support.ts'
      to: 'src/lib/chatbot/persona-router.ts'
      via: 'registerPersona(supportPersona) side-effect call'
      pattern: 'registerPersona'
---

<objective>
Build lead-gen and support persona vertical slices: knowledge bases, tool definitions, and persona configs with i18n starters.

Purpose: Create personas 3 and 4 of 5. Lead-gen demonstrates B2B SaaS lead qualification. Support demonstrates knowledge base / helpdesk capabilities. Both are demo personas showcasing FMai chatbot capabilities to prospects.

Output: 6 new files — knowledge, tools, and persona config for lead-gen and support personas.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/lib/chatbot/types.ts
@src/lib/chatbot/persona-router.ts
@src/lib/chatbot/tool-executor.ts

<interfaces>
<!-- Phase 15 interfaces that persona files must conform to -->

From src/lib/chatbot/types.ts:

```typescript
export interface PersonaConfig {
  id: string
  name: string
  description: string
  staticPrefix: string
  topicDefinitions: TopicDefinition[]
  tools: Record<string, unknown>
  defaultModel: ComplexityLevel
  complexityKeywords?: string[]
  maxTokens: number
  temperature: number
}

export interface TopicDefinition {
  key: string
  content: string
  keywords: string[]
  priority: number
}

export type ComplexityLevel = 'haiku' | 'sonnet'
```

From src/lib/chatbot/tool-executor.ts (DEMO_TOOL pattern):

```typescript
import { tool } from 'ai'
import { z } from 'zod'

export const DEMO_TOOL = tool({
  description: 'A demo tool that echoes input',
  inputSchema: z.object({
    input: z.string().describe('The text to echo back'),
  }),
  execute: async ({ input }) => ({
    echo: input,
    timestamp: new Date().toISOString(),
  }),
})
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lead-gen persona — knowledge base, tools, and persona config</name>
  <files>src/lib/chatbot/knowledge/leadgen-kb.ts, src/lib/chatbot/tools/leadgen-tools.ts, src/lib/chatbot/personas/leadgen.ts</files>
  <action>
Create 3 files for the lead qualification bot demo persona:

**1. `src/lib/chatbot/knowledge/leadgen-kb.ts`** — Export `LEADGEN_TOPICS: TopicDefinition[]`

Create 4-5 topic definitions for a mock B2B SaaS lead qualification scenario. The mock SaaS product is a "marketing automation platform" (generic, not FMai-branded).

Topics:

- `qualification` (priority 10): BANT + MEDDIC lite framework. Budget (what range?), Authority (decision maker?), Need (pain points?), Timeline (when to implement?). Lead scoring: 0-30 cold, 31-60 warm, 61-85 hot, 86-100 qualified. Include ICP criteria: B2B, 20-500 employees, marketing team of 3+, current manual processes, EUR 1000+/mo budget.
- `pricing_tiers` (priority 9): 3 tiers — Starter (EUR 299/mo, up to 5 users, basic automation), Professional (EUR 799/mo, up to 20 users, advanced workflows + analytics), Enterprise (EUR 1999/mo, unlimited users, custom integrations + dedicated CSM). Annual discount 20%.
- `roi_data` (priority 8): ROI benchmarks — average 3.2x ROI in 6 months, 15 hours/week time savings per marketing team member, 40% increase in qualified leads, 25% reduction in CAC. Include formula: ROI = (time_saved_hours _ hourly_rate + additional_revenue - subscription_cost) / subscription_cost _ 100.
- `product_features` (priority 7): Key features — email sequences, landing page builder, lead scoring, CRM integration, analytics dashboard, A/B testing, multi-channel campaigns, workflow automation. Integrations: Salesforce, HubSpot, Slack, Zapier.
- `competitors` (priority 6): Positioning vs competitors — faster implementation (2 weeks vs 2 months), more affordable (30-50% less), better support (dedicated CSM from Professional tier), AI-powered optimization.

**2. `src/lib/chatbot/tools/leadgen-tools.ts`** — Export `leadgenTools` record

Create 4 tools:

- `qualify_lead`: inputSchema `{ companySize: z.number().optional(), budget: z.number().optional(), timeline: z.enum(['immediate', '1-3months', '3-6months', '6months+']).optional(), useCase: z.string().optional(), isDecisionMaker: z.boolean().optional() }`. Execute calculates a lead score (0-100) based on inputs: companySize 20-500 (+20), budget >1000 (+25), timeline immediate/1-3mo (+20), useCase provided (+15), isDecisionMaker true (+20). Return `{ score, qualification: 'cold'|'warm'|'hot'|'qualified', recommendation, nextSteps: string[] }`.
- `get_pricing_info`: inputSchema `{ tier: z.enum(['starter', 'professional', 'enterprise', 'all']).default('all') }`. Execute returns pricing data from static object matching the knowledge base tiers.
- `schedule_demo`: inputSchema `{ name: z.string(), email: z.string(), companyName: z.string().optional(), preferredTime: z.string().optional() }`. Execute returns `{ action: 'schedule_demo', confirmationId: 'DEMO-' + Date.now(), message: 'Demo scheduled! ...' }` (simulated).
- `get_roi_estimate`: inputSchema `{ teamSize: z.number(), currentHoursOnMarketing: z.number(), averageHourlyRate: z.number().default(50), monthlySubscription: z.number().default(799) }`. Execute calculates ROI: timeSaved = teamSize _ 15 _ 4, costSaved = timeSaved _ hourlyRate, roi = ((costSaved - monthlySubscription) / monthlySubscription _ 100). Return `{ monthlySavings, annualSavings, roiPercent, paybackPeriodMonths }`.

**3. `src/lib/chatbot/personas/leadgen.ts`** — Export `leadgenPersona: PersonaConfig` and `LEADGEN_STARTERS: Record<string, string[]>`

PersonaConfig:

- `id`: `'leadgen'`
- `name`: `'Sales Assistant'`
- `description`: `'Lead qualification bot demo — B2B SaaS prospect scoring and ROI estimation'`
- `staticPrefix`: System prompt ~1500-2000 tokens. Role: SDR for a B2B SaaS marketing automation platform. Communication style: professional, consultative, asks smart qualifying questions naturally (don't fire a questionnaire), use the qualify_lead tool after gathering enough info. Decision rules: new visitor → ask about company/role, sizing info given → ask about pain points, pain identified → estimate ROI, qualified lead → suggest booking demo. What NOT to do: no pushy sales tactics, no fake urgency, no made-up customer names, always be helpful even if lead is unqualified.
- `topicDefinitions`: Import `LEADGEN_TOPICS`
- `tools`: `{ qualify_lead: true, get_pricing_info: true, schedule_demo: true, get_roi_estimate: true }`
- `defaultModel`: `'haiku'`
- `complexityKeywords`: `['ROI calculation', 'competitor comparison', 'enterprise requirements', 'custom integration']`
- `maxTokens`: 500
- `temperature`: 0.7

Call `registerPersona(leadgenPersona)` at module scope.

LEADGEN_STARTERS:

- EN: "I'm looking for a marketing automation tool", "What are your pricing plans?", "We're a team of 50 people", "Can you estimate our ROI?"
- NL: "Ik zoek een marketing automation tool", "Wat zijn jullie prijzen?", "We zijn een team van 50 mensen", "Kun je onze ROI inschatten?"
- ES: "Busco una herramienta de automatizacion de marketing", "Cuales son sus planes de precios?", "Somos un equipo de 50 personas", "Pueden estimar nuestro ROI?"
  </action>
  <verify>
  <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit --pretty 2>&1 | grep -E "leadgen|knowledge/lead|tools/lead" | head -20</automated>
  </verify>
  <done>3 lead-gen files created. qualify_lead tool calculates score from inputs. get_roi_estimate tool computes ROI formula. PersonaConfig conforms to interface. registerPersona called. Starters in EN/NL/ES.</done>
  </task>

<task type="auto">
  <name>Task 2: Support persona — knowledge base, tools, and persona config</name>
  <files>src/lib/chatbot/knowledge/support-kb.ts, src/lib/chatbot/tools/support-tools.ts, src/lib/chatbot/personas/support.ts</files>
  <action>
Create 3 files for the knowledge base / support demo persona:

**1. `src/lib/chatbot/knowledge/support-kb.ts`** — Export `SUPPORT_TOPICS: TopicDefinition[]`

Create 4-5 topic definitions for a mock SaaS helpdesk. Include mock FAQ articles.

Topics:

- `billing` (priority 10): FAQ articles about billing — how to update payment method, how to upgrade/downgrade plan, refund policy (14-day money-back), invoice access (Settings → Billing → Invoices), cancellation process (Settings → Subscription → Cancel, takes effect end of billing period).
- `technical` (priority 9): FAQ articles — API rate limits (1000 req/min starter, 5000 professional, unlimited enterprise), webhook setup (Settings → Integrations → Webhooks), SSO configuration (Enterprise only, SAML 2.0), data export (Settings → Data → Export, CSV or JSON format).
- `account` (priority 8): FAQ articles — password reset (login page → Forgot Password → email link), 2FA setup (Settings → Security → Enable 2FA, supports authenticator apps), team member management (Settings → Team → Invite, roles: admin/editor/viewer), account deletion (contact support, 30-day data retention).
- `getting_started` (priority 7): FAQ articles — first-time setup checklist (connect email → import contacts → create first campaign → set up tracking), quickstart guide (5 steps to first campaign), integration setup (one-click for major platforms).

Also export a `KB_ARTICLES` array of article objects for the search tool: `{ id: string, title: string, category: string, content: string, tags: string[] }`. Include 15-20 articles across all categories.

**2. `src/lib/chatbot/tools/support-tools.ts`** — Export `supportTools` record

Import `KB_ARTICLES` from `../knowledge/support-kb`.

Create 4 tools:

- `search_knowledge_base`: inputSchema `{ query: z.string(), category: z.enum(['billing', 'technical', 'account', 'getting_started', 'all']).default('all'), limit: z.number().min(1).max(5).default(3) }`. Execute searches KB_ARTICLES by matching query (case-insensitive substring in title, content, or tags). If category !== 'all', filter by category first. Return `{ articles: matched.slice(0, limit).map(a => ({ id, title, category, snippet: a.content.slice(0, 200) })) }`.
- `create_ticket`: inputSchema `{ category: z.enum(['billing', 'technical', 'account', 'bug', 'feature_request']), subject: z.string(), description: z.string(), priority: z.enum(['low', 'medium', 'high']).default('medium') }`. Execute returns `{ ticketId: 'TKT-' + Date.now(), status: 'open', category, subject, priority, estimatedResponse: '24 hours', message: 'Ticket created successfully' }` (simulated).
- `check_status`: inputSchema `{ ticketId: z.string() }`. Execute returns mock status: `{ ticketId, status: 'in_progress', lastUpdate: 'Our team is reviewing your request', updatedAt: new Date().toISOString() }` (always returns in_progress for demo).
- `escalate_to_human`: inputSchema `{ reason: z.string(), ticketId: z.string().optional() }`. Execute returns `{ action: 'escalate', message: 'Connecting you with a human agent...', estimatedWait: '2-5 minutes', ticketId }` (simulated handoff).

**3. `src/lib/chatbot/personas/support.ts`** — Export `supportPersona: PersonaConfig` and `SUPPORT_STARTERS: Record<string, string[]>`

PersonaConfig:

- `id`: `'support'`
- `name`: `'Support Agent'`
- `description`: `'Knowledge base support bot demo — FAQ search, ticket creation, and human escalation'`
- `staticPrefix`: System prompt ~1500-2000 tokens. Role: support agent for a SaaS product. Communication style: empathetic, patient, solution-oriented, always search knowledge base before saying "I don't know". Decision rules: common question → search KB first, KB has answer → share with source reference, KB no answer → offer to create ticket, frustrated customer → offer to escalate to human, bug report → create high-priority ticket. What NOT to do: no making up answers not in KB, no sharing internal processes, no promising specific resolution times beyond "24 hours", always offer escalation as an option.
- `topicDefinitions`: Import `SUPPORT_TOPICS`
- `tools`: `{ search_knowledge_base: true, create_ticket: true, check_status: true, escalate_to_human: true }`
- `defaultModel`: `'haiku'`
- `complexityKeywords`: `['data migration', 'API integration issue', 'security concern', 'compliance']`
- `maxTokens`: 500
- `temperature`: 0.5 (lower for support — more deterministic)

Call `registerPersona(supportPersona)` at module scope.

SUPPORT_STARTERS:

- EN: "How do I reset my password?", "I'm having trouble with my integration", "I want to cancel my subscription", "Can I talk to a human?"
- NL: "Hoe reset ik mijn wachtwoord?", "Ik heb problemen met mijn integratie", "Ik wil mijn abonnement opzeggen", "Kan ik met een mens praten?"
- ES: "Como restablezco mi contrasena?", "Tengo problemas con mi integracion", "Quiero cancelar mi suscripcion", "Puedo hablar con un humano?"
  </action>
  <verify>
  <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit --pretty 2>&1 | grep -E "support|knowledge/sup|tools/sup" | head -20</automated>
  </verify>
  <done>3 support files created. KB_ARTICLES array with 15-20 articles. search_knowledge_base filters by query + category. create_ticket returns simulated ticket. PersonaConfig conforms to interface. registerPersona called. Starters in EN/NL/ES.</done>
  </task>

</tasks>

<verification>
```bash
cd C:/Users/daley/Desktop/Futuremarketingai && npx tsc --noEmit --pretty 2>&1 | tail -5
```
- TypeScript compilation passes with no errors
- Both persona files call registerPersona() at module scope
- Tool names in persona.tools records match tool names in *-tools.ts exports
- KB_ARTICLES array has 15-20 entries
- Lead scoring formula produces scores 0-100
</verification>

<success_criteria>

- 6 new files created (3 per persona)
- Lead-gen qualify_lead tool computes score from BANT inputs
- Lead-gen get_roi_estimate tool computes ROI from team/cost inputs
- Support search_knowledge_base tool searches 15-20 article entries
- Both PersonaConfigs conform to types.ts interface
- All 8 tools use AI SDK v6 tool() + Zod 4 inputSchema pattern
- registerPersona() called at module scope in both persona files
- LEADGEN_STARTERS and SUPPORT_STARTERS export EN/NL/ES arrays
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/16-chatbot-personas-knowledge/16-02-SUMMARY.md`
</output>
