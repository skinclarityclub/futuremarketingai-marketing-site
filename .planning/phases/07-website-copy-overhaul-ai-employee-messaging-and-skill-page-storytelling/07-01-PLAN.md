---
phase: 07-website-copy-overhaul-ai-employee-messaging-and-skill-page-storytelling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/src/app/[locale]/(skills)/skills/chatbot/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/email/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/content-creator/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/voice-agent/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/lead-qualifier/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/social-media/page.tsx
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/es.json
autonomous: true
requirements:
  - WEB-01

must_haves:
  truths:
    - 'All 8 skill pages use t() translation functions for every visible string'
    - 'Chatbot and email pages have full translation namespace keys in all 3 JSON files'
    - 'No hardcoded English strings remain in any skill page JSX (pricing props, trust metrics, social proof, section headings)'
    - 'NL and ES locale pages render translated text for chatbot and email skills'
  artifacts:
    - path: 'fmai-nextjs/src/app/[locale]/(skills)/skills/chatbot/page.tsx'
      provides: 'Chatbot page using getTranslations with skills-chatbot namespace'
      contains: 'getTranslations'
    - path: 'fmai-nextjs/src/app/[locale]/(skills)/skills/email/page.tsx'
      provides: 'Email page using getTranslations with skills-email namespace'
      contains: 'getTranslations'
    - path: 'fmai-nextjs/messages/en.json'
      provides: 'Full skills-chatbot and skills-email namespaces with all keys'
      contains: 'skills-chatbot'
    - path: 'fmai-nextjs/messages/nl.json'
      provides: 'Dutch translations for chatbot and email namespaces'
      contains: 'skills-chatbot'
    - path: 'fmai-nextjs/messages/es.json'
      provides: 'Spanish translations for chatbot and email namespaces'
      contains: 'skills-chatbot'
  key_links:
    - from: 'fmai-nextjs/src/app/[locale]/(skills)/skills/chatbot/page.tsx'
      to: 'fmai-nextjs/messages/en.json'
      via: 'getTranslations namespace binding'
      pattern: 'getTranslations.*skills-chatbot'
    - from: 'fmai-nextjs/src/app/[locale]/(skills)/skills/email/page.tsx'
      to: 'fmai-nextjs/messages/en.json'
      via: 'getTranslations namespace binding'
      pattern: 'getTranslations.*skills-email'
---

<objective>
Fix translation architecture across all 8 skill pages so every visible string uses the t() translation function.

Purpose: Chatbot and email pages are almost entirely hardcoded English. Four other pages (content-creator, voice-agent, lead-qualifier, social-media) have hardcoded pricing/trust/social-proof props. All hardcoded strings must be extracted to translation keys before the Clyde copy rewrite can proceed. This is the prerequisite for all subsequent plans.

Output: All 8 skill pages fully using t() calls; full translation namespaces in all 3 JSON files for chatbot and email; hardcoded pricing/trust/social-proof props extracted to translation keys on remaining 4 pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-website-copy-overhaul-ai-employee-messaging-and-skill-page-storytelling/07-RESEARCH.md

@fmai-nextjs/src/app/[locale]/(skills)/skills/chatbot/page.tsx
@fmai-nextjs/src/app/[locale]/(skills)/skills/email/page.tsx
@fmai-nextjs/src/app/[locale]/(skills)/skills/content-creator/page.tsx
@fmai-nextjs/src/app/[locale]/(skills)/skills/voice-agent/page.tsx
@fmai-nextjs/src/app/[locale]/(skills)/skills/lead-qualifier/page.tsx
@fmai-nextjs/src/app/[locale]/(skills)/skills/social-media/page.tsx
@fmai-nextjs/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert chatbot and email pages to use translation keys</name>
  <files>
    fmai-nextjs/src/app/[locale]/(skills)/skills/chatbot/page.tsx
    fmai-nextjs/src/app/[locale]/(skills)/skills/email/page.tsx
    fmai-nextjs/messages/en.json
    fmai-nextjs/messages/nl.json
    fmai-nextjs/messages/es.json
  </files>
  <action>
    For BOTH chatbot/page.tsx and email/page.tsx:

    1. Add `getTranslations` import from 'next-intl/server' (alongside existing setRequestLocale)
    2. Add `const t = await getTranslations({ locale, namespace: 'skills-chatbot' })` (or 'skills-email') at the top of the component
    3. Replace EVERY hardcoded string with a t() call. This includes:
       - WebPageJsonLd name and description
       - Hero badge, h1 title, description, CTA button text
       - All use case items (4 per page, each with title/description)
       - SectionHeading texts ("Use Cases", "Pricing", "Results That Speak", etc.)
       - PricingTiers props (3 tiers each with name/price/period/features array/badge)
       - TrustMetrics props (3 metrics each with value/label/description)
       - Final CTA section description and button text

    4. In en.json, nl.json, and es.json, create full `skills-chatbot` and `skills-email` namespaces following the established pattern from 07-RESEARCH.md. Use the EXACT key structure documented:
       - meta (title, description)
       - hero (badge, title, description)
       - features (heading, items with title/description)
       - how_it_works (heading, steps with title/description)
       - use_cases (heading, items with title/description)
       - pricing (heading, tiers array with name/price/period/features/badge)
       - trust (heading, metrics with value/label/description)
       - cta (title, subtitle, button)

    5. For en.json: Use the CURRENT hardcoded English strings as initial values (preserve existing copy for now -- Clyde rewrite happens in Plan 02/03)
    6. For nl.json: Write proper Dutch translations of the English strings (not machine-translated -- natural Dutch)
    7. For es.json: Write proper Spanish translations (natural Spanish, not literal)

    Keep the exact same key names across all 3 JSON files. JSON structure must match exactly.

    IMPORTANT: Do NOT change the visual layout or component structure of the pages. Only replace string literals with t() calls.

  </action>
  <verify>
    Run `cd fmai-nextjs && npx next build` -- no build errors. Visit /en/skills/chatbot, /nl/skills/chatbot, /es/skills/chatbot in dev and confirm text renders in correct language. Same for /skills/email.
  </verify>
  <done>
    Chatbot and email pages render fully translated text on EN/NL/ES locales. Zero hardcoded English strings remain in either page.tsx file. All 3 JSON files have matching skills-chatbot and skills-email namespace structures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract hardcoded props from content-creator, voice-agent, lead-qualifier, social-media pages</name>
  <files>
    fmai-nextjs/src/app/[locale]/(skills)/skills/content-creator/page.tsx
    fmai-nextjs/src/app/[locale]/(skills)/skills/voice-agent/page.tsx
    fmai-nextjs/src/app/[locale]/(skills)/skills/lead-qualifier/page.tsx
    fmai-nextjs/src/app/[locale]/(skills)/skills/social-media/page.tsx
    fmai-nextjs/messages/en.json
    fmai-nextjs/messages/nl.json
    fmai-nextjs/messages/es.json
  </files>
  <action>
    These 4 pages already use getTranslations for hero/features/how-it-works/use-cases/cta sections, but have HARDCODED English strings passed as props to shared components. Extract them to translation keys:

    **Content Creator page:**
    - SocialProof props (quote, author, role, company) -> t('social_proof.quote'), etc.
    - FeatureShowcase features array (6 items with title/description/icon) -> t('showcase.items.{key}.title'), etc.
    - SectionHeading strings ("The AI Content Engine", "See It in Action", "Pricing") -> t('sections.engine'), etc.
    - ProductMedia props (title, description) -> t('product_media.title'), etc.
    - PricingTiers props (3 tiers) -> t('pricing.tiers.{tier}.name'), etc.

    **Voice Agent page:**
    - SectionHeading "Pricing", "Results That Speak" -> t('sections.pricing'), t('sections.results')
    - PricingTiers props (3 tiers) -> t('pricing.tiers.{tier}.name'), etc.
    - TrustMetrics props (3 metrics) -> t('trust.metrics.{key}.value'), etc.

    **Lead Qualifier page:**
    - Same pattern as voice-agent: pricing + trust sections

    **Social Media page:**
    - SocialProof props -> translation keys
    - FeatureShowcase features array (6 items) -> translation keys
    - SectionHeading strings -> translation keys
    - PricingTiers props (3 tiers) -> translation keys
    - TrustMetrics props (3 metrics) -> translation keys

    Add corresponding keys to each page's existing namespace in en.json (current English strings as values), nl.json (Dutch translations), and es.json (Spanish translations).

    IMPORTANT: Do NOT rename existing keys that already work. Only ADD new keys for the hardcoded props.

  </action>
  <verify>
    Run `cd fmai-nextjs && npx next build` -- no build errors. Grep for remaining hardcoded strings in all 4 page files: `grep -n '"[A-Z]' page.tsx` should only match import paths and CSS classes, not visible text.
  </verify>
  <done>
    All 8 skill pages use t() for every user-visible string. Zero hardcoded English text remains in any skill page JSX. All 3 JSON files have matching key structures for every namespace.
  </done>
</task>

</tasks>

<verification>
- `cd fmai-nextjs && npx next build` completes without errors
- All 8 skill pages render correct text on /en/, /nl/, and /es/ locales
- No hardcoded English strings in any skill page.tsx file (only in import paths, class names, component names)
- All 3 JSON files (en.json, nl.json, es.json) have identical key structures for skills-chatbot and skills-email namespaces
</verification>

<success_criteria>
Every skill page is fully internationalized with zero hardcoded strings. The translation architecture is clean and ready for the Clyde copy rewrite in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/07-website-copy-overhaul-ai-employee-messaging-and-skill-page-storytelling/07-01-SUMMARY.md`
</output>
