---
phase: 10-production-integrity-domain-ssot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/src/lib/seo-config.ts
  - fmai-nextjs/src/lib/og-image.tsx
  - fmai-nextjs/src/app/[locale]/layout.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/contact/page.tsx
  - fmai-nextjs/src/components/layout/HeaderClient.tsx
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/es.json
  - fmai-nextjs/.env.example
  - fmai-nextjs/vercel.json
  - CLAUDE.md
  - fmai-nextjs/CLAUDE.md
autonomous: false

user_setup:
  - service: vercel-domain
    why: 'Set future-marketing.ai as canonical domain and 301-redirect futuremarketingai.com.'
    dashboard_config:
      - task: 'Confirm future-marketing.ai is the primary (starred) domain in Vercel project settings'
        location: 'Vercel dashboard -> fmai-nextjs project -> Settings -> Domains'
      - task: 'Set futuremarketingai.com (and www.*) to 301-redirect to future-marketing.ai with path preservation'
        location: 'Same page, three-dot menu on the legacy domain -> Redirect to'
      - task: 'Verify DNS for future-marketing.ai points to Vercel (A 76.76.21.21 or CNAME cname.vercel-dns.com)'
        location: 'Your DNS provider (e.g. Cloudflare / TransIP / Hetzner)'

requirements:
  - PHASE-10

must_haves:
  truths:
    - 'SITE_URL in src/lib/seo-config.ts equals https://future-marketing.ai'
    - 'ORG_EMAIL in src/lib/seo-config.ts equals hello@future-marketing.ai'
    - 'Zero occurrences of futuremarketingai.com remain in src/ messages/ or .env.example (public/llms*.txt handled in 10-03)'
    - 'All three locale files use future-marketing.ai and hello@future-marketing.ai for customer-facing copy'
    - 'privacy@future-marketing.ai is still present on the legal page and only there'
    - 'CLAUDE.md root and fmai-nextjs/CLAUDE.md explicitly state future-marketing.ai as canonical'
    - 'Vercel 301-redirect from futuremarketingai.com to future-marketing.ai is live (verified via curl with -L)'
  artifacts:
    - path: 'fmai-nextjs/src/lib/seo-config.ts'
      provides: 'Canonical SITE_URL and ORG_EMAIL'
      contains: 'future-marketing.ai'
    - path: 'fmai-nextjs/.env.example'
      provides: 'All email env vars use future-marketing.ai'
      contains: 'future-marketing.ai'
    - path: 'fmai-nextjs/vercel.json'
      provides: 'Redirect fallback for legacy domain (Option B backup if dashboard redirect is not enough)'
      contains: 'futuremarketingai.com'
  key_links:
    - from: 'fmai-nextjs/src/lib/seo-config.ts'
      to: 'Every JSON-LD, sitemap, canonical, hreflang, OG URL in the app'
      via: 'SITE_URL export'
      pattern: 'SITE_URL'
---

<objective>
Unify the domain and customer-facing email across the entire marketing site on the canonical `https://future-marketing.ai` plus `hello@future-marketing.ai`.

Purpose: today 54 occurrences across 12 files use the legacy `futuremarketingai.com` domain while CLAUDE.md and the user's memory say `future-marketing.ai` is the live domain. This inconsistency cascades into every canonical URL, hreflang, JSON-LD, sitemap, OG card, email, and AI crawler surface. Five different contact emails (hello@, privacy@, info@, apply@, contact@) are also in use. A prospect landing on the contact page sees `hello@`, then the chatbot suggests `contact@`, then `Organization` schema emits `info@`. Trust leak plus schema drift.

Output: one canonical domain (future-marketing.ai), one customer-facing email (hello@future-marketing.ai), privacy@ retained only on the legal page, legacy domain 301-redirected at the Vercel edge with path preserved.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-production-integrity-domain-ssot/README.md
@.planning/phases/10-production-integrity-domain-ssot/RESEARCH.md
@fmai-nextjs/CLAUDE.md
@fmai-nextjs/src/lib/seo-config.ts
@fmai-nextjs/.env.example
@fmai-nextjs/messages/nl.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Daley confirms canonical domain plus DNS plus Vercel dashboard redirect</name>
  <action>
Orchestrator pauses and asks Daley to verify in the Vercel dashboard:

1. Open https://vercel.com/dashboard, select `fmai-nextjs` project, Settings -> Domains.
2. Confirm `future-marketing.ai` is listed and starred as Primary. If not present: click "Add" and follow DNS instructions (A 76.76.21.21 or CNAME cname.vercel-dns.com).
3. Confirm `futuremarketingai.com` (and `www.futuremarketingai.com`) are present. If so, click the three-dot menu next to each and select "Redirect to -> future-marketing.ai", 301, preserve path.
4. From a terminal, run `curl -I -L https://futuremarketingai.com/nl/pricing` and verify the final URL is `https://future-marketing.ai/nl/pricing` with one 301 hop.

Daley must respond with one of:
- "dashboard done" (skip Task 7, the vercel.json fallback)
- "use vercel.json" (Task 7 adds infra-as-code redirect)
- "need DNS help" (execution pauses, Daley adds DNS records before proceeding)
  </action>
  <what-built>Confirmation that future-marketing.ai is the production canonical and legacy domain is redirected.</what-built>
  <how-to-verify>curl -I -L https://futuremarketingai.com | head -5 shows 301 to https://future-marketing.ai.</how-to-verify>
  <resume-signal>Type "dashboard done" or "use vercel.json" or describe issue</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update seo-config.ts with canonical domain and email</name>
  <files>fmai-nextjs/src/lib/seo-config.ts</files>
  <action>
Edit `fmai-nextjs/src/lib/seo-config.ts`:

Replace line 1:
```ts
export const SITE_URL = 'https://futuremarketingai.com'
```
with:
```ts
export const SITE_URL = 'https://future-marketing.ai'
```

Replace line 3:
```ts
export const ORG_EMAIL = 'info@futuremarketingai.com'
```
with:
```ts
export const ORG_EMAIL = 'hello@future-marketing.ai'
```

Leave `SITE_NAME`, `LINKEDIN_URL`, `ENTITY_DESCRIPTION` unchanged.

Also update `PAGE_DATES['/legal']` from `'2026-03-18'` to `'2026-04-24'` to reflect freshness after this phase lands.
  </action>
  <verify>
  <automated>cd fmai-nextjs && grep -c 'future-marketing.ai' src/lib/seo-config.ts && grep -L 'futuremarketingai.com' src/lib/seo-config.ts && echo PASS || echo FAIL</automated>
  </verify>
  <done>SITE_URL equals https://future-marketing.ai, ORG_EMAIL equals hello@future-marketing.ai, no legacy domain remains in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Update og-image.tsx, contact page, HeaderClient and layout references</name>
  <files>fmai-nextjs/src/lib/og-image.tsx, fmai-nextjs/src/app/[locale]/layout.tsx, fmai-nextjs/src/app/[locale]/(marketing)/contact/page.tsx, fmai-nextjs/src/components/layout/HeaderClient.tsx</files>
  <action>
For each file, replace every string occurrence of `futuremarketingai.com` with `future-marketing.ai` and every email `@futuremarketingai.com` with `@future-marketing.ai`. Do NOT touch imports, types, env var names, or comments.

Expected replacements (verify with grep first):

1. `src/lib/og-image.tsx`: 1 occurrence, likely a footer URL rendered in the OG card SVG.
2. `src/app/[locale]/layout.tsx`: 1 occurrence, likely inside a metadata object (canonical or metadataBase) or JSON-LD wrapper.
3. `src/app/[locale]/(marketing)/contact/page.tsx`: 1 occurrence. Inspect: probably an `<a href="mailto:hello@futuremarketingai.com">`. Replace to `mailto:hello@future-marketing.ai`.
4. `src/components/layout/HeaderClient.tsx`: 1 occurrence. Inspect: probably a hardcoded CTA link or footer element.

Command sequence:
```bash
cd fmai-nextjs
grep -n 'futuremarketingai\.com' src/lib/og-image.tsx src/app/[locale]/layout.tsx 'src/app/[locale]/(marketing)/contact/page.tsx' src/components/layout/HeaderClient.tsx
```

Read each hit, apply Edit with `old_string` = full enclosing line, `new_string` = same line with domain swapped. If the line contains both `https://futuremarketingai.com` and an email `X@futuremarketingai.com`, update both. Keep indentation and quoting as-is.

Do NOT bulk sed-replace, because some JSON-LD builder might have `${SITE_URL}` already and not a literal string.
  </action>
  <verify>
  <automated>cd fmai-nextjs && COUNT=$(grep -rn 'futuremarketingai\.com' src/lib/og-image.tsx src/app/[locale]/layout.tsx 'src/app/[locale]/(marketing)/contact/page.tsx' src/components/layout/HeaderClient.tsx 2>/dev/null | wc -l) && test "$COUNT" -eq 0 && echo PASS || echo "FAIL: $COUNT occurrences remain"</automated>
  </verify>
  <done>All 4 targeted files use `future-marketing.ai` exclusively. No occurrences of legacy domain remain in these files.</done>
</task>

<task type="auto">
  <name>Task 4: Update all three locale message files with canonical domain plus unified email</name>
  <files>fmai-nextjs/messages/nl.json, fmai-nextjs/messages/en.json, fmai-nextjs/messages/es.json</files>
  <action>
Each locale file has 5 occurrences of `futuremarketingai.com` per RESEARCH.md section 8. Locations per audit 06 section 2.1 and 8.1:
- contact block (~line 822, 1569): `hello@futuremarketingai.com`
- legal block (~line 915): `privacy@futuremarketingai.com`
- possibly inline URLs in legal subscription_terms or about text

Strategy: run `grep -n 'futuremarketingai\.com' messages/nl.json` to list every line. For each:

Rule 1 — EMAIL rewriting
- `hello@futuremarketingai.com` becomes `hello@future-marketing.ai`
- `privacy@futuremarketingai.com` stays as `privacy@future-marketing.ai` (domain only, keep `privacy@` prefix)
- `info@futuremarketingai.com` becomes `hello@future-marketing.ai` (unify onto customer-facing)
- `contact@futuremarketingai.com` becomes `hello@future-marketing.ai` (unify)
- `apply@futuremarketingai.com` only exists in .env.example (handled Task 5)

Rule 2 — URL rewriting
- Any `https://futuremarketingai.com/...` becomes `https://future-marketing.ai/...`
- Any bare `futuremarketingai.com` in prose becomes `future-marketing.ai`

Apply identical edits in `en.json` and `es.json` on the same line numbers (they mirror NL structure).

IMPORTANT: `messages/nl.json` line 915 and mirror lines should end up with `privacy@future-marketing.ai` (legal page only). All other customer-facing contacts: `hello@future-marketing.ai`.

Verify via: `grep -c 'hello@future-marketing.ai' messages/*.json` (should be >= 3 per file), `grep -c 'privacy@future-marketing.ai' messages/*.json` (should be >= 1 per file), `grep -c 'futuremarketingai\.com' messages/*.json` (should be 0).
  </action>
  <verify>
  <automated>cd fmai-nextjs && for f in messages/nl.json messages/en.json messages/es.json; do CNT=$(grep -c 'futuremarketingai\.com' "$f"); HELLO=$(grep -c 'hello@future-marketing.ai' "$f"); PRIV=$(grep -c 'privacy@future-marketing.ai' "$f"); echo "$f legacy=$CNT hello=$HELLO privacy=$PRIV"; done</automated>
  </verify>
  <done>Each of nl/en/es.json has 0 legacy occurrences, at least 2 hello@future-marketing.ai matches, and at least 1 privacy@future-marketing.ai match.</done>
</task>

<task type="auto">
  <name>Task 5: Update .env.example with canonical domain and standardized email vars</name>
  <files>fmai-nextjs/.env.example</files>
  <action>
Read current `.env.example`. Apply these line-exact edits:

Line 9 replace:
```
NEXT_PUBLIC_SITE_URL=https://futuremarketingai.com
```
with:
```
NEXT_PUBLIC_SITE_URL=https://future-marketing.ai
```

Line 17 replace:
```
APPLY_EMAIL_TO=hello@futuremarketingai.com
```
with:
```
APPLY_EMAIL_TO=hello@future-marketing.ai
```

Line 18 replace:
```
APPLY_EMAIL_FROM=apply@futuremarketingai.com
```
with:
```
APPLY_EMAIL_FROM=apply@future-marketing.ai
```

Line 21 replace:
```
CONTACT_EMAIL_TO=info@futuremarketingai.com
```
with:
```
CONTACT_EMAIL_TO=hello@future-marketing.ai
```

Add below line 21 (new):
```
CONTACT_EMAIL_FROM=contact@future-marketing.ai
```

Also add a new block below the Contact block (before the Stripe block), these three env vars that plans 10-02 and 10-04 rely on:
```
# Supabase — applications and contact_submissions tables live here (server-only)
# Used by /api/apply and /api/contact to persist leads alongside the Resend email.
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Upstash Redis — cross-isolate rate-limit for /api/apply and /api/contact
# Free tier is enough. Use EU region (Frankfurt) for AVG.
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxxxx

# Salt for hashing visitor IPs before storing in Supabase (AVG: never store raw IPs).
IP_HASH_SALT=change-me-to-a-random-32-char-string
```
  </action>
  <verify>
  <automated>cd fmai-nextjs && grep -c 'future-marketing.ai' .env.example && grep -c 'futuremarketingai\.com' .env.example | grep -q '^0$' && grep -q 'UPSTASH_REDIS_REST_URL' .env.example && grep -q 'SUPABASE_SERVICE_ROLE_KEY' .env.example && grep -q 'IP_HASH_SALT' .env.example && echo PASS || echo FAIL</automated>
  </verify>
  <done>.env.example uses canonical domain, customer email unified, new env vars for Supabase, Upstash and IP salt are documented for plans 10-02.</done>
</task>

<task type="auto">
  <name>Task 6: Update CLAUDE.md files with explicit canonical domain statement</name>
  <files>CLAUDE.md, fmai-nextjs/CLAUDE.md</files>
  <action>
The root `CLAUDE.md` (at `C:/Users/daley/Desktop/Futuremarketingai/CLAUDE.md`) already says "Productie: `future-marketing.ai`" in the one-liner section. Leave it but add a dated note under the "Actieve Werkstroom" section or at the top:

```
> Canonical domain (2026-04-24 unified): **https://future-marketing.ai**. Any legacy `futuremarketingai.com` in git history is 301-redirected at the Vercel edge. Do not re-introduce the legacy domain in new code.
```

For `fmai-nextjs/CLAUDE.md`, locate the "Environment" section (near the bottom, ~line 180). Rewrite the production line from:
```
- Productie: Vercel — `future-marketing.ai`
```
to:
```
- Productie: Vercel — canonical `https://future-marketing.ai`. `futuremarketingai.com` 301-redirects (dashboard rule set 2026-04-24). `NEXT_PUBLIC_SITE_URL` in .env.local must match the canonical.
```

Also find the "Red Flags" section and add a new bullet:
```
- Hardcoded `futuremarketingai.com` anywhere in code or copy — the domain was unified to `future-marketing.ai` on 2026-04-24. Always use the canonical. Customer-facing email is `hello@future-marketing.ai` except on the legal page which keeps `privacy@future-marketing.ai`.
```
  </action>
  <verify>
  <automated>grep -q 'future-marketing.ai' C:/Users/daley/Desktop/Futuremarketingai/CLAUDE.md && grep -q 'Hardcoded' C:/Users/daley/Desktop/Futuremarketingai/fmai-nextjs/CLAUDE.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>Both CLAUDE.md files have an explicit canonical-domain rule dated 2026-04-24 that prevents future regressions.</done>
</task>

<task type="auto">
  <name>Task 7: Add vercel.json infra-as-code redirect fallback (only if Daley chose "use vercel.json" in Task 1)</name>
  <files>fmai-nextjs/vercel.json</files>
  <action>
Skip this task entirely if Daley responded "dashboard done" in Task 1 (the dashboard redirect covers it).

If Daley responded "use vercel.json", create or extend `fmai-nextjs/vercel.json` (the file currently exists per audit 08 Appendix E `vercel.json:1-5`). Read it first and merge, do not overwrite.

Add a `redirects` array alongside whatever is already there:

```json
{
  "redirects": [
    {
      "source": "/(.*)",
      "has": [{ "type": "host", "value": "futuremarketingai.com" }],
      "destination": "https://future-marketing.ai/$1",
      "permanent": true
    },
    {
      "source": "/(.*)",
      "has": [{ "type": "host", "value": "www.futuremarketingai.com" }],
      "destination": "https://future-marketing.ai/$1",
      "permanent": true
    },
    {
      "source": "/(.*)",
      "has": [{ "type": "host", "value": "www.future-marketing.ai" }],
      "destination": "https://future-marketing.ai/$1",
      "permanent": true
    }
  ]
}
```

Note: redirects in vercel.json only trigger for domains added to the project in the Vercel dashboard. Adding here without adding the domain gives 404, not redirect.
  </action>
  <verify>
  <automated>cd fmai-nextjs && test -f vercel.json && cat vercel.json | grep -q 'future-marketing.ai' && echo PASS || echo "SKIPPED (dashboard path chosen)"</automated>
  </verify>
  <done>If vercel.json path was chosen, the file contains 3 redirect rules for legacy hosts. Otherwise task was skipped per Daley response.</done>
</task>

<task type="auto">
  <name>Task 8: Run full grep plus build plus lint to prove no domain regression</name>
  <files>fmai-nextjs/ (verification only)</files>
  <action>
Execute these commands in order:

```bash
cd fmai-nextjs

# 1. Full codebase grep should return zero (public/llms*.txt are handled by plan 10-03)
grep -rn 'futuremarketingai\.com' src/ messages/ .env.example 2>/dev/null | wc -l
# Expected: 0

# 2. Spot-check canonical URL is in new code
grep -rn 'https://future-marketing\.ai' src/lib/seo-config.ts | wc -l
# Expected: 1 (the SITE_URL export line)

# 3. Confirm build still passes
npm run build
# Expected: passes, 87 static pages, no new errors

# 4. Confirm lint has not regressed (pre-existing 36 errors may still be present from audit 08, they are cleaned in plan 10-04)
npm run lint 2>&1 | tail -5

# 5. Confirm OG card and metadata render with canonical
# (manual, open http://localhost:3000/nl after npm run dev and view source to confirm canonical href)
```

If the grep returns non-zero, iterate: read every hit, apply one Edit per hit, repeat grep. Do not merge.
  </action>
  <verify>
  <automated>cd fmai-nextjs && LEGACY=$(grep -rn 'futuremarketingai\.com' src/ messages/ .env.example 2>/dev/null | wc -l) && test "$LEGACY" -eq 0 && npm run build 2>&1 | grep -q 'Compiled successfully' && echo PASS || echo "FAIL legacy=$LEGACY"</automated>
  </verify>
  <done>Codebase is clean of legacy domain (except public/llms*.txt which plan 10-03 handles), build passes, canonical SITE_URL is live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Human smoke test — domain redirect plus canonical metadata</name>
  <action>Daley verifies the production redirect and canonical propagation.</action>
  <what-built>Unified canonical domain future-marketing.ai plus unified customer email hello@future-marketing.ai.</what-built>
  <how-to-verify>
1. `curl -I -L https://futuremarketingai.com/nl/pricing` shows 301 to `https://future-marketing.ai/nl/pricing` and final 200.
2. Open https://future-marketing.ai/nl, view source, confirm `<link rel="canonical" href="https://future-marketing.ai/nl">` and `<meta property="og:url" content="https://future-marketing.ai/nl">`.
3. Open https://future-marketing.ai/nl/contact, confirm visible email shows `hello@future-marketing.ai`, not `info@` or `contact@`.
4. Open https://future-marketing.ai/nl/legal, confirm privacy email still shows `privacy@future-marketing.ai` (retained separation).
5. View page source on any page, search for `futuremarketingai.com`: expect zero matches (llms*.txt regen lives in plan 10-03 and can still hit, that is acceptable until that plan lands).
  </how-to-verify>
  <resume-signal>Type "approved" or describe drift</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn 'futuremarketingai\.com' src/ messages/ .env.example` returns 0
- `grep -c 'https://future-marketing\.ai' src/lib/seo-config.ts` returns 1
- `npm run build` passes
- curl -I -L of legacy domain returns 301 to canonical
- Customer-facing page source shows canonical URL everywhere
</verification>

<success_criteria>
Canonical domain is `https://future-marketing.ai`, customer-facing email is `hello@future-marketing.ai`, privacy email is preserved only on the legal page, legacy domain 301-redirects at the Vercel edge with path preservation, both CLAUDE.md files document the rule dated 2026-04-24.
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-integrity-domain-ssot/10-01-SUMMARY.md`.
</output>
