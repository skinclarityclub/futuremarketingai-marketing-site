---
phase: 08-clyde-chatbot-personality
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - fmai-nextjs/src/components/chatbot/ChatWidgetIsland.tsx
  - fmai-nextjs/src/components/chatbot/ChatHeader.tsx
  - fmai-nextjs/src/lib/chatbot/engine.ts
autonomous: true
requirements:
  - WEB-01

must_haves:
  truths:
    - "Chat header shows 'Clyde' with subtitle 'AI Marketing Employee'"
    - 'Welcome message adapts per page (12 different pages mapped)'
    - 'Suggested prompts adapt per page context'
    - 'All 17 tools available on every page (no context filtering)'
    - "Floating chatbot uses personaId 'clyde' instead of 'flagship'"
  artifacts:
    - path: 'fmai-nextjs/src/components/chatbot/ChatWidgetIsland.tsx'
      provides: 'Context-aware welcome messages and suggested prompts based on pathname'
      contains: 'WELCOME_MESSAGES'
    - path: 'fmai-nextjs/src/components/chatbot/ChatHeader.tsx'
      provides: 'Clyde branding with AI Marketing Employee subtitle'
      contains: 'AI Marketing Employee'
    - path: 'fmai-nextjs/src/lib/chatbot/engine.ts'
      provides: 'All tools always available (no filterToolsByContext for Clyde)'
      contains: "persona.id === 'clyde'"
  key_links:
    - from: 'fmai-nextjs/src/components/chatbot/ChatWidgetIsland.tsx'
      to: 'fmai-nextjs/src/components/chatbot/ChatWidget.tsx'
      via: 'props (personaId, welcomeMessage, suggestedPrompts)'
      pattern: 'personaId="clyde"'
    - from: 'fmai-nextjs/src/lib/chatbot/engine.ts'
      to: 'fmai-nextjs/src/lib/chatbot/tool-executor.ts'
      via: 'createPersonaTools returns all tools'
      pattern: 'createPersonaTools'
---

<objective>
Wire Clyde's context-aware UI: update the floating chatbot widget to show "Clyde" branding, adapt welcome messages and suggested prompts per page, and remove tool filtering so all 17 tools are available everywhere.

Purpose: Users see "Clyde - AI Marketing Employee" in the chat header, get page-relevant welcome messages (12 pages mapped), see context-appropriate suggested prompts, and Clyde has full tool access on every page.

Output: Updated ChatWidgetIsland, ChatHeader, and engine with Clyde-specific behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@docs/plans/2026-03-21-clyde-chatbot-personality-design.md
@.planning/phases/08-clyde-chatbot-personality-unified-persona-and-context-aware-messaging/08-01-SUMMARY.md
@fmai-nextjs/src/components/chatbot/ChatWidgetIsland.tsx
@fmai-nextjs/src/components/chatbot/ChatWidget.tsx
@fmai-nextjs/src/components/chatbot/ChatHeader.tsx
@fmai-nextjs/src/lib/chatbot/engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatWidgetIsland context-aware welcome messages and suggested prompts</name>
  <files>
    fmai-nextjs/src/components/chatbot/ChatWidgetIsland.tsx
  </files>
  <action>
Rewrite `ChatWidgetIsland.tsx` to pass context-aware props based on current pathname.

1. Add a `WELCOME_MESSAGES` map (Record<string, string>) using EXACT messages from the design doc:

| Path                      | Message                                                                                |
| ------------------------- | -------------------------------------------------------------------------------------- |
| `/`                       | "I'm Clyde. Ask me anything about what I can do for your agency."                      |
| `/skills/content-creator` | "I'm Clyde. Want to see me write content for one of your clients?"                     |
| `/skills/voice-agent`     | "I'm Clyde. I can show you how I handle phone calls — or answer any questions."        |
| `/skills/chatbot`         | "I'm Clyde. You're chatting with me right now. This is exactly what your clients get." |
| `/skills/lead-qualifier`  | "I'm Clyde. Let me show you how I score and qualify leads."                            |
| `/skills/social-media`    | "I'm Clyde. Want me to create a content calendar for your client?"                     |
| `/skills/ad-creator`      | "I'm Clyde. I can generate ad variations from any campaign brief."                     |
| `/skills/email`           | "I'm Clyde. I handle email campaigns, follow-ups, and inbox management."               |
| `/skills/reporting`       | "I'm Clyde. I can show you what a weekly performance report looks like."               |
| `/pricing`                | "I'm Clyde. I can calculate your ROI or walk you through the tiers."                   |
| `/about`                  | "I'm Clyde. Want to know more about how I work?"                                       |

Default (fallback for any other page): "I'm Clyde, your AI marketing employee. What do you need?"

2. Add a `SUGGESTED_PROMPTS` map (Record<string, string[]>) using messages from the design doc:

| Path                      | Prompts                                                                                             |
| ------------------------- | --------------------------------------------------------------------------------------------------- |
| `/`                       | ["What skills do you have?", "Show me a demo", "Calculate my ROI", "Book a strategy call"]          |
| `/skills/content-creator` | ["Write a blog post about SEO", "Create social posts for a restaurant", "Plan next week's content"] |
| `/skills/voice-agent`     | ["How do you handle calls?", "Show me a call script", "What languages do you support?"]             |
| `/skills/chatbot`         | ["Show me your capabilities", "How are you trained?", "Deploy a chatbot for my client"]             |
| `/skills/lead-qualifier`  | ["Score a sample lead", "How does BANT scoring work?", "Show qualification criteria"]               |
| `/pricing`                | ["Calculate ROI for my agency", "Compare tiers", "What's included in Growth?"]                      |

Default: ["What skills do you have?", "Show me a demo", "Calculate my ROI", "Book a strategy call"] (same as homepage)

3. Update the ChatWidget props:
   - `personaId="clyde"` (was "flagship")
   - `personaName="Clyde"` (was "FMai Concierge")
   - `welcomeMessage={WELCOME_MESSAGES[pathname] || WELCOME_MESSAGES.default}` (use a helper to resolve, matching pathname against the map with a fallback to `default` key)
   - `suggestedPrompts={SUGGESTED_PROMPTS[pathname] || SUGGESTED_PROMPTS.default}`
   - Keep `mode="floating"`, `pageContext={{ pathname }}`
   - Remove the hardcoded "Start guided demo" from suggestedPrompts -- the demo mode is still triggered via the DemoOrchestrator button in ChatMessages, not through suggested prompts

Store the default welcome message and default prompts under a `'default'` key in each map for clean fallback.
</action>
<verify>
Run `npx tsc --noEmit` from `fmai-nextjs/`. Grep `ChatWidgetIsland.tsx` for `personaId="clyde"` and `WELCOME_MESSAGES`. Count entries in WELCOME_MESSAGES map -- should be 12 (11 pages + default).
</verify>
<done>
ChatWidgetIsland passes personaId "clyde", context-aware welcome messages for 12 paths, and context-aware suggested prompts. No more "FMai Concierge" or "flagship" references.
</done>
</task>

<task type="auto">
  <name>Task 2: ChatHeader Clyde branding and engine tool-filtering removal</name>
  <files>
    fmai-nextjs/src/components/chatbot/ChatHeader.tsx
    fmai-nextjs/src/lib/chatbot/engine.ts
  </files>
  <action>
**ChatHeader.tsx changes:**

1. Change the subtitle text from `"AI Assistant"` to `"AI Marketing Employee"` (line 53-54). This serves as the EU AI Act disclosure per design doc.

2. No other changes needed -- the personaName prop already receives "Clyde" from ChatWidgetIsland (updated in Task 1).

**engine.ts changes:**

1. Update the tool filtering logic (lines 172-185) to give Clyde ALL 17 tools on every page. Replace the `if (persona.id === 'flagship')` block:

```typescript
// For Clyde: all tools always available (no page-based filtering)
// For demo mode: exclude navigate_to_page (catches too many queries)
if (persona.id === 'clyde' || persona.id === 'flagship') {
  if (context?.demoMode) {
    const { navigate_to_page: _, ...demoTools } = tools
    tools = demoTools
  }
  // No else -- Clyde gets all tools on every page
}
```

This removes the `filterToolsByContext` call for Clyde. Keep the `filterToolsByContext` function and constants (`ALWAYS_AVAILABLE`, `PAGE_TOOLS`) in the file for now -- they are dead code but harmless, and removing them could break if any other code references them.

2. Keep the `'flagship'` check as a fallback so if any persisted store data still references 'flagship', it still works. The `||` handles both.
   </action>
   <verify>
   Run `npx tsc --noEmit` from `fmai-nextjs/`. Grep `ChatHeader.tsx` for "AI Marketing Employee". Grep `engine.ts` for "clyde" to confirm the new persona check exists. Verify no `filterToolsByContext` call remains in the active Clyde code path.
   </verify>
   <done>
   Chat header shows "AI Marketing Employee" subtitle. Clyde has all 17 tools on every page with no context filtering. Demo mode still excludes navigate_to_page. Flagship fallback preserved for persisted store compatibility.
   </done>
   </task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. ChatWidgetIsland uses personaId "clyde", personaName "Clyde"
3. Welcome messages map has 12 entries (11 pages + default)
4. Suggested prompts map has entries for 6+ pages + default
5. ChatHeader subtitle reads "AI Marketing Employee"
6. Engine gives Clyde all tools without page-based filtering
7. Dev server renders floating chatbot with "Clyde" header
</verification>

<success_criteria>

- Chat header displays "Clyde" with "AI Marketing Employee" subtitle
- Welcome message changes based on current page path (12 variants)
- Suggested prompts change based on current page path
- All 17 tools available to Clyde regardless of page
- TypeScript compiles without errors
- Floating chatbot widget works in dev server
  </success_criteria>

<output>
After completion, create `.planning/phases/08-clyde-chatbot-personality-unified-persona-and-context-aware-messaging/08-02-SUMMARY.md`
</output>
