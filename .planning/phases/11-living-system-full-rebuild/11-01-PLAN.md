---
phase: 11-living-system-full-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - tailwind.config.js
  - src/index.css
  - src/components/common/GradientMesh.tsx
  - src/components/common/index.ts
autonomous: true
requirements:
  - REQ-LIVING-SYSTEM-REBUILD

must_haves:
  truths:
    - 'DM Sans loads as primary body font across all pages'
    - 'Three animated gradient blobs float behind all page content'
    - 'Card and button border-radius tokens match prototype (20px cards, 14px buttons)'
    - 'CSS keyframe animations for blobs, fadeIn, fadeInUp, and spin are available globally'
  artifacts:
    - path: 'index.html'
      provides: 'DM Sans Google Font link'
      contains: 'DM+Sans'
    - path: 'tailwind.config.js'
      provides: 'Updated fontFamily.sans, borderRadius.card, borderRadius.btn'
      contains: 'DM Sans'
    - path: 'src/index.css'
      provides: 'Blob CSS, card-gradient-border utility, fadeIn/fadeInUp/spin keyframes'
      contains: 'blobFloat1'
    - path: 'src/components/common/GradientMesh.tsx'
      provides: 'Global fixed gradient mesh background'
      exports: ['GradientMesh']
  key_links:
    - from: 'index.html'
      to: 'tailwind.config.js'
      via: 'Font family declaration matches loaded font'
      pattern: 'DM Sans'
    - from: 'src/components/common/GradientMesh.tsx'
      to: 'src/index.css'
      via: 'Uses blob CSS classes defined in index.css'
      pattern: 'blob'
---

<objective>
Foundation layer: typography switch (Inter to DM Sans), global GradientMesh background component, CSS animations, and Tailwind config updates (border-radius tokens).

Purpose: All subsequent plans depend on these foundation changes. Typography, background, and border-radius tokens must exist before rebuilding pages.
Output: DM Sans loaded, GradientMesh component created, CSS animations defined, Tailwind config updated with card/btn radius tokens.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-living-system-full-rebuild/11-RESEARCH.md
@index.html
@tailwind.config.js
@src/index.css
@src/components/common/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typography + Tailwind config + CSS animations</name>
  <files>index.html, tailwind.config.js, src/index.css</files>
  <action>
1. **index.html** — Update the Google Fonts link to add DM Sans weights 300,400,500,600,700. Keep Inter, JetBrains Mono, Space Grotesk. The updated href should be:
   `https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;700&family=Space+Grotesk:wght@400;500;600;700&display=swap`

2. **tailwind.config.js** — Make these changes:
   - `fontFamily.sans`: Change from `['Inter', ...]` to `['DM Sans', '-apple-system', 'BlinkMacSystemFont', 'Segoe UI', 'sans-serif']`
   - `fontFamily.display`: Change from `['Space Grotesk', 'Inter', ...]` to `['Space Grotesk', 'DM Sans', 'sans-serif']`
   - `borderRadius`: Add `'card': '20px'` and `'btn': '14px'` entries (keep all existing values)

3. **src/index.css** — Add the following at the end of the `@layer utilities` block (or create one if needed):

   CSS keyframe animations (add to root level, not inside @layer):

   ```css
   @keyframes blobFloat1 {
     0%,
     100% {
       transform: translate(0, 0) scale(1);
     }
     33% {
       transform: translate(30px, -50px) scale(1.05);
     }
     66% {
       transform: translate(-20px, 20px) scale(0.95);
     }
   }
   @keyframes blobFloat2 {
     0%,
     100% {
       transform: translate(0, 0) scale(1);
     }
     33% {
       transform: translate(-40px, 30px) scale(1.08);
     }
     66% {
       transform: translate(25px, -35px) scale(0.92);
     }
   }
   @keyframes blobFloat3 {
     0%,
     100% {
       transform: translate(0, 0) scale(1);
     }
     33% {
       transform: translate(35px, 25px) scale(0.96);
     }
     66% {
       transform: translate(-30px, -40px) scale(1.04);
     }
   }
   @keyframes fadeIn {
     from {
       opacity: 0;
     }
     to {
       opacity: 1;
     }
   }
   @keyframes fadeInUp {
     from {
       opacity: 0;
       transform: translateY(24px);
     }
     to {
       opacity: 1;
       transform: translateY(0);
     }
   }
   @keyframes spin {
     from {
       transform: rotate(0deg);
     }
     to {
       transform: rotate(360deg);
     }
   }
   ```

   Blob classes (inside @layer utilities or after it):

   ```css
   .blob {
     position: absolute;
     border-radius: 50%;
     filter: blur(100px);
     opacity: 0.12;
     will-change: transform;
   }
   .blob-warm {
     width: 600px;
     height: 600px;
     background: #f5a623;
     top: -10%;
     right: -5%;
     animation: blobFloat1 25s ease-in-out infinite;
   }
   .blob-cool {
     width: 500px;
     height: 500px;
     background: #0abab5;
     bottom: -15%;
     left: -10%;
     animation: blobFloat2 30s ease-in-out infinite;
   }
   .blob-mixed {
     width: 400px;
     height: 400px;
     background: linear-gradient(135deg, #f5a623, #0abab5);
     top: 40%;
     left: 50%;
     animation: blobFloat3 22s ease-in-out infinite;
   }
   ```

   Card gradient border utility (inside @layer utilities):

   ```css
   .card-gradient-border {
     position: relative;
     overflow: hidden;
   }
   .card-gradient-border::before {
     content: '';
     position: absolute;
     inset: 0;
     border-radius: inherit;
     padding: 1px;
     background: linear-gradient(135deg, transparent, transparent);
     -webkit-mask:
       linear-gradient(#fff 0 0) content-box,
       linear-gradient(#fff 0 0);
     -webkit-mask-composite: xor;
     mask-composite: exclude;
     transition: background 0.5s ease;
     pointer-events: none;
   }
   .card-gradient-border:hover::before {
     background: linear-gradient(135deg, #f5a623, #0abab5);
   }
   ```

   Also add `prefers-reduced-motion` respect for blob animations:

   ```css
   @media (prefers-reduced-motion: reduce) {
     .blob {
       animation: none !important;
     }
   }
   ```

     </action>
     <verify>
       <automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep "DM.Sans" index.html && grep "DM Sans" tailwind.config.js && grep "blobFloat1" src/index.css && grep "card-gradient-border" src/index.css && grep "'card'" tailwind.config.js && grep "'btn'" tailwind.config.js</automated>
     </verify>
     <done>DM Sans loaded in index.html, tailwind.config.js has DM Sans as primary sans + card/btn radius tokens, index.css has all keyframes + blob classes + card-gradient-border utility</done>
   </task>

<task type="auto">
  <name>Task 2: Create GradientMesh component and wire globally</name>
  <files>src/components/common/GradientMesh.tsx, src/components/common/index.ts</files>
  <action>
1. **Create `src/components/common/GradientMesh.tsx`:**
   ```tsx
   import React from 'react'

/\*\*
_ GradientMesh — Global animated gradient mesh background.
_ Uses 3 CSS-only blobs with blur(100px) and CSS keyframe animations.
_ Replaces the 4 Framer Motion background layers (NeuralNetwork, HolographicGrid, FloatingParticles, GradientOrbs).
_ Rendered once in App.tsx — position: fixed covers all pages.
\*/
export const GradientMesh: React.FC = () => (
<div className="fixed inset-0 z-0 overflow-hidden pointer-events-none" aria-hidden="true">
<div className="blob blob-warm" />
<div className="blob blob-cool" />
<div className="blob blob-mixed" />
</div>
)

export default GradientMesh

```

2. **Update `src/components/common/index.ts`:**
Add export: `export { GradientMesh } from './GradientMesh'`

NOTE: Do NOT wire GradientMesh into App.tsx or AppLayout yet — that happens in Plan 03 when the Hero background layers are removed, to avoid Pitfall 5 (removing backgrounds without replacement).
</action>
<verify>
 <automated>cd C:/Users/daley/Desktop/Futuremarketingai && test -f src/components/common/GradientMesh.tsx && grep "GradientMesh" src/components/common/index.ts</automated>
</verify>
<done>GradientMesh.tsx created with 3 blob divs, exported from common/index.ts, ready for global wiring in Plan 03</done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- DM Sans appears as body font in browser
- Blob keyframes and card-gradient-border utility exist in CSS
- GradientMesh component is importable from common barrel
</verification>

<success_criteria>
- DM Sans is the primary sans font in tailwind.config.js and loaded in index.html
- Tailwind has `rounded-card` (20px) and `rounded-btn` (14px) tokens
- All 6 CSS keyframe animations defined (blobFloat1-3, fadeIn, fadeInUp, spin)
- Blob classes and card-gradient-border utility in index.css
- GradientMesh component created and exported
- Build passes (`npx vite build`)
</success_criteria>

<output>
After completion, create `.planning/phases/11-living-system-full-rebuild/11-01-SUMMARY.md`
</output>
```
