---
phase: 11-living-system-full-rebuild
plan: 07
type: execute
wave: 4
depends_on:
  - 11-03
files_modified:
  - src/components/landing/SimplifiedHeroMobile.tsx
autonomous: false
requirements:
  - REQ-LIVING-SYSTEM-REBUILD

must_haves:
  truths:
    - 'Mobile hero uses blob background styling (not grid/particles)'
    - 'Mobile hero CTAs use warm gradient (inherited from CTAButton update)'
    - 'Mobile hero content matches new design language (DM Sans, rounded corners)'
    - 'Visual quality confirmed by human review across all key pages'
  artifacts:
    - path: 'src/components/landing/SimplifiedHeroMobile.tsx'
      provides: 'Rebuilt mobile hero matching new design language'
  key_links:
    - from: 'src/components/landing/SimplifiedHeroMobile.tsx'
      to: 'src/components/common/CTAButton.tsx'
      via: 'Mobile hero uses CTAButton (warm gradient inherited)'
      pattern: 'CTAButton'
---

<objective>
Rebuild SimplifiedHeroMobile to match the new design language, then human-verify the full site visual quality.

Purpose: Mobile hero is the last component needing structural rebuild. Final visual verification ensures all 7 plans produced a cohesive result.
Output: Mobile hero rebuilt. Full site verified visually.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-living-system-full-rebuild/11-RESEARCH.md
@.planning/phases/11-living-system-full-rebuild/11-03-SUMMARY.md
@src/components/landing/SimplifiedHeroMobile.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild SimplifiedHeroMobile for new design language</name>
  <files>src/components/landing/SimplifiedHeroMobile.tsx</files>
  <action>
Rebuild the mobile hero to match the Living System Full Rebuild design language. Keep ALL existing content, i18n translations, CTA behavior, and conditional rendering logic. Change only the visual structure:

1. **Background:** Remove any grid/particle/neural-network background elements. The mobile hero should use a simplified version of the blob background — a single `.blob-warm` element positioned at top-right with reduced size (300px, opacity 0.08) for mobile performance. Or rely on the global GradientMesh (check if it's visible on mobile).

2. **Layout:** Center-aligned content (mobile convention). Stack vertically: badge → headline → description → CTA buttons.

3. **Typography:**
   - Badge: Small, `font-mono text-xs tracking-wider uppercase text-accent-system`
   - Headline: `text-3xl font-display font-bold text-text-primary`
   - Description: `text-base text-text-secondary leading-relaxed`

4. **CTA buttons:** Use CTAButton component (already updated with warm gradient). Stack vertically on mobile with gap-3.

5. **Card styling:** If mobile hero has any cards, use `rounded-card bg-white/[0.02] border border-border-primary` (no gradient border hover on mobile — hover doesn't work on touch).

6. **Remove:** Any old grid backgrounds (CSS grid overlay), particle animations, heavy Framer Motion wrappers. Replace with CSS fadeIn where needed.

7. **Spacing:** `px-6 py-16` padding (mobile-appropriate).

CRITICAL (from CLAUDE.md): "Desktop components NEVER modified for mobile." — This task modifies ONLY SimplifiedHeroMobile.tsx, which is the mobile-specific component. Do NOT touch Hero.tsx.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep -c "grid-background\|particle\|neural" src/components/landing/SimplifiedHeroMobile.tsx | grep "^0$" && grep "font-display\|rounded-card\|CTAButton" src/components/landing/SimplifiedHeroMobile.tsx</automated>
</verify>
<done>SimplifiedHeroMobile uses new design language: DM Sans body, font-display headings, warm gradient CTAs, no old background layers, rounded-card if applicable</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full-site visual verification</name>
  <files>All rebuilt pages</files>
  <action>
Human reviews the complete Living System Full Rebuild across all pages. Built: typography (DM Sans), gradient mesh background, left-aligned hero with orbit visual, 2x2 service cards, warm gradient CTAs, backdrop-blur navigation, card-gradient-border on all cards, rebuilt mobile hero, MarketingMachinePage i18n, Footer token cleanup.

Verification steps:

1. Start dev server: `npm run dev`
2. Homepage (desktop): Hero left-aligned + orbit visual, 2x2 service cards with gradient border hover, warm amber CTA buttons, floating gradient blobs, DM Sans body + Space Grotesk headings
3. Navigation: Full-width backdrop-blur, FM + gradient ai logo, gradient underline on link hover, warm gradient nav CTA
4. Service pages (/automations, /chatbots, /voice-agents): Cards with gradient border hover, rounded-card corners, consistent design
5. Supporting pages (/about, /pricing, /how-it-works, /contact): Same card patterns, pricing highlight styling, glass contact form
6. MarketingMachinePage (/marketing-machine): Renders in EN, NL, ES via language switcher, cards with gradient borders
7. Footer: No blue/indigo color inconsistencies
8. Mobile (375px): Mobile hero renders, warm gradient CTAs, no horizontal scroll
9. Performance: Smooth scrolling, fast page loads
   </action>
   <verify>
   <automated>cd C:/Users/daley/Desktop/Futuremarketingai && npx vite build 2>&1 | tail -5</automated>
   </verify>
   <done>Human confirms full-site visual quality matches prototype design language. Type "approved" or describe issues.</done>
   </task>

</tasks>

<verification>
- All pages visually match the prototype-2-living-system.html design language
- No old glassmorphism, flat teal, or sharp-corner patterns remain
- Mobile hero works correctly
- i18n works on MarketingMachinePage in all 3 languages
- Build passes: `npx vite build`
</verification>

<success_criteria>

- SimplifiedHeroMobile rebuilt with new design language
- Human visual review confirms full-site consistency
- No regression in any page's functionality
- Build passes
- Phase 11 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/11-living-system-full-rebuild/11-07-SUMMARY.md`
</output>
