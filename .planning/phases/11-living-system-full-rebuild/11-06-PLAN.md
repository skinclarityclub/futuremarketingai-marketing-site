---
phase: 11-living-system-full-rebuild
plan: 06
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/pages/MarketingMachinePage.tsx
  - src/locales/en/common.json
  - src/locales/nl/common.json
  - src/locales/es/common.json
autonomous: true
requirements:
  - REQ-LIVING-SYSTEM-REBUILD

must_haves:
  truths:
    - 'MarketingMachinePage uses useTranslation for all user-visible strings'
    - 'EN, NL, ES common.json files have marketing_machine namespace with all required keys'
    - 'MarketingMachinePage uses prototype card/layout patterns (card-gradient-border, rounded-card)'
    - 'NL and ES translations are present (can be English placeholders initially)'
  artifacts:
    - path: 'src/pages/MarketingMachinePage.tsx'
      provides: 'i18n-enabled MarketingMachinePage with prototype layout'
    - path: 'src/locales/en/common.json'
      provides: 'marketing_machine namespace with EN strings'
      contains: 'marketing_machine'
    - path: 'src/locales/nl/common.json'
      provides: 'marketing_machine namespace with NL strings'
      contains: 'marketing_machine'
    - path: 'src/locales/es/common.json'
      provides: 'marketing_machine namespace with ES strings'
      contains: 'marketing_machine'
  key_links:
    - from: 'src/pages/MarketingMachinePage.tsx'
      to: 'src/locales/en/common.json'
      via: "useTranslation('common') + t('marketing_machine.*') calls"
      pattern: 'marketing_machine'
---

<objective>
Add i18n support to MarketingMachinePage (currently all hardcoded EN) and rebuild layout to match prototype aesthetic.

Purpose: MarketingMachinePage is the only page without i18n support. Adding translations + structural rebuild completes the full-site consistency.
Output: MarketingMachinePage fully internationalized (EN/NL/ES) with prototype card/layout patterns.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-living-system-full-rebuild/11-RESEARCH.md
@src/pages/MarketingMachinePage.tsx
@src/locales/en/common.json
@src/locales/nl/common.json
@src/locales/es/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys for MarketingMachinePage to all 3 locale files</name>
  <files>src/locales/en/common.json, src/locales/nl/common.json, src/locales/es/common.json</files>
  <action>
Read MarketingMachinePage.tsx to inventory ALL hardcoded English strings. Create a `marketing_machine` key block in each locale's common.json.

**Process:**

1. Read `src/pages/MarketingMachinePage.tsx` completely
2. Extract every user-visible hardcoded string (headings, descriptions, button text, feature names, benefit lists, pricing teaser text, CTAs)
3. Create translation keys under `marketing_machine` namespace

**Key structure example:**

```json
"marketing_machine": {
  "hero_badge": "FutureMarketingAI",
  "hero_title": "Your AI Marketing Machine",
  "hero_description": "...",
  "hero_cta": "Book Strategy Call",
  "features_title": "...",
  "features": {
    "feature_1": { "title": "...", "description": "..." },
    "feature_2": { "title": "...", "description": "..." }
  },
  "benefits_title": "...",
  "social_proof_title": "...",
  "pricing_teaser": {
    "title": "...",
    "description": "...",
    "cta": "..."
  }
}
```

**EN:** Use the existing hardcoded English text as values.
**NL:** Translate to Dutch. Use natural marketing Dutch (not formal/stilted).
**ES:** Translate to Spanish. Use natural marketing Spanish.

**CRITICAL:**

- Do NOT create duplicate top-level keys in any JSON file (Phase 10 decision — ES had this issue)
- Add `marketing_machine` as a NEW top-level key, do not nest inside existing keys
- Validate JSON is valid after editing (no trailing commas, proper escaping)
  </action>
  <verify>
  <automated>cd C:/Users/daley/Desktop/Futuremarketingai && node -e "JSON.parse(require('fs').readFileSync('src/locales/en/common.json'))" && node -e "JSON.parse(require('fs').readFileSync('src/locales/nl/common.json'))" && node -e "JSON.parse(require('fs').readFileSync('src/locales/es/common.json'))" && node -e "const j=JSON.parse(require('fs').readFileSync('src/locales/en/common.json'));console.log(j.marketing_machine ? 'EN OK' : 'EN MISSING')" && node -e "const j=JSON.parse(require('fs').readFileSync('src/locales/nl/common.json'));console.log(j.marketing_machine ? 'NL OK' : 'NL MISSING')" && node -e "const j=JSON.parse(require('fs').readFileSync('src/locales/es/common.json'));console.log(j.marketing_machine ? 'ES OK' : 'ES MISSING')"</automated>
  </verify>
  <done>All 3 locale files have valid JSON with marketing_machine namespace containing all required keys. EN has original text, NL has Dutch translations, ES has Spanish translations.</done>
  </task>

<task type="auto">
  <name>Task 2: Convert MarketingMachinePage to use i18n + rebuild layout</name>
  <files>src/pages/MarketingMachinePage.tsx</files>
  <action>
**i18n conversion:**
1. Add `import { useTranslation } from 'react-i18next'` if not already imported
2. Add `const { t } = useTranslation('common')` at top of component
3. Replace ALL hardcoded English strings with `t('marketing_machine.key_name')` calls
4. Every string visible to the user must use a translation key

**Structural rebuild (apply simultaneously):**

1. **Cards:** All feature/benefit cards get `card-gradient-border rounded-card bg-white/[0.02] border border-border-primary` + hover effects
2. **Hero section:** CSS fadeIn/fadeInUp animations (style props) instead of Framer Motion load wrappers
3. **Section padding:** `max-w-7xl mx-auto px-12`
4. **Typography:** Headings use `font-display`
5. **Remove local backgrounds** if any exist
6. **Keep:** Lazy imports for heavy section components (VisionTimeline, FeatureShowcase, etc. — Phase 10 decision), Suspense fallback, existing route behavior

**Test the i18n by verifying** that switching language (if language switcher exists on the page) would render different text from the translation files.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep "useTranslation" src/pages/MarketingMachinePage.tsx && grep "marketing_machine" src/pages/MarketingMachinePage.tsx && grep "card-gradient-border" src/pages/MarketingMachinePage.tsx</automated>
</verify>
<done>MarketingMachinePage uses useTranslation with t('marketing_machine.\*') for all strings, has card-gradient-border + rounded-card on cards, CSS fadeIn hero</done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- All 3 locale JSON files parse without errors
- MarketingMachinePage renders correctly in EN, NL, ES
- No hardcoded English strings remain in MarketingMachinePage component
- Cards have gradient border hover effect
</verification>

<success_criteria>

- marketing_machine namespace exists in EN, NL, ES common.json
- All user-visible strings in MarketingMachinePage use t() calls
- NL has proper Dutch translations (not just English copies)
- ES has proper Spanish translations
- All JSON files are valid (no parse errors)
- Page layout matches prototype patterns
- Build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/11-living-system-full-rebuild/11-06-SUMMARY.md`
</output>
