---
phase: 11-living-system-full-rebuild
plan: 05
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/pages/AboutPage.tsx
  - src/pages/PricingPage.tsx
  - src/pages/HowItWorksPage.tsx
  - src/pages/ContactPage.tsx
  - src/pages/LegalPage.tsx
autonomous: true
requirements:
  - REQ-LIVING-SYSTEM-REBUILD

must_haves:
  truths:
    - 'All 5 supporting pages use card-gradient-border + rounded-card on cards'
    - 'Supporting page hero sections use CSS fadeIn animations'
    - 'Pricing cards use prototype card patterns with gradient border hover'
    - 'Contact form uses rounded-card container with glass styling'
  artifacts:
    - path: 'src/pages/AboutPage.tsx'
      provides: 'Rebuilt About page with prototype patterns'
    - path: 'src/pages/PricingPage.tsx'
      provides: 'Rebuilt Pricing page with prototype card patterns'
    - path: 'src/pages/HowItWorksPage.tsx'
      provides: 'Rebuilt HowItWorks page with prototype patterns'
    - path: 'src/pages/ContactPage.tsx'
      provides: 'Rebuilt Contact page with glass form container'
    - path: 'src/pages/LegalPage.tsx'
      provides: 'Rebuilt Legal page with consistent typography'
  key_links:
    - from: 'src/pages/PricingPage.tsx'
      to: 'src/index.css'
      via: 'Pricing cards use card-gradient-border'
      pattern: 'card-gradient-border'
---

<objective>
Structural rebuild of all 5 supporting pages (About, Pricing, HowItWorks, Contact, Legal) to match prototype aesthetic — card-gradient-border, rounded-card, CSS animations, glass form containers.

Purpose: Supporting pages need visual consistency with the rebuilt homepage and service pages.
Output: All 5 supporting pages rebuilt with prototype patterns.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-living-system-full-rebuild/11-RESEARCH.md
@src/pages/AboutPage.tsx
@src/pages/PricingPage.tsx
@src/pages/HowItWorksPage.tsx
@src/pages/ContactPage.tsx
@src/pages/LegalPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild AboutPage + PricingPage structural layout</name>
  <files>src/pages/AboutPage.tsx, src/pages/PricingPage.tsx</files>
  <action>
Apply the same prototype structural patterns to AboutPage and PricingPage:

**AboutPage:**

1. **Team/value cards:** Add `card-gradient-border rounded-card bg-white/[0.02] border border-border-primary` + hover effects (`hover:bg-white/[0.03] hover:-translate-y-1 hover:shadow-[0_20px_60px_rgba(0,0,0,0.3)] transition-all duration-500`)
2. **Hero section:** Replace Framer Motion load animations with CSS `fadeIn`/`fadeInUp` style props
3. **Section padding:** `max-w-7xl mx-auto px-12`
4. **Remove local backgrounds:** Delete any gradient orbs or particles

**PricingPage:**

1. **Pricing tier cards:** Apply `card-gradient-border rounded-card bg-white/[0.02] border border-border-primary` + hover effects
2. **Highlighted/featured tier:** Keep the visual distinction but use `border-accent-human/30` instead of any old blue/indigo highlight. The highlighted badge should use `bg-accent-human/10 text-accent-human rounded-btn` styling
3. **Price text:** Keep font-mono for pricing numbers
4. **Feature lists:** Keep existing tick/check lists, ensure text uses `text-text-secondary`
5. **Hero section:** CSS fadeIn/fadeInUp on load elements

Both pages: preserve ALL content, i18n translations, CTA buttons (already globally updated), and route behavior.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep "card-gradient-border" src/pages/AboutPage.tsx && grep "card-gradient-border" src/pages/PricingPage.tsx && grep "rounded-card" src/pages/PricingPage.tsx</automated>
</verify>
<done>AboutPage and PricingPage use card-gradient-border + rounded-card, CSS fadeIn hero, no local backgrounds</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild HowItWorksPage + ContactPage + LegalPage</name>
  <files>src/pages/HowItWorksPage.tsx, src/pages/ContactPage.tsx, src/pages/LegalPage.tsx</files>
  <action>
**HowItWorksPage:**
1. **Step/process cards:** `card-gradient-border rounded-card bg-white/[0.02] border border-border-primary` + hover effects
2. **Step numbers:** Use `font-mono text-accent-human` for numbered steps
3. **Hero:** CSS fadeIn/fadeInUp
4. **Section padding:** `max-w-7xl mx-auto px-12`

**ContactPage:**

1. **Form container:** Apply glass styling — `bg-white/[0.04] backdrop-blur-[12px] border border-white/[0.08] rounded-card p-10`
2. **Form inputs:** Ensure inputs use `bg-white/[0.04] border border-border-primary rounded-btn text-text-primary placeholder-text-muted focus:border-accent-system`
3. **Contact info cards:** `card-gradient-border rounded-card` pattern
4. **Hero:** CSS fadeIn/fadeInUp

**LegalPage:**

1. **Minimal changes:** LegalPage is primarily text content. Main updates:
   - Ensure any card containers use `rounded-card`
   - Section headings use `font-display`
   - Body text uses proper `text-text-secondary` color
   - Any remaining hardcoded colors replaced with tokens
2. **Section padding:** `max-w-7xl mx-auto px-12`

All pages: preserve ALL content, i18n, form logic, legal text.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/Futuremarketingai && grep "card-gradient-border\|rounded-card" src/pages/HowItWorksPage.tsx && grep "backdrop-blur\|rounded-card" src/pages/ContactPage.tsx && grep "rounded-card\|font-display" src/pages/LegalPage.tsx</automated>
</verify>
<done>HowItWorksPage has card-gradient-border steps, ContactPage has glass form container, LegalPage has consistent typography. All content preserved.</done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- All 5 supporting pages: cards have gradient border hover
- Pricing cards: highlighted tier uses accent-human, not old blue/indigo
- Contact form: glass container with backdrop-blur
- All i18n translations render correctly
</verification>

<success_criteria>

- All card-containing pages use card-gradient-border + rounded-card
- Pricing highlighted tier uses accent-human styling
- Contact form in glass container
- Legal page has consistent typography with font-display headings
- CSS fadeIn on all hero sections
- All existing content, i18n, form logic preserved
- Build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/11-living-system-full-rebuild/11-05-SUMMARY.md`
</output>
