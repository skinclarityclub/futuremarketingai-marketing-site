---
phase: 02-dashboard-reframe
plan: 03
type: execute
wave: 2
depends_on: ['02-01']
files_modified:
  - fma-app/src/app/(protected)/dashboard/page.tsx
  - fma-app/src/app/(protected)/clients/page.tsx
  - fma-app/src/app/(protected)/clients/[id]/page.tsx
  - fma-app/src/lib/actions/skills.ts
  - fma-app/src/app/(protected)/agent-activity/page.tsx
  - fma-app/src/app/(protected)/tasks/page.tsx
autonomous: true
requirements:
  - DASH-02
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - 'Dashboard overview shows AI Employee status card with agent activity summary'
    - 'Clients page displays client workspaces with active skills badges per client'
    - 'Client detail page has skill activation toggles that persist to database'
    - 'Toggling a skill on/off updates fma_clients.active_skills array'
    - 'Agent Activity page exists at /agent-activity (placeholder for pipeline data)'
    - 'Tasks page exists at /tasks (placeholder for campaigns data)'
  artifacts:
    - path: 'fma-app/src/app/(protected)/dashboard/page.tsx'
      provides: 'AI Employee overview dashboard'
      contains: 'AI Employee'
    - path: 'fma-app/src/app/(protected)/clients/[id]/page.tsx'
      provides: 'Client workspace detail with skill toggles'
      contains: 'toggleClientSkill'
    - path: 'fma-app/src/lib/actions/skills.ts'
      provides: 'Server action for skill toggle'
      exports: ['toggleClientSkill']
  key_links:
    - from: 'fma-app/src/app/(protected)/clients/[id]/page.tsx'
      to: 'fma-app/src/lib/actions/skills.ts'
      via: 'toggleClientSkill server action'
      pattern: 'toggleClientSkill'
    - from: 'fma-app/src/lib/actions/skills.ts'
      to: 'fma_clients table'
      via: 'Supabase update active_skills'
      pattern: 'active_skills'
---

<objective>
Dashboard overview rewrite, client workspace management with skill toggles, and placeholder pages for renamed routes.

Purpose: Agency owners need to see their AI Employee status at a glance, manage client workspaces, and toggle skills per client. The renamed routes (agent-activity, tasks) need landing pages so navigation works.

Output: Rewritten dashboard page, enhanced clients page with skill badges, client detail page with toggles, skill toggle server action, placeholder pages for agent-activity and tasks.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-reframe/02-RESEARCH.md
@.planning/phases/02-dashboard-reframe/02-01-SUMMARY.md

**IMPORTANT:** This phase targets the fma-app repository at `C:\Users\daley\Desktop\fma-app`, NOT the website repo.

<interfaces>
<!-- From 02-01: skills.ts exports -->
From fma-app/src/lib/skills.ts (created in 02-01):
```typescript
export const SKILLS = {
  contentCreator: { id, name, description, icon, n8nWorkflows },
  socialMedia: { ... },
  leadQualifier: { ... },
  voiceAgent: { ... },
  adCreator: { ... },
  reporting: { ... },
} as const

export const SKILL_IDS = Object.keys(SKILLS) as [keyof typeof SKILLS, ...]
export type SkillId = keyof typeof SKILLS

````

From fma-app/src/lib/dal.ts:
```typescript
export const getOrgContext = cache(async (): Promise<OrgContext> => { ... })
// Returns { userId, organizationId, role }
````

From fma-app/src/lib/supabase/server.ts:

```typescript
export async function createClient() { ... }      // RLS-scoped client
export function createAdminClient() { ... }        // Bypasses RLS
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard overview + skill toggle server action</name>
  <files>fma-app/src/app/(protected)/dashboard/page.tsx, fma-app/src/lib/actions/skills.ts</files>
  <action>
  **Work in the fma-app repo (`C:\Users\daley\Desktop\fma-app`).**

**1. Create `src/lib/actions/skills.ts`** — server action for toggling skills.

'use server' directive. Implements `toggleClientSkill(input)`:

- Validate with zod: { clientId: uuid, skillId: enum of SKILL_IDS, enabled: boolean }
- Get orgContext to verify authorization
- Use admin client to fetch client's current active_skills, verifying client.organization_id matches orgCtx.organizationId
- If enabled: add skillId to active_skills array (deduplicated via Set)
- If disabled: remove skillId from active_skills array
- Update fma_clients.active_skills via admin client
- Call revalidatePath for the client detail page
- Return { success: true }

Follow exact pattern from 02-RESEARCH.md toggleClientSkill example.

**2. Rewrite `src/app/(protected)/dashboard/page.tsx`** as a server component.

The page should:
a) Fetch org context via getOrgContext()
b) Query fma_organizations for subscription_tier, display_name, onboarding_completed_at
c) Query fma_clients count and total active_skills count for this org
d) Display:

**AI Employee Status Card:**

- Title: "Your AI Employee" or agency display_name
- Status indicator: green dot + "Active" if subscription_tier is not 'free', yellow + "Setup Required" if free
- Current tier badge
- Stats row: total clients, total active skills, subscription tier

**Quick Stats Grid (2x2):**

- Client Workspaces: count of fma_clients for this org
- Active Skills: count of all active_skills entries across clients
- Current Plan: subscription tier name
- Next Invoice: "Manage in Billing" link to /billing

**Quick Actions:**

- "Add Client Workspace" → link to /clients/new
- "Manage Skills" → link to /clients
- "View Billing" → link to /billing

Use Tailwind dark theme styling consistent with existing dashboard. Use Card-like containers with borders. Import icons from lucide-react.

Remove all SKC-specific widgets (content pipeline, social media stats, etc.) from the old dashboard page.
</action>
<verify>
<automated>cd C:/Users/daley/Desktop/fma-app && grep -c "AI Employee\|subscription_tier\|active_skills" src/app/\(protected\)/dashboard/page.tsx && test -f src/lib/actions/skills.ts && echo "skills action exists"</automated>
</verify>
<done>Dashboard shows AI Employee status, client count, active skills count, subscription tier. toggleClientSkill server action validates input, verifies org ownership, and updates active_skills array.</done>
</task>

<task type="auto">
  <name>Task 2: Client workspace pages + renamed route placeholders</name>
  <files>fma-app/src/app/(protected)/clients/page.tsx, fma-app/src/app/(protected)/clients/[id]/page.tsx, fma-app/src/app/(protected)/agent-activity/page.tsx, fma-app/src/app/(protected)/tasks/page.tsx</files>
  <action>
  **Work in the fma-app repo (`C:\Users\daley\Desktop\fma-app`).**

**1. Enhance `src/app/(protected)/clients/page.tsx`** — client workspaces list.

Rewrite as server component:

- Fetch org context, query fma_clients for this org (id, name, active_skills, created_at)
- Display as card grid (not table) showing each client workspace:
  - Client name
  - Active skills shown as small colored badges (use SKILLS config for names/icons)
  - "X skills active" summary
  - Created date
  - Link to client detail page `/clients/{id}`
- "Add Client Workspace" button → link to /clients/new
- Empty state if no clients: "No client workspaces yet. Create your first workspace to get started."
- Keep existing page structure/layout patterns

**2. Enhance `src/app/(protected)/clients/[id]/page.tsx`** — client workspace detail with skill toggles.

Rewrite as server component:

- Fetch org context, query single fma_client by id (verify organization_id match)
- Display client name as heading
- **Skills Activation Section:**
  - Grid of 6 skill cards (one per skill from SKILLS config)
  - Each card shows: skill icon, name, description
  - Toggle switch (or button) showing active/inactive state based on client.active_skills
  - Toggle calls toggleClientSkill server action via a client component wrapper
  - Active skills have accent border/highlight, inactive are dimmed
- **Client Info Section:**
  - Display client metadata (name, created_at)
  - Link back to /clients

Create a small client component `SkillToggle` (can be inline in the same file or a separate component) that:

- Receives clientId, skillId, enabled, skillName, skillDescription
- Has an onClick that calls toggleClientSkill and updates local state optimistically
- Shows loading state during toggle

**3. Create placeholder pages for renamed routes.**

Create `src/app/(protected)/agent-activity/page.tsx`:

- Simple server component with heading "Agent Activity"
- Subtitle: "Pipeline and task execution history"
- "Coming soon — this page will show your AI Employee's activity log."
- This is a placeholder; the actual content will come from existing pipeline data in future work

Create `src/app/(protected)/tasks/page.tsx`:

- Simple server component with heading "Tasks"
- Subtitle: "Campaign and content tasks"
- "Coming soon — this page will show your AI Employee's active tasks."
- This is a placeholder; maps to the old campaigns concept
  </action>
  <verify>
  <automated>cd C:/Users/daley/Desktop/fma-app && grep -c "active_skills\|SKILLS" src/app/\(protected\)/clients/page.tsx && grep -c "toggleClientSkill\|SkillToggle" src/app/\(protected\)/clients/\[id\]/page.tsx && test -f src/app/\(protected\)/agent-activity/page.tsx && test -f src/app/\(protected\)/tasks/page.tsx && echo "all pages exist"</automated>
  </verify>
  <done>Clients page shows workspace cards with skill badges. Client detail page has 6 skill toggle cards that persist via server action. agent-activity and tasks placeholder pages exist and are navigable from sidebar.</done>
  </task>

</tasks>

<verification>
1. `grep "AI Employee" fma-app/src/app/(protected)/dashboard/page.tsx` — dashboard uses new language
2. `grep "toggleClientSkill" fma-app/src/lib/actions/skills.ts` — server action exists
3. `grep "active_skills" fma-app/src/app/(protected)/clients/page.tsx` — clients show skills
4. Directory `fma-app/src/app/(protected)/agent-activity/` exists with page.tsx
5. Directory `fma-app/src/app/(protected)/tasks/` exists with page.tsx
</verification>

<success_criteria>

- Dashboard overview communicates "AI Employee" status with agency-relevant metrics
- Client workspaces show active skills as badges
- Skill toggles on client detail page persist to database
- All sidebar links resolve to valid pages (no 404s)
- No SKC-specific content remains on dashboard overview
  </success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-reframe/02-03-SUMMARY.md`
</output>
