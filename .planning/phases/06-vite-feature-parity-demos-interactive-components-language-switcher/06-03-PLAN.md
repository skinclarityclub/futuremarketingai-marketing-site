---
phase: 06-vite-feature-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - fmai-nextjs/src/hooks/useElevenLabsCall.ts
  - fmai-nextjs/src/components/voice/VoiceDemoSection.tsx
  - fmai-nextjs/src/components/voice/VoiceDemoPhone.tsx
  - fmai-nextjs/src/components/voice/PhoneMockup.tsx
  - fmai-nextjs/src/components/voice/VoiceDemoFAB.tsx
  - fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx
autonomous: true
requirements:
  - WEB-03
must_haves:
  truths:
    - 'Voice agents page has an interactive voice demo section with phone mockup'
    - 'useElevenLabsCall hook gracefully degrades when API is unavailable'
    - 'VoiceDemoFAB floating button appears when voice demo scrolls out of view'
    - 'Voice agents page has pricing tiers section with 3 tiers (Starter/Growth/Scale)'
    - 'Voice agents page has trust metrics section with 3 stats'
    - 'Partnership section renders as a styled card instead of plain text'
  artifacts:
    - path: 'fmai-nextjs/src/hooks/useElevenLabsCall.ts'
      provides: 'ElevenLabs voice call hook with graceful degradation'
      exports: ['useElevenLabsCall']
    - path: 'fmai-nextjs/src/components/voice/VoiceDemoSection.tsx'
      provides: 'Interactive voice demo with phone mockup'
      exports: ['VoiceDemoSection']
    - path: 'fmai-nextjs/src/components/voice/VoiceDemoPhone.tsx'
      provides: 'Phone UI with call controls'
      exports: ['VoiceDemoPhone']
    - path: 'fmai-nextjs/src/components/voice/PhoneMockup.tsx'
      provides: 'Phone frame wrapper component'
      exports: ['PhoneMockup']
    - path: 'fmai-nextjs/src/components/voice/VoiceDemoFAB.tsx'
      provides: 'Floating action button for voice demo'
      exports: ['VoiceDemoFAB']
    - path: 'fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx'
      provides: 'Full voice agents page with all sections'
  key_links:
    - from: 'fmai-nextjs/src/components/voice/VoiceDemoSection.tsx'
      to: 'useElevenLabsCall'
      via: 'hook import'
      pattern: 'useElevenLabsCall'
    - from: 'fmai-nextjs/src/components/voice/VoiceDemoFAB.tsx'
      to: 'IntersectionObserver'
      via: 'scroll visibility detection'
      pattern: 'IntersectionObserver|useInView'
    - from: 'fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx'
      to: 'VoiceDemoSection'
      via: 'dynamic import'
      pattern: 'dynamic.*VoiceDemoSection'
---

<objective>
Port the voice demo interactive components from Vite (VoiceDemoSection, VoiceDemoPhone, PhoneMockup, VoiceDemoFAB, useElevenLabsCall) and enhance the voice agents page with pricing tiers, trust metrics, and improved partnership section.

Purpose: Voice agents page reaches full feature parity with the interactive voice demo and supporting sections from the Vite version.
Output: 5 new components/hook in `fmai-nextjs/src/components/voice/` and `fmai-nextjs/src/hooks/`, fully-featured voice agents page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-vite-feature-parity-demos-interactive-components-language-switcher/06-RESEARCH.md
@.planning/phases/06-vite-feature-parity-demos-interactive-components-language-switcher/06-01-SUMMARY.md

@src/pages/VoiceAgentsPage.tsx
@src/components/voice/VoiceDemoSection.tsx
@src/components/voice/VoiceDemoPhone.tsx
@src/components/voice/PhoneMockup.tsx
@src/components/voice/VoiceDemoFAB.tsx
@src/hooks/useElevenLabsCall.ts
@fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port voice demo components and useElevenLabsCall hook</name>
  <files>
    fmai-nextjs/src/hooks/useElevenLabsCall.ts
    fmai-nextjs/src/components/voice/VoiceDemoSection.tsx
    fmai-nextjs/src/components/voice/VoiceDemoPhone.tsx
    fmai-nextjs/src/components/voice/PhoneMockup.tsx
    fmai-nextjs/src/components/voice/VoiceDemoFAB.tsx
  </files>
  <action>
  Read all Vite source files for voice components:
  - `src/hooks/useElevenLabsCall.ts`
  - `src/components/voice/VoiceDemoSection.tsx`
  - `src/components/voice/VoiceDemoPhone.tsx`
  - `src/components/voice/PhoneMockup.tsx`
  - `src/components/voice/VoiceDemoFAB.tsx`
  - Also read `src/components/voice/WaveformVisualizer.tsx` and `src/components/voice/CallTranscript.tsx` if referenced

**useElevenLabsCall.ts:**
Port the hook to `fmai-nextjs/src/hooks/useElevenLabsCall.ts`. This MUST gracefully degrade:

- Wrap the ElevenLabs SDK initialization in try/catch
- If the agent ID or API key is missing, return a mock state with `isAvailable: false`
- Use `process.env.NEXT_PUBLIC_ELEVENLABS_AGENT_ID` for the agent ID
- Return: `{ isConnected, isCallActive, isMuted, toggleMute, startCall, endCall, transcript, isAvailable }`
- When `isAvailable === false`, VoiceDemoSection should show a fallback UI ("Voice demo temporarily unavailable") instead of crashing

**PhoneMockup.tsx:**
Port to `fmai-nextjs/src/components/voice/PhoneMockup.tsx` as 'use client' component.

- Phone frame with notch, rounded corners, dark bezel
- Accepts `children` prop for inner content
- Map tokens: bg-dark-card -> bg-bg-elevated, border-white/10 -> border-border-primary

**VoiceDemoPhone.tsx:**
Port to `fmai-nextjs/src/components/voice/VoiceDemoPhone.tsx` as 'use client' component.

- Renders inside PhoneMockup: caller info, call duration timer, mute/end buttons
- Uses useElevenLabsCall hook for call state
- WaveformVisualizer: if Vite version uses canvas, port it. If simple CSS, keep simple. Check the source.
- Show 3 feature indicators below the phone (as in Vite version)

**VoiceDemoSection.tsx:**
Port to `fmai-nextjs/src/components/voice/VoiceDemoSection.tsx` as 'use client' component.

- Contains PhoneMockup + VoiceDemoPhone + descriptive text
- Shows the direct phone number (+1 570-783-8236) from Vite version (read to confirm)
- Responsive layout: phone mockup on left, text/features on right (desktop), stacked (mobile)
- When `isAvailable === false`, show graceful fallback instead of call controls
- Use `useRef` for intersection observer target (needed by VoiceDemoFAB)

**VoiceDemoFAB.tsx:**
Port to `fmai-nextjs/src/components/voice/VoiceDemoFAB.tsx` as 'use client' component.

- Fixed-position floating action button (bottom-right)
- Appears when VoiceDemoSection scrolls out of viewport (IntersectionObserver)
- On click: scrolls to VoiceDemoSection
- Framer Motion: slide-in from bottom with spring animation
- Phone icon (Lucide: Phone)
- z-index: 40 (below modals/overlays)

All components: use Next.js design tokens, Framer Motion for animations, Lucide for icons.
</action>
<verify>Run `cd fmai-nextjs && npx tsc --noEmit` -- no TypeScript errors. All 5 files exist with correct exports. useElevenLabsCall has `isAvailable` field in return type.</verify>
<done>Voice demo components ported: PhoneMockup renders phone frame, VoiceDemoPhone shows call UI, VoiceDemoSection composes them with fallback, VoiceDemoFAB appears on scroll, useElevenLabsCall degrades gracefully when API unavailable.</done>
</task>

<task type="auto">
  <name>Task 2: Add voice demo, pricing, trust metrics, and enhanced partnership to voice agents page</name>
  <files>fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx</files>
  <action>
  Read the Vite `src/pages/VoiceAgentsPage.tsx` for section order and pricing data.

Update `fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx` to add sections in this order:

1. **Hero** (existing -- keep as-is)
2. **Quick Answer Block** (existing -- keep as-is)
3. **Use Cases** (existing -- keep as-is)
4. **NEW: Voice Demo section** -- dynamically import VoiceDemoSection with `next/dynamic` and `{ ssr: false }` (uses browser APIs). Wrap in section with SectionHeading. Also dynamically import VoiceDemoFAB with `{ ssr: false }` and place it outside sections (fixed position).
5. **NEW: Trust Metrics section** -- import TrustMetrics from `@/components/common/TrustMetrics`, pass voice-specific metrics:
   - `{ value: "99.9%", label: "Uptime", description: "Enterprise-grade availability" }`
   - `{ value: "10,000+", label: "Calls Completed", description: "Across all voice agents" }`
   - `{ value: "95%+", label: "Accuracy", description: "Intent recognition rate" }`
6. **ENHANCED: Partnership section** -- change from plain text to a styled GlassCard with icon (Lucide: Handshake), heading, description, and subtle accent border-left (border-l-4 border-accent-system). Keep existing translation keys.
7. **NEW: Pricing Tiers section** -- import PricingTiers from `@/components/common/PricingTiers`, pass voice tier data from Vite source:
   - Starter: 200 min/mo, features, price
   - Growth: 500 min/mo, features, price (highlighted)
   - Scale: 1000 min/mo, features, price
     Read exact data from Vite `VoiceAgentsPage.tsx`.
8. **FAQ** (existing -- keep as-is)
9. **Related Services** (existing -- keep as-is)
10. **CTA** (existing -- keep as-is)

Consistent padding, alternating backgrounds, ScrollReveal animations.
</action>
<verify>Run `cd fmai-nextjs && npx next build` -- builds without errors. Verify voice-agents page.tsx imports VoiceDemoSection, VoiceDemoFAB, TrustMetrics, PricingTiers.</verify>
<done>Voice agents page has voice demo with phone mockup, floating action button, trust metrics (3 stats), enhanced partnership card, and pricing tiers (3 tiers).</done>
</task>

</tasks>

<verification>
- `cd fmai-nextjs && npx next build` passes
- `fmai-nextjs/src/hooks/useElevenLabsCall.ts` exports hook with `isAvailable` field
- `fmai-nextjs/src/components/voice/` contains 4 component files
- Voice agents page imports all new components via dynamic imports
</verification>

<success_criteria>
Voice agents page reaches full feature parity with Vite version: interactive voice demo with phone mockup, floating action button, trust metrics, enhanced partnership card, and pricing tiers. Voice demo gracefully degrades when ElevenLabs API is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-vite-feature-parity-demos-interactive-components-language-switcher/06-03-SUMMARY.md`
</output>
