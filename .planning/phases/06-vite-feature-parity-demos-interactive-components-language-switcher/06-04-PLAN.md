---
phase: 06-vite-feature-parity
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - fmai-nextjs/src/components/marketing-machine/VisionTimeline.tsx
  - fmai-nextjs/src/components/marketing-machine/FeatureShowcase.tsx
  - fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx
  - fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx
autonomous: true
requirements:
  - WEB-01
  - WEB-03
must_haves:
  truths:
    - 'Marketing machine page has VisionTimeline showing 3-era AI adoption timeline'
    - 'Marketing machine page has FeatureShowcase with 6 AI module cards in visual hierarchy'
    - 'Marketing machine page has social proof section with quote'
    - 'Marketing machine page has pricing tiers section with 3 tiers'
    - 'Marketing machine page has product demo media placeholder'
    - 'Automations page has pricing tiers section with 3 tiers (5/10/20 workflows)'
    - 'Automations page has trust metrics section with 3 stats'
    - 'Automations page has product demo media placeholder'
    - 'Automations page What We Automate grid has icons'
  artifacts:
    - path: 'fmai-nextjs/src/components/marketing-machine/VisionTimeline.tsx'
      provides: '3-era AI adoption timeline'
      exports: ['VisionTimeline']
    - path: 'fmai-nextjs/src/components/marketing-machine/FeatureShowcase.tsx'
      provides: '6-module AI feature showcase with visual hierarchy'
      exports: ['FeatureShowcase']
    - path: 'fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx'
      provides: 'Full marketing machine page with all sections'
    - path: 'fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx'
      provides: 'Full automations page with all sections'
  key_links:
    - from: 'fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx'
      to: 'VisionTimeline'
      via: 'dynamic import'
      pattern: 'VisionTimeline'
    - from: 'fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx'
      to: 'PricingTiers'
      via: 'import from common'
      pattern: 'PricingTiers'
    - from: 'fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx'
      to: 'TrustMetrics'
      via: 'import from common'
      pattern: 'TrustMetrics'
---

<objective>
Port VisionTimeline and FeatureShowcase for the marketing machine page, add pricing/social proof/product media sections to marketing machine page, and add pricing/trust metrics/product media/icons to automations page.

Purpose: Both remaining service pages reach full feature parity with Vite versions.
Output: 2 new components, 2 fully-featured service pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-vite-feature-parity-demos-interactive-components-language-switcher/06-RESEARCH.md
@.planning/phases/06-vite-feature-parity-demos-interactive-components-language-switcher/06-01-SUMMARY.md

@src/pages/MarketingMachinePage.tsx
@src/pages/AutomationsPage.tsx
@fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx
@fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VisionTimeline + FeatureShowcase and enhance marketing machine page</name>
  <files>
    fmai-nextjs/src/components/marketing-machine/VisionTimeline.tsx
    fmai-nextjs/src/components/marketing-machine/FeatureShowcase.tsx
    fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx
  </files>
  <action>
  Read the Vite source `src/pages/MarketingMachinePage.tsx` for section order, pricing data, and component references.
  Check if Vite has `src/components/marketing-machine/VisionTimeline.tsx` and `src/components/marketing-machine/FeatureShowcase.tsx` -- if so, read them. If not, recreate from the page source.

**VisionTimeline.tsx:**
Create `fmai-nextjs/src/components/marketing-machine/VisionTimeline.tsx` as 'use client' component (animated).

- 3-era horizontal timeline: "Manual Marketing" -> "Basic Automation" -> "AI Marketing Employee"
- Each era: icon, title, description, year/period label
- Timeline connector line between eras with animated gradient (Framer Motion)
- Current era (AI Marketing Employee) highlighted with accent-system glow
- Responsive: horizontal on desktop, vertical stacked on mobile
- Design tokens: bg-bg-elevated, text-accent-system, border-border-primary, text-text-primary/secondary

**FeatureShowcase.tsx:**
Create `fmai-nextjs/src/components/marketing-machine/FeatureShowcase.tsx` as server component (static cards).

- Props: none (uses hardcoded 6 modules matching existing FEATURE_KEYS or accepts feature data as props)
- Actually: make this props-based: `features: Array<{ title: string; description: string; icon: string }>`
- 6 cards in a 2x3 grid (lg:grid-cols-3, md:grid-cols-2)
- Visual hierarchy: first card (or "orchestrator" card) is larger/highlighted (col-span-2 on lg, accent border)
- Each card: Lucide icon, title, description, hover effect (scale slightly, border glow)
- GlassCard styling with cursor-pointer and transition-all

**Marketing Machine page update:**
Add sections in order (matching Vite structure):

1. **Hero** (existing)
2. **Quick Answer Block** (existing)
3. **NEW: SocialProof section** -- import SocialProof from `@/components/common/SocialProof`, use a marketing-relevant quote (read Vite source for the exact quote, or use: "Since deploying the Marketing Machine, our content output increased 10x while our team stayed the same size." -- adjust to match Vite)
4. **Features section** (existing 6-item grid) -- REPLACE the existing simple GlassCard grid with FeatureShowcase component for enhanced visual hierarchy. Pass feature data from translation keys.
5. **NEW: VisionTimeline section** -- dynamically import with `next/dynamic` and `{ ssr: false }`. Wrap in section with SectionHeading.
6. **NEW: Product Media section** -- import ProductMedia from `@/components/common/ProductMedia`, title "See the Marketing Machine in Action", no videoUrl (placeholder mode)
7. **NEW: Pricing Tiers section** -- import PricingTiers from `@/components/common/PricingTiers`, pass marketing machine tiers from Vite:
   - Starter, Growth (highlighted with "Marketing Machine" badge), Scale
     Read exact pricing from Vite source.
8. **How It Works** (existing)
9. **FAQ** (existing)
10. **Related Services** (existing)
11. **CTA** (existing)
    </action>
    <verify>Run `cd fmai-nextjs && npx tsc --noEmit` -- no TypeScript errors. VisionTimeline.tsx and FeatureShowcase.tsx exist with exports. Marketing machine page imports all new components.</verify>
    <done>Marketing machine page has VisionTimeline (3-era), FeatureShowcase (6 modules with visual hierarchy), SocialProof quote, ProductMedia placeholder, and PricingTiers (3 tiers).</done>
    </task>

<task type="auto">
  <name>Task 2: Enhance automations page with pricing, trust metrics, product media, and icons</name>
  <files>fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx</files>
  <action>
  Read the Vite `src/pages/AutomationsPage.tsx` for section order and pricing data.

Update `fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx`:

**Enhance What We Automate grid with Lucide icons:**
The existing grid has 6 items with no icons. Add a Lucide icon to each automation item:

- lead_qualification: Target
- email_sequences: Mail
- social_media: Share2
- invoicing: Receipt
- onboarding: UserPlus
- data_sync: RefreshCw

Import icons from `lucide-react`. Add icon above the text in each GlassCard: `<Icon className="w-8 h-8 text-accent-system mb-3 mx-auto" />`

**Add new sections in order:**

1. **Hero** (existing)
2. **Quick Answer Block** (existing)
3. **Challenges/Pain Points** (existing)
4. **What We Automate** (existing -- enhanced with icons)
5. **NEW: Trust Metrics section** -- import TrustMetrics from `@/components/common/TrustMetrics`, pass:
   - `{ value: "1,200+", label: "Hours Saved Monthly", description: "Across all client automations" }`
   - `{ value: "< 24h", label: "Delivery Time", description: "From audit to first workflow" }`
   - `{ value: "99.7%", label: "Success Rate", description: "Workflow execution reliability" }`
6. **NEW: Product Media section** -- import ProductMedia from `@/components/common/ProductMedia`, title "Automation Dashboard Preview", no videoUrl (placeholder mode)
7. **NEW: Pricing Tiers section** -- import PricingTiers from `@/components/common/PricingTiers`, pass automation tiers from Vite:
   - Starter: 5 workflows, features, price
   - Growth: 10 workflows, features, price (highlighted)
   - Scale: 20 workflows, features, price
     Read exact data from Vite source.
8. **Process Steps** (existing)
9. **FAQ** (existing)
10. **Related Services** (existing)
11. **CTA** (existing)

Consistent section structure: `<section aria-labelledby="..." className="py-20 px-6 lg:px-12">` with alternating bg-bg-surface/30 backgrounds.
</action>
<verify>Run `cd fmai-nextjs && npx next build` -- builds without errors. Automations page imports Lucide icons, TrustMetrics, PricingTiers, ProductMedia.</verify>
<done>Automations page has Lucide icons in automation grid, trust metrics (3 stats), product media placeholder, and pricing tiers (3 tiers for 5/10/20 workflows).</done>
</task>

</tasks>

<verification>
- `cd fmai-nextjs && npx next build` passes
- VisionTimeline.tsx exists with 3-era timeline
- FeatureShowcase.tsx exists with props-based card grid
- Marketing machine page imports VisionTimeline, FeatureShowcase, SocialProof, ProductMedia, PricingTiers
- Automations page imports Lucide icons, TrustMetrics, ProductMedia, PricingTiers
- Both pages have all sections in correct order
</verification>

<success_criteria>
Marketing machine page and automations page both reach full feature parity with Vite versions. All 4 service pages now have pricing tiers, trust metrics (where applicable), interactive demos (where applicable), and enhanced visual components.
</success_criteria>

<output>
After completion, create `.planning/phases/06-vite-feature-parity-demos-interactive-components-language-switcher/06-04-SUMMARY.md`
</output>
