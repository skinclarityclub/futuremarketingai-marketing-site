---
phase: 06-vite-feature-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/src/components/layout/HeaderClient.tsx
  - fmai-nextjs/src/components/common/TrustMetrics.tsx
  - fmai-nextjs/src/components/common/PricingTiers.tsx
  - fmai-nextjs/src/components/common/SocialProof.tsx
  - fmai-nextjs/src/components/common/ProductMedia.tsx
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - 'Language switcher shows flag emojis next to language codes on desktop and mobile'
    - 'Selected language has animated checkmark indicator'
    - 'Language switch fires gtag analytics event'
    - 'TrustMetrics component renders a 3-column grid of stat cards'
    - 'PricingTiers component renders 3 tier cards with highlighted recommended tier'
    - 'SocialProof component renders trust badge with quote'
    - 'ProductMedia component renders video placeholder with play overlay'
  artifacts:
    - path: 'fmai-nextjs/src/components/layout/HeaderClient.tsx'
      provides: 'Enhanced language switcher with flags, checkmarks, analytics'
    - path: 'fmai-nextjs/src/components/common/TrustMetrics.tsx'
      provides: 'Reusable trust metrics 3-column grid'
      exports: ['TrustMetrics']
    - path: 'fmai-nextjs/src/components/common/PricingTiers.tsx'
      provides: 'Reusable pricing tier cards'
      exports: ['PricingTiers']
    - path: 'fmai-nextjs/src/components/common/SocialProof.tsx'
      provides: 'Reusable social proof section'
      exports: ['SocialProof']
    - path: 'fmai-nextjs/src/components/common/ProductMedia.tsx'
      provides: 'Reusable product media video placeholder'
      exports: ['ProductMedia']
  key_links:
    - from: 'fmai-nextjs/src/components/layout/HeaderClient.tsx'
      to: 'router.replace(pathname, { locale })'
      via: 'next-intl locale switch'
      pattern: "router\\.replace"
    - from: 'fmai-nextjs/src/components/common/PricingTiers.tsx'
      to: 'Service pages'
      via: 'props-based tier data'
      pattern: 'tiers.*map'
---

<objective>
Enhance the language switcher with flag emojis, checkmarks, and analytics tracking, and create 4 shared reusable components (TrustMetrics, PricingTiers, SocialProof, ProductMedia) that all service pages will use.

Purpose: Foundation components needed by all 4 service page plans. Language switcher is a site-wide quick win.
Output: Enhanced HeaderClient.tsx + 4 new shared components in `fmai-nextjs/src/components/common/`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-vite-feature-parity-demos-interactive-components-language-switcher/06-RESEARCH.md

@fmai-nextjs/src/components/layout/HeaderClient.tsx
@src/components/common/LanguageSwitcher.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance language switcher with flags, checkmarks, and analytics</name>
  <files>fmai-nextjs/src/components/layout/HeaderClient.tsx</files>
  <action>
  Read the Vite `src/components/common/LanguageSwitcher.tsx` for reference UI patterns.

Modify `fmai-nextjs/src/components/layout/HeaderClient.tsx` (lines ~236-279 desktop, ~374-391 mobile):

**Desktop locale button (lines ~236-279):**

- Add flag emoji before each language code in the dropdown trigger and items
- Flag mapping: `{ en: '\u{1F1EC}\u{1F1E7}', nl: '\u{1F1F3}\u{1F1F1}', es: '\u{1F1EA}\u{1F1F8}' }` (use the actual unicode flag sequences, NOT emoji literals since the skill says no-emoji-icons but these are functional locale flags, not decorative icons)
- Show full language name next to flag in dropdown items: "EN English", "NL Nederlands", "ES Espanol"
- Add animated SVG checkmark (Framer Motion scale + opacity, duration 200ms) next to the currently selected locale
- Keep the existing `router.replace(pathname, { locale })` mechanism -- do NOT change the switching logic

**Mobile locale buttons (lines ~374-391):**

- Add flag emoji before each language code in the mobile menu buttons
- Add checkmark indicator for selected language (same pattern as desktop)

**Analytics tracking:**

- On language switch, fire: `if (typeof window !== 'undefined' && window.gtag) { window.gtag('event', 'language_switch', { from_language: currentLocale, to_language: newLocale }) }`
- Add this in the click handler before `router.replace()`

**Framer Motion animations:**

- Wrap the selected checkmark in `<motion.span>` with `initial={{ scale: 0, opacity: 0 }}` `animate={{ scale: 1, opacity: 1 }}` transition 200ms
- Keep existing dropdown animations, enhance if simpler than Vite version

Use Next.js design tokens: `text-accent-system` for selected state, `text-text-secondary` for unselected, `bg-bg-elevated` for dropdown background.
</action>
<verify>Run `cd fmai-nextjs && npx next build` -- no TypeScript errors. Visually inspect that flag emojis render in the header locale switcher.</verify>
<done>Desktop and mobile language switchers show flag emojis, full language names, animated checkmark on selected language, and fire gtag event on switch.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared reusable components (TrustMetrics, PricingTiers, SocialProof, ProductMedia)</name>
  <files>
    fmai-nextjs/src/components/common/TrustMetrics.tsx
    fmai-nextjs/src/components/common/PricingTiers.tsx
    fmai-nextjs/src/components/common/SocialProof.tsx
    fmai-nextjs/src/components/common/ProductMedia.tsx
  </files>
  <action>
  Create `fmai-nextjs/src/components/common/` directory and 4 server components. These are static content -- NO 'use client' needed.

**TrustMetrics.tsx:**

- Props: `metrics: Array<{ value: string; label: string; description?: string }>`
- Renders a 3-column responsive grid (1-col mobile, 3-col md+)
- Each metric: large bold value (text-4xl font-bold text-accent-system), label below (text-lg font-semibold text-text-primary), optional description (text-sm text-text-secondary)
- Wrapped in GlassCard-style container (bg-bg-elevated/50 border border-border-primary rounded-xl p-6)
- Import ScrollReveal for staggered reveal (delay per item)

**PricingTiers.tsx:**

- Props: `tiers: Array<{ name: string; price: string; period: string; features: string[]; highlighted?: boolean; badge?: string }>`, `ctaHref?: string`, `ctaLabel?: string`
- Renders 3 tier cards in a responsive grid (1-col mobile, 3-col lg)
- Highlighted tier: `border-accent-system ring-1 ring-accent-system/30` + badge rendered as absolute-positioned pill
- Each card: name (text-xl font-bold), price (text-3xl font-bold text-accent-system), period (text-text-muted), feature list with checkmark SVG icons (text-accent-success), CTA button at bottom
- Use CTAButton component for the CTA, variant="primary" for highlighted, variant="secondary" for others

**SocialProof.tsx:**

- Props: `quote: string; author: string; role: string; company?: string`
- Renders a centered blockquote with quotation marks (text-accent-system/20 text-6xl), quote text (text-lg text-text-secondary italic), author line (text-text-primary font-semibold)
- GlassCard wrapper with max-w-3xl mx-auto

**ProductMedia.tsx:**

- Props: `title: string; description?: string; videoUrl?: string; thumbnailSrc?: string`
- Renders a 16:9 aspect-ratio container with dark overlay
- If no videoUrl: shows a placeholder with play button SVG overlay and "Coming Soon" text
- If videoUrl: renders an iframe (lazy loaded)
- Rounded-xl with border-border-primary, bg-bg-elevated

All components: use Next.js design tokens from research (bg-bg-elevated, text-accent-system, border-border-primary, text-text-primary, text-text-secondary, text-text-muted). Import ScrollReveal from `@/components/motion/ScrollReveal`. Import GlassCard from `@/components/ui/GlassCard` where appropriate. Import CTAButton from `@/components/ui/CTAButton` for PricingTiers.
</action>
<verify>Run `cd fmai-nextjs && npx tsc --noEmit` -- no TypeScript errors on new components. Verify each file exports its named component.</verify>
<done>4 shared components exist in `fmai-nextjs/src/components/common/`, are properly typed, and export named components ready for import by service pages.</done>
</task>

</tasks>

<verification>
- `cd fmai-nextjs && npx next build` completes without errors
- HeaderClient.tsx contains flag emoji mapping and gtag event
- All 4 common components exist and export named components
- No 'use client' on static server components (TrustMetrics, PricingTiers, SocialProof, ProductMedia)
</verification>

<success_criteria>
Language switcher enhanced with flags + checkmarks + analytics. Four shared components created and ready for service page integration in plans 06-02 through 06-04.
</success_criteria>

<output>
After completion, create `.planning/phases/06-vite-feature-parity-demos-interactive-components-language-switcher/06-01-SUMMARY.md`
</output>
