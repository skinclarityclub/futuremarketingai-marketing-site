---
phase: 21-guided-demo-mode
plan: 02
type: execute
wave: 2
depends_on: ['21-01']
files_modified:
  - src/components/chatbot/demo/DemoScenarioCard.tsx
  - src/components/chatbot/demo/DemoCheckpoint.tsx
  - src/components/chatbot/demo/DemoProgress.tsx
  - src/components/chatbot/demo/DemoCompletionCard.tsx
  - src/index.css
autonomous: true
requirements:
  - REQ-DEMO-UI-COMPONENTS
  - REQ-DEMO-PROGRESS
  - REQ-DEMO-CHECKPOINTS
  - REQ-DEMO-COMPLETION

must_haves:
  truths:
    - 'DemoScenarioCard renders clickable scenario cards with icon, title, subtitle, step count'
    - 'DemoCheckpoint renders primary/secondary/ghost action buttons with 800ms delay animation'
    - 'DemoProgress renders progress dots with gradient fills and animated checkmarks'
    - 'DemoCompletionCard shows scenario title, duration, social proof, and 3 CTAs'
    - 'pulseGlow CSS keyframe animation is defined in index.css'
  artifacts:
    - path: 'src/components/chatbot/demo/DemoScenarioCard.tsx'
      provides: 'Scenario selection card for demo start'
      exports: ['DemoScenarioCard']
    - path: 'src/components/chatbot/demo/DemoCheckpoint.tsx'
      provides: 'Checkpoint action buttons between demo steps'
      exports: ['DemoCheckpoint']
    - path: 'src/components/chatbot/demo/DemoProgress.tsx'
      provides: 'Step progress indicator with animated dots'
      exports: ['DemoProgress']
    - path: 'src/components/chatbot/demo/DemoCompletionCard.tsx'
      provides: 'Scenario completion card with social proof and CTAs'
      exports: ['DemoCompletionCard']
    - path: 'src/index.css'
      provides: 'pulseGlow keyframe for checkpoint button animation'
      contains: 'pulseGlow'
  key_links:
    - from: 'src/components/chatbot/demo/DemoScenarioCard.tsx'
      to: 'lucide-react'
      via: 'Briefcase, ShoppingBag, Headphones icons'
      pattern: 'ICON_MAP'
    - from: 'src/components/chatbot/demo/DemoCheckpoint.tsx'
      to: 'src/index.css'
      via: 'animate-[pulseGlow_2s_ease-in-out_2s_infinite]'
      pattern: 'pulseGlow'
---

<objective>
Build the 4 demo UI components: DemoScenarioCard (scenario selection), DemoCheckpoint (step pause buttons), DemoProgress (animated progress dots), and DemoCompletionCard (completion summary with social proof). Plus the pulseGlow CSS animation.

Purpose: These are the visual building blocks that the DemoOrchestrator (Plan 03) renders at different stages of the demo flow. All components are pure presentational — they receive props and fire callbacks.
Output: 4 styled React components + 1 CSS keyframe, ready for orchestrator integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-guided-demo-mode/21-CONTEXT.md
@.planning/phases/21-guided-demo-mode/21-RESEARCH.md
@src/components/chatbot/demo/scenarios.ts
@src/index.css
@docs/plans/2026-03-16-guided-demo-implementation.md
@docs/plans/2026-03-16-guided-demo-mode-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DemoScenarioCard component</name>
  <files>
    src/components/chatbot/demo/DemoScenarioCard.tsx
  </files>
  <action>
**Create `src/components/chatbot/demo/DemoScenarioCard.tsx`:**

A clickable card for scenario selection at demo start. Each card displays:

- Icon (from Lucide: Briefcase, ShoppingBag, Headphones) mapped via ICON_MAP record
- Title and subtitle
- Step count in mono font
- Staggered entrance animation (delay based on index \* 0.1s)
- Hover effects: border glow (accent-system/40), shadow lift

Props: id, title, subtitle, icon (Lucide name string), stepCount, index, onSelect callback

Use the exact implementation from `docs/plans/2026-03-16-guided-demo-implementation.md` Task 4.
</action>
<verify>
`npx tsc --noEmit` succeeds. Component accepts all required props.
</verify>
<done>
DemoScenarioCard renders scenario selection cards with icons, hover effects, and staggered animations.
</done>
</task>

<task type="auto">
  <name>Task 2: DemoCheckpoint component + pulseGlow CSS</name>
  <files>
    src/components/chatbot/demo/DemoCheckpoint.tsx
    src/index.css
  </files>
  <action>
**1. Create `src/components/chatbot/demo/DemoCheckpoint.tsx`:**

Checkpoint buttons that appear between demo steps. Renders a prompt text and action buttons.

Props: prompt (string), options (array of { label, action }), onSelect callback

Button styling by position:

- First button (primary): cyan filled, rounded-full, with `animate-[pulseGlow_2s_ease-in-out_2s_infinite]` for pulsing glow after 2s
- Last button when action is 'end' (ghost): subtle, low-contrast
- Middle buttons (secondary): outline style

All buttons appear with 800ms delay animation (delay: 0.8 + i \* 0.1), scale 0.9 to 1.0.

Use the exact implementation from `docs/plans/2026-03-16-guided-demo-implementation.md` Task 5.

**2. Add `pulseGlow` keyframe to `src/index.css`:**

Find the existing `@keyframes` section and add:

```css
@keyframes pulseGlow {
  0%,
  100% {
    box-shadow: 0 0 0 0 rgba(0, 212, 255, 0);
  }
  50% {
    box-shadow: 0 0 8px 2px rgba(0, 212, 255, 0.3);
  }
}
```

  </action>
  <verify>
  `npx tsc --noEmit` succeeds. pulseGlow keyframe exists in index.css.
  </verify>
  <done>
  DemoCheckpoint renders action buttons with pulsing glow. pulseGlow keyframe added to CSS.
  </done>
</task>

<task type="auto">
  <name>Task 3: DemoProgress component</name>
  <files>
    src/components/chatbot/demo/DemoProgress.tsx
  </files>
  <action>
**Create `src/components/chatbot/demo/DemoProgress.tsx`:**

Progress dots indicator showing current position in the demo scenario.

Props: totalSteps (number), currentStep (number)

Rendering:

- Array of dots from 0 to totalSteps-1
- Completed steps (i < currentStep): gradient background (accent-system to accent-secondary), white Check icon with spring animation (scale 0→1)
- Current step (i === currentStep): border-2 accent-system, pulsing dot inside (scale 1→1.3→1, infinite)
- Future steps: border-primary, elevated background

Use the exact implementation from `docs/plans/2026-03-16-guided-demo-implementation.md` Task 6.
</action>
<verify>
`npx tsc --noEmit` succeeds. Component renders correct number of dots.
</verify>
<done>
DemoProgress renders animated progress dots with gradient fills and spring checkmarks.
</done>
</task>

<task type="auto">
  <name>Task 4: DemoCompletionCard component</name>
  <files>
    src/components/chatbot/demo/DemoCompletionCard.tsx
  </files>
  <action>
**Create `src/components/chatbot/demo/DemoCompletionCard.tsx`:**

Scenario completion summary card displayed after the last step.

Props: scenarioTitle (string), durationSeconds (number), onTryAnother, onBookCall, onEndDemo callbacks

Content:

- PartyPopper icon with gradient background
- "Demo Complete" title
- Scenario title + duration (in minutes, ceil)
- Social proof: "86% of prospects book a call after this demo"
- 3 CTAs: "Book a call" (primary, with ArrowRight icon), "Try another" (secondary, with RotateCcw icon), "End demo" (ghost)

Use the exact implementation from `docs/plans/2026-03-16-guided-demo-implementation.md` Task 7.
</action>
<verify>
`npx tsc --noEmit` succeeds. Component renders all 3 CTAs.
</verify>
<done>
DemoCompletionCard shows completion summary with social proof and actionable CTAs.
</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no TypeScript errors
- All 4 demo UI components export correctly
- DemoScenarioCard has ICON_MAP for Briefcase, ShoppingBag, Headphones
- DemoCheckpoint buttons have correct styling hierarchy (primary/secondary/ghost)
- DemoProgress renders dots array with 3 visual states (completed/current/future)
- DemoCompletionCard has social proof text and 3 CTA buttons
- pulseGlow keyframe exists in src/index.css
</verification>

<success_criteria>

- 4 presentational demo components created with correct props interfaces
- Premium animations: staggered entry, spring checkmarks, pulsing glow
- Living System design tokens used throughout (accent-system, accent-secondary, bg-elevated, etc.)
- Build passes with no errors
  </success_criteria>

<output>
After completion, create `.planning/phases/21-guided-demo-mode/21-02-SUMMARY.md`
</output>
