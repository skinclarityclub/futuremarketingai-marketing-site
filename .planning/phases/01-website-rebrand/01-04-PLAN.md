---
phase: 01-website-rebrand
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/es.json
  - fmai-nextjs/src/app/[locale]/(marketing)/how-it-works/page.tsx
autonomous: true
gap_closure: true
requirements: [WEB-03, WEB-05, WEB-14]

must_haves:
  truths:
    - 'Visiting /en/skills/content-creator renders hero, features, how-it-works steps, use cases, and CTA with real content'
    - 'All 6 skill pages render with content in EN, NL, and ES (no MISSING_MESSAGE errors)'
    - 'Visiting /en/founding-member renders hero with scarcity badge, pricing card, benefits grid, FAQ, and CTA'
    - 'How-it-works page shows 4 agent onboarding steps (choose/activate/connect/working) not 6 old steps'
  artifacts:
    - path: 'fmai-nextjs/messages/en.json'
      provides: '7 new namespaces: skills-content-creator, skills-voice-agent, skills-lead-qualifier, skills-social-media, skills-ad-creator, skills-reporting, founding-member'
    - path: 'fmai-nextjs/messages/nl.json'
      provides: 'Same 7 namespaces in Dutch'
    - path: 'fmai-nextjs/messages/es.json'
      provides: 'Same 7 namespaces in Spanish'
    - path: 'fmai-nextjs/src/app/[locale]/(marketing)/how-it-works/page.tsx'
      provides: 'STEP_KEYS matching locale keys'
  key_links:
    - from: 'skills/content-creator/page.tsx'
      to: 'messages/en.json'
      via: "getTranslations namespace 'skills-content-creator'"
      pattern: 'skills-content-creator'
    - from: 'founding-member/page.tsx'
      to: 'messages/en.json'
      via: "getTranslations namespace 'founding-member'"
      pattern: 'founding-member'
    - from: 'how-it-works/page.tsx'
      to: 'messages/en.json'
      via: 'STEP_KEYS matching how-it-works.process.steps.*'
      pattern: 'choose|activate|connect|working'
---

<objective>
Add all missing locale namespaces for 7 pages (6 skill pages + founding-member) across 3 locales, and fix the how-it-works STEP_KEYS mismatch.

Purpose: Phase 1 plans 01-02 and 01-03 created page route files that reference translation namespaces that were never added to the locale files. All 7 pages render blank. The how-it-works page has a STEP_KEYS array mismatched with its locale. These are blocking requirements WEB-03, WEB-05, and WEB-14.

Output: 7 new translation namespaces in en.json, nl.json, es.json; fixed STEP_KEYS in how-it-works page.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-website-rebrand/01-VERIFICATION.md

Key reference files:
@fmai-nextjs/src/app/[locale]/(skills)/skills/content-creator/page.tsx
@fmai-nextjs/src/app/[locale]/(marketing)/founding-member/page.tsx
@fmai-nextjs/src/app/[locale]/(marketing)/how-it-works/page.tsx
@fmai-nextjs/messages/en.json

<interfaces>
<!-- Translation key structures required by each page component -->

Content Creator page (namespace: skills-content-creator):
FEATURE_KEYS = ['blog', 'social', 'newsletters', 'repurposing']
STEP_KEYS = ['connect', 'generate', 'review']
USE_CASE_KEYS = ['agencies', 'ecommerce']
Keys: meta.title, meta.description, hero.badge, hero.title, hero.description,
features.heading, features.items.{key}.title, features.items.{key}.description,
how_it_works.heading, how_it_works.steps.{key}.title, how_it_works.steps.{key}.description,
use_cases.heading, use_cases.items.{key}.title, use_cases.items.{key}.description,
cta.title, cta.subtitle, cta.button

Voice Agent page (namespace: skills-voice-agent):
FEATURE_KEYS = ['inbound', 'outbound', 'qualification', 'booking']
STEP_KEYS = ['setup', 'deploy', 'optimize']
USE_CASE_KEYS = ['agencies', 'sales']
Same key structure as Content Creator.

Lead Qualifier page (namespace: skills-lead-qualifier):
FEATURE_KEYS = ['engage', 'qualify', 'handoff', 'nurture']
STEP_KEYS = ['configure', 'embed', 'convert']
USE_CASE_KEYS = ['agencies', 'saas']
Same key structure as Content Creator.

Social Media page (namespace: skills-social-media):
FEATURE_KEYS = ['scheduling', 'analytics', 'creation', 'monitoring']
STEP_KEYS = ['connect', 'plan', 'publish']
USE_CASE_KEYS = ['agencies', 'brands']
Same key structure as Content Creator.

Ad Creator page (namespace: skills-ad-creator):
FEATURE_KEYS = ['static', 'video', 'copy', 'variants']
STEP_KEYS = ['brief', 'generate', 'launch']
USE_CASE_KEYS = ['agencies', 'performance']
Same key structure as Content Creator.

Reporting page (namespace: skills-reporting):
FEATURE_KEYS = ['dashboards', 'weekly', 'trends', 'roi']
STEP_KEYS = ['integrate', 'analyze', 'report']
USE_CASE_KEYS = ['agencies', 'managers']
Same key structure as Content Creator.

Founding Member page (namespace: founding-member):
BENEFIT_KEYS = ['price_lock', 'priority', 'influence', 'early_access', 'community', 'onboarding']
FAQ_KEYS = ['q1', 'q2', 'q3', 'q4', 'q5']
Keys: meta.title, meta.description, hero.badge, hero.title, hero.description,
pricing.heading, pricing.currency, pricing.price, pricing.period, pricing.locked, pricing.comparison, pricing.includes,
benefits.heading, benefits.items.{key}.title, benefits.items.{key}.description,
faq.heading, faq.items.{key}.question, faq.items.{key}.answer,
cta.title, cta.subtitle, cta.button

How-it-works page STEP_KEYS fix:
Current: STEP_KEYS = ['research', 'content', 'workflow', 'publishing', 'learning', 'ads']
Required: STEP_KEYS = ['choose', 'activate', 'connect', 'working']
(Locale files already have the correct 4-step keys)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 missing namespaces to EN locale and fix how-it-works STEP_KEYS</name>
  <files>fmai-nextjs/messages/en.json, fmai-nextjs/src/app/[locale]/(marketing)/how-it-works/page.tsx</files>
  <action>
1. Read the full en.json to understand existing namespace patterns and the existing `how-it-works` namespace structure (to verify the 4-step keys exist).

2. Add 6 top-level skill namespaces to en.json. Each skill namespace must have this exact structure (all keys required by the page components):
   - `meta.title` — SEO title including "AI Marketing Employee" and skill name
   - `meta.description` — SEO description (150-160 chars)
   - `hero.badge` — Short badge text (e.g. "Content Creator Skill")
   - `hero.title` — Main heading
   - `hero.description` — 1-2 sentence skill overview
   - `features.heading` — Section heading
   - `features.items.{key}.title` — Feature title (for each FEATURE_KEY)
   - `features.items.{key}.description` — Feature description (for each FEATURE_KEY)
   - `how_it_works.heading` — Section heading
   - `how_it_works.steps.{key}.title` — Step title (for each STEP_KEY)
   - `how_it_works.steps.{key}.description` — Step description (for each STEP_KEY)
   - `use_cases.heading` — Section heading
   - `use_cases.items.{key}.title` — Use case title (for each USE_CASE_KEY)
   - `use_cases.items.{key}.description` — Use case description (for each USE_CASE_KEY)
   - `cta.title` — CTA heading
   - `cta.subtitle` — CTA description
   - `cta.button` — CTA button text

   Skill content guidance (agency-focused, matching AaaS positioning):
   - **skills-content-creator**: Blog posts, social content, newsletters, content repurposing for agency clients. Features: blog (AI blog writing), social (social media posts), newsletters (email newsletters), repurposing (cross-platform content recycling). Steps: connect (connect client brand), generate (AI creates content), review (approve and publish).
   - **skills-voice-agent**: AI-powered inbound/outbound calling for agency clients. Features: inbound (answer calls 24/7), outbound (proactive outreach), qualification (qualify leads by phone), booking (schedule appointments). Steps: setup (configure voice persona), deploy (activate on client number), optimize (improve from call data).
   - **skills-lead-qualifier**: Chatbot that qualifies website leads 24/7 for agency clients. Features: engage (real-time visitor engagement), qualify (BANT qualification scoring), handoff (warm handoff to sales), nurture (automated follow-up sequences). Steps: configure (set qualification criteria), embed (add to client website), convert (start qualifying leads).
   - **skills-social-media**: Multi-platform social media management for agency clients. Features: scheduling (cross-platform scheduling), analytics (performance tracking), creation (AI content generation), monitoring (brand mention tracking). Steps: connect (link social accounts), plan (AI creates content calendar), publish (auto-post on schedule).
   - **skills-ad-creator**: AI-generated static and video ads for agency clients. Features: static (image ad generation), video (short-form video ads), copy (ad copy variants), variants (A/B test variations). Steps: brief (input campaign brief), generate (AI creates ad variations), launch (push to ad platforms).
   - **skills-reporting**: Cross-platform dashboards and weekly reports for agency clients. Features: dashboards (real-time client dashboards), weekly (automated weekly reports), trends (trend detection and alerts), roi (ROI tracking and attribution). Steps: integrate (connect data sources), analyze (AI processes metrics), report (auto-generate reports).

3. Add 1 top-level `founding-member` namespace to en.json with:
   - `meta.title`: "Founding Member — AI Marketing Employee | FMai"
   - `meta.description`: 150-160 char description about the founding member offer
   - `hero.badge`: "10 Spots Only"
   - `hero.title`: "Become a Founding Member"
   - `hero.description`: 1-2 sentences about the exclusive founding member program
   - `pricing.heading`: "Founding Member Pricing"
   - `pricing.currency`: "EUR"
   - `pricing.price`: "997"
   - `pricing.period`: "/month"
   - `pricing.locked`: "Price locked for life"
   - `pricing.comparison`: "Regular price starts at EUR 1,497/month"
   - `pricing.includes`: "Full AI Marketing Employee + 1 skill of your choice"
   - `benefits.heading`: "Founding Member Benefits"
   - `benefits.items.{key}.title` and `.description` for each of the 6 BENEFIT_KEYS:
     - price_lock: Locked-in pricing forever
     - priority: Priority support and direct founder access
     - influence: Shape the product roadmap
     - early_access: First to get new skills and features
     - community: Exclusive founding member community
     - onboarding: White-glove onboarding with the founder
   - `faq.heading`: "Frequently Asked Questions"
   - `faq.items.{key}.question` and `.answer` for 5 FAQ items (q1-q5) covering: what's included, commitment length, what happens after founding period, how many spots remain, money-back guarantee
   - `cta.title`: "Ready to Be First?"
   - `cta.subtitle`: "Join the founding member program and lock in the lowest price forever."
   - `cta.button`: "Apply Now"

4. Fix how-it-works/page.tsx: Change STEP_KEYS from `['research', 'content', 'workflow', 'publishing', 'learning', 'ads']` to `['choose', 'activate', 'connect', 'working']`. Also update HOW_TO_STEPS array to match the 4 new steps (for the HowToJsonLd component) with agent onboarding descriptions.
   </action>
   <verify>
   <automated>cd fmai-nextjs && grep -c "skills-content-creator" messages/en.json && grep -c "founding-member" messages/en.json | head -1 && grep "choose" src/app/\[locale\]/\(marketing\)/how-it-works/page.tsx</automated>
   </verify>
   <done>en.json contains all 7 new namespaces with every key the page components reference. how-it-works STEP_KEYS matches locale keys (choose/activate/connect/working).</done>
   </task>

<task type="auto">
  <name>Task 2: Add 7 missing namespaces to NL and ES locales</name>
  <files>fmai-nextjs/messages/nl.json, fmai-nextjs/messages/es.json</files>
  <action>
1. Read en.json to get the exact key structures and content of the 7 new namespaces added in Task 1.

2. Add all 7 namespaces to nl.json with Dutch translations:
   - All skill page content translated to Dutch (agency-focused language)
   - Use "AI Marketing Medewerker" for "AI Marketing Employee"
   - Use "bureaus" for "agencies"
   - founding-member: hero.badge "Slechts 10 Plekken", hero.title "Word Founding Member", pricing.locked "Prijs voor altijd vastgelegd", etc.
   - Translate all feature descriptions, step descriptions, use case descriptions, FAQ questions/answers, and CTAs to natural Dutch

3. Add all 7 namespaces to es.json with Spanish translations:
   - All skill page content translated to Spanish (agency-focused language)
   - Use "Empleado de Marketing IA" for "AI Marketing Employee"
   - Use "agencias" for "agencies"
   - founding-member: hero.badge "Solo 10 Plazas", hero.title "Conviertete en Miembro Fundador", pricing.locked "Precio bloqueado de por vida", etc.
   - Translate all feature descriptions, step descriptions, use case descriptions, FAQ questions/answers, and CTAs to natural Spanish

4. Validate key parity: every key in en.json's 7 new namespaces MUST have a corresponding key in nl.json and es.json. Do a quick key-count comparison after adding.

CRITICAL: The key structure must EXACTLY match en.json — same nesting, same key names, only the string values are translated.
</action>
<verify>
<automated>cd fmai-nextjs && grep -c "skills-content-creator" messages/nl.json && grep -c "skills-content-creator" messages/es.json && grep -c "founding-member" messages/nl.json && grep -c "founding-member" messages/es.json</automated>
</verify>
<done>nl.json and es.json contain all 7 new namespaces with translated content. Key structure matches en.json exactly. No MISSING_MESSAGE errors in build.</done>
</task>

</tasks>

<verification>
1. `cd fmai-nextjs && npx next build 2>&1 | grep -i "MISSING_MESSAGE"` — should return nothing (no missing translation errors)
2. `cd fmai-nextjs && npx next build 2>&1 | grep -i "error"` — build should succeed without errors
3. Grep for all 7 namespace names in each locale file to confirm presence
4. Verify how-it-works STEP_KEYS contains exactly ['choose', 'activate', 'connect', 'working']
</verification>

<success_criteria>

- All 6 skill pages render with full content at /en/skills/_, /nl/skills/_, /es/skills/\*
- Founding member page renders with full content at /en/founding-member, /nl/founding-member, /es/founding-member
- How-it-works page shows 4 agent onboarding steps (not 6 old steps)
- No MISSING_MESSAGE errors in build output
- Requirements WEB-03, WEB-05, WEB-14 fully satisfied
  </success_criteria>

<output>
After completion, create `.planning/phases/01-website-rebrand/01-04-SUMMARY.md`
</output>
