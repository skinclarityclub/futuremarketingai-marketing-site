---
phase: 01-website-rebrand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/src/components/layout/HeaderClient.tsx
  - fmai-nextjs/src/components/layout/Footer.tsx
  - fmai-nextjs/next.config.ts
  - fmai-nextjs/src/app/sitemap.ts
  - fmai-nextjs/src/app/[locale]/(skills)/skills/content-creator/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/voice-agent/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/lead-qualifier/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/social-media/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/ad-creator/page.tsx
  - fmai-nextjs/src/app/[locale]/(skills)/skills/reporting/page.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/founding-member/page.tsx
autonomous: true
requirements:
  - WEB-03
  - WEB-11
  - WEB-12

must_haves:
  truths:
    - 'Navigation shows Skills dropdown with 6 skill links instead of Services dropdown'
    - 'Visiting /en/skills/content-creator renders a skill page (not 404)'
    - 'Visiting /en/chatbots redirects to /en/skills/lead-qualifier (301)'
    - 'Footer links point to skill pages and use AaaS language'
    - 'Sitemap includes all 6 skill pages and founding-member page'
    - 'Founding member page route exists at /en/founding-member'
  artifacts:
    - path: 'fmai-nextjs/src/components/layout/HeaderClient.tsx'
      provides: 'Skills dropdown navigation replacing Services'
      contains: 'SKILL_ITEMS'
    - path: 'fmai-nextjs/src/components/layout/Footer.tsx'
      provides: 'Updated footer with skill links'
    - path: 'fmai-nextjs/next.config.ts'
      provides: '301 redirects from old service URLs to new skill URLs'
      contains: 'redirects'
    - path: 'fmai-nextjs/src/app/sitemap.ts'
      provides: 'Updated sitemap with skill and founding-member pages'
      contains: 'skills/content-creator'
    - path: 'fmai-nextjs/src/app/[locale]/(skills)/skills/content-creator/page.tsx'
      provides: 'Content Creator skill page'
    - path: 'fmai-nextjs/src/app/[locale]/(marketing)/founding-member/page.tsx'
      provides: 'Founding member landing page route'
  key_links:
    - from: 'fmai-nextjs/src/components/layout/HeaderClient.tsx'
      to: '/skills/*'
      via: 'SKILL_ITEMS href values'
      pattern: '/skills/'
    - from: 'fmai-nextjs/next.config.ts'
      to: '/skills/*'
      via: 'redirects() destination'
      pattern: 'destination.*skills'
    - from: 'fmai-nextjs/src/app/sitemap.ts'
      to: 'all page routes'
      via: 'pages array'
      pattern: 'skills/content-creator'
---

<objective>
Restructure website navigation from Services to Skills, create route scaffolds for 6 skill pages and founding member landing page, add 301 redirects from old service URLs, and update sitemap and footer.

Purpose: Establishes the new URL structure and navigation framework. Skill page files are scaffolds (minimal content) because Plan 01 provides the translation content they consume.
Output: Working navigation with Skills dropdown, 6 skill page routes, founding member page route, redirects from old URLs, updated sitemap.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-website-rebrand/01-RESEARCH.md

Source files to read:
@fmai-nextjs/src/components/layout/HeaderClient.tsx
@fmai-nextjs/src/components/layout/Footer.tsx
@fmai-nextjs/next.config.ts
@fmai-nextjs/src/app/sitemap.ts
@fmai-nextjs/src/lib/seo-config.ts
@fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx (template reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure navigation and footer</name>
  <files>fmai-nextjs/src/components/layout/HeaderClient.tsx, fmai-nextjs/src/components/layout/Footer.tsx</files>
  <action>
**HeaderClient.tsx:**
1. Read the current file. Find the SERVICE_ITEMS array and NAV_ITEMS array.
2. Replace SERVICE_ITEMS with SKILL_ITEMS containing 6 skills:
   - Content Creator (PenTool icon) → /skills/content-creator — "Blog posts, social content, newsletters — on autopilot"
   - Voice Agent (Mic icon) → /skills/voice-agent — "AI-powered inbound and outbound calls"
   - Lead Qualifier (Bot icon) → /skills/lead-qualifier — "Chatbot that qualifies leads 24/7"
   - Social Media (Share2 icon) → /skills/social-media — "Multi-platform scheduling and analytics"
   - Ad Creator (Megaphone icon) → /skills/ad-creator — "AI-generated static and video ads"
   - Reporting (BarChart3 icon) → /skills/reporting — "Cross-platform dashboards and weekly reports"
3. Update NAV_ITEMS: change first item from "Services" to "Skills" with href '/#skills'. Keep Pricing, About, How It Works.
4. Rename SERVICE_ITEMS references throughout the component to SKILL_ITEMS.
5. Keep the dropdown rendering logic unchanged — just swap the data.

NOTE: HeaderClient.tsx has hardcoded English strings (not translated via next-intl). Per research recommendation, keep hardcoded but update the strings.

**Footer.tsx:**

1. Read the current file. Find navigation link sections.
2. Update service/skill links to point to new /skills/\* URLs.
3. Update any tagline or description text to use AaaS language. Footer uses translation keys — update the references if key names changed, but the actual translated content is handled by Plan 01.
4. Add a "Founding Member" link in the appropriate footer section pointing to /founding-member.
   </action>
   <verify>
   <automated>cd fmai-nextjs && npx tsc --noEmit 2>&1 | tail -20</automated>
   </verify>
   <done>Header shows Skills dropdown with 6 skill items. Footer links point to /skills/\* URLs. No "Services" label remains in navigation. TypeScript compiles without errors.</done>
   </task>

<task type="auto">
  <name>Task 2: Create skill page routes, founding member page, redirects, and sitemap</name>
  <files>fmai-nextjs/src/app/[locale]/(skills)/skills/content-creator/page.tsx, fmai-nextjs/src/app/[locale]/(skills)/skills/voice-agent/page.tsx, fmai-nextjs/src/app/[locale]/(skills)/skills/lead-qualifier/page.tsx, fmai-nextjs/src/app/[locale]/(skills)/skills/social-media/page.tsx, fmai-nextjs/src/app/[locale]/(skills)/skills/ad-creator/page.tsx, fmai-nextjs/src/app/[locale]/(skills)/skills/reporting/page.tsx, fmai-nextjs/src/app/[locale]/(marketing)/founding-member/page.tsx, fmai-nextjs/next.config.ts, fmai-nextjs/src/app/sitemap.ts</files>
  <action>
**Step 1: Read an existing service page (e.g., chatbots/page.tsx) as a template.** Copy its structure: generateStaticParams, generateMetadata, server component with getTranslations, PageShell wrapper, JSON-LD.

**Step 2: Create 6 skill page files** using the (skills) route group:

- `src/app/[locale]/(skills)/skills/content-creator/page.tsx`
- `src/app/[locale]/(skills)/skills/voice-agent/page.tsx`
- `src/app/[locale]/(skills)/skills/lead-qualifier/page.tsx`
- `src/app/[locale]/(skills)/skills/social-media/page.tsx`
- `src/app/[locale]/(skills)/skills/ad-creator/page.tsx`
- `src/app/[locale]/(skills)/skills/reporting/page.tsx`

Each skill page should:

- Use `generateStaticParams()` returning locales from routing config
- Use `generateMetadata()` with `generatePageMetadata({ locale, namespace: 'skills-{slug}', path: '/skills/{slug}' })`
- Use `getTranslations({ locale, namespace: 'skills-{slug}' })` (Plan 01 provides content)
- Wrap content in `<PageShell>`
- Include `<WebPageJsonLd>` and `<BreadcrumbJsonLd>` (following existing page patterns)
- Render sections: Hero (headline + description), Features/capabilities list, How it works, Use cases, CTA to pricing
- Use existing UI components: GlassCard, SectionHeading, ScrollReveal, CTAButton

**Step 3: Create founding member landing page:**

- `src/app/[locale]/(marketing)/founding-member/page.tsx`
- Same structure as skill pages but with:
  - Hero: scarcity headline ("Only 10 Founding Member Spots"), benefits list
  - Pricing section: EUR 997/mo locked price, comparison to regular pricing
  - Benefits grid: 4-6 founding member benefits (using GlassCard)
  - FAQ specific to founding members
  - CTA: Link to contact/Calendly (per research: use existing CTA pattern for v1)
- Namespace: 'founding-member' (Plan 01 provides translated content)

**Step 4: Add redirects in next.config.ts:**

```typescript
async redirects() {
  return [
    { source: '/:locale/chatbots', destination: '/:locale/skills/lead-qualifier', permanent: true },
    { source: '/:locale/automations', destination: '/:locale/skills/content-creator', permanent: true },
    { source: '/:locale/voice-agents', destination: '/:locale/skills/voice-agent', permanent: true },
    { source: '/:locale/marketing-machine', destination: '/:locale/skills/content-creator', permanent: true },
  ]
}
```

**Step 5: Update sitemap.ts:**

- Remove old service page URLs (/chatbots, /automations, /voice-agents, /marketing-machine)
- Add all 6 skill page URLs (/skills/content-creator, etc.)
- Add /founding-member URL
- Keep all other existing pages

**Step 6: Update seo-config.ts PAGE_DATES** (if it exists):

- Add entries for new skill pages and founding-member page
- Remove entries for old service pages

Do NOT delete old service page files yet — the redirects handle the transition. Old files can be cleaned up in a future phase.
</action>
<verify>
<automated>cd fmai-nextjs && npx next build 2>&1 | tail -30</automated>
</verify>
<done>All 6 skill pages render at /en/skills/{slug} without 404. Founding member page renders at /en/founding-member. Old URLs (/chatbots, /automations, /voice-agents, /marketing-machine) redirect with 301 to new skill URLs. Sitemap includes all new pages. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd fmai-nextjs && npx next build` completes without errors
2. Skill page files exist: `ls fmai-nextjs/src/app/\[locale\]/\(skills\)/skills/*/page.tsx` shows 6 files
3. Founding member page exists: `ls fmai-nextjs/src/app/\[locale\]/\(marketing\)/founding-member/page.tsx`
4. `grep -c "SKILL_ITEMS" fmai-nextjs/src/components/layout/HeaderClient.tsx` returns >= 1
5. `grep -c "skills/" fmai-nextjs/src/app/sitemap.ts` returns >= 6
6. `grep -c "redirects" fmai-nextjs/next.config.ts` returns >= 1
</verification>

<success_criteria>

- Navigation shows "Skills" dropdown with 6 skill pages
- All 6 skill page routes work (not 404)
- Founding member page route works
- Old service URLs redirect to corresponding skill pages (301)
- Footer links updated for skills
- Sitemap includes all new pages, excludes old service pages
  </success_criteria>

<output>
After completion, create `.planning/phases/01-website-rebrand/01-02-SUMMARY.md`
</output>
