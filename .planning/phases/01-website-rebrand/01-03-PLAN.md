---
phase: 01-website-rebrand
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx
  - fmai-nextjs/src/app/[locale]/page.tsx
autonomous: false
requirements:
  - WEB-04
  - WEB-05
  - WEB-15

must_haves:
  truths:
    - 'Pricing page displays 4 agent tiers (Founding Member EUR 997, Starter EUR 1,497, Growth EUR 1,997, Agency EUR 3,497) with skill add-ons'
    - "Founding Member tier shows '10 Spots Only' badge and links to founding member landing page"
    - 'Chatbot demo personas demonstrate agency use cases (not generic e-commerce/support)'
    - 'Homepage SERVICE_CARDS array updated to reference skills'
  artifacts:
    - path: 'fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx'
      provides: 'Redesigned pricing page with 4 tiers + add-ons'
      contains: 'founding'
    - path: 'fmai-nextjs/src/app/[locale]/page.tsx'
      provides: 'Updated homepage with skill cards and agency copy references'
  key_links:
    - from: 'fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx'
      to: 'fmai-nextjs/messages/en.json'
      via: "getTranslations namespace 'pricing'"
      pattern: 'getTranslations.*pricing'
    - from: 'fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx'
      to: '/founding-member'
      via: 'CTA button href'
      pattern: 'founding-member'
---

<objective>
Redesign the pricing page for 4 agent tiers with skill add-ons, update homepage component references to use new skill content, and update chatbot demo personas for agency use cases.

Purpose: Completes the visual and functional rebrand by ensuring the two highest-impact pages (pricing, homepage) correctly present the AaaS offering, and the chatbot demo showcases agency-relevant scenarios.
Output: Working pricing page with 4 tiers, updated homepage sections, agency-focused chatbot personas.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-website-rebrand/01-RESEARCH.md
@.planning/phases/01-website-rebrand/01-01-SUMMARY.md
@.planning/phases/01-website-rebrand/01-02-SUMMARY.md

Source files to read:
@fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx
@fmai-nextjs/src/app/[locale]/page.tsx
@fmai-nextjs/messages/en.json (updated by Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign pricing page for 4 agent tiers</name>
  <files>fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx</files>
  <action>
Read the current pricing page. The existing page has a 3-column grid with TIER_KEYS array rendering 3 tiers. This must be restructured for 4 tiers + skill add-ons.

**Changes to the pricing page component:**

1. Update TIER_KEYS array from 3 to 4 items: ['founding', 'starter', 'growth', 'agency']
2. Change the grid layout from 3-column to either:
   - 4-column on xl screens (grid-cols-4), 2-column on md (grid-cols-2), 1-column on mobile
   - Or keep 3-column with the Founding Member tier displayed prominently above as a featured card
     Choose whichever matches the existing design patterns better.
3. The Founding Member tier card should:
   - Show a "10 Spots Only" badge (from translation: `pricing.tiers.founding.badge`)
   - Be visually highlighted (use GlassCard `highlighted` prop if available, or add a border accent)
   - CTA links to /founding-member page (not generic contact)
4. Add a "Skill Add-ons" section below the tiers:
   - Section heading from translations
   - Grid of skill cards showing: skill name, price, description
   - Each skill links to its /skills/{slug} page
5. Keep all text from translation keys (Plan 01 already populated these in pricing namespace)
6. Keep existing JSON-LD components (update ServiceJsonLd if it references old service structure)
7. Use existing UI components: GlassCard, SectionHeading, ScrollReveal, CTAButton

**Layout approach:** The existing GlassCard component supports `highlighted` prop. Use it for the "Growth" tier (marked "Most Popular") or the "Founding Member" tier — check existing pattern and apply consistently.
</action>
<verify>
<automated>cd fmai-nextjs && npx next build 2>&1 | tail -20</automated>
</verify>
<done>Pricing page renders 4 agent tiers with correct prices. Founding Member shows "10 Spots Only" badge and links to /founding-member. Skill add-ons section displays below tiers. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update homepage component references and chatbot personas</name>
  <files>fmai-nextjs/src/app/[locale]/page.tsx</files>
  <action>
Read the current homepage page.tsx. This file contains JavaScript arrays (SERVICE_CARDS, STAT_KEYS, FAQ_KEYS, etc.) that reference translation keys. These must be updated to match the new content from Plan 01.

**Homepage updates:**

1. **SERVICE_CARDS array** (or similarly named): Replace with SKILL_CARDS referencing the 6 skills. Each card should:
   - Use the translation key from the updated messages (e.g., `home.services.items.contentCreator.*` or whatever key structure Plan 01 established)
   - Link to /skills/{slug}
   - Use appropriate Lucide icons (PenTool, Mic, Bot, Share2, Megaphone, BarChart3)
2. **STAT_KEYS array**: Verify keys match what Plan 01 put in `home.stats.*`. Update key names if Plan 01 changed them.
3. **FAQ_KEYS array**: Verify keys match what Plan 01 put in `home.faq.items.*`. Update count/keys if Plan 01 changed the number of FAQ items.
4. **Any hardcoded strings**: Check for any remaining English strings in the component that bypass translations. Update them.
5. **CTA buttons**: Ensure primary CTA points to /skills (or /#skills section) and secondary CTA points to /pricing. Add a tertiary CTA or banner linking to /founding-member if design allows.

**Chatbot personas (WEB-15):**
Find where chatbot demo personas are configured. Based on research, the DemoPlayground component uses `chatbots.demo.tabs.*` translation keys. The content was updated by Plan 01 in the message files. However, if there's a `scenarios.ts` or similar config file with persona logic:

1. Read the chatbot demo configuration files
2. Update persona IDs and scenarios for agency use cases:
   - Persona 1: "Agency Client Onboarding" — demonstrates how the AI employee learns a new client's brand
   - Persona 2: "Content Creation Demo" — shows content skill creating a blog post for a client
   - Persona 3: "ROI Calculator" — chatbot that estimates agency savings with AI employee
3. If personas are purely translation-driven (no code changes needed beyond Plan 01's message updates), verify the translation keys are correctly referenced and skip code changes.

Check if chatbot demo appears on homepage or on a separate /chatbots page. If on homepage, update there. If separate page, that page's content is already handled by Plan 01 translations — just verify references.
</action>
<verify>
<automated>cd fmai-nextjs && npx next build 2>&1 | tail -20</automated>
</verify>
<done>Homepage shows 6 skill cards linking to /skills/\* pages. Stats and FAQ sections render correctly with new agency content. Chatbot personas demonstrate agency use cases. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete rebrand</name>
  <files>none (visual verification only)</files>
  <action>
Human verifies the complete website rebrand visually across all pages and locales. Dev server must be running.
  </action>
  <what-built>Complete website rebrand from "AI automation agency" to "AI Marketing Employee for agencies" — all copy updated across 3 locales, navigation restructured to Skills, 6 skill pages created, founding member page created, pricing page redesigned with 4 tiers, chatbot personas updated.</what-built>
  <how-to-verify>
1. Start dev server: `cd fmai-nextjs && npm run dev`
2. Visit http://localhost:3000/en — Homepage:
   - Hero says "AI Marketing Employee" not "AI automation"
   - Skills section shows 6 skills (Content Creator, Voice Agent, Lead Qualifier, Social Media, Ad Creator, Reporting)
   - Stats use agency metrics
   - Trust badges say "GDPR-First", "Powered by Enterprise AI", "Dutch Support"
   - FAQ answers agency questions
3. Click "Skills" dropdown in header — should show 6 skill pages
4. Visit http://localhost:3000/en/skills/content-creator — skill page renders
5. Visit http://localhost:3000/en/pricing — shows 4 tiers (Founding/Starter/Growth/Agency)
6. Visit http://localhost:3000/en/founding-member — landing page with scarcity CTA
7. Visit http://localhost:3000/en/chatbots — should redirect to a skill page
8. Switch to NL: http://localhost:3000/nl — Dutch copy throughout
9. Switch to ES: http://localhost:3000/es — Spanish copy throughout
10. Visit http://localhost:3000/en/about — agency-focused mission
11. Visit http://localhost:3000/en/how-it-works — agent onboarding journey
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
  <verify>
    <automated>cd fmai-nextjs && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>User has approved the visual rebrand across all pages and all 3 locales (EN/NL/ES).</done>
</task>

</tasks>

<verification>
1. `cd fmai-nextjs && npx next build` completes without errors
2. Pricing page renders 4 tiers with correct prices
3. Homepage skill cards link to /skills/* pages
4. Chatbot demo shows agency personas
5. Full visual review in dev mode (all 3 locales)
</verification>

<success_criteria>

- Pricing page displays 4 agent tiers with skill add-ons section
- Founding Member tier links to /founding-member landing page
- Homepage sections all reference new skill/agency content
- Chatbot personas demonstrate agency use cases
- User approves visual verification across all 3 locales
  </success_criteria>

<output>
After completion, create `.planning/phases/01-website-rebrand/01-03-SUMMARY.md`
</output>
