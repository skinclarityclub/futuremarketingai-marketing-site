---
phase: 01-foundation-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/package.json
  - fmai-nextjs/next.config.ts
  - fmai-nextjs/postcss.config.mjs
  - fmai-nextjs/tsconfig.json
  - fmai-nextjs/proxy.ts
  - fmai-nextjs/app/globals.css
  - fmai-nextjs/app/[locale]/layout.tsx
  - fmai-nextjs/app/[locale]/page.tsx
  - fmai-nextjs/lib/fonts.ts
  - fmai-nextjs/lib/metadata.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-09

must_haves:
  truths:
    - 'Next.js app starts locally with `npm run dev` and serves pages via App Router'
    - 'Visiting /en/ renders a server-side page with correct HTML (view-source shows content)'
    - 'DM Sans, JetBrains Mono, and Space Grotesk load via next/font with zero layout shift'
    - 'Tailwind v4 design tokens produce correct Living System colors (bg-bg-deep renders #0a0d14, text-accent-system renders #00d4aa)'
  artifacts:
    - path: 'fmai-nextjs/package.json'
      provides: 'Project dependencies'
      contains: 'next-intl'
    - path: 'fmai-nextjs/next.config.ts'
      provides: 'Next.js config with next-intl plugin and React Compiler'
      contains: 'createNextIntlPlugin'
    - path: 'fmai-nextjs/app/globals.css'
      provides: 'Tailwind v4 theme with all Living System design tokens'
      contains: '@theme'
    - path: 'fmai-nextjs/lib/fonts.ts'
      provides: 'next/font setup for DM Sans, JetBrains Mono, Space Grotesk'
      contains: 'DM_Sans'
    - path: 'fmai-nextjs/app/[locale]/layout.tsx'
      provides: 'Root layout with font CSS variables and locale setup'
      contains: 'generateStaticParams'
    - path: 'fmai-nextjs/lib/metadata.ts'
      provides: 'Shared metadata generator for all pages'
      exports: ['generatePageMetadata']
  key_links:
    - from: 'fmai-nextjs/app/[locale]/layout.tsx'
      to: 'fmai-nextjs/lib/fonts.ts'
      via: 'font CSS variable classes on html element'
      pattern: "dmSans\\.variable.*jetbrainsMono\\.variable"
    - from: 'fmai-nextjs/app/globals.css'
      to: 'fmai-nextjs/lib/fonts.ts'
      via: 'CSS variables referenced in @theme --font-sans/--font-mono/--font-display'
      pattern: "var\\(--font-dm-sans\\)"
    - from: 'fmai-nextjs/next.config.ts'
      to: 'next-intl/plugin'
      via: 'withNextIntl wrapper'
      pattern: 'withNextIntl'
---

<objective>
Scaffold the Next.js 16 project in a NEW directory (fmai-nextjs) with App Router, Tailwind CSS 4 design system with all Living System tokens, next/font typography (DM Sans, JetBrains Mono, Space Grotesk), and core configuration files. This is the foundation everything else builds on.

Purpose: Establish the project skeleton that subsequent plans (i18n, stores, page migration) depend on. A broken foundation means rework on everything.
Output: A running Next.js app at fmai-nextjs/ with correct design tokens, fonts, and App Router structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
@.planning/research/STACK.md

Source codebase for reference:
@tailwind.config.js (Living System design tokens to migrate)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and install dependencies</name>
  <files>
    fmai-nextjs/package.json
    fmai-nextjs/next.config.ts
    fmai-nextjs/postcss.config.mjs
    fmai-nextjs/tsconfig.json
  </files>
  <action>
    Create the new project directory at C:\Users\daley\Desktop\fmai-nextjs (sibling to Futuremarketingai, NOT inside it).

    Run: `npx create-next-app@latest fmai-nextjs --typescript --tailwind --eslint --app --src-dir` from C:\Users\daley\Desktop\

    After scaffold completes, install Phase 1 dependencies:
    ```
    npm install next-intl zustand tailwind-merge
    npm install --save-dev schema-dts @tailwindcss/postcss postcss babel-plugin-react-compiler
    ```

    Configure next.config.ts:
    - Import createNextIntlPlugin from 'next-intl/plugin'
    - Wrap config with withNextIntl()
    - Set reactCompiler: true
    - Configure images.remotePatterns as empty array (add later)

    Configure postcss.config.mjs:
    - Single plugin: '@tailwindcss/postcss': {}

    If create-next-app generates middleware.ts, rename it to proxy.ts (Next.js 16 rename).

    Create stub proxy.ts at project root with next-intl createMiddleware using routing config (will be fleshed out in Plan 02, but needs to exist for the app to route correctly):
    ```typescript
    import createMiddleware from 'next-intl/middleware';
    import { routing } from './src/i18n/routing';
    export default createMiddleware(routing);
    export const config = {
      matcher: '/((?!api|trpc|_next|_vercel|.*\\..*).*)',
    };
    ```

    Create minimal i18n routing stub at src/i18n/routing.ts:
    ```typescript
    import { defineRouting } from 'next-intl/routing';
    export const routing = defineRouting({
      locales: ['en', 'nl', 'es'],
      defaultLocale: 'en',
      localePrefix: 'always',
    });
    ```

    Create minimal i18n request config at src/i18n/request.ts:
    ```typescript
    import { getRequestConfig } from 'next-intl/server';
    import { routing } from './routing';
    export default getRequestConfig(async ({ requestLocale }) => {
      let locale = await requestLocale;
      if (!locale || !routing.locales.includes(locale as any)) {
        locale = routing.defaultLocale;
      }
      return {
        locale,
        messages: (await import(`../../messages/${locale}.json`)).default,
      };
    });
    ```

    Create minimal messages/en.json with stub content:
    ```json
    {
      "hero": {
        "meta": { "title": "FutureMarketingAI - Your AI Marketing Team", "description": "AI-powered marketing automation for B2B companies" },
        "headline": "Your AI Marketing Team",
        "subheadline": "Automate your marketing with AI agents that work 24/7"
      }
    }
    ```

    Also create messages/nl.json and messages/es.json with the same structure but translated stubs (Dutch and Spanish respectively).

    Note: The i18n stubs are minimal -- Plan 02 will do the full translation migration. These exist so the app can boot.

  </action>
  <verify>
    Run `cd C:\Users\daley\Desktop\fmai-nextjs && npm run dev` -- app starts without errors on localhost:3000.
    Run `npm run build` -- builds successfully.
  </verify>
  <done>Next.js 16 project exists at C:\Users\daley\Desktop\fmai-nextjs with all Phase 1 dependencies installed, next.config.ts configured with next-intl plugin and React Compiler, and the app boots locally.</done>
</task>

<task type="auto">
  <name>Task 2: Tailwind v4 design system, next/font typography, root layout, and metadata utility</name>
  <files>
    fmai-nextjs/src/app/globals.css
    fmai-nextjs/src/lib/fonts.ts
    fmai-nextjs/src/lib/metadata.ts
    fmai-nextjs/src/app/[locale]/layout.tsx
    fmai-nextjs/src/app/[locale]/page.tsx
  </files>
  <action>
    **1. Tailwind v4 globals.css with full Living System tokens:**

    Replace the scaffold globals.css with a complete Tailwind v4 setup. Use `@import 'tailwindcss'` (NOT the old @tailwind directives). Define ALL tokens from the existing tailwind.config.js via `@theme` directive:

    Colors (map ALL from tailwind.config.js):
    - --color-bg-deep: #0a0d14
    - --color-bg-surface: #111520
    - --color-bg-elevated: #1a1f2e
    - --color-accent-system: #00d4aa
    - --color-accent-human: #f5a623
    - --color-status-active: #22c55e
    - --color-success: #22c55e
    - --color-warning: #f5a623
    - --color-error: #ef4444
    - --color-info: #00d4aa
    - --color-text-primary: #e8ecf4
    - --color-text-secondary: #9ba3b5
    - --color-text-muted: #5a6378
    - --color-border-primary: rgba(255, 255, 255, 0.06)
    - --color-border-accent: rgba(0, 212, 170, 0.3)
    - --color-divider: rgba(255, 255, 255, 0.04)

    Fonts (reference next/font CSS variables):
    - --font-sans: var(--font-dm-sans), -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
    - --font-display: var(--font-space-grotesk), var(--font-dm-sans), sans-serif
    - --font-mono: var(--font-jetbrains-mono), Consolas, Monaco, monospace

    Border radius:
    - --radius-card: 20px
    - --radius-btn: 14px

    Shadows (map ALL from tailwind.config.js):
    - --shadow-glow-sm: 0 0 10px rgba(0, 212, 170, 0.2)
    - --shadow-glow: 0 0 20px rgba(0, 212, 170, 0.3)
    - --shadow-glow-lg: 0 0 40px rgba(0, 212, 170, 0.4)
    - --shadow-glow-amber: 0 0 20px rgba(245, 166, 35, 0.3)
    - --shadow-glow-active: 0 0 20px rgba(34, 197, 94, 0.3)
    - --shadow-inner-glow: inset 0 0 20px rgba(0, 212, 170, 0.15)

    Add custom keyframes and @utility directives for ALL 7 animations from tailwind.config.js:
    - glow-pulse, float, slide-up, slide-down, fade-in, status-pulse, data-flow

    Add custom background-image utilities via @utility for gradient-system, gradient-human, gradient-flow.

    **2. next/font setup (src/lib/fonts.ts):**

    Configure three Google Fonts with CSS variables:
    - DM_Sans: variable '--font-dm-sans', weights 400/500/600/700, subsets ['latin'], display 'swap'
    - JetBrains_Mono: variable '--font-jetbrains-mono', subsets ['latin'], display 'swap'
    - Space_Grotesk: variable '--font-space-grotesk', weights 400/500/600/700, subsets ['latin'], display 'swap'

    **3. Root layout (src/app/[locale]/layout.tsx):**

    Server Component (NO "use client"). Must:
    - Import fonts from @/lib/fonts
    - Import globals.css
    - Accept params: Promise<{ locale: string }>
    - Validate locale against routing.locales, call notFound() if invalid
    - Call setRequestLocale(locale) for static generation support
    - Load messages via getMessages()
    - Wrap children with NextIntlClientProvider (messages passed)
    - Apply font CSS variables to <html> element: `${dmSans.variable} ${jetbrainsMono.variable} ${spaceGrotesk.variable}`
    - Set <html lang={locale}>
    - Set <body> classes: bg-bg-deep text-text-primary font-sans antialiased
    - Export generateStaticParams returning all 3 locales

    **4. Homepage placeholder (src/app/[locale]/page.tsx):**

    Server Component. Must:
    - Call setRequestLocale(locale)
    - Use getTranslations('hero') for server-side translations
    - Export generateMetadata using the metadata utility
    - Render a simple page with <main>, <h1> (translated headline), <p> (translated subheadline)
    - Include a visual token test section: a div with bg-bg-surface, text-accent-system, shadow-glow, font-display classes to visually verify design tokens work

    **5. Metadata utility (src/lib/metadata.ts):**

    Create generatePageMetadata function accepting { title, description, locale, path, ogImage? }. Returns Next.js Metadata object with:
    - title, description
    - alternates.canonical pointing to the locale-specific URL
    - alternates.languages with en/nl/es URLs
    - openGraph with title, description, url, siteName 'FutureMarketingAI', locale, type 'website'
    - Base URL: 'https://futuremarketingai.com'

    **6. Create route group stubs:**

    Create empty directories with .gitkeep files:
    - src/app/[locale]/(marketing)/
    - src/app/[locale]/(services)/
    - src/app/[locale]/(legal)/

    Remove the default scaffold page.tsx and layout.tsx from src/app/ (the [locale] versions replace them). Keep only a root layout.tsx at src/app/layout.tsx that re-exports or redirects -- actually, with next-intl, the root src/app/layout.tsx should be minimal (just children pass-through) since the [locale]/layout.tsx handles everything. Follow the next-intl pattern: root layout is a bare shell, [locale]/layout.tsx is the real layout.

  </action>
  <verify>
    Run `cd C:\Users\daley\Desktop\fmai-nextjs && npm run dev` and visit http://localhost:3000/en/ in browser.
    Verify:
    1. Page renders with dark background (#0a0d14)
    2. Text appears in DM Sans font
    3. The token test section shows teal accent color (#00d4aa)
    4. View page source -- HTML contains the h1 and p content (SSR working)
    5. Visit /nl/ and /es/ -- both render with their stub translations
    6. Run `npm run build` -- builds without errors
  </verify>
  <done>Tailwind v4 renders all Living System design tokens correctly. DM Sans, JetBrains Mono, and Space Grotesk load via next/font. Root layout serves locale-routed pages via App Router. Metadata utility generates correct meta tags. The app is visually a dark-themed page with the correct brand colors.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npm run build` completes without errors
3. http://localhost:3000/ redirects to /en/ (locale prefix always present)
4. http://localhost:3000/en/ renders server-side HTML visible in view-source
5. All 3 locales (/en/, /nl/, /es/) render correctly
6. Design tokens produce correct colors (inspect element confirms hex values)
7. Fonts load without layout shift (no FOUT visible on page load)
</verification>

<success_criteria>
A Next.js 16 project exists at C:\Users\daley\Desktop\fmai-nextjs that boots locally, serves server-rendered pages at /en/, /nl/, /es/ via App Router, renders the Living System design tokens identically to the Vite site's color scheme, and loads DM Sans + JetBrains Mono + Space Grotesk via next/font with zero layout shift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md`
</output>
