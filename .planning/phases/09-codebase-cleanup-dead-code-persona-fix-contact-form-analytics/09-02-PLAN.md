---
phase: 09-codebase-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/src/lib/chatbot/personas/index.ts
  - fmai-nextjs/src/lib/chatbot/personas/concierge.ts
  - fmai-nextjs/src/lib/chatbot/personas/demo-guide.ts
  - fmai-nextjs/src/lib/chatbot/personas/flagship.ts
  - fmai-nextjs/src/lib/chatbot/personas/ecommerce.ts
  - fmai-nextjs/src/lib/chatbot/personas/leadgen.ts
  - fmai-nextjs/src/lib/chatbot/personas/support.ts
  - fmai-nextjs/src/lib/chatbot/engine.ts
  - fmai-nextjs/src/components/chatbot/DemoPlayground.tsx
  - fmai-nextjs/src/app/[locale]/(services)/
autonomous: true
requirements:
  - WEB-01

must_haves:
  truths:
    - 'DemoPlayground does not crash when switching persona tabs'
    - 'All 4 demo tabs send messages through Clyde persona without error'
    - '6 orphaned persona files are deleted from the codebase'
    - 'Old (services) pages redirect to corresponding (skills) pages or are deleted'
  artifacts:
    - path: 'fmai-nextjs/src/lib/chatbot/personas/index.ts'
      provides: 'Clean persona index with only Clyde'
    - path: 'fmai-nextjs/src/components/chatbot/DemoPlayground.tsx'
      provides: 'DemoPlayground using Clyde persona for all tabs'
    - path: 'fmai-nextjs/src/lib/chatbot/engine.ts'
      provides: 'Engine without hardcoded flagship fallback'
  key_links:
    - from: 'fmai-nextjs/src/components/chatbot/DemoPlayground.tsx'
      to: 'fmai-nextjs/src/lib/chatbot/personas/index.ts'
      via: 'persona ID sent to chat API'
      pattern: 'personaId.*clyde'
---

<objective>
Fix the DemoPlayground persona crash, delete orphaned persona files, and handle old (services) pages.

Purpose: DemoPlayground sends persona IDs (ecommerce, leadgen, support, concierge) that are not registered, causing potential crashes. Six persona files are dead code. Four (services) pages are orphaned from navigation.
Output: Working DemoPlayground, clean persona system, no orphaned pages.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@fmai-nextjs/src/components/chatbot/DemoPlayground.tsx
@fmai-nextjs/src/lib/chatbot/personas/index.ts
@fmai-nextjs/src/lib/chatbot/engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DemoPlayground persona crash and delete orphaned persona files</name>
  <files>fmai-nextjs/src/components/chatbot/DemoPlayground.tsx, fmai-nextjs/src/lib/chatbot/personas/index.ts, fmai-nextjs/src/lib/chatbot/engine.ts, fmai-nextjs/src/lib/chatbot/personas/concierge.ts, fmai-nextjs/src/lib/chatbot/personas/demo-guide.ts, fmai-nextjs/src/lib/chatbot/personas/flagship.ts, fmai-nextjs/src/lib/chatbot/personas/ecommerce.ts, fmai-nextjs/src/lib/chatbot/personas/leadgen.ts, fmai-nextjs/src/lib/chatbot/personas/support.ts</files>
  <action>
1. Read DemoPlayground.tsx to understand how persona IDs are sent to the API. Update all 4 demo tab configs to use `personaId: 'clyde'` instead of individual persona IDs (ecommerce, leadgen, support, concierge). The tabs can still have distinct display names and starter prompts but must all route through the single Clyde persona.

2. In engine.ts, find the hardcoded 'flagship' fallback reference (line ~177) and change it to 'clyde'. Add a graceful fallback: if an unknown persona ID is received, default to 'clyde' instead of returning a 404 error.

3. Delete the 6 orphaned persona files:
   `git rm fmai-nextjs/src/lib/chatbot/personas/concierge.ts fmai-nextjs/src/lib/chatbot/personas/demo-guide.ts fmai-nextjs/src/lib/chatbot/personas/flagship.ts fmai-nextjs/src/lib/chatbot/personas/ecommerce.ts fmai-nextjs/src/lib/chatbot/personas/leadgen.ts fmai-nextjs/src/lib/chatbot/personas/support.ts`

4. Clean up personas/index.ts: remove any commented-out imports or references to deleted personas. Should only export/import clyde.ts.

5. Check if any dead knowledge base files in fmai-nextjs/src/lib/chatbot/knowledge/ are only referenced by deleted personas. If so, delete them too.
   </action>
   <verify>
   <automated>cd fmai-nextjs && npx tsc --noEmit 2>&1 | head -20 && echo "---" && test ! -f src/lib/chatbot/personas/flagship.ts && test ! -f src/lib/chatbot/personas/concierge.ts && echo "PASS: personas deleted" || echo "FAIL"</automated>
   </verify>
   <done>DemoPlayground sends 'clyde' as personaId for all 4 tabs. Engine falls back to 'clyde' for unknown persona IDs. 6 orphaned persona files deleted. TypeScript compiles without errors.</done>
   </task>

<task type="auto">
  <name>Task 2: Handle orphaned (services) pages</name>
  <files>fmai-nextjs/src/app/[locale]/(services)/, fmai-nextjs/src/middleware.ts</files>
  <action>
The (services) route group has 4 pages (/chatbots, /marketing-machine, /voice-agents, /automations) that are not linked from navigation (header links to /skills/* pages instead). These pages still have hardcoded English strings and duplicated pricing data.

Decision: Delete the (services) pages and add redirects from old URLs to corresponding (skills) pages. This eliminates dead code and prevents SEO confusion from duplicate content.

1. Add redirects in next.config.js (or next.config.mjs -- check which exists):
   - /chatbots -> /skills/chatbot
   - /marketing-machine -> /skills/content-creator
   - /voice-agents -> /skills/voice-agent
   - /automations -> /skills/ad-creator

   Use permanent redirects (308) with locale prefix handling. Check how existing redirects are configured in the project and follow the same pattern.

2. Delete the entire (services) route group:
   `git rm -r "fmai-nextjs/src/app/[locale]/(services)/"`

3. Verify that no other files import from or link to the deleted (services) pages. Search for `/chatbots`, `/marketing-machine`, `/voice-agents`, `/automations` in the codebase and update any remaining references.
   </action>
   <verify>
   <automated>cd fmai-nextjs && npx tsc --noEmit 2>&1 | head -20 && test ! -d "src/app/[locale]/(services)" && echo "PASS: services deleted" || echo "FAIL"</automated>
   </verify>
   <done>4 (services) pages deleted. Permanent redirects configured from old URLs to corresponding (skills) pages. No broken imports or links remain.</done>
   </task>

</tasks>

<verification>
- `cd fmai-nextjs && npm run build` succeeds
- No TypeScript errors from deleted persona imports
- DemoPlayground tabs all function with Clyde persona
- Old service page URLs return 308 redirects to skills pages
</verification>

<success_criteria>
DemoPlayground works without crashes on all 4 tabs. Orphaned persona files are gone. Old (services) pages are deleted with redirects in place. Clean chatbot persona system with only Clyde.
</success_criteria>

<output>
After completion, create `.planning/phases/09-codebase-cleanup-dead-code-persona-fix-contact-form-analytics/09-02-SUMMARY.md`
</output>
