---
phase: 09-codebase-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - fmai-nextjs/src/app/api/contact/route.ts
  - fmai-nextjs/src/app/layout.tsx
  - fmai-nextjs/package.json
  - fmai-nextjs/src/components/layout/HeaderClient.tsx
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/es.json
autonomous: false
requirements:
  - WEB-01

user_setup:
  - service: resend
    why: 'Email delivery for contact form submissions'
    env_vars:
      - name: RESEND_API_KEY
        source: 'Resend Dashboard (resend.com) -> API Keys -> Create API Key'
      - name: CONTACT_EMAIL_TO
        source: 'The email address where contact form submissions should be sent (e.g., info@futuremarketingai.com)'
    dashboard_config:
      - task: 'Verify sending domain or use onboarding@resend.dev for testing'
        location: 'Resend Dashboard -> Domains'
  - service: google-analytics
    why: 'Website visitor tracking and page view analytics'
    env_vars:
      - name: NEXT_PUBLIC_GA_MEASUREMENT_ID
        source: 'Google Analytics -> Admin -> Data Streams -> Web -> Measurement ID (starts with G-)'

must_haves:
  truths:
    - 'Contact form submission sends an email to the configured recipient'
    - 'Page views are tracked in Google Analytics'
    - 'Header navigation labels change when locale switches to NL or ES'
  artifacts:
    - path: 'fmai-nextjs/src/app/api/contact/route.ts'
      provides: 'Contact form email delivery via Resend'
      contains: 'resend'
    - path: 'fmai-nextjs/src/app/layout.tsx'
      provides: 'Google Analytics script tag'
      contains: 'gtag'
    - path: 'fmai-nextjs/src/components/layout/HeaderClient.tsx'
      provides: 'Header nav with translation keys'
      contains: 'useTranslations'
  key_links:
    - from: 'fmai-nextjs/src/app/api/contact/route.ts'
      to: 'Resend API'
      via: 'resend.emails.send()'
      pattern: "resend\\.emails\\.send"
    - from: 'fmai-nextjs/src/app/layout.tsx'
      to: 'Google Analytics'
      via: 'gtag script'
      pattern: 'googletagmanager|gtag'
---

<objective>
Add contact form email delivery (Resend), website analytics (Google Analytics 4), and internationalize header navigation.

Purpose: Contact form currently logs submissions to console (lost on deploy). No analytics exist in the Next.js project. Header nav is hardcoded English. These are the final missing features for a production-ready website.
Output: Working contact email, GA4 tracking, and translated navigation.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@fmai-nextjs/src/app/api/contact/route.ts
@fmai-nextjs/src/components/layout/HeaderClient.tsx
@fmai-nextjs/src/app/layout.tsx
@fmai-nextjs/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Resend email delivery to contact form</name>
  <files>fmai-nextjs/package.json, fmai-nextjs/src/app/api/contact/route.ts</files>
  <action>
1. Install Resend: `cd fmai-nextjs && npm install resend`

2. Update `fmai-nextjs/src/app/api/contact/route.ts`:
   - Import `Resend` from 'resend'
   - Create Resend instance: `const resend = new Resend(process.env.RESEND_API_KEY)`
   - Replace the `console.log` block (lines ~86-95) with actual email sending:
     ```
     await resend.emails.send({
       from: 'FutureMarketingAI <onboarding@resend.dev>',  // Use verified domain in production
       to: process.env.CONTACT_EMAIL_TO || 'info@futuremarketingai.com',
       subject: `Contact Form: ${validated.subject}`,
       html: `<h2>New Contact Form Submission</h2>
              <p><strong>Name:</strong> ${validated.name}</p>
              <p><strong>Email:</strong> ${validated.email}</p>
              <p><strong>Company:</strong> ${validated.company || 'Not provided'}</p>
              <p><strong>Subject:</strong> ${validated.subject}</p>
              <p><strong>Message:</strong></p>
              <p>${validated.message}</p>`,
       replyTo: validated.email,
     })
     ```
   - Keep the existing rate limiting and validation logic unchanged
   - Add error handling: if Resend fails, log the error and still return success to user (fire-and-forget pattern) OR return 500 with a generic error message
   - Tighten CORS: change `Access-Control-Allow-Origin: '*'` to the production domain `https://futuremarketingai.com` (or use env var `NEXT_PUBLIC_SITE_URL`). For development, check the origin header and allow localhost too.

3. Add to `.env.example` (or `.env.local.example` if it exists): RESEND_API_KEY, CONTACT_EMAIL_TO
   </action>
   <verify>
   <automated>cd fmai-nextjs && npx tsc --noEmit 2>&1 | head -10 && grep -q "resend" src/app/api/contact/route.ts && echo "PASS" || echo "FAIL"</automated>
   </verify>
   <done>Contact form sends email via Resend API when submitted. CORS restricted to production domain. Env vars documented.</done>
   </task>

<task type="auto">
  <name>Task 2: Add Google Analytics 4 and internationalize header navigation</name>
  <files>fmai-nextjs/src/app/layout.tsx, fmai-nextjs/src/components/layout/HeaderClient.tsx, fmai-nextjs/messages/en.json, fmai-nextjs/messages/nl.json, fmai-nextjs/messages/es.json</files>
  <action>
**Google Analytics 4:**

1. Add GA4 script to `fmai-nextjs/src/app/layout.tsx` (or the root layout):
   - Use `next/script` with strategy="afterInteractive"
   - Load `https://www.googletagmanager.com/gtag/js?id=${process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID}`
   - Add inline gtag config script
   - Only render if NEXT_PUBLIC_GA_MEASUREMENT_ID env var is set (graceful degradation for dev)
   - No npm dependency needed -- GA4 loads via script tag

**Header Navigation i18n:**

2. Read HeaderClient.tsx to identify all hardcoded English strings in:
   - SKILL_CATEGORIES array (6 items with name + description)
   - NAV_ITEMS array
   - "Meet Clyde" / "Try your AI employee live" CTA text
   - Any other hardcoded labels

3. Add a `nav` namespace to all 3 locale files (en.json, nl.json, es.json) with keys for:
   - Each skill category name and description
   - Nav item labels
   - CTA text
   - Mobile menu labels

4. Update HeaderClient.tsx:
   - Add `const t = useTranslations('nav')` (it's already a client component so useTranslations is fine)
   - Replace all hardcoded strings with `t('key')` calls
   - Keep the SKILL_CATEGORIES array structure but populate labels from translations

Provide native-quality Dutch and Spanish translations (not machine-translated). Follow patterns from STATE.md decisions (e.g., "Clyde" stays "Clyde" in all languages, per [Phase 07-04] decision).
</action>
<verify>
<automated>cd fmai-nextjs && npx tsc --noEmit 2>&1 | head -10 && grep -q "useTranslations" src/components/layout/HeaderClient.tsx && grep -q "gtag" src/app/layout.tsx && echo "PASS" || echo "FAIL"</automated>
</verify>
<done>GA4 tracking fires on every page view when measurement ID is configured. Header navigation displays translated labels in NL and ES locales. All nav strings use translation keys.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify contact form, analytics, and navigation translations</name>
  <action>Human verifies the three features added in Tasks 1 and 2 work correctly in the browser.</action>
  <what-built>Contact form email delivery (Resend), Google Analytics 4 integration, and header navigation internationalization</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/contact and submit the form -- check that an email arrives at the configured CONTACT_EMAIL_TO address
    2. Open browser DevTools Network tab -- verify gtag.js loads (if GA_MEASUREMENT_ID is set)
    3. Switch locale to NL using the language switcher -- verify header navigation labels are in Dutch
    4. Switch locale to ES -- verify header navigation labels are in Spanish
    5. Open the Skills dropdown menu in each locale -- verify skill names and descriptions are translated
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Contact form sends real email (not just console.log)
- GA4 script loads on all pages
- Header nav is fully translated in EN/NL/ES
- `npm run build` succeeds without errors
</verification>

<success_criteria>
Contact form submissions trigger real email delivery. Page views are tracked via Google Analytics. Header navigation displays correct translations in all 3 supported locales.
</success_criteria>

<output>
After completion, create `.planning/phases/09-codebase-cleanup-dead-code-persona-fix-contact-form-analytics/09-03-SUMMARY.md`
</output>
