---
phase: 05-content-hub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/next.config.ts
  - fmai-nextjs/mdx-components.tsx
  - fmai-nextjs/content/blog/ai-marketing-automation-guide.mdx
  - fmai-nextjs/src/lib/blog.ts
  - fmai-nextjs/src/app/globals.css
  - fmai-nextjs/src/components/blog/BlogPostCard.tsx
  - fmai-nextjs/src/components/blog/CategoryFilter.tsx
  - fmai-nextjs/src/app/[locale]/(blog)/blog/page.tsx
  - fmai-nextjs/package.json
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02

must_haves:
  truths:
    - 'MDX files in content/blog/ are compiled by the build system'
    - 'Blog listing page at /en/blog renders server-side with post cards'
    - 'Category filter narrows displayed posts on the listing page'
    - 'Blog listing page is accessible under all 3 locale prefixes'
  artifacts:
    - path: 'fmai-nextjs/next.config.ts'
      provides: '@next/mdx plugin chained with next-intl'
      contains: 'createMDX'
    - path: 'fmai-nextjs/mdx-components.tsx'
      provides: 'Required MDX component overrides for App Router'
      exports: ['useMDXComponents']
    - path: 'fmai-nextjs/src/lib/blog.ts'
      provides: 'Blog utility functions for reading MDX metadata'
      exports: ['getAllPosts', 'getPostSlugs', 'BlogPostMeta']
    - path: 'fmai-nextjs/src/components/blog/CategoryFilter.tsx'
      provides: 'Client island for category filtering'
      contains: 'use client'
    - path: 'fmai-nextjs/src/app/[locale]/(blog)/blog/page.tsx'
      provides: 'Blog listing page server component'
      contains: 'getAllPosts'
  key_links:
    - from: 'fmai-nextjs/src/app/[locale]/(blog)/blog/page.tsx'
      to: 'fmai-nextjs/src/lib/blog.ts'
      via: 'getAllPosts import'
      pattern: 'import.*getAllPosts.*from.*lib/blog'
    - from: 'fmai-nextjs/next.config.ts'
      to: '@next/mdx'
      via: 'plugin chain'
      pattern: 'withNextIntl.*withMDX'
---

<objective>
Set up the complete MDX blog infrastructure and create the blog listing page with category filtering.

Purpose: Establish the content pipeline from MDX files to rendered blog pages, extending the existing Next.js project with @next/mdx, typography styling, and a server-rendered listing page.
Output: Working blog listing at /[locale]/blog that reads MDX file metadata and displays post cards with category filtering.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-hub/05-RESEARCH.md
@fmai-nextjs/next.config.ts
@fmai-nextjs/src/app/globals.css
@fmai-nextjs/src/lib/seo-config.ts
@fmai-nextjs/src/lib/metadata.ts
@fmai-nextjs/src/components/seo/JsonLd.tsx
@fmai-nextjs/src/app/sitemap.ts
@fmai-nextjs/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install MDX dependencies and configure build pipeline</name>
  <files>
    fmai-nextjs/package.json
    fmai-nextjs/next.config.ts
    fmai-nextjs/mdx-components.tsx
    fmai-nextjs/src/app/globals.css
  </files>
  <action>
    1. Install dependencies from the fmai-nextjs directory:
       ```
       npm install @next/mdx @mdx-js/loader @mdx-js/react @types/mdx @tailwindcss/typography remark-gfm rehype-slug gray-matter
       ```

    2. Update next.config.ts to chain @next/mdx with next-intl. Order matters -- next-intl must be outermost:
       ```typescript
       import createMDX from '@next/mdx'
       // ... existing imports ...
       const nextConfig: NextConfig = {
         pageExtensions: ['js', 'jsx', 'md', 'mdx', 'ts', 'tsx'],
         images: { remotePatterns: [] },
       }
       const withMDX = createMDX({
         options: {
           remarkPlugins: [],
           rehypePlugins: [],
         },
       })
       export default withNextIntl(withMDX(nextConfig))
       ```
       NOTE: Use empty plugin arrays initially. Remark-gfm and rehype-slug may need string-based references for Turbopack compatibility -- test during verification and add if they work. Do NOT add rehype-pretty-code yet (adds complexity, can be added later).

    3. Create mdx-components.tsx at fmai-nextjs/ root (same level as src/):
       ```typescript
       import type { MDXComponents } from 'mdx/types'
       export function useMDXComponents(): MDXComponents {
         return {}
       }
       ```
       This file is REQUIRED by @next/mdx in App Router even if empty.

    4. Add the typography plugin to globals.css. Insert `@plugin "@tailwindcss/typography";` right after the `@import 'tailwindcss';` line. Do NOT modify any existing @theme tokens.

  </action>
  <verify>
    Run `cd fmai-nextjs && npm run build` -- must complete without errors. The MDX plugin chain should not break existing pages.
  </verify>
  <done>
    @next/mdx is installed and configured, mdx-components.tsx exists, typography plugin loaded, build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create blog utilities, listing page, and category filter</name>
  <files>
    fmai-nextjs/content/blog/ai-marketing-automation-guide.mdx
    fmai-nextjs/src/lib/blog.ts
    fmai-nextjs/src/components/blog/BlogPostCard.tsx
    fmai-nextjs/src/components/blog/CategoryFilter.tsx
    fmai-nextjs/src/app/[locale]/(blog)/blog/page.tsx
  </files>
  <action>
    1. Create a placeholder seed MDX file at content/blog/ai-marketing-automation-guide.mdx with YAML frontmatter (NOT exported metadata -- gray-matter needs frontmatter for listing page scanning):
       ```
       ---
       title: "The Complete Guide to AI Marketing Automation in 2026"
       description: "How AI-powered marketing automation transforms B2B growth with chatbots, voice agents, and integrated marketing machines."
       author: "Daley Maat"
       publishedAt: "2026-03-18"
       updatedAt: "2026-03-18"
       category: "ai-marketing"
       tags: ["AI", "marketing automation", "B2B"]
       locale: "en"
       ---

       # The Complete Guide to AI Marketing Automation in 2026

       Placeholder content -- will be replaced with real article in Plan 02.
       ```

    2. Create src/lib/blog.ts with these exports:
       - `BlogPostMeta` interface: { slug, title, description, author, publishedAt, updatedAt, category, tags: string[], locale }
       - `BLOG_CATEGORIES`: array of { id: string, label: string } with 4 categories: ai-marketing, automation, chatbots, voice-agents
       - `getAllPosts(locale?: string)`: reads all .mdx files from content/blog/ using fs.readdirSync + gray-matter, returns BlogPostMeta[] sorted by publishedAt desc. Filter by locale if provided, default to 'en' posts.
       - `getPostSlugs()`: returns string[] of all slugs (for generateStaticParams)
       Use `path.join(process.cwd(), 'content', 'blog')` for the content directory. Handle empty directory gracefully.

    3. Create BlogPostCard.tsx as a server component (no "use client"):
       - Accepts BlogPostMeta as props
       - Renders a card with: title (h3), description (truncated), category badge, date (formatted), "Read more" link to /blog/{slug}
       - Use existing project Tailwind tokens: bg-bg-elevated, text-text-primary, text-text-secondary, border-border-primary
       - Use next/link for the card link with locale-aware href

    4. Create CategoryFilter.tsx as a "use client" island:
       - Accepts categories (from BLOG_CATEGORIES), activeCategory (string | null), and locale
       - Renders horizontal button row for each category + "All" button
       - Uses URL search params for filtering (router.push with ?category=X) so it works with server-rendered listing
       - Style with accent-system color for active state

    5. Create the blog listing page at src/app/[locale]/(blog)/blog/page.tsx:
       - Server component that calls getAllPosts()
       - Reads searchParams.category to filter posts
       - Renders: page title (h1 "Blog"), CategoryFilter (client island), grid of BlogPostCard components
       - Add generateMetadata using the existing generatePageMetadata helper pattern -- create blog namespace translation keys or use hardcoded metadata (English-only is fine for v1, matching Phase 4 FAQ decision)
       - Add `export const revalidate = 3600` for ISR
       - Grid layout: 1 col mobile, 2 cols md, 3 cols lg
       - Handle empty state: "No posts found" message when no posts match filter

    NOTE: Blog UI chrome (title, filter labels, empty state) can use hardcoded English strings for v1. i18n for blog chrome is a nice-to-have, not required.

  </action>
  <verify>
    Run `cd fmai-nextjs && npm run build` -- must pass. Then run `npm run dev` and visit http://localhost:3000/en/blog -- should render the listing page with the seed article card. Click a category filter -- URL should update with ?category=X parameter.
  </verify>
  <done>
    Blog listing page renders at /en/blog with the seed article card visible, category filter toggles work via URL params, and the page is server-rendered (check page source for content in HTML).
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- /en/blog renders server-side with seed article card
- Category filter updates URL and filters posts
- /nl/blog and /es/blog also render (locale routing works for blog routes)
- MDX compilation works (no build errors related to .mdx files)
- Page source contains blog content (SSR verified)
</verification>

<success_criteria>
Blog infrastructure is operational: @next/mdx compiles MDX files, the listing page reads metadata via gray-matter, category filtering works, and the page is server-rendered under all locale prefixes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-hub/05-01-SUMMARY.md`
</output>
