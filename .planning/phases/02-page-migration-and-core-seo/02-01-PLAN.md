---
phase: 02-page-migration-and-core-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/src/lib/metadata.ts
  - fmai-nextjs/src/lib/seo-config.ts
  - fmai-nextjs/src/components/seo/JsonLd.tsx
  - fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx
  - fmai-nextjs/src/components/seo/WebSiteJsonLd.tsx
  - fmai-nextjs/src/components/seo/WebPageJsonLd.tsx
  - fmai-nextjs/src/components/seo/ServiceJsonLd.tsx
  - fmai-nextjs/src/components/seo/BreadcrumbJsonLd.tsx
  - fmai-nextjs/src/app/sitemap.ts
  - fmai-nextjs/src/app/robots.ts
  - fmai-nextjs/src/app/[locale]/not-found.tsx
  - fmai-nextjs/src/app/[locale]/error.tsx
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/es.json
autonomous: true
requirements:
  - SEO-02
  - SEO-04
  - SEO-05
  - SEO-06
  - SEO-10
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - SCHEMA-05
  - SCHEMA-08

must_haves:
  truths:
    - 'Every page can generate localized metadata (title, description, OG) by calling generatePageMetadata'
    - 'JSON-LD renders inline as script tags in SSR HTML, not via client hydration'
    - 'sitemap.xml is accessible at /sitemap.xml with all 10 pages x 3 locales (30 entries)'
    - 'robots.txt allows search engines and AI retrieval bots, blocks AI training bots'
    - 'Visiting a non-existent URL returns HTTP 404 with localized error content'
    - 'All JSON-LD schemas include dateModified for content freshness'
  artifacts:
    - path: 'fmai-nextjs/src/lib/metadata.ts'
      provides: 'Shared generatePageMetadata function with hreflang alternates and OG tags'
      exports: ['generatePageMetadata']
    - path: 'fmai-nextjs/src/lib/seo-config.ts'
      provides: 'Site URL, org name, social profiles, PAGE_DATES config'
      exports: ['SITE_URL', 'SITE_NAME', 'PAGE_DATES']
    - path: 'fmai-nextjs/src/components/seo/JsonLd.tsx'
      provides: 'Generic JSON-LD renderer with XSS sanitization'
      exports: ['JsonLd']
    - path: 'fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx'
      provides: 'Organization schema for root layout'
      exports: ['OrganizationJsonLd']
    - path: 'fmai-nextjs/src/components/seo/BreadcrumbJsonLd.tsx'
      provides: 'BreadcrumbList schema from route segments'
      exports: ['BreadcrumbJsonLd']
    - path: 'fmai-nextjs/src/app/sitemap.ts'
      provides: 'XML sitemap with locale alternates'
    - path: 'fmai-nextjs/src/app/robots.ts'
      provides: 'robots.txt with AI crawler policy'
    - path: 'fmai-nextjs/src/app/[locale]/not-found.tsx'
      provides: 'Localized 404 page'
  key_links:
    - from: 'fmai-nextjs/src/lib/metadata.ts'
      to: 'next-intl/server'
      via: 'getTranslations for localized meta'
      pattern: 'getTranslations.*locale.*namespace'
    - from: 'fmai-nextjs/src/components/seo/JsonLd.tsx'
      to: 'schema-dts'
      via: 'TypeScript types for JSON-LD'
      pattern: 'import.*schema-dts'
    - from: 'fmai-nextjs/src/components/seo/ServiceJsonLd.tsx'
      to: 'fmai-nextjs/src/lib/seo-config.ts'
      via: 'PAGE_DATES for dateModified'
      pattern: 'PAGE_DATES'
---

<objective>
Build the SEO infrastructure layer: shared metadata generator, 6 JSON-LD schema components, XML sitemap, robots.txt, and error pages.

Purpose: Every page migrated in Plans 02-04 will call these utilities. Building infrastructure first prevents duplication and ensures consistency across all 10 pages.
Output: Reusable SEO components and utilities that Plans 02-04 consume.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-migration-and-core-seo/02-RESEARCH.md
@.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SEO config, shared metadata generator, and all JSON-LD components</name>
  <files>
    fmai-nextjs/src/lib/seo-config.ts
    fmai-nextjs/src/lib/metadata.ts
    fmai-nextjs/src/components/seo/JsonLd.tsx
    fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx
    fmai-nextjs/src/components/seo/WebSiteJsonLd.tsx
    fmai-nextjs/src/components/seo/WebPageJsonLd.tsx
    fmai-nextjs/src/components/seo/ServiceJsonLd.tsx
    fmai-nextjs/src/components/seo/BreadcrumbJsonLd.tsx
    fmai-nextjs/src/app/[locale]/layout.tsx
  </files>
  <action>
    Create `lib/seo-config.ts` with constants:
    - SITE_URL = 'https://futuremarketingai.com'
    - SITE_NAME = 'Future Marketing AI'
    - ORG_EMAIL = 'info@futuremarketingai.com'
    - LINKEDIN_URL (Future Marketing AI LinkedIn)
    - PAGE_DATES: Record<string, string> mapping paths to ISO 8601 dates (all initially set to '2026-03-18T00:00:00Z')

    Update `lib/metadata.ts` (already exists from Phase 1 with generatePageMetadata). Verify it includes:
    - Localized title via getTranslations({locale, namespace}) with `meta.title` and `meta.description` keys
    - `alternates.canonical` with locale prefix
    - `alternates.languages` with all 3 locales + x-default pointing to /en/
    - OpenGraph with locale-specific `locale` field (en_US, nl_NL, es_ES)
    - Twitter card: summary_large_image
    If the existing utility is missing any of the above, add them. Import SITE_URL and SITE_NAME from seo-config.ts.

    Create `components/seo/JsonLd.tsx`: Generic JSON-LD renderer using schema-dts types. Use `dangerouslySetInnerHTML` with `JSON.stringify(data).replace(/</g, '\\u003c')` for XSS protection. This is a Server Component (no "use client").

    Create `components/seo/OrganizationJsonLd.tsx`: Renders Organization schema with name, url, logo, description, sameAs (LinkedIn), contactPoint. Import from seo-config.ts.

    Create `components/seo/WebSiteJsonLd.tsx`: Renders WebSite schema with name, url, potentialAction.SearchAction. Homepage-only component.

    Create `components/seo/WebPageJsonLd.tsx`: Accepts name, description, path, locale, optional dateModified. Renders WebPage schema with url, name, description, dateModified (from PAGE_DATES if not provided), isPartOf referencing the WebSite.

    Create `components/seo/ServiceJsonLd.tsx`: Accepts name, description, serviceType, locale, path. Renders Service schema with provider (Organization), areaServed (Worldwide), dateModified from PAGE_DATES.

    Create `components/seo/BreadcrumbJsonLd.tsx`: Accepts items array [{name, path}] and locale. Renders BreadcrumbList with itemListElement positions.

    All JSON-LD components are Server Components. All import JsonLd from ./JsonLd. All use schema-dts types (WithContext<Organization>, WithContext<WebSite>, etc.).

    Update `app/[locale]/layout.tsx` to render OrganizationJsonLd inside the body (renders on ALL pages). This satisfies SCHEMA-01.

    Reference the research code examples directly -- they are verified patterns from Next.js 16 official docs.

  </action>
  <verify>
    Run `npm run build` in fmai-nextjs/ -- should pass with no errors.
    Verify all 8 new files exist on disk.
    Verify OrganizationJsonLd is rendered in layout.tsx.
    Run `npm run dev` and check page source at localhost for `<script type="application/ld+json">` containing Organization schema.
  </verify>
  <done>
    generatePageMetadata produces localized metadata with hreflang alternates. 6 JSON-LD components render type-safe structured data as SSR script tags. Organization JSON-LD appears on every page via layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sitemap.ts, robots.ts, not-found.tsx, and error.tsx</name>
  <files>
    fmai-nextjs/src/app/sitemap.ts
    fmai-nextjs/src/app/robots.ts
    fmai-nextjs/src/app/[locale]/not-found.tsx
    fmai-nextjs/src/app/[locale]/error.tsx
    fmai-nextjs/messages/en.json
    fmai-nextjs/messages/nl.json
    fmai-nextjs/messages/es.json
  </files>
  <action>
    Create `app/sitemap.ts` (OUTSIDE [locale] segment -- this is critical, see Pitfall 2 in research):
    - Import MetadataRoute from 'next'
    - Define all 10 page paths: /, /automations, /chatbots, /voice-agents, /marketing-machine, /about, /pricing, /how-it-works, /contact, /legal
    - For each page, generate entry with url (en version), lastModified (new Date()), changeFrequency (weekly for homepage, monthly for others), priority (1 for homepage, 0.8 for services, 0.6 for others)
    - Each entry has alternates.languages with all 3 locale URLs
    - Import SITE_URL from seo-config.ts

    Create `app/robots.ts` (OUTSIDE [locale] segment):
    - Default rule: allow /, disallow /api/ and /_next/
    - AI retrieval bots (ChatGPT-User, Claude-SearchBot, OAI-SearchBot, PerplexityBot): allow /
    - AI training bots (GPTBot, ClaudeBot, CCBot, Google-Extended, anthropic-ai): disallow /
    - Reference sitemap URL

    Create `app/[locale]/not-found.tsx`:
    - Use useTranslations('errors') for localized content
    - Render semantic HTML with h1 "404", message paragraph, and Link back to home
    - Import Link from @/i18n/navigation
    - Style with Tailwind: centered, min-h-[60vh], uses design system colors (text-accent-system for 404, text-text-secondary for message)

    Create `app/[locale]/error.tsx`:
    - Mark as "use client" (required by Next.js for error boundaries)
    - Accept error and reset props
    - Render "Something went wrong" message with a "Try again" button calling reset()
    - Style consistently with not-found.tsx

    Add translation keys to all 3 locale files under "errors" namespace:
    - errors.notFound.title: "Page Not Found" / "Pagina niet gevonden" / "Pagina no encontrada"
    - errors.notFound.message: "The page you're looking for doesn't exist." (localized)
    - errors.notFound.backHome: "Back to Home" (localized)
    - errors.generic.title: "Something went wrong" (localized)
    - errors.generic.tryAgain: "Try again" (localized)

  </action>
  <verify>
    Run `npm run build` -- should pass.
    Verify sitemap.ts is at app/sitemap.ts (NOT inside [locale]).
    Verify robots.ts is at app/robots.ts (NOT inside [locale]).
    Run `npm run dev` and:
    - Visit /sitemap.xml -- should show XML with 10 page entries, each with 3 locale alternates (30 total URLs)
    - Visit /robots.txt -- should show allow/disallow rules with AI bot distinctions
    - Visit /en/nonexistent-page -- should show 404 content (not default Next.js 404)
  </verify>
  <done>
    sitemap.xml lists all 30 locale-page combinations. robots.txt distinguishes retrieval vs training AI bots. Custom 404 and error pages render localized content with proper HTTP status codes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- Page source at any page contains Organization JSON-LD script tag
- /sitemap.xml returns valid XML with 10 pages x 3 locales
- /robots.txt contains separate rules for AI retrieval vs training bots
- /en/nonexistent returns HTTP 404 with localized content
- All JSON-LD components are Server Components (no "use client" directive)
</verification>

<success_criteria>
SEO infrastructure is complete: metadata generator, 6 JSON-LD components, sitemap, robots.txt, and error pages are all functional and ready for consumption by page migration plans (02-03 and 02-04).
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-migration-and-core-seo/02-01-SUMMARY.md`
</output>
