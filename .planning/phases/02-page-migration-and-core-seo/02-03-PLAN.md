---
phase: 02-page-migration-and-core-seo
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - fmai-nextjs/src/app/[locale]/page.tsx
  - fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx
  - fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx
  - fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx
  - fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/es.json
autonomous: true
requirements:
  - SCHEMA-02
  - SCHEMA-04
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-11

must_haves:
  truths:
    - 'Homepage renders server-side with service cards, hero section, and WebSite JSON-LD'
    - 'All 4 service pages render with Service JSON-LD, BreadcrumbList, and WebPage schemas'
    - 'Every page has unique, localized generateMetadata with correct hreflang alternates'
    - 'Page content is structured with semantic HTML: one h1, h2 sections, aria-labelledby'
    - 'All pages use setRequestLocale for static generation compatibility'
    - 'Meta titles and descriptions are SEO-optimized (60 char title, 155 char description)'
  artifacts:
    - path: 'fmai-nextjs/src/app/[locale]/page.tsx'
      provides: 'Homepage with WebSite JSON-LD, service cards, hero'
      contains: 'WebSiteJsonLd'
    - path: 'fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx'
      provides: 'Automations service page with Service JSON-LD'
      contains: 'ServiceJsonLd'
    - path: 'fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx'
      provides: 'Chatbots service page with Service JSON-LD'
      contains: 'ServiceJsonLd'
    - path: 'fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx'
      provides: 'Voice Agents service page with Service JSON-LD'
      contains: 'ServiceJsonLd'
    - path: 'fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx'
      provides: 'Marketing Machine service page with Service JSON-LD'
      contains: 'ServiceJsonLd'
  key_links:
    - from: 'fmai-nextjs/src/app/[locale]/page.tsx'
      to: 'fmai-nextjs/src/components/seo/WebSiteJsonLd.tsx'
      via: 'WebSite schema on homepage only'
      pattern: '<WebSiteJsonLd'
    - from: 'fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx'
      to: 'fmai-nextjs/src/lib/metadata.ts'
      via: 'generatePageMetadata in generateMetadata export'
      pattern: 'generatePageMetadata'
    - from: 'fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx'
      to: 'fmai-nextjs/src/components/seo/ServiceJsonLd.tsx'
      via: 'Service schema per service page'
      pattern: '<ServiceJsonLd'
---

<objective>
Migrate the Homepage and 4 service pages (Automations, Chatbots, Voice Agents, Marketing Machine) from the Vite codebase to Next.js Server Components with full SEO metadata and JSON-LD structured data.

Purpose: These 5 pages are the core marketing pages. They need per-page localized metadata, Service JSON-LD, BreadcrumbList, and semantic HTML. Interactive elements (animations, orbit visual, demo playground) are deferred to Phase 3 as client islands.
Output: 5 fully server-rendered pages with structured data, consuming the SEO infra from Plan 01 and UI components from Plan 02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-migration-and-core-seo/02-RESEARCH.md
@.planning/phases/02-page-migration-and-core-seo/02-01-SUMMARY.md
@.planning/phases/02-page-migration-and-core-seo/02-02-SUMMARY.md

Reference files in Vite codebase for migration:
@src/pages/LandingPage.tsx
@src/pages/AutomationsPage.tsx
@src/pages/ChatbotsPage.tsx
@src/pages/VoiceAgentsPage.tsx
@src/pages/MarketingMachinePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Homepage with WebSite JSON-LD and service cards</name>
  <files>
    fmai-nextjs/src/app/[locale]/page.tsx
    fmai-nextjs/messages/en.json
    fmai-nextjs/messages/nl.json
    fmai-nextjs/messages/es.json
  </files>
  <action>
    Replace the existing homepage (which is a Phase 1 stub) with the full migrated content from LandingPage.tsx.

    Page structure (all Server Component, no "use client"):
    1. `generateMetadata` export calling generatePageMetadata({locale, namespace: 'home', path: '/'})
    2. `generateStaticParams` returning all 3 locales
    3. Call `setRequestLocale(locale)` at the top of the component
    4. Load translations: `const t = await getTranslations('home')`

    JSON-LD (rendered at top of `<main>`):
    - WebSiteJsonLd (homepage-only, includes SearchAction)
    - WebPageJsonLd with name, description, path='/', locale
    - BreadcrumbJsonLd with single item [{name: 'Home', path: '/'}]

    Content sections (semantic HTML, using PageShell wrapper):
    - Hero section: `<section aria-labelledby="hero">` with h1 (main headline), subtitle paragraph, primary CTA button
    - Services overview: `<section aria-labelledby="services">` with h2, 4 GlassCards linking to each service page (Automations, Chatbots, Voice Agents, Marketing Machine). Each card has icon placeholder (div with CSS), title, short description, and link.
    - Social proof / trust: `<section aria-labelledby="trust">` with h2, trust indicators (text-based, no complex visuals)
    - Final CTA: `<section aria-labelledby="cta">` with h2, compelling copy, CTAButton

    Interactive elements DEFERRED to Phase 3: orbit visual, gradient mesh background, scroll reveal animations, animated counters. Render static placeholders or omit entirely.

    Use next/image for any images. Use GlassCard, CTAButton, SectionHeading from Plan 02.

    SEO copy (PAGE-11): Update meta.title and meta.description keys in all 3 locale translation files for the 'home' namespace:
    - EN: title "AI Automation, Chatbots & Voice Agents for Growth Teams" (under 60 chars), description ~155 chars focusing on core value prop
    - NL: Localized equivalent
    - ES: Localized equivalent

  </action>
  <verify>
    Run `npm run build` -- should pass and generate static pages for all 3 locales.
    Run `npm run dev` and verify:
    - /en/ renders full homepage with hero, service cards, trust section, CTA
    - /nl/ and /es/ render with localized content
    - View page source: contains WebSite JSON-LD with SearchAction, WebPage JSON-LD, BreadcrumbList JSON-LD
    - Page has exactly one h1, sections use h2, all with aria-labelledby
  </verify>
  <done>
    Homepage renders server-side with all content sections, WebSite + WebPage + Breadcrumb JSON-LD, localized metadata, and semantic HTML. All 3 locales work.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate 4 service pages with Service JSON-LD and SEO copy</name>
  <files>
    fmai-nextjs/src/app/[locale]/(services)/automations/page.tsx
    fmai-nextjs/src/app/[locale]/(services)/chatbots/page.tsx
    fmai-nextjs/src/app/[locale]/(services)/voice-agents/page.tsx
    fmai-nextjs/src/app/[locale]/(services)/marketing-machine/page.tsx
    fmai-nextjs/messages/en.json
    fmai-nextjs/messages/nl.json
    fmai-nextjs/messages/es.json
  </files>
  <action>
    Create route group `(services)` directory under [locale] (parenthesized = invisible in URL).

    Each service page follows the SAME pattern (reference Pattern 3 from research):

    ```
    generateMetadata → generatePageMetadata({locale, namespace, path})
    generateStaticParams → [{locale: 'en'}, {locale: 'nl'}, {locale: 'es'}]
    setRequestLocale(locale) at top
    getTranslations(namespace) for content
    ```

    **Automations page** (`/automations`, namespace: 'automations', ~454 lines in Vite):
    - ServiceJsonLd: serviceType "AI Marketing Automation"
    - BreadcrumbJsonLd: Home > Automations
    - WebPageJsonLd: with dateModified
    - Sections: Hero (h1), pain points/challenges (h2), automation solutions grid (h2 with cards), process steps (h2), CTA
    - Reference src/pages/AutomationsPage.tsx for content structure
    - Strip all framer-motion, useState, useEffect -- render static content only

    **Chatbots page** (`/chatbots`, namespace: 'chatbots'):
    - ServiceJsonLd: serviceType "AI Chatbot Solutions"
    - BreadcrumbJsonLd: Home > Chatbots
    - WebPageJsonLd: with dateModified
    - Sections: Hero (h1), features/capabilities (h2), demo placeholder (h2 -- actual playground deferred to Phase 3), use cases (h2), CTA
    - Demo playground section: render a static placeholder with text like "Interactive demo coming soon" or just the section heading. Phase 3 will add the interactive persona playground.

    **Voice Agents page** (`/voice-agents`, namespace: 'voice-agents'):
    - ServiceJsonLd: serviceType "AI Voice Agent Solutions"
    - BreadcrumbJsonLd: Home > Voice Agents
    - WebPageJsonLd: with dateModified
    - Sections: Hero (h1), capabilities (h2), use cases (h2), integration info (h2), CTA

    **Marketing Machine page** (`/marketing-machine`, namespace: 'marketing-machine'):
    - ServiceJsonLd: serviceType "AI Marketing Platform"
    - BreadcrumbJsonLd: Home > Marketing Machine
    - WebPageJsonLd: with dateModified
    - Sections: Hero (h1), feature showcase (h2), how it works overview (h2), CTA

    For ALL 4 pages:
    - Use GlassCard for feature/capability cards
    - Use CTAButton for call-to-action buttons
    - Use SectionHeading for section headers (ensures consistent h2 + aria-labelledby)
    - Use PageShell as the outer wrapper
    - Migrate text content from translation files (keys already exist from Phase 1 migration)
    - All are Server Components -- no "use client"
    - Use next/image (with explicit width/height) for any images. If the Vite page uses ProductMedia, replace with next/image.

    SEO copy (PAGE-11): Add/update meta.title and meta.description for each service namespace in all 3 locale files:
    - Automations: "AI Marketing Automation | Future Marketing AI" / description about workflow automation
    - Chatbots: "AI Chatbot Solutions | Future Marketing AI" / description about conversational AI
    - Voice Agents: "AI Voice Agents | Future Marketing AI" / description about voice automation
    - Marketing Machine: "The Marketing Machine | Future Marketing AI" / description about the platform
    - All within SEO limits: title <60 chars, description ~155 chars, primary keyword first

  </action>
  <verify>
    Run `npm run build` -- should pass and generate static pages for all service pages x 3 locales (12 pages).
    Run `npm run dev` and verify each page:
    - /en/automations, /en/chatbots, /en/voice-agents, /en/marketing-machine all render content
    - /nl/ and /es/ variants render with localized content
    - View page source for each: contains Service JSON-LD with correct serviceType, BreadcrumbList JSON-LD, WebPage JSON-LD with dateModified
    - Each page has exactly one h1, sections use h2 with aria-labelledby
    - URLs do NOT contain "(services)" -- route group is invisible
  </verify>
  <done>
    4 service pages render server-side with Service + BreadcrumbList + WebPage JSON-LD, localized metadata, semantic HTML, and SEO-optimized meta tags. All 3 locales work. Interactive features are deferred placeholder-only.
  </done>
</task>

</tasks>

<verification>
- `npm run build` generates static pages for homepage + 4 service pages in all 3 locales (15 pages total)
- Each page source contains correct JSON-LD (WebSite on homepage only, Service on service pages, BreadcrumbList on all, WebPage on all)
- Each page has unique localized metadata (different title/description per page)
- Semantic HTML: one h1 per page, h2 sections with aria-labelledby, proper heading hierarchy
- /en/automations URL works (route group invisible), /en/(services)/automations does NOT work
- All pages use setRequestLocale for SSG compatibility
</verification>

<success_criteria>
Homepage and 4 service pages are fully migrated with server-side rendering, structured data, localized metadata, and semantic HTML. Pages consume SEO infrastructure from Plan 01 and UI components from Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-migration-and-core-seo/02-03-SUMMARY.md`
</output>
