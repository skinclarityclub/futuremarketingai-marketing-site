---
phase: 02-page-migration-and-core-seo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/src/components/layout/Header.tsx
  - fmai-nextjs/src/components/layout/HeaderClient.tsx
  - fmai-nextjs/src/components/layout/Footer.tsx
  - fmai-nextjs/src/components/layout/PageShell.tsx
  - fmai-nextjs/src/components/ui/GlassCard.tsx
  - fmai-nextjs/src/components/ui/CTAButton.tsx
  - fmai-nextjs/src/components/ui/SectionHeading.tsx
  - fmai-nextjs/src/app/[locale]/layout.tsx
autonomous: true
requirements:
  - SEO-07
  - SEO-08

must_haves:
  truths:
    - 'Header renders as semantic <header> with <nav> containing all page links'
    - 'Footer renders as semantic <footer> with navigation, social links, and legal links'
    - 'Every page body is wrapped in semantic <main> via PageShell'
    - 'GlassCard renders as a reusable card component matching the Vite design'
    - 'CTAButton supports primary/secondary variants and renders as <a> or <button>'
    - 'All content sections use <section> with aria-labelledby'
  artifacts:
    - path: 'fmai-nextjs/src/components/layout/Header.tsx'
      provides: 'Server Component header with semantic nav'
      exports: ['Header']
    - path: 'fmai-nextjs/src/components/layout/Footer.tsx'
      provides: 'Server Component footer with semantic footer element'
      exports: ['Footer']
    - path: 'fmai-nextjs/src/components/layout/PageShell.tsx'
      provides: 'Shared page wrapper with semantic main and section structure'
      exports: ['PageShell']
    - path: 'fmai-nextjs/src/components/ui/GlassCard.tsx'
      provides: 'Reusable card component with glass morphism styling'
      exports: ['GlassCard']
    - path: 'fmai-nextjs/src/components/ui/CTAButton.tsx'
      provides: 'CTA button with primary/secondary/ghost variants'
      exports: ['CTAButton']
  key_links:
    - from: 'fmai-nextjs/src/components/layout/Header.tsx'
      to: '@/i18n/navigation'
      via: 'Locale-aware Link for navigation'
      pattern: 'import.*Link.*navigation'
    - from: 'fmai-nextjs/src/app/[locale]/layout.tsx'
      to: 'fmai-nextjs/src/components/layout/Header.tsx'
      via: 'Header rendered in layout'
      pattern: '<Header'
    - from: 'fmai-nextjs/src/app/[locale]/layout.tsx'
      to: 'fmai-nextjs/src/components/layout/Footer.tsx'
      via: 'Footer rendered in layout'
      pattern: '<Footer'
---

<objective>
Migrate shared UI components (Header, Footer, PageShell, GlassCard, CTAButton) from the Vite codebase to Next.js Server Components with semantic HTML.

Purpose: These layout and UI components are used across all 10 pages. Migrating them first prevents duplication in page migration plans. Semantic HTML (nav, header, footer, main, section) satisfies SEO-07.
Output: Reusable layout and UI components integrated into the app layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-migration-and-core-seo/02-RESEARCH.md
@.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-infrastructure/01-02-SUMMARY.md

Reference files in Vite codebase for migration:
@src/components/common/FloatingNav.tsx
@src/components/common/Footer.tsx
@src/components/common/GlassCard.tsx
@src/components/common/CTAButton.tsx
@src/components/common/AppLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Header, Footer, and PageShell as Server Components with semantic HTML</name>
  <files>
    fmai-nextjs/src/components/layout/Header.tsx
    fmai-nextjs/src/components/layout/HeaderClient.tsx
    fmai-nextjs/src/components/layout/Footer.tsx
    fmai-nextjs/src/components/layout/PageShell.tsx
    fmai-nextjs/src/app/[locale]/layout.tsx
  </files>
  <action>
    Create `components/layout/Header.tsx` as a Server Component:
    - Semantic structure: `<header>` wrapping `<nav aria-label="Main navigation">`
    - Render the FMai logo/brand linking to / (using Link from @/i18n/navigation)
    - Navigation links to all main pages: Automations, Chatbots, Voice Agents, Marketing Machine, Pricing, About, Contact
    - Use getTranslations('common') for localized nav labels (keys already exist in translation files under common.nav.*)
    - Desktop layout: horizontal nav with flex, logo left, links center/right
    - Style with Tailwind: fixed top, glass-effect background (bg-bg-primary/80 backdrop-blur), z-50
    - Add floating style: top-4 left-4 right-4 rounded-2xl (per UI/UX skill guidelines)
    - Do NOT add mobile hamburger menu or hover animations -- those are Phase 3 interactive features
    - For language switcher and mobile menu toggle: create a minimal `HeaderClient.tsx` "use client" island that renders a language switcher dropdown. Import it into Header.tsx.

    Create `components/layout/HeaderClient.tsx` as a "use client" component:
    - Language switcher using usePathname and useRouter from @/i18n/navigation
    - Renders 3 locale buttons (EN/NL/ES) that switch locale while preserving current path
    - Keep minimal -- just the interactive parts that need client hooks

    Create `components/layout/Footer.tsx` as a Server Component:
    - Semantic structure: `<footer>` with internal `<nav aria-label="Footer navigation">`
    - 3-column layout: Company links (About, How It Works, Contact), Services (Automations, Chatbots, Voice Agents, Marketing Machine), Legal (Legal page, Privacy)
    - Copyright line with current year
    - Use getTranslations('common') for footer labels
    - Links use Link from @/i18n/navigation
    - Style: bg-bg-secondary, border-t border-border-primary, py-16

    Create `components/layout/PageShell.tsx` as a Server Component:
    - Accepts children, optional className
    - Wraps content in `<main className="min-h-screen pt-24">` (pt-24 accounts for fixed header)
    - Provides consistent page padding: px-4 sm:px-6 lg:px-8, max-w-7xl mx-auto

    Update `app/[locale]/layout.tsx`:
    - Import and render Header above {children}
    - Import and render Footer below {children}
    - Keep existing Providers wrapper and OrganizationJsonLd (from Plan 01)
    - The layout now provides: Providers > OrganizationJsonLd > Header > {children} > Footer

  </action>
  <verify>
    Run `npm run build` -- should pass.
    Run `npm run dev` and verify:
    - Header renders at top of page with navigation links
    - Footer renders at bottom with 3-column link layout
    - Language switcher changes locale (URL updates from /en/ to /nl/)
    - View page source: confirm `<header>`, `<nav>`, `<footer>` semantic elements present
    - Header has `<nav aria-label="Main navigation">`
  </verify>
  <done>
    Header with semantic nav, Footer with semantic footer, and PageShell wrapper are integrated into the layout. All pages automatically get consistent navigation and footer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate GlassCard, CTAButton, and SectionHeading UI components</name>
  <files>
    fmai-nextjs/src/components/ui/GlassCard.tsx
    fmai-nextjs/src/components/ui/CTAButton.tsx
    fmai-nextjs/src/components/ui/SectionHeading.tsx
  </files>
  <action>
    Create `components/ui/GlassCard.tsx` as a Server Component:
    - Accepts children, className (optional), as (optional, defaults to 'div')
    - Renders a glass-morphism card: bg-bg-surface/50 backdrop-blur-sm border border-border-primary rounded-2xl p-6
    - Supports hover state via Tailwind: hover:border-accent-primary/30 transition-colors
    - Add cursor-pointer class per UI/UX skill guidelines when onClick or href is implied
    - Reference src/components/common/GlassCard.tsx from Vite codebase for exact styling

    Create `components/ui/CTAButton.tsx` as a Server Component:
    - Accepts: children, variant ('primary' | 'secondary' | 'ghost'), href (optional), className (optional), size ('sm' | 'md' | 'lg')
    - If href provided: render as Link from @/i18n/navigation (internal) or <a> (external)
    - If no href: render as <button>
    - Primary variant: bg-gradient-to-r from-accent-primary to-accent-secondary text-white
    - Secondary variant: border border-accent-primary text-accent-primary bg-transparent
    - Ghost variant: text-text-secondary hover:text-text-primary
    - Sizes: sm (px-4 py-2 text-sm), md (px-6 py-3 text-base), lg (px-8 py-4 text-lg)
    - All variants: rounded-xl font-semibold transition-all duration-200
    - Reference src/components/common/CTAButton.tsx from Vite codebase

    Create `components/ui/SectionHeading.tsx` as a Server Component:
    - Accepts: title (string), subtitle (optional string), id (string for aria-labelledby), align ('left' | 'center', default 'center')
    - Renders: h2 with the id, optional p below for subtitle
    - This component enforces consistent heading hierarchy (h2 for sections) across all pages
    - Style: text-3xl font-bold text-text-primary for h2, text-lg text-text-secondary for subtitle

    All 3 components are Server Components (no "use client"). They render static markup. Interactive variants (hover animations, click handlers) will be added in Phase 3 as client island wrappers.

    Use next/image (import Image from 'next/image') anywhere images appear in these components. Add explicit width/height or use fill with sized parent. This satisfies SEO-08 for shared component images.

  </action>
  <verify>
    Run `npm run build` -- should pass.
    Verify all 3 component files exist.
    Verify no "use client" directive in GlassCard, CTAButton, or SectionHeading.
    Import and render a test instance on the homepage to confirm they render correctly (can remove after verification).
  </verify>
  <done>
    GlassCard, CTAButton, and SectionHeading are available as Server Components with consistent styling matching the Vite design system. Ready for use in all page migrations.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- Page source contains semantic HTML: `<header>`, `<nav>`, `<footer>`, `<main>`
- Header navigation links work across all locales
- Language switcher changes locale while preserving path
- Footer renders with 3-column layout and all navigation links
- GlassCard, CTAButton, SectionHeading render without "use client"
</verification>

<success_criteria>
All shared layout and UI components are migrated with semantic HTML. The app layout renders Header and Footer on every page. Reusable card, button, and heading components are ready for page migration plans.
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-migration-and-core-seo/02-02-SUMMARY.md`
</output>
