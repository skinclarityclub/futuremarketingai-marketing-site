---
phase: 12-brand-copy-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/src/components/layout/HeaderClient.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx
  - fmai-nextjs/src/components/pricing/SkillsTierMatrix.tsx
  - fmai-nextjs/src/components/chatbot/ChatWidget.tsx
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/es.json
autonomous: true
requirements:
  - WEB-01

must_haves:
  truths:
    - 'HeaderClient.tsx SKILL_CATEGORIES renders via useTranslations("header.skills.*"), no hardcoded EN labels'
    - 'HeaderClient.tsx NAV_ITEMS renders via useTranslations("header.nav.*")'
    - 'pricing/page.tsx:127 "Most popular" replaced with t("pricing.tiers.professional.mostPopular")'
    - 'SkillsTierMatrix.tsx:85 "Coming soon" replaced with t("common.comingSoon")'
    - 'ChatWidget.tsx placeholders + limit messages use t()'
    - 'Flagging task 7 created: VoiceDemoSection phone number flagged for Daley (no code change in this plan)'
  artifacts:
    - path: 'fmai-nextjs/messages/{nl,en,es}.json'
      provides: 'New header.* namespace + common.comingSoon + chat.placeholder keys'
    - path: 'fmai-nextjs/src/components/layout/HeaderClient.tsx'
      provides: 'Fully localized header in NL/EN/ES'
  key_links:
    - from: 'fmai-nextjs/src/components/layout/HeaderClient.tsx'
      to: 'fmai-nextjs/messages/nl.json'
      via: 'header.skills.create, header.skills.engage, header.skills.grow namespaces'
      pattern: 'useTranslations\\(.header.*'
---

<objective>
Migrate all remaining hardcoded English user-facing strings into i18n.

Purpose: The header mega-menu is hardcoded EN — Dutch and Spanish visitors see English labels for 12 skill categories. `pricing/page.tsx:127` hardcodes "Most popular". `SkillsTierMatrix.tsx:85` hardcodes "Coming soon". `ChatWidget.tsx` hardcodes "Demo limit reached" and "Type a message...". `error.tsx` is fixed in 12-01. `ContactForm.tsx` stays with Phase 11 (a11y form work). `VoiceDemoSection.tsx` US phone number is a decision-gate — flag for Daley, do not auto-fix.

Output: zero hardcoded user-facing EN strings in the 4 target files. New `header.*`, `common.comingSoon`, `chat.*` i18n namespaces exist in NL/EN/ES.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-brand-copy-polish/README.md
@.planning/phases/12-brand-copy-polish/RESEARCH.md
@docs/audits/2026-04-24-full-audit/06-data-accuracy.md
@docs/audits/2026-04-24-full-audit/02-copywriting.md
@fmai-nextjs/src/components/layout/HeaderClient.tsx
@fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx
@fmai-nextjs/src/components/pricing/SkillsTierMatrix.tsx
@fmai-nextjs/src/components/chatbot/ChatWidget.tsx
@fmai-nextjs/messages/nl.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create header.* namespace in NL/EN/ES</name>
  <files>fmai-nextjs/messages/nl.json, fmai-nextjs/messages/en.json, fmai-nextjs/messages/es.json</files>
  <action>
Add a new top-level `header.*` namespace to all 3 locale files. Place it after the existing `nav` namespace (or merge into `nav` if `nav` is already reserved for mobile-only — read the file first to choose the best location).

**NL** (`messages/nl.json`):

```json
"header": {
  "skills": {
    "create": {
      "label": "Maak & Publiceer",
      "description": "Contentproductie op schaal",
      "items": {
        "socialMedia": {
          "title": "Social Media",
          "description": "Captions, inplanning, carrousels en stories"
        },
        "blogFactory": {
          "title": "Blog Factory",
          "description": "Longform SEO-artikelen van 1.500 tot 3.000 woorden"
        },
        "adCreator": {
          "title": "Ad Creator",
          "description": "Statische ads en video-ads voor Meta"
        },
        "reelBuilder": {
          "title": "Reel Builder",
          "description": "Verticale video's met captions en muziek"
        }
      }
    },
    "engage": {
      "label": "Engage & Converteer",
      "description": "Leadcapture en klantinteractie",
      "items": {
        "voiceAgent": {
          "title": "Voice Agent",
          "description": "Inkomende en uitgaande AI-telefoongesprekken"
        },
        "leadQualifier": {
          "title": "Lead Qualifier",
          "description": "Website-chatbot met leadscoring"
        },
        "emailManagement": {
          "title": "Email Management",
          "description": "Gmail-classificatie met dagelijkse digest"
        },
        "manychat": {
          "title": "ManyChat DM",
          "description": "Instagram DM-triggers met AI-antwoorden"
        }
      }
    },
    "grow": {
      "label": "Groei & Optimaliseer",
      "description": "Analyse, onderzoek en orkestratie",
      "items": {
        "reporting": {
          "title": "Reporting",
          "description": "Dashboards, digests en anomaliewaarschuwingen"
        },
        "seoGeo": {
          "title": "SEO / GEO Analyst",
          "description": "SEO-audits en AI-citatiemonitoring"
        },
        "research": {
          "title": "Research",
          "description": "Marktonderzoek en trendmonitoring"
        },
        "clyde": {
          "title": "Clyde AI Employee",
          "description": "De centrale orkestrator van alle vaardigheden"
        }
      }
    },
    "comingSoon": "Binnenkort"
  },
  "nav": {
    "skills": "Vaardigheden",
    "memory": "Geheugen",
    "caseStudies": "Cases",
    "pricing": "Prijzen",
    "about": "Over",
    "login": "Inloggen",
    "apply": "Plan een gesprek"
  }
}
```

**EN** (`messages/en.json`): same shape, English copy (preserve existing EN strings from SKILL_CATEGORIES where possible, but apply Clyde tone + glossary):

```json
"header": {
  "skills": {
    "create": {
      "label": "Create & Publish",
      "description": "Content production at scale",
      "items": {
        "socialMedia": { "title": "Social Media", "description": "Captions, scheduling, carousels and stories" },
        "blogFactory": { "title": "Blog Factory", "description": "Longform SEO articles 1,500 to 3,000 words" },
        "adCreator": { "title": "Ad Creator", "description": "Static and video ads for Meta" },
        "reelBuilder": { "title": "Reel Builder", "description": "Vertical videos with captions and music" }
      }
    },
    "engage": {
      "label": "Engage & Convert",
      "description": "Lead capture and client interaction",
      "items": {
        "voiceAgent": { "title": "Voice Agent", "description": "Inbound and outbound AI phone calls" },
        "leadQualifier": { "title": "Lead Qualifier", "description": "Website chatbot with lead scoring" },
        "emailManagement": { "title": "Email Management", "description": "Gmail classification with daily digest" },
        "manychat": { "title": "ManyChat DM", "description": "Instagram DM triggers with AI replies" }
      }
    },
    "grow": {
      "label": "Grow & Optimize",
      "description": "Analytics, research and orchestration",
      "items": {
        "reporting": { "title": "Reporting", "description": "Dashboards, digests and anomaly alerts" },
        "seoGeo": { "title": "SEO / GEO Analyst", "description": "SEO audits and AI citation monitoring" },
        "research": { "title": "Research", "description": "Market research and trend monitoring" },
        "clyde": { "title": "Clyde AI Employee", "description": "The central orchestrator of all skills" }
      }
    },
    "comingSoon": "Coming soon"
  },
  "nav": {
    "skills": "Skills",
    "memory": "Memory",
    "caseStudies": "Case Studies",
    "pricing": "Pricing",
    "about": "About",
    "login": "Log in",
    "apply": "Book a call"
  }
}
```

**ES** (`messages/es.json`): same shape, Spanish. Reuse existing strings from `es.json` where possible. Example:

```json
"header": {
  "skills": {
    "create": {
      "label": "Crea y Publica",
      "description": "Produccion de contenido a escala",
      "items": {
        "socialMedia": { "title": "Social Media", "description": "Subtitulos, programacion, carruseles y stories" },
        "blogFactory": { "title": "Blog Factory", "description": "Articulos SEO largos de 1.500 a 3.000 palabras" },
        "adCreator": { "title": "Ad Creator", "description": "Anuncios estaticos y de video para Meta" },
        "reelBuilder": { "title": "Reel Builder", "description": "Videos verticales con subtitulos y musica" }
      }
    },
    "engage": {
      "label": "Participa y Convierte",
      "description": "Captura de leads e interaccion con clientes",
      "items": {
        "voiceAgent": { "title": "Voice Agent", "description": "Llamadas AI entrantes y salientes" },
        "leadQualifier": { "title": "Lead Qualifier", "description": "Chatbot web con puntuacion de leads" },
        "emailManagement": { "title": "Email Management", "description": "Clasificacion Gmail con resumen diario" },
        "manychat": { "title": "ManyChat DM", "description": "Triggers Instagram DM con respuestas AI" }
      }
    },
    "grow": {
      "label": "Crece y Optimiza",
      "description": "Analisis, investigacion y orquestacion",
      "items": {
        "reporting": { "title": "Reporting", "description": "Paneles, resumenes y alertas de anomalias" },
        "seoGeo": { "title": "SEO / GEO Analyst", "description": "Auditorias SEO y monitoreo de citas AI" },
        "research": { "title": "Research", "description": "Investigacion de mercado y monitoreo de tendencias" },
        "clyde": { "title": "Clyde AI Employee", "description": "El orquestador central de todas las habilidades" }
      }
    },
    "comingSoon": "Proximamente"
  },
  "nav": {
    "skills": "Habilidades",
    "memory": "Memoria",
    "caseStudies": "Casos",
    "pricing": "Precios",
    "about": "Sobre",
    "login": "Entrar",
    "apply": "Agenda una llamada"
  }
}
```

**If a `nav.*` top-level namespace already exists** (audit hints at one at `nl.json:2182`), merge header.nav keys into existing nav namespace rather than nesting `header.nav`. Adjust all i18n keys references in Task 2 accordingly. Confirm by reading the top of each JSON file.
  </action>
  <verify>
    <automated>cd fmai-nextjs && jq -e '.header.skills.create.items.socialMedia.title' messages/nl.json messages/en.json messages/es.json >/dev/null && jq -e '.header.skills.comingSoon' messages/nl.json >/dev/null && echo "PASS: header.* namespace complete in 3 locales" || echo "FAIL"</automated>
  </verify>
  <done>New header.* namespace exists in NL/EN/ES with 12 skill items × 3 categories + comingSoon key. jq assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor HeaderClient.tsx to consume header.* i18n</name>
  <files>fmai-nextjs/src/components/layout/HeaderClient.tsx</files>
  <action>
Restructure `SKILL_CATEGORIES` and `NAV_ITEMS` constants to hold only structural metadata (icons, hrefs, comingSoon flags) — all `label`, `description`, and `title` strings move to i18n calls.

**Step A: restructure SKILL_CATEGORIES** (lines 33-127) to use i18n keys instead of literal strings:

```typescript
// Structural metadata only — copy lives in messages/*.header.skills.*
const SKILL_CATEGORIES = [
  {
    key: 'create',
    items: [
      { key: 'socialMedia', icon: Share2, href: '/skills/social-media' as const },
      { key: 'blogFactory', icon: BookOpen, href: '/skills/blog-factory' as const },
      { key: 'adCreator', icon: Megaphone, href: '/skills/ad-creator' as const },
      { key: 'reelBuilder', icon: Clapperboard, href: '/skills/reel-builder' as const, comingSoon: true },
    ],
  },
  {
    key: 'engage',
    items: [
      { key: 'voiceAgent', icon: Phone, href: '/skills/voice-agent' as const },
      { key: 'leadQualifier', icon: MessageSquare, href: '/skills/lead-qualifier' as const },
      { key: 'emailManagement', icon: Mail, href: '/skills/email-management' as const, comingSoon: true },
      { key: 'manychat', icon: MessageCircle, href: '/skills/manychat' as const, comingSoon: true },
    ],
  },
  {
    key: 'grow',
    items: [
      { key: 'reporting', icon: BarChart3, href: '/skills/reporting' as const },
      { key: 'seoGeo', icon: Globe, href: '/skills/seo-geo' as const },
      { key: 'research', icon: Search, href: '/skills/research' as const },
      { key: 'clyde', icon: Bot, href: '/skills/clyde' as const },
    ],
  },
] as const
```

**Step B: restructure NAV_ITEMS** (lines 129-135):

```typescript
const NAV_ITEMS = [
  { key: 'skills', href: '/#skills' as const, hasDropdown: true },
  { key: 'memory', href: '/memory' as const },
  { key: 'caseStudies', href: '/case-studies/skinclarity-club' as const },
  { key: 'pricing', href: '/pricing' as const },
  { key: 'about', href: '/about' as const },
] as const
```

**Step C: update the JSX render blocks** (lines 241 onward and 398 onward — two render sites for desktop + mobile):

Change the existing line 139 import from `useTranslations('nav')` to add `useTranslations('header')`:

```tsx
const t = useTranslations('header')
// If nav namespace is separate:
const tNav = useTranslations('header.nav')
```

Render site replaces hardcoded `category.label`, `category.description`, `item.title`, `item.description` with translation calls:

```tsx
{SKILL_CATEGORIES.map((category) => (
  <div key={category.key}>
    <h3>{t(`skills.${category.key}.label`)}</h3>
    <p>{t(`skills.${category.key}.description`)}</p>
    {category.items.map((item) => (
      <Link href={item.href} key={item.key}>
        <item.icon />
        <span>{t(`skills.${category.key}.items.${item.key}.title`)}</span>
        <span>{t(`skills.${category.key}.items.${item.key}.description`)}</span>
        {item.comingSoon && <span className="text-xs">{t('skills.comingSoon')}</span>}
      </Link>
    ))}
  </div>
))}
```

And for NAV_ITEMS:

```tsx
{NAV_ITEMS.map((item) => (
  <Link href={item.href} key={item.key}>
    {tNav(item.key)}
  </Link>
))}
```

**Coordinate with existing `useTranslations('nav')` at line 139**: if the code already pulls from `nav.*` (mobile drawer uses `nav.apply` etc.), leave that as-is and only add `useTranslations('header')` for the new mega-menu. The two namespaces coexist.

**Icons not localized** — lucide-react icon imports stay. Only strings move.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -E "label: 'Create & Publish'|label: 'Engage & Convert'|label: 'Grow & Optimize'|title: 'Social Media'|title: 'Blog Factory'" src/components/layout/HeaderClient.tsx && grep -q "t(\`skills\\." src/components/layout/HeaderClient.tsx && echo "PASS: HeaderClient localized" || echo "FAIL"</automated>
  </verify>
  <done>HeaderClient SKILL_CATEGORIES has zero hardcoded labels/descriptions/titles. All render via t() call. Icons + hrefs + comingSoon flags remain as structural metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Fix 'Most popular' in pricing/page.tsx:127 and 'Coming soon' in SkillsTierMatrix.tsx:85</name>
  <files>fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx, fmai-nextjs/src/components/pricing/SkillsTierMatrix.tsx, fmai-nextjs/messages/nl.json, fmai-nextjs/messages/en.json, fmai-nextjs/messages/es.json</files>
  <action>
**Step A: add i18n keys**:

In all 3 locales (`messages/{nl,en,es}.json`), add to existing `pricing.tiers.professional` object (around line 587 in nl.json):

NL: `"mostPopular": "Meest gekozen"`
EN: `"mostPopular": "Most popular"`
ES: `"mostPopular": "Mas elegido"`

Additionally, add to a `common` top-level namespace (or create if missing):

NL: `"common": { ..., "comingSoon": "Binnenkort", ... }`
EN: `"common": { ..., "comingSoon": "Coming soon", ... }`
ES: `"common": { ..., "comingSoon": "Proximamente", ... }`

(Note: `header.skills.comingSoon` exists per Task 1 — we reuse `common.comingSoon` as the canonical key for all non-header use sites. HeaderClient can switch to `common.comingSoon` too for consistency — optional cleanup, not required.)

**Step B: fix pricing/page.tsx:127**:

| File:Line | Current | Should be |
|---|---|---|
| `src/app/[locale]/(marketing)/pricing/page.tsx:127` | `: 'Most popular'}` | `: t('pricing.tiers.professional.mostPopular')}` |

Verify the file already has `useTranslations` or `getTranslations` in scope; if not, add the import + invocation.

**Step C: fix SkillsTierMatrix.tsx:85**:

| File:Line | Current | Should be |
|---|---|---|
| `src/components/pricing/SkillsTierMatrix.tsx:85` | `Coming soon` (literal in JSX) | `{t('common.comingSoon')}` (or `{t('comingSoon')}` if component uses `useTranslations('common')`) |

Read the component first to check its current `useTranslations` scope.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep "'Most popular'" src/app/[locale]/(marketing)/pricing/page.tsx && ! grep "Coming soon" src/components/pricing/SkillsTierMatrix.tsx && jq -e '.pricing.tiers.professional.mostPopular' messages/nl.json >/dev/null && jq -e '.common.comingSoon' messages/nl.json >/dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>"Most popular" + "Coming soon" strings removed from source, replaced by i18n calls. New keys exist in 3 locales.</done>
</task>

<task type="auto">
  <name>Task 4: Fix ChatWidget.tsx placeholders</name>
  <files>fmai-nextjs/src/components/chatbot/ChatWidget.tsx, fmai-nextjs/messages/nl.json, fmai-nextjs/messages/en.json, fmai-nextjs/messages/es.json</files>
  <action>
**Step A: add i18n keys** to the existing `chat.*` or `chatbot.*` namespace (read file to confirm namespace location):

NL: add to existing chatbot message namespace:
```json
"widget": {
  "typeMessage": "Typ een bericht...",
  "demoLimitReached": "Demolimiet bereikt",
  "demoLimitMessage": "Je hebt de demolimiet bereikt.",
  "demoLimitCta": "Plan een gesprek om verder te praten."
}
```

EN:
```json
"widget": {
  "typeMessage": "Type a message...",
  "demoLimitReached": "Demo limit reached",
  "demoLimitMessage": "You've reached the demo limit.",
  "demoLimitCta": "Book a call to continue the conversation."
}
```

ES:
```json
"widget": {
  "typeMessage": "Escribe un mensaje...",
  "demoLimitReached": "Limite de demo alcanzado",
  "demoLimitMessage": "Has alcanzado el limite de demo.",
  "demoLimitCta": "Agenda una llamada para continuar."
}
```

**Step B: fix `ChatWidget.tsx`** — 3 replacements:

| File:Line | Current | Should be |
|---|---|---|
| `src/components/chatbot/ChatWidget.tsx:107` | `Demo limit reached.{' '}` (inside a line that says more context) | `{t('chat.widget.demoLimitReached')}.{' '}` (with the full demo-limit-message CTA following from `demoLimitCta` key) |
| `src/components/chatbot/ChatWidget.tsx:180` | `placeholder={isAtLimit ? 'Demo limit reached' : 'Type a message...'}` | `placeholder={isAtLimit ? t('chat.widget.demoLimitReached') : t('chat.widget.typeMessage')}` |
| `src/components/chatbot/ChatWidget.tsx:230` | `placeholder={isAtLimit ? 'Demo limit reached' : 'Type a message...'}` | `placeholder={isAtLimit ? t('chat.widget.demoLimitReached') : t('chat.widget.typeMessage')}` |

Add the `useTranslations('chat.widget')` call near the top of the component. Verify existing translation imports; may need to switch namespace scope. If component currently uses `useTranslations('chatbot')`, namespace the keys as `chatbot.widget.*` instead.

Read the full file first to select the best namespace placement.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -E "'Demo limit reached'|'Type a message\\.\\.\\.'" src/components/chatbot/ChatWidget.tsx && grep -q "t(.*demoLimitReached\\|t(.*typeMessage" src/components/chatbot/ChatWidget.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ChatWidget.tsx has zero hardcoded English placeholders. 3 places fixed. Keys exist in 3 locales.</done>
</task>

<task type="manual">
  <name>Task 5: FLAG VoiceDemoSection US phone number for Daley decision</name>
  <files>fmai-nextjs/src/components/voice/VoiceDemoSection.tsx</files>
  <action>
**NO CODE CHANGE.** This task is a decision-gate flag.

`src/components/voice/VoiceDemoSection.tsx:67,93` displays US phone number `+1 (570) 783-8236` on a Dutch marketing site. Audit 06 flags this as data-accuracy issue MEDIUM.

Options Daley must choose from:

1. **Swap to NL number**: rent a Dutch DID via Vapi (EUR 5/mo), update number in `VoiceDemoSection.tsx:67,93` and in Vapi assistant config. Effort: 1 uur (Vapi CLI + code edit + test call).
2. **Hide number, show CTA only**: remove the phone display, render only the "Bel demo" button linked to Vapi web call (client-side SDK). Effort: 30 min.
3. **Keep** US number: acknowledge mismatch, defer to after Vapi infrastructure migration. Effort: 0.

**This plan does not execute any of the 3 options.** Instead, create a parked decision note:

Add a file `.planning/phases/12-brand-copy-polish/DECISION-PENDING-phone-number.md`:

```markdown
# Decision Pending — VoiceDemoSection phone number

**Context**: `src/components/voice/VoiceDemoSection.tsx:67,93` shows US number `+1 (570) 783-8236` on NL site.
**Surfaced by**: audit 06-data-accuracy §Executive Summary, Phase 12 RESEARCH §10.
**Decision required from**: Daley.
**Options**:
1. Swap to NL Vapi number — 1 uur, ongoing EUR 5/mo.
2. Hide number, keep CTA-only — 30 min, no recurring cost.
3. Keep US number, acknowledge mismatch — 0 min.
**Default if not answered within 2 weeks**: option 2 (hide), since it's lowest-risk + cost-free and voice demo still works via web SDK.
```

After the decision is made in a later sprint, execute the chosen option as an isolated 1-task plan (outside Phase 12 scope).
  </action>
  <verify>
    <automated>test -f .planning/phases/12-brand-copy-polish/DECISION-PENDING-phone-number.md && echo "PASS: decision note created" || echo "FAIL"</automated>
  </verify>
  <done>Decision note created. VoiceDemoSection.tsx unchanged. Blocked on Daley.</done>
</task>

<task type="auto">
  <name>Task 6: Regression check — dev server multi-locale render</name>
  <files></files>
  <action>
Visit all 3 locales on home, pricing, any /skills/* page:

- `http://localhost:3000/nl/` — header mega-menu (hover Skills) shows Dutch labels: "Vaardigheden", "Maak & Publiceer", "Captions, inplanning, carrousels en stories", "Binnenkort".
- `http://localhost:3000/en/` — same menu in English.
- `http://localhost:3000/es/` — same menu in Spanish.
- `http://localhost:3000/nl/pricing` — Professional tier "Meest gekozen" badge (not "Most popular").
- `http://localhost:3000/en/pricing` — "Most popular".
- `http://localhost:3000/es/pricing` — "Mas elegido".
- `http://localhost:3000/nl/` (click chat widget) — placeholder "Typ een bericht..." (not "Type a message...").

No i18n missing-key warnings in browser console. No untranslated strings visible.

Capture one screenshot per locale of header + pricing CTA.
  </action>
  <verify>
    <manual>
      - NL/EN/ES header mega-menu renders in correct locale.
      - pricing Professional badge matches locale.
      - chat placeholder matches locale.
      - zero missing-key console warnings.
    </manual>
  </verify>
  <done>Header + pricing + chat fully localized across NL/EN/ES. Screenshots captured.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors
- `npm run build` — compiles
- `grep -E "'Create & Publish'|'Engage & Convert'|'Grow & Optimize'|'Most popular'|'Coming soon'" src/` — zero matches in HeaderClient, pricing/page.tsx, SkillsTierMatrix
- `grep -E "'Type a message'|'Demo limit reached'" src/components/chatbot/ChatWidget.tsx` — zero matches
- jq assertions for `header.skills.create.items.socialMedia.title` in 3 locales pass
- Phone decision note exists in .planning/phases/12-brand-copy-polish/
</verification>

<success_criteria>
HeaderClient renders in correct locale. Pricing "Most popular" localized. SkillsTierMatrix "Coming soon" localized. ChatWidget placeholders localized. VoiceDemoSection phone number flagged for Daley via decision note. ContactForm.tsx NOT touched (Phase 11 owns it).
</success_criteria>

<output>
After completion, create `.planning/phases/12-brand-copy-polish/12-03-SUMMARY.md` with:
- Count of new header.* keys added per locale (expected: ~35-40 keys × 3 = ~115 new keys)
- Total hardcoded EN strings eliminated (expected: 12 header skill titles + 3 category labels + 3 category descriptions + 5 nav items + "Most popular" + "Coming soon" + 3 ChatWidget strings = 27 strings × 2 locales that needed them = ~54 new translations)
- Phone decision note status
- Any ContactForm.tsx strings flagged (should be ZERO — they are Phase 11 scope)
</output>
