---
phase: 12-brand-copy-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fmai-nextjs/public/og-image.png
  - fmai-nextjs/public/logo.png
  - fmai-nextjs/public/og-image-square.png
  - fmai-nextjs/src/app/api/og/route.tsx
  - fmai-nextjs/src/lib/og-theme.ts
  - fmai-nextjs/src/components/interactive/CookieConsentBanner.tsx
  - fmai-nextjs/src/app/[locale]/not-found.tsx
  - fmai-nextjs/src/app/[locale]/error.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoCheckpoint.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoCompletionCard.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoOrchestrator.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoProgress.tsx
  - fmai-nextjs/src/components/chatbot/demo/DemoScenarioCard.tsx
  - fmai-nextjs/src/components/chatbot/DemoContextCard.tsx
  - fmai-nextjs/src/components/chatbot/PersonaSelector.tsx
  - fmai-nextjs/src/components/memory/MemoryLayersDiagram.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/memory/page.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/about/page.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/case-studies/skinclarity-club/page.tsx
  - fmai-nextjs/scripts/verify-palette.sh
  - fmai-nextjs/package.json
  - fmai-nextjs/CLAUDE.md
autonomous: true
requirements:
  - WEB-01

must_haves:
  truths:
    - 'GET /og-image.png returns 200 with 1200x630 PNG'
    - 'GET /logo.png returns 200 with 512x512 PNG'
    - 'Zero occurrences of #050814 / #00D4FF / #A855F7 in src/ .tsx .ts .css files (Spline bundle assets excepted)'
    - 'fmai-nextjs/CLAUDE.md Theme section reads #0a0d14 / #00d4aa'
    - 'CookieConsentBanner renders with CSS variables, not hardcoded hex'
    - 'not-found.tsx uses text-accent-system / bg-accent-system tokens'
    - 'error.tsx uses approved palette tokens, no purple'
    - 'scripts/verify-palette.sh exits 0 on clean tree'
  artifacts:
    - path: 'fmai-nextjs/public/og-image.png'
      provides: 'Social share card for LinkedIn/Twitter/WhatsApp + OrganizationJsonLd.logo source'
    - path: 'fmai-nextjs/public/logo.png'
      provides: 'Organization logo for schema.org + email footer + PWA icon'
    - path: 'fmai-nextjs/src/app/api/og/route.tsx'
      provides: 'Optional: dynamic per-locale OG image at /api/og?locale=nl'
    - path: 'fmai-nextjs/scripts/verify-palette.sh'
      provides: 'Grep-based regression test for stale color hex'
  key_links:
    - from: 'fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx:10'
      to: 'fmai-nextjs/public/og-image.png'
      via: 'logo field of schema.org Organization'
      pattern: 'logo.*og-image.png'
---

<objective>
Ship missing brand assets and complete palette migration.

Purpose: Social share cards render broken on LinkedIn/Twitter because `public/og-image.png` does not exist (404 on every OG card request). `OrganizationJsonLd` references the same missing file as `logo`, poisoning schema.org crawlers. Meanwhile 3 files plus 20+ individual component references still hardcode the deprecated `#050814` / `#00D4FF` / `#A855F7` palette — visual dialect breaks on 404, 500, cookie banner, and chatbot demo components.
Output: working OG asset, working logo asset, zero hardcoded stale hex in src/, regression-test script, and updated CLAUDE.md Theme documentation.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-brand-copy-polish/README.md
@.planning/phases/12-brand-copy-polish/RESEARCH.md
@docs/audits/2026-04-24-full-audit/MASTER-ACTION-PLAN.md
@docs/audits/2026-04-24-full-audit/07-ux-accessibility.md
@fmai-nextjs/src/app/globals.css
@fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx
@fmai-nextjs/src/components/interactive/CookieConsentBanner.tsx
@fmai-nextjs/src/app/[locale]/not-found.tsx
@fmai-nextjs/src/app/[locale]/error.tsx
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Design + export og-image.png (1200x630) and logo.png (512x512)</name>
  <files>fmai-nextjs/public/og-image.png, fmai-nextjs/public/logo.png</files>
  <action>
Daley designs and exports two PNG assets using Figma, Orshot, or equivalent.

**og-image.png** (1200x630 px, PNG, <200 KB):
- Background: solid `#0a0d14`
- Top-left: FMai wordmark ("future-marketing.ai") in `#00d4aa`, 32pt, JetBrains Mono, 64px margin
- Center: primary headline "Clyde" in `#00d4aa`, 180pt bold, DM Sans
- Below headline: "Jouw AI Marketing Medewerker" in white `#e8ecf4`, 56pt DM Sans
- Bottom-right: subtitle "Voor Nederlandse bureaus" in `#9ba3b5`, 28pt DM Sans, 64px margin
- Optional: teal-to-amber gradient accent line (2px) at bottom edge: `linear-gradient(90deg, #00d4aa 0%, #f5a623 100%)`
- Save to `fmai-nextjs/public/og-image.png`

**og-image-square.png** (1200x1200 px, optional — only needed if a specific meta tag references it; audit did not confirm). Same composition, square canvas.

**logo.png** (512x512 px, PNG with transparent background, <50 KB):
- Center: FMai icon (either the "F" mark or wordmark compacted)
- Color: `#00d4aa` on transparent background
- Used by `OrganizationJsonLd.tsx:10` via `${SITE_URL}/og-image.png` (note: current code points logo AT og-image.png — inconsistent but works; after this task, optionally update OrganizationJsonLd to point logo at `/logo.png` and keep og-image at `/og-image.png`)

Save both to `fmai-nextjs/public/`.

Reference the existing FMai visual language (teal + amber) already documented in `fmai-nextjs/CLAUDE.md` §Theme and in `globals.css`.

**No code changes in this task.** Purely asset production.
  </action>
  <verify>
    <manual>
      - Open `fmai-nextjs/public/og-image.png` — verify 1200x630 px, correct copy.
      - Test via LinkedIn Post Inspector: paste `https://future-marketing.ai/og-image.png` after Vercel deploy.
      - Test via `opengraph.xyz/url/https%3A%2F%2Ffuture-marketing.ai` post-deploy.
    </manual>
    <automated>cd fmai-nextjs && test -f public/og-image.png && test -f public/logo.png && file public/og-image.png | grep -q "1200 x 630" && echo "PASS: assets present + correct dimensions" || echo "FAIL"</automated>
  </verify>
  <done>og-image.png (1200x630) and logo.png (512x512) committed under fmai-nextjs/public/. Both dimensions verified.</done>
</task>

<task type="auto">
  <name>Task 2: Update OrganizationJsonLd to point logo at /logo.png (not og-image.png)</name>
  <files>fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx</files>
  <action>
Separate logo (small square icon) from OG image (wide banner). Currently line 10 hardcodes `${SITE_URL}/og-image.png` as `logo`. After task 1, `logo.png` exists — point logo at the correct file.

Single edit in `fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx`:

| File:Line | Current | Should be |
|---|---|---|
| `src/components/seo/OrganizationJsonLd.tsx:10` | `logo: ${SITE_URL}/og-image.png,` | `logo: ${SITE_URL}/logo.png,` |

No other changes. Do not touch `hasOfferCatalog` (owned by P1 rank 8, separate phase).
  </action>
  <verify>
    <automated>cd fmai-nextjs && grep -n "logo: \`\${SITE_URL}/logo.png\`" src/components/seo/OrganizationJsonLd.tsx && echo "PASS: logo path updated" || echo "FAIL"</automated>
  </verify>
  <done>OrganizationJsonLd logo points to /logo.png, not og-image.png.</done>
</task>

<task type="auto">
  <name>Task 3: Palette migration in CookieConsentBanner</name>
  <files>fmai-nextjs/src/components/interactive/CookieConsentBanner.tsx</files>
  <action>
Replace all hardcoded hex in CookieConsentBanner (lines 44, 54, 55) with CSS variable references.

Exact replacements:

| File:Line | Current | Should be |
|---|---|---|
| `src/components/interactive/CookieConsentBanner.tsx:44` | `background: '#050814',` | `background: 'var(--color-bg-deep)',` |
| `src/components/interactive/CookieConsentBanner.tsx:45` | `borderTop: '1px solid rgba(0, 212, 255, 0.2)',` | `borderTop: '1px solid var(--color-border-accent)',` |
| `src/components/interactive/CookieConsentBanner.tsx:54` | `background: '#00D4FF',` | `background: 'var(--color-accent-system)',` |
| `src/components/interactive/CookieConsentBanner.tsx:55` | `color: '#050814',` | `color: 'var(--color-bg-deep)',` |

CSS variables referenced come from `src/app/globals.css` `@theme` block (already defined: `--color-bg-deep: #0a0d14`, `--color-accent-system: #00d4aa`, `--color-border-accent: rgba(0, 212, 170, 0.3)`). No new tokens needed.

Keep the other style props unchanged (they use neutral grays already).
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -E "#050814|#00D4FF" src/components/interactive/CookieConsentBanner.tsx && echo "PASS: no stale hex in CookieConsentBanner" || echo "FAIL"</automated>
  </verify>
  <done>CookieConsentBanner renders with CSS variables only. No `#050814`, `#00D4FF`, or `rgba(0, 212, 255, ...)` remain.</done>
</task>

<task type="auto">
  <name>Task 4: Palette migration in not-found.tsx</name>
  <files>fmai-nextjs/src/app/[locale]/not-found.tsx</files>
  <action>
Replace hardcoded `#00D4FF` + `#050814` with Tailwind utility classes that resolve to `--color-accent-system` (teal) and `--color-bg-deep`.

Exact replacements:

| File:Line | Current | Should be |
|---|---|---|
| `src/app/[locale]/not-found.tsx:9` | `<p className="text-8xl font-bold text-[#00D4FF]">404</p>` | `<p className="text-8xl font-bold text-accent-system">404</p>` |
| `src/app/[locale]/not-found.tsx:14` | `className="mt-8 inline-block rounded-lg bg-[#00D4FF] px-6 py-3 text-sm font-medium text-[#050814] transition-colors hover:bg-[#00D4FF]/80"` | `className="mt-8 inline-block rounded-lg bg-accent-system px-6 py-3 text-sm font-medium text-bg-deep transition-colors hover:bg-accent-system/80"` |

Verify Tailwind config maps `accent-system` and `bg-deep` utilities to the CSS vars. If not (because Tailwind 4 uses `@theme` directly, these should auto-resolve), fall back to `text-[var(--color-accent-system)]` arbitrary-value form.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -E "#00D4FF|#050814" src/app/[locale]/not-found.tsx && echo "PASS: not-found clean" || echo "FAIL"</automated>
  </verify>
  <done>404 page renders in teal `#00d4aa` not cyan `#00D4FF`. Visual dialect matches rest of site.</done>
</task>

<task type="auto">
  <name>Task 5: Palette migration + i18n in error.tsx</name>
  <files>fmai-nextjs/src/app/[locale]/error.tsx, fmai-nextjs/messages/nl.json, fmai-nextjs/messages/en.json, fmai-nextjs/messages/es.json</files>
  <action>
Rewrite `error.tsx` entirely: remove `#A855F7` purple (not in design system), remove `#00D4FF` cyan (deprecated), remove hardcoded EN strings, wire up `useTranslations('errors.generic')`.

**Step A: add new message keys** to `messages/{nl,en,es}.json` under `errors.generic`:

NL (`messages/nl.json`), add to existing `errors` namespace (after `notFound` block):

```json
"generic": {
  "title": "Er ging iets mis.",
  "description": "Een onverwachte fout trad op. Probeer het opnieuw of keer terug naar de homepage.",
  "retryButton": "Probeer opnieuw",
  "homeButton": "Terug naar home"
}
```

EN (`messages/en.json`):

```json
"generic": {
  "title": "Something went wrong.",
  "description": "An unexpected error occurred. Please try again or return to the homepage.",
  "retryButton": "Try again",
  "homeButton": "Back to home"
}
```

ES (`messages/es.json`):

```json
"generic": {
  "title": "Algo salio mal.",
  "description": "Ha ocurrido un error inesperado. Intentalo de nuevo o vuelve a la pagina de inicio.",
  "retryButton": "Intentar de nuevo",
  "homeButton": "Volver al inicio"
}
```

**Step B: rewrite `src/app/[locale]/error.tsx`**:

Replace the entire file content with:

```tsx
'use client'

import { useTranslations } from 'next-intl'

export default function Error({
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  const t = useTranslations('errors.generic')

  return (
    <main className="flex min-h-[60vh] flex-col items-center justify-center px-4 text-center">
      <p className="text-6xl font-bold text-accent-system">!</p>
      <h1 className="mt-4 text-3xl font-semibold text-text-primary">{t('title')}</h1>
      <p className="mt-2 text-lg text-text-secondary">{t('description')}</p>
      <button
        onClick={reset}
        className="mt-8 inline-block rounded-lg bg-accent-system px-6 py-3 text-sm font-medium text-bg-deep transition-colors hover:bg-accent-system/80"
      >
        {t('retryButton')}
      </button>
    </main>
  )
}
```

Removals:
- `#A855F7` purple (the "!" was purple — now teal matching accent-system)
- `#00D4FF` cyan (button now uses accent-system token)
- `#050814` text-bg-deep token
- Hardcoded EN: "Something went wrong", "An unexpected error occurred.", "Try again"
- `error.message` rendering (security/info-leak — hidden by default, surfaced only in dev via `console.error` if needed)

The `error` prop is still received (Next.js requires the signature), but we do not render `error.message` to the user — only to console if debug needed. Mark `error` as unused via destructuring (only `reset` is used). TypeScript may warn about unused prop — suppress with `// eslint-disable-next-line @typescript-eslint/no-unused-vars` or use `_error` rename.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -E "#A855F7|#00D4FF|#050814|Something went wrong|Try again" src/app/[locale]/error.tsx && grep -q "useTranslations" src/app/[locale]/error.tsx && echo "PASS: error.tsx localized + palette migrated" || echo "FAIL"</automated>
  </verify>
  <done>error.tsx renders in 3 locales with zero hardcoded colors or strings. No purple. No raw error.message leak.</done>
</task>

<task type="auto">
  <name>Task 6: Palette sweep across remaining chatbot demo + memory components</name>
  <files>fmai-nextjs/src/components/chatbot/demo/DemoCheckpoint.tsx, fmai-nextjs/src/components/chatbot/demo/DemoCompletionCard.tsx, fmai-nextjs/src/components/chatbot/demo/DemoOrchestrator.tsx, fmai-nextjs/src/components/chatbot/demo/DemoProgress.tsx, fmai-nextjs/src/components/chatbot/demo/DemoScenarioCard.tsx, fmai-nextjs/src/components/chatbot/DemoContextCard.tsx, fmai-nextjs/src/components/chatbot/PersonaSelector.tsx, fmai-nextjs/src/components/memory/MemoryLayersDiagram.tsx, fmai-nextjs/src/app/[locale]/(marketing)/memory/page.tsx, fmai-nextjs/src/app/[locale]/(marketing)/about/page.tsx, fmai-nextjs/src/app/[locale]/(marketing)/case-studies/skinclarity-club/page.tsx</files>
  <action>
Run sweep: replace every remaining `#00D4FF` with `accent-system` / `var(--color-accent-system)`, every `#050814` with `bg-deep`, every `#A855F7` purple with `accent-human` (amber `#f5a623`) OR remove entirely. Table of required edits based on grep output:

**`src/components/chatbot/demo/DemoCheckpoint.tsx`** (line 36, 39):
- `border-[#00D4FF]/40 bg-[#00D4FF]/20 ... text-[#00D4FF] ... hover:bg-[#00D4FF]/30` → `border-accent-system/40 bg-accent-system/20 text-accent-system hover:bg-accent-system/30`
- `hover:border-[#00D4FF]/30` → `hover:border-accent-system/30`

**`src/components/chatbot/demo/DemoCompletionCard.tsx`** (line 28, 31, 32, 39, 49):
- `border-[#00D4FF]/20 bg-gradient-to-br from-[#00D4FF]/10 to-[#A855F7]/5` → `border-accent-system/20 bg-gradient-to-br from-accent-system/10 to-accent-human/5` (purple → amber — visual warmth via approved token)
- `bg-[#00D4FF]/20` → `bg-accent-system/20`
- `text-[#00D4FF]` → `text-accent-system`
- `text-[#00D4FF]/70` → `text-accent-system/70`
- `border-[#00D4FF]/40 bg-[#00D4FF]/20 ... text-[#00D4FF] ... hover:bg-[#00D4FF]/30` → `border-accent-system/40 bg-accent-system/20 text-accent-system hover:bg-accent-system/30`

**`src/components/chatbot/demo/DemoOrchestrator.tsx`** (line 181, 184, 192):
- All `#00D4FF` refs → `accent-system`.

**`src/components/chatbot/demo/DemoProgress.tsx`** (line 23, 25, 40):
- `bg-gradient-to-br from-[#00D4FF] to-[#A855F7]` → `bg-gradient-to-br from-accent-system to-accent-human`
- `border-[#00D4FF] bg-[#00D4FF]/10` → `border-accent-system bg-accent-system/10`
- `bg-[#00D4FF]` → `bg-accent-system`

**`src/components/chatbot/demo/DemoScenarioCard.tsx`** (line 40, 43, 44):
- `bg-[#0A0E27]/80` → `bg-bg-surface/80` (note: `#0A0E27` is also stale — old surface color, migrate to current `--color-bg-surface: #111520`)
- `hover:border-[#00D4FF]/40 hover:shadow-lg hover:shadow-[#00D4FF]/5` → `hover:border-accent-system/40 hover:shadow-lg hover:shadow-accent-system/5`
- `bg-gradient-to-br from-[#00D4FF]/20 to-[#A855F7]/10` → `bg-gradient-to-br from-accent-system/20 to-accent-human/10`
- `text-[#00D4FF]` → `text-accent-system`

**`src/components/chatbot/DemoContextCard.tsx`** (line 35):
- `text-[#00D4FF]` → `text-accent-system`

**`src/components/chatbot/PersonaSelector.tsx`** (line 38):
- `border-[#00D4FF]/50 bg-[#00D4FF]/10` → `border-accent-system/50 bg-accent-system/10`

**`src/components/memory/MemoryLayersDiagram.tsx`** (line 13, 14):
- `accent: 'text-[#00D4FF]', borderAccent: 'border-[#00D4FF]/40'` → `accent: 'text-accent-system', borderAccent: 'border-accent-system/40'`
- `accent: 'text-[#A855F7]', borderAccent: 'border-[#A855F7]/40'` → `accent: 'text-accent-human', borderAccent: 'border-accent-human/40'` (the `context` layer — amber instead of purple)

**`src/app/[locale]/(marketing)/memory/page.tsx`** (line 69):
- `linear-gradient(135deg, #00D4AA 0%, #A855F7 100%)` → `linear-gradient(135deg, #00d4aa 0%, #f5a623 100%)` (amber replacing purple to match `--bg-gradient-flow` token).

**`src/app/[locale]/(marketing)/about/page.tsx`** (line 235):
- `from-accent-system to-[#A855F7]` → `from-accent-system to-accent-human`

**`src/app/[locale]/(marketing)/case-studies/skinclarity-club/page.tsx`** (line 214):
- `from-accent-system to-[#A855F7]` → `from-accent-system to-accent-human`

**Rationale**: `#A855F7` purple is not in the design system. Replace with `accent-human` (amber `#f5a623`), the approved secondary accent. This is a deliberate design-token normalization, not a purely mechanical substitution.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -rE "#00D4FF|#050814|#A855F7|#0A0E27" src/components/ src/app/ 2>/dev/null | grep -v "\.spec\." && echo "PASS: zero stale hex in src" || (echo "FAIL — remaining refs:" && grep -rnE "#00D4FF|#050814|#A855F7|#0A0E27" src/components/ src/app/ 2>/dev/null | head -20)</automated>
  </verify>
  <done>All chatbot demo, memory, about, case-study palette refs migrated to design tokens. Zero `#A855F7` purple anywhere in src/.</done>
</task>

<task type="auto">
  <name>Task 7: Create verify-palette.sh regression script + wire into package.json</name>
  <files>fmai-nextjs/scripts/verify-palette.sh, fmai-nextjs/package.json</files>
  <action>
Create `fmai-nextjs/scripts/verify-palette.sh`:

```bash
#!/usr/bin/env bash
#
# verify-palette.sh
# WHY: after Phase 12 palette migration, prevent regression.
# Exits 1 if any deprecated hex leaks back into src/.

set -euo pipefail

STALE_HEX='#050814|#00D4FF|#A855F7|#0A0E27'
MATCHES=$(grep -rnE "$STALE_HEX" \
  --include='*.tsx' \
  --include='*.ts' \
  --include='*.css' \
  fmai-nextjs/src/ 2>/dev/null || true)

if [ -n "$MATCHES" ]; then
  echo "FAIL: deprecated palette hex found in src/:"
  echo "$MATCHES"
  exit 1
fi

echo "PASS: no stale palette hex in src/"
exit 0
```

Make executable: `chmod +x fmai-nextjs/scripts/verify-palette.sh`.

Add to `fmai-nextjs/package.json` scripts block:

```json
"scripts": {
  ...existing...
  "check:palette": "bash scripts/verify-palette.sh"
}
```

(Do NOT wire into `prebuild` — we add that in a later phase after Daley confirms no false-positives across weeks of use. For now the script is opt-in.)
  </action>
  <verify>
    <automated>cd fmai-nextjs && test -x scripts/verify-palette.sh && npm run check:palette && echo "PASS: script runs clean" || echo "FAIL"</automated>
  </verify>
  <done>scripts/verify-palette.sh exists, executable, and passes on clean tree. package.json has check:palette script.</done>
</task>

<task type="auto">
  <name>Task 8: Update fmai-nextjs/CLAUDE.md Theme section</name>
  <files>fmai-nextjs/CLAUDE.md</files>
  <action>
Current CLAUDE.md says:

```
## Theme (dark-only)
- Background: `#050814`
- Surface: `#0A0E27`
- Primary accent: `#00D4FF` (cyan)
- Secondary accent: `#A855F7` (purple)
- Success: `#00FF88` (green)
- Typography: Inter (sans), JetBrains Mono (mono)
```

Replace with actual live palette (from `globals.css` `@theme` block):

```
## Theme (dark-only)

- Background: `#0a0d14` (--color-bg-deep)
- Surface: `#111520` (--color-bg-surface)
- Elevated surface: `#1a1f2e` (--color-bg-elevated)
- Primary accent: `#00d4aa` (--color-accent-system, teal)
- Secondary accent: `#f5a623` (--color-accent-human, amber)
- Success / Status active: `#22c55e` (--color-status-active)
- Error: `#ef4444` (--color-error)
- Text primary: `#e8ecf4` (--color-text-primary)
- Text secondary: `#9ba3b5` (--color-text-secondary)
- Text muted: `#5a6378` (--color-text-muted) — note: low contrast 3.23:1, pending bump to `#8C98AD` in Phase 11 a11y work (audit 07 §2.4.3, B-7)
- Typography: DM Sans (sans), Space Grotesk (display), JetBrains Mono (mono)

**Deprecated palette** (do NOT use — enforcement via `npm run check:palette`):
- `#050814` old background
- `#0A0E27` old surface
- `#00D4FF` old cyan accent
- `#A855F7` purple (not in system)
```

Also update the single line at the top if it mentions typography: current says `Inter (sans), JetBrains Mono (mono)`. Replace Inter with DM Sans (actual deployed font per `globals.css:24`).

This is the one place where Daley or future agents read the palette truth. Must match reality.
  </action>
  <verify>
    <automated>cd fmai-nextjs && grep -q "#0a0d14" CLAUDE.md && grep -q "#00d4aa" CLAUDE.md && ! grep -q "Inter (sans)" CLAUDE.md && echo "PASS: CLAUDE.md updated" || echo "FAIL"</automated>
  </verify>
  <done>fmai-nextjs/CLAUDE.md Theme section matches src/app/globals.css live values. Inter replaced by DM Sans. Deprecated palette documented as forbidden.</done>
</task>

<task type="auto" optional="true">
  <name>Task 9 (optional): Dynamic OG image route at /api/og</name>
  <files>fmai-nextjs/src/app/api/og/route.tsx, fmai-nextjs/src/lib/og-theme.ts</files>
  <action>
**OPTIONAL** — ship only if Task 1 static PNG is done and time permits. Adds per-locale dynamic OG variants.

Create `fmai-nextjs/src/lib/og-theme.ts`:

```ts
// WHY: OG images use inline styles only (Satori subset), no Tailwind classes.
// Mirror design tokens here so they stay in sync with globals.css @theme.

export const OG_THEME = {
  bgDeep: '#0a0d14',
  accentSystem: '#00d4aa',
  accentHuman: '#f5a623',
  textPrimary: '#e8ecf4',
  textSecondary: '#9ba3b5',
} as const

export const OG_TAGLINES = {
  nl: 'Jouw AI Marketing Medewerker. Voor Nederlandse bureaus.',
  en: 'Your AI Marketing Employee. For agencies.',
  es: 'Tu Empleado AI de Marketing. Para agencias.',
} as const
```

Create `fmai-nextjs/src/app/api/og/route.tsx`:

```tsx
import { ImageResponse } from 'next/og'
import { OG_THEME, OG_TAGLINES } from '@/lib/og-theme'

export const runtime = 'edge'

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url)
  const locale = (searchParams.get('locale') ?? 'nl') as keyof typeof OG_TAGLINES
  const tagline = OG_TAGLINES[locale] ?? OG_TAGLINES.nl

  return new ImageResponse(
    (
      <div
        style={{
          width: '1200px',
          height: '630px',
          background: OG_THEME.bgDeep,
          color: OG_THEME.textPrimary,
          display: 'flex',
          flexDirection: 'column',
          padding: '64px',
          fontFamily: 'sans-serif',
        }}
      >
        <div style={{ fontSize: 32, color: OG_THEME.accentSystem }}>future-marketing.ai</div>
        <div
          style={{
            flex: 1,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
          }}
        >
          <div style={{ fontSize: 180, fontWeight: 700, color: OG_THEME.accentSystem }}>Clyde</div>
          <div style={{ fontSize: 56, color: OG_THEME.textPrimary, marginTop: 16 }}>
            {tagline}
          </div>
        </div>
      </div>
    ),
    {
      width: 1200,
      height: 630,
      headers: {
        'cache-control': 's-maxage=31536000, stale-while-revalidate',
      },
    }
  )
}
```

Usage: `/api/og?locale=nl` → teal Clyde card with NL tagline. `/api/og?locale=es` → ES tagline. Fallback to NL on unknown locale.

Note: next-intl metadata per-locale can switch between static `/og-image.png` and dynamic `/api/og?locale=XX` — defer that metadata-config decision to a follow-up; Task 9 only ships the route.
  </action>
  <verify>
    <automated>cd fmai-nextjs && npx tsc --noEmit 2>&1 | head -10 && echo "---" && npm run dev &> /dev/null & sleep 5 && curl -sI http://localhost:3000/api/og?locale=nl | head -3; kill %1 2>/dev/null; true</automated>
  </verify>
  <done>/api/og?locale=nl returns 200 with content-type image/png. Dynamic OG image available but not yet wired into metadata (static og-image.png still served for now).</done>
</task>

</tasks>

<verification>
- `cd fmai-nextjs && npm run build` — must succeed with zero new errors
- `cd fmai-nextjs && npm run check:palette` — must exit 0
- `grep -rnE "#050814|#00D4FF|#A855F7|#0A0E27" fmai-nextjs/src/` — zero hits
- `test -f fmai-nextjs/public/og-image.png && test -f fmai-nextjs/public/logo.png` — assets present
- Manual: view `/nl/not-found-test-url` in dev — 404 renders teal
- Manual: throw intentional error on a test route — error page renders teal, no purple, localized copy
- Manual: LinkedIn Post Inspector preview — og-image renders after deploy
</verification>

<success_criteria>
All 4 stale hex values (`#050814`, `#00D4FF`, `#A855F7`, `#0A0E27`) purged from `fmai-nextjs/src/`. `og-image.png` + `logo.png` shipped. `error.tsx` fully localized. `fmai-nextjs/CLAUDE.md` Theme section accurate. Regression script in place. Optional `/api/og` route shipped if time allows.
</success_criteria>

<output>
After completion, create `.planning/phases/12-brand-copy-polish/12-01-SUMMARY.md` documenting:
- og-image dimensions + file size
- logo file size
- Total hex replacements made (expect ~25-30 refs across ~12 files)
- Whether Task 9 (dynamic OG) shipped or deferred
- Any false-positive hex refs flagged by `verify-palette.sh` that were allow-listed
</output>
