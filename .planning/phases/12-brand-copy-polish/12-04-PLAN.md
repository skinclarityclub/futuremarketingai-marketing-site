---
phase: 12-brand-copy-polish
plan: 04
type: execute
wave: 2
depends_on: [12-03]
files_modified:
  - fmai-nextjs/messages/nl.json
  - fmai-nextjs/messages/en.json
  - fmai-nextjs/messages/es.json
  - fmai-nextjs/src/app/[locale]/layout.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/pricing/page.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/about/page.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/contact/page.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/founding-member/page.tsx
autonomous: true
requirements:
  - WEB-01

must_haves:
  truths:
    - '8 "klanten" → "merken" rewrites applied in messages/nl.json'
    - 'Credit pack "Onbeperkt" renamed to "Max" (or alternative Daley confirmed)'
    - '11 IK/WIJ slips rewritten in about + contact + founding-member'
    - '"Boek een strategiegesprek" renamed to "Plan een gesprek" in about.cta.demo_button'
    - '2 "Feature-verzoeken" → "verzoeken voor nieuwe vaardigheden"'
    - '2 skills-clyde glossary slips rewritten (losse tools → uitvoerders/schermen)'
    - 'MAX_PARTNERS_PER_YEAR interpolated via defaultTranslationValues at NextIntlClientProvider'
    - '8 message keys use {maxPartners} variable'
    - 'legal.last_updated shows "24 april 2026" (NL), "April 24, 2026" (EN), "24 de abril de 2026" (ES)'
  artifacts:
    - path: 'fmai-nextjs/src/app/[locale]/layout.tsx'
      provides: 'NextIntlClientProvider configured with defaultTranslationValues: { maxPartners: MAX_PARTNERS_PER_YEAR }'
    - path: 'fmai-nextjs/messages/nl.json'
      provides: 'Glossary-compliant NL content with merken, merkstem, IK-voice across about/contact/founding-member'
  key_links:
    - from: 'fmai-nextjs/src/app/[locale]/layout.tsx'
      to: 'fmai-nextjs/src/lib/constants.ts'
      via: 'MAX_PARTNERS_PER_YEAR import + defaultTranslationValues'
      pattern: 'defaultTranslationValues.*maxPartners'
---

<objective>
Close all remaining copy-glossary violations, IK/WIJ slips, and interpolation gaps flagged by audit 02 + 06.

Purpose: Brand voice is 84/100 NL-strong but leaks in predictable places — apply form uses "klanten" instead of "merken" on the most conversion-critical page, the "Onbeperkt" credit pack (15k credits) contradicts the transparency claim, about/contact/founding-member drop out of IK-voice into WIJ or third-person, and "MAX_PARTNERS_PER_YEAR=20" is baked into 8 message keys that are hard to keep in sync when the cap changes. Legal dates stale.

Output: glossary-clean NL copy, IK-voice consistent on Daley-voice pages, interpolation for max-partners cap, legal dates bumped.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-brand-copy-polish/README.md
@.planning/phases/12-brand-copy-polish/RESEARCH.md
@docs/audits/2026-04-24-full-audit/02-copywriting.md
@fmai-nextjs/messages/nl.json
@fmai-nextjs/src/lib/constants.ts
@fmai-nextjs/src/app/[locale]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 8x klanten → merken rewrites in messages/nl.json</name>
  <files>fmai-nextjs/messages/nl.json</files>
  <action>
Apply 8 exact `klanten` → `merken` rewrites per audit 02 §2.4. Each edit is a single-line replacement using the Edit tool.

| Key path | File:Line | Current | Should be |
|---|---|---|---|
| `pricing.tiers.growth.description` | `nl.json:568` | `"Voor bureaus met 1 tot 5 klanten..."` | `"Voor bureaus met 1 tot 5 merken in hun portfolio..."` |
| `pricing.tiers.professional.description` | `nl.json:587` | `"Voor bureaus met 5 tot 15 klanten..."` | `"Voor bureaus met 5 tot 15 merken in hun portfolio..."` |
| `pricing.tiers.enterprise.description` | `nl.json:606` | `"Voor bureaus met 15 of meer klanten die Clyde als kern-infrastructuur..."` | `"Voor bureaus met 15 of meer merken die Clyde als kern-infrastructuur..."` |
| `memory.isolation.body1` | `nl.json:1461` | `"Clyde werkt voor meerdere klanten en meerdere merken tegelijk..."` | `"Clyde werkt voor meerdere bureaus en meerdere merken tegelijk..."` |
| `apply.form.clientCountLabel` | `nl.json:1544` | `"Aantal actieve klanten"` | `"Aantal actieve merken in je portfolio"` |
| `apply.form.clientCountOptions.1-5` | `nl.json:1545` | `"1 tot 5 klanten"` | `"1 tot 5 merken"` |
| `apply.form.clientCountOptions.5-15` | `nl.json:1546` | `"5 tot 15 klanten"` | `"5 tot 15 merken"` |
| `apply.form.clientCountOptions.15-30` | `nl.json:1547` | `"15 tot 30 klanten"` | `"15 tot 30 merken"` |
| `apply.form.clientCountOptions.30+` | `nl.json:1548` | `"30+ klanten"` | `"30+ merken"` |
| `apply.form.clientCountOptions.unsure` | `nl.json:1549,1550` | (check — may say "aantal klanten" in text) | replace "klanten" with "merken" if present |
| `apply.form.problemPlaceholder` | `nl.json:1562` | `'...voor 15 klanten...'` | `'...voor 15 merken...'` |
| `case_studies.skc.cta.subtitle` | `nl.json:1657` | `"...bij jouw merken en klanten."` | `"...bij jouw bureau en merkenportfolio."` |
| `skills-email-management.useCase1.body` | `nl.json:1719` | `"Met 15 klanten krijg je al snel 100+ mails per dag."` | `"Met 15 merken in beheer krijg je al snel 100+ mails per dag."` |

**Note**: line numbers are from audit — actual positions may shift. Use search-based Edit calls with enough surrounding context to disambiguate.

**Do NOT touch**:
- `pricing.tiers.founding.description` (:624) — "oprichtende klanten" refers to FMai's customers (bureaus), not agencies' clients. Correct usage.
- `home.trust.clientCount` (if exists) — any context where FMai talks about its own customers.

**Double-apply in chatbots.* namespace** (not orphan — see RESEARCH §3):

| Key path | Current | Should be |
|---|---|---|
| `chatbots.related.automations` (`nl.json:257`) | `"...Automatisering op maat die jouw tools en processen aan elkaar verbindt."` | `"...Automatisering op maat die jouw systemen en processen aan elkaar verbindt."` |
| `chatbots.process.discovery` (`nl.json:268`) | `"We brengen je use case in kaart en plannen de koppeling met je bestaande tools."` | `"We brengen je use case in kaart en plannen de koppeling met je bestaande systemen."` |

Total NL rewrites in Task 1: **~14 edits**.
  </action>
  <verify>
    <automated>cd fmai-nextjs && grep -c '"[^"]*klanten[^"]*"' messages/nl.json && echo "---above count should be low (allowed: founding.description use and any FMai-own-customer refs only)" && ! grep -E 'apply\\.form.*klanten|"1 tot 5 klanten"|"15 tot 30 klanten"' messages/nl.json && echo "PASS: 8 apply + descriptions no longer say klanten" || echo "FAIL"</automated>
  </verify>
  <done>Audit 02 §2.4 "klanten" schendingen eliminated. FMai's own-customer refs preserved. chatbots.* glossary also cleaned.</done>
</task>

<task type="auto">
  <name>Task 2: Rename credit pack Onbeperkt → Max (default) in 3 locales</name>
  <files>fmai-nextjs/messages/nl.json, fmai-nextjs/messages/en.json, fmai-nextjs/messages/es.json</files>
  <action>
Rename the 15,000-credit pack's display NAME (not the key). Default choice per RESEARCH §7: `"Max"`. If Daley prefers `"Scale XL"` or `"Ultra"`, substitute throughout.

**NL** (`messages/nl.json`):
| Key path | File:Line | Current | Should be |
|---|---|---|---|
| `pricing.creditPacks.items.unlimited.name` | `:668` | `"Onbeperkt"` | `"Max"` |
| `pricing.faq.q2.answer` | `:719` | `"...Partner Top-Up €39 tot Onbeperkt €697)"` | `"...Partner Top-Up €39 tot Max €697)"` |

**EN** (`messages/en.json`):
| Key path | Current | Should be |
|---|---|---|
| `pricing.creditPacks.items.unlimited.name` | `"Unlimited"` | `"Max"` |
| `pricing.faq.q2.answer` | any `Unlimited €697` ref | `Max €697` |

**ES** (`messages/es.json`):
| Key path | Current | Should be |
|---|---|---|
| `pricing.creditPacks.items.unlimited.name` | `"Ilimitado"` | `"Max"` |
| `pricing.faq.q2.answer` | `Ilimitado €697` | `Max €697` |

**Do NOT rename the JSON key** (`items.unlimited` stays as `items.unlimited`). Only the `.name` value changes. Renaming the key would touch Stripe webhook integration in `fma-app`.

**Cross-reference check** (grep-based):
```bash
grep -rn "Onbeperkt €697\|Unlimited €697\|Ilimitado €697" messages/
```

Any hits not in the 2 keys above → update.

**Post-execution Stripe-side note for Daley**: the Stripe product name in the dashboard may still say `"Unlimited Credit Pack"`. Daley should rename to `"Max Credit Pack"` via Stripe dashboard to keep invoices consistent. Add to `DECISION-PENDING-credit-pack-name.md` if Daley hasn't confirmed.
  </action>
  <verify>
    <automated>cd fmai-nextjs && jq -r '.pricing.creditPacks.items.unlimited.name' messages/nl.json messages/en.json messages/es.json | grep -q "Max" && echo "PASS: credit pack renamed across 3 locales" || echo "FAIL"</automated>
  </verify>
  <done>Credit pack shows "Max" in all 3 locales. Pricing FAQ answer mentions "Max €697". JSON key unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: 11 IK/WIJ rewrites in about + contact + founding-member</name>
  <files>fmai-nextjs/messages/nl.json</files>
  <action>
Enforce IK-voice across `about.*`, `contact.*`, `founding-member.*` namespaces per audit 02 §3 / Appendix E.

**about** (7 rewrites):

| Key path | File:Line | Current | Should be |
|---|---|---|---|
| `about.mission.heading` | `:440` | `"Onze missie"` | `"Mijn missie"` |
| `about.infra.selfHosted.body` | `:494` | `"...we bouwen transparante pipelines die we samen bijstellen."` | `"...ik bouw transparante pipelines die we samen bijstellen."` |
| `about.capacity.body` | `:511` | `"We kiezen kwaliteit boven volume... We kijken of..."` | `"Ik kies kwaliteit boven volume... Ik kijk of..."` |
| `about.capacity.body` | `:511` (same key, second sentence) | `"Daley begeleidt elke nieuwe klant persoonlijk..."` | `"Ik begeleid elk nieuw bureau persoonlijk..."` |
| `about.capacity.reasoning` | `:512` | `"We kiezen kwaliteit boven volume..."` | `"Ik kies kwaliteit boven volume..."` |
| `about.infra.title` | `:490` | `"Onze infrastructuur"` | `"Mijn infrastructuur"` (alternative: `"De infrastructuur"`) |
| `about.cta.demo_button` | `:472` | `"Boek een strategiegesprek"` | `"Plan een gesprek"` |

**contact** (2 rewrites):

| Key path | File:Line | Current | Should be |
|---|---|---|---|
| `contact.form.message_placeholder` | `:811` | `"Vertel ons over je bureau..."` | `"Vertel me over je bureau..."` |
| `contact.applyCallout.body` | `:830` | `"...Daley leest elke aanvraag persoonlijk..."` | `"...ik lees elke aanvraag persoonlijk..."` |

**founding-member** (3 rewrites):

| Key path | File:Line | Current | Should be |
|---|---|---|---|
| `founding-member.value.line1` | `:1345` | `"Founding partners zijn de bureaus waarmee we Clyde samen vormgeven."` | `"Founding partners zijn de bureaus waarmee ik Clyde samen vormgeef."` |
| `founding-member.benefits.community.description` | `:1370` | `"Directe Slack-ruimte met Daley en de andere 9 Founding partners."` | `"Directe Slack-ruimte met mij en de andere 9 Founding partners."` |
| `founding-member.cta.subtitle` | `:1410` | `"...reserveren we je plek."` | `"...reserveer ik je plek."` |

**Template-repetition cleanup on 12 skill pages** (audit 02 §4.9 + Appendix G) — OPTIONAL POLISH, include if time permits:

- Vary `integrations.subtitle` per skill (12 identical strings today) — 12 rewrites.
- Vary `allocation.subtitle` per skill — 12 rewrites.
- Vary `cta.subtitle` per skill — 12 rewrites (11 + 1 variant).

**For Phase 12, EXCLUDE the template-repetition cleanup from the must_haves.** Add as a stretch goal only if Tasks 1-6 of this plan finish with time to spare. Rationale: it's low-conversion-impact polish, and the risk of introducing translation drift (NL correct, EN missed) is higher than the benefit. Defer to a dedicated polish sprint.

**Total REQUIRED edits in Task 3: 12 IK/WIJ rewrites + 1 CTA rename = 13 edits**.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -E '"Onze missie"|"Boek een strategiegesprek"|"Daley leest elke aanvraag"|"Daley begeleidt elke"' messages/nl.json && grep -q '"Mijn missie"' messages/nl.json && echo "PASS: IK-slips fixed" || echo "FAIL"</automated>
  </verify>
  <done>13 IK/WIJ + CTA rewrites applied. No "Onze missie" / "Boek een strategiegesprek" / third-person "Daley" on IK-context pages.</done>
</task>

<task type="auto">
  <name>Task 4: Glossary cleanups (Feature-verzoeken + losse tools in skills-clyde)</name>
  <files>fmai-nextjs/messages/nl.json</files>
  <action>
Small glossary fixes per audit 02 §2.3 + §2.1.

**"Feature-verzoeken" → "verzoeken voor nieuwe vaardigheden"** (2 rewrites):

| Key path | File:Line | Current | Should be |
|---|---|---|---|
| `pricing.tiers.enterprise.features_7` | `:614` | `"Voorrang op feature-requests"` | `"Voorrang op verzoeken voor nieuwe vaardigheden"` |
| `founding-member.benefits.priority.description` | `:1358` | `"Feature-verzoeken van Founding partners gaan voor."` | `"Verzoeken voor nieuwe vaardigheden van Founding partners krijgen voorrang."` |

**skills-clyde "losse tools" → "uitvoerders" / "schermen"** (2 rewrites):

| Key path | File:Line | Current | Should be |
|---|---|---|---|
| `skills-clyde.hero.subtitle` | `:1966` | `"De andere 11 vaardigheden zijn losse tools. Clyde is de medewerker die ze orkestreert."` | `"De andere 11 vaardigheden zijn gespecialiseerde uitvoerders. Clyde is de medewerker die ze orkestreert."` |
| `skills-clyde.useCases.useCase1` | `:2015` | `"In plaats van 12 tools openen: vraag Clyde in de chat."` | `"In plaats van 12 schermen openen: vraag Clyde in de chat."` |

**ES mechanical fixes** (from RESEARCH §6):

| Key path | Current | Should be |
|---|---|---|
| `home.badges.gdpr` (es.json:95) | `"IA con RGPD primero"` | `"IA con RGPD por defecto"` |
| `home.faq.q3.answer` (es.json:122) | `"Clyde es una partnership AI high-touch, no una herramienta SaaS."` | `"Clyde es una asociacion AI high-touch, no una herramienta SaaS."` |

**EN contraction polish** (from RESEARCH §6, audit 02 §10) — include 3 low-risk edits:

| Key path | Current | Should be |
|---|---|---|
| `home.faq.q3.answer` (en.json) | `"A generic sign-up does not fit what we deliver."` | `"A generic sign-up doesn't fit what we deliver."` |
| `home.icp.notFit1` (en.json) | `"The budget does not fit, and we do not want to slow your growth."` | `"The budget doesn't fit, and we don't want to slow your growth."` |
| `home.icp.subtitle` (en.json) | `"Honestly: we would rather not work with everyone."` | `"Honestly, we'd rather not work with everyone."` |

**Total Task 4 edits: 4 NL + 2 ES + 3 EN = 9 edits**.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -E '"feature-requests"|"Feature-verzoeken"' messages/nl.json && ! grep "losse tools" messages/nl.json | head -1 && grep -q "gespecialiseerde uitvoerders" messages/nl.json && echo "PASS: glossary slips fixed" || echo "FAIL"</automated>
  </verify>
  <done>Feature-verzoeken rewritten. skills-clyde losse-tools slips cleaned. ES + EN polish applied.</done>
</task>

<task type="auto">
  <name>Task 5: MAX_PARTNERS_PER_YEAR interpolation via defaultTranslationValues</name>
  <files>fmai-nextjs/src/app/[locale]/layout.tsx, fmai-nextjs/messages/nl.json, fmai-nextjs/messages/en.json, fmai-nextjs/messages/es.json</files>
  <action>
**Step A: wire defaultTranslationValues** at `NextIntlClientProvider` in `src/app/[locale]/layout.tsx`:

Read layout.tsx first. Identify the `<NextIntlClientProvider>` wrapper. Add `defaultTranslationValues` prop:

```tsx
import { MAX_PARTNERS_PER_YEAR } from '@/lib/constants'

// ...inside layout:
<NextIntlClientProvider
  messages={messages}
  locale={locale}
  defaultTranslationValues={{
    maxPartners: MAX_PARTNERS_PER_YEAR,
    // preserve any existing defaults for FOUNDING_SPOTS_TAKEN/TOTAL if present
  }}
>
```

**Also check server-side `getTranslations` usage**: next-intl v4.x reads `defaultTranslationValues` from the same config. If `src/i18n/request.ts` defines the message config, add defaults there too. Otherwise rely on the client provider prop.

**Step B: rewrite 8 message keys** to use `{maxPartners}` variable.

**NL** (`messages/nl.json`):

| Key path | File:Line | Current | Should be |
|---|---|---|---|
| `home.hero.trustAnchor` | `:12` | `"Gebouwd met Nederlandse bureaus in de praktijk. Maximaal 20 partners per jaar."` | `"Gebouwd met Nederlandse bureaus in de praktijk. Maximaal {maxPartners} partners per jaar."` |
| `home.trust.founderAccess` | `:55` | `"Maximaal 20 bureaus per jaar..."` | `"Maximaal {maxPartners} bureaus per jaar..."` |
| `home.cta.subtitle` | `:104` | `"Maximaal 20 bureaus per jaar. {taken} van {total} founding plekken bezet..."` | `"Maximaal {maxPartners} bureaus per jaar. {taken} van {total} founding plekken bezet..."` |
| `about.capacity.reasoning` | `:512` | `"We kiezen kwaliteit boven volume. Liever 20 bureaus..."` → this already became IK-voice in Task 3, so combine: | `"Ik kies kwaliteit boven volume. Liever {maxPartners} bureaus waar Clyde echt meerwaarde levert, dan 200 waar ik niet genoeg aandacht aan kan geven. De wachtlijst schaalt mee met de vraag. Zitten we vol, dan plannen we een gesprek voor het volgende cohort."` |
| `pricing.hero.description` | `:524` | `"...Geen verborgen kosten, geen self-service. Maximaal 20 bureaus per jaar."` | `"...Geen verborgen kosten, geen self-service. Maximaal {maxPartners} bureaus per jaar."` |
| `pricing.cta.description` | `:737` | `"Maximaal 20 bureaus per jaar. Plan een gesprek..."` | `"Maximaal {maxPartners} bureaus per jaar. Plan een gesprek..."` |
| `apply.meta.description` | `:1503` | `"Aanmelden voor een partnership-gesprek. Maximaal 20 bureaus per jaar..."` | `"Aanmelden voor een partnership-gesprek. Maximaal {maxPartners} bureaus per jaar..."` |

**Note on `about.capacity.reasoning`** — the string mentions `20` TWICE (first "Liever 20 bureaus", second "dan 200 waar"). Only the FIRST occurrence gets interpolated; the second stays as literal `200` (rhetorical reference to an impossibly-large number, not the actual cap).

**EN** (`messages/en.json`) — same 7 key paths, replace `20` → `{maxPartners}`:

Sample:
| Key path | Current | Should be |
|---|---|---|
| `home.hero.trustAnchor` | `"Max 20 partners per year."` | `"Max {maxPartners} partners per year."` |
| `home.trust.founderAccess` | `"Max 20 agencies per year..."` | `"Max {maxPartners} agencies per year..."` |
| `home.cta.subtitle` | `"Max 20 agencies per year. {taken} of {total} founding spots taken..."` | `"Max {maxPartners} agencies per year. {taken} of {total} founding spots taken..."` |
| `pricing.hero.description` | `"...Max 20 agencies per year."` | `"...Max {maxPartners} agencies per year."` |
| `pricing.cta.description` | `"Max 20 agencies per year..."` | `"Max {maxPartners} agencies per year..."` |
| `apply.meta.description` | `"Apply for a partnership call. Max 20 agencies per year..."` | `"Apply for a partnership call. Max {maxPartners} agencies per year..."` |
| `about.capacity.reasoning` | `"Better 20 agencies where Clyde truly adds value than 200..."` | `"Better {maxPartners} agencies where Clyde truly adds value than 200..."` |

**ES** (`messages/es.json`) — same pattern:
| Key path | Current | Should be |
|---|---|---|
| `home.hero.trustAnchor` | `"Max. 20 partners al ano."` | `"Max. {maxPartners} partners al ano."` |
| rest analogous | | |

**Grep sweep** to confirm no more hardcoded `20 bureaus|20 agencies|20 partners` remain:
```bash
grep -nE '20 (bureaus|agencies|partners|agencias)' messages/
```
Only the `200` rhetorical references should remain.

**Total Task 5 edits: 1 layout.tsx (+ optionally i18n/request.ts) + 7 keys × 3 locales = 22 edits**.
  </action>
  <verify>
    <automated>cd fmai-nextjs && grep -q "MAX_PARTNERS_PER_YEAR" src/app/[locale]/layout.tsx && grep -q "defaultTranslationValues" src/app/[locale]/layout.tsx && grep -q "{maxPartners}" messages/nl.json && grep -q "{maxPartners}" messages/en.json && grep -q "{maxPartners}" messages/es.json && ! grep -nE "Maximaal 20 bureaus|Max 20 agencies|Max\\. 20 partners" messages/ && echo "PASS: interpolation wired" || echo "FAIL"</automated>
  </verify>
  <done>defaultTranslationValues configured at layout. 7 message keys × 3 locales use {maxPartners}. Grep confirms no hardcoded "20" caps remain (except rhetorical "200").</done>
</task>

<task type="auto">
  <name>Task 6: Legal last_updated dates → 24 april 2026</name>
  <files>fmai-nextjs/messages/nl.json, fmai-nextjs/messages/en.json, fmai-nextjs/messages/es.json</files>
  <action>
Bump `legal.last_updated` per RESEARCH §9 (decision: localized long form).

| Locale | File:Line | Current | Should be |
|---|---|---|---|
| NL | `messages/nl.json:928` | `"Laatst bijgewerkt: maart 2026"` | `"Laatst bijgewerkt: 24 april 2026"` |
| EN | `messages/en.json:928` | `"Last updated: March 2026"` | `"Last updated: April 24, 2026"` |
| ES | `messages/es.json:928` | `"Ultima actualizacion: marzo de 2026"` | `"Ultima actualizacion: 24 de abril de 2026"` |

**Note**: audit 06 says dates live at the key `legal.last_updated` (single key, reused on /legal/privacy, /legal/terms, /legal/cookies). Verify by reading the 3 legal page components. If the date is duplicated per subpage (e.g. `legal.privacy.last_updated` + `legal.terms.last_updated` + `legal.cookies.last_updated`), update all variants.

If Daley wants ISO format (`2026-04-24`) instead — update accordingly. Default per RESEARCH §9 is localized long form.
  </action>
  <verify>
    <automated>cd fmai-nextjs && ! grep -E "maart 2026|March 2026|marzo de 2026" messages/ && grep -q "24 april 2026" messages/nl.json && grep -q "April 24, 2026" messages/en.json && grep -q "24 de abril de 2026" messages/es.json && echo "PASS: legal dates current" || echo "FAIL"</automated>
  </verify>
  <done>3 legal date values bumped to 2026-04-24 localized.</done>
</task>

<task type="auto">
  <name>Task 7: Full regression build + multi-locale smoke test</name>
  <files></files>
  <action>
Final verification across all 4 plans.

**Step A: build**:
```bash
cd fmai-nextjs
npm run build
```
Expect: zero errors, zero new warnings beyond baseline.

**Step B: i18n diff check** — confirm 3 locales have same key structure:
```bash
cd fmai-nextjs
jq -S '.' messages/nl.json > /tmp/nl-sorted.json
jq -S '.' messages/en.json > /tmp/en-sorted.json
jq -S '.' messages/es.json > /tmp/es-sorted.json
# Compare key structures (values will differ — keys must match):
diff <(jq 'paths | join(".")' messages/nl.json | sort -u) <(jq 'paths | join(".")' messages/en.json | sort -u) | head -40
```
Expect: no orphan keys (keys in one locale but not another). If diff shows keys missing in EN or ES → add them.

**Step C: dev-server multi-locale sweep** — visit:
- `http://localhost:3000/nl/` — scroll home. Look for: IK-voice on about teaser, "Max 20" is now `Maximaal {maxPartners}` with actual `20` rendered (not literal `{maxPartners}` — means interpolation worked).
- `http://localhost:3000/nl/pricing` — credit pack card says "Max" not "Onbeperkt".
- `http://localhost:3000/nl/apply` — form labels say "merken" not "klanten".
- `http://localhost:3000/nl/about` — "Mijn missie", "Ik begeleid", "Plan een gesprek".
- `http://localhost:3000/nl/contact` — "Vertel me over je bureau".
- `http://localhost:3000/nl/founding-member` — "ik Clyde samen vormgeef", "Slack-ruimte met mij".
- `http://localhost:3000/nl/legal/privacy` — "Laatst bijgewerkt: 24 april 2026".
- Repeat for `/en/` and `/es/` — spot check counterparts.

**Capture screenshots** to `test-results/phase-12/`:
- `pricing-credit-pack-{nl,en,es}.png` (zoom into credit-pack grid)
- `apply-form-nl.png`
- `about-nl.png`
- `legal-privacy-{nl,en,es}.png`

**Step D: i18n missing-key audit** — open dev-tools console on each page above. No missing-key warnings from next-intl.
  </action>
  <verify>
    <automated>cd fmai-nextjs && npm run build 2>&1 | tail -15 | grep -q "Compiled successfully" && echo "PASS: build green" || echo "FAIL"</automated>
    <manual>
      - Screenshots show "Max" credit pack, "merken" apply form, IK-voice about, current legal date in 3 locales.
      - Zero console warnings about missing i18n keys.
    </manual>
  </verify>
  <done>Build green. 3-locale spot-checks confirm all 12-04 changes render live. Screenshots captured.</done>
</task>

</tasks>

<verification>
- `npm run build` green, no new errors
- `grep 'klanten' messages/nl.json` returns <=5 hits (only FMai-own-customer refs)
- `grep 'Onbeperkt' messages/nl.json | grep creditPacks` returns 0 hits
- `grep '"Boek een strategiegesprek"' messages/nl.json` returns 0 hits
- `grep '"Onze missie"' messages/nl.json` returns 0 hits
- `grep '{maxPartners}' messages/{nl,en,es}.json` returns 7 hits per locale
- `grep 'MAX_PARTNERS_PER_YEAR' src/app/[locale]/layout.tsx` returns 1 hit
- `grep -E 'maart 2026|March 2026|marzo de 2026' messages/` returns 0 hits
- `grep '24 april 2026' messages/nl.json` returns 1 hit
- Dev-server screenshots captured in test-results/phase-12/
</verification>

<success_criteria>
Glossary compliance enforced (merken, merkstem, IK-voice). Credit pack renamed "Max". MAX_PARTNERS_PER_YEAR interpolated via single-source defaultTranslationValues. Legal dates current. 3 locales render correctly with zero missing-key warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/12-brand-copy-polish/12-04-SUMMARY.md` with:
- Total NL edits: expected ~30 (14 klanten→merken including chatbots.*, 13 IK/WIJ+CTA, 4 glossary cleanup, 7 {maxPartners}, 1 legal date, 2 credit pack)
- Total EN edits: expected ~13 (3 contractions, 7 {maxPartners}, 1 legal date, 2 credit pack)
- Total ES edits: expected ~12 (2 ES mechanical, 7 {maxPartners}, 1 legal date, 2 credit pack)
- Template-repetition status (did we ship 12 skill page variants, or defer?)
- Credit pack final name (Max unless Daley overrode)
- Legal date format used (localized long form unless Daley overrode to ISO)
- Any orphan-key warnings surfaced by jq diff check
</output>
