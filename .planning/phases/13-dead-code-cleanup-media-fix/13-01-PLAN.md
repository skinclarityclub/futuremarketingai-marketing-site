---
phase: 13-dead-code-cleanup-media-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/common/SystemPanel.tsx
  - src/components/common/StatusIndicator.tsx
  - src/components/common/MetricDisplay.tsx
  - src/components/common/SectionContainer.tsx
  - src/components/common/index.ts
  - src/hooks/useTilt.ts
autonomous: true
requirements:
  - REQ-COMPONENTS
must_haves:
  truths:
    - 'SystemPanel, StatusIndicator, MetricDisplay, SectionContainer files no longer exist in src/components/common/'
    - 'useTilt.ts no longer exists in src/hooks/'
    - 'Barrel export index.ts no longer references deleted components'
    - 'CTAButton, GlassCard, and all other active components remain exported and functional'
    - 'vite build succeeds with zero import errors'
  artifacts:
    - path: 'src/components/common/index.ts'
      provides: 'Updated barrel export without orphaned components'
  key_links:
    - from: 'src/components/common/index.ts'
      to: 'src/components/common/CTAButton.tsx'
      via: 'export statement'
      pattern: 'export.*CTAButton'
---

<objective>
Delete 4 orphaned Phase 3 shared components (SystemPanel, StatusIndicator, MetricDisplay, SectionContainer) and the unused useTilt hook. Clean barrel exports.

Purpose: Remove dead code superseded by Phase 11 Living System rebuild (inline Tailwind patterns) and Phase 12-02 (CSS card-tilt class). Reduces maintenance surface and eliminates confusion about which components are active.
Output: 5 files deleted, barrel export cleaned, build passes.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-dead-code-cleanup-media-fix/13-RESEARCH.md
@src/components/common/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned component files and useTilt hook</name>
  <files>
    src/components/common/SystemPanel.tsx
    src/components/common/StatusIndicator.tsx
    src/components/common/MetricDisplay.tsx
    src/components/common/SectionContainer.tsx
    src/hooks/useTilt.ts
  </files>
  <action>
Before deleting, run a safety grep for each file to confirm zero external imports:

```bash
grep -r "SystemPanel" src/ --include="*.tsx" --include="*.ts" | grep -v "SystemPanel.tsx" | grep -v "index.ts"
grep -r "StatusIndicator" src/ --include="*.tsx" --include="*.ts" | grep -v "StatusIndicator.tsx" | grep -v "index.ts" | grep -v "SystemHealthBar.tsx"
grep -r "MetricDisplay" src/ --include="*.tsx" --include="*.ts" | grep -v "MetricDisplay.tsx" | grep -v "index.ts"
grep -r "SectionContainer" src/ --include="*.tsx" --include="*.ts" | grep -v "SectionContainer.tsx" | grep -v "index.ts"
grep -r "useTilt" src/ --include="*.tsx" --include="*.ts" | grep -v "useTilt.ts"
```

If any grep returns results (unexpected imports), STOP and investigate before deleting.

If all greps return empty (safe), delete all 5 files:

- `rm src/components/common/SystemPanel.tsx`
- `rm src/components/common/StatusIndicator.tsx`
- `rm src/components/common/MetricDisplay.tsx`
- `rm src/components/common/SectionContainer.tsx`
- `rm src/hooks/useTilt.ts`

IMPORTANT: Do NOT delete GlassCard.tsx — it is still imported by ~30 legacy command-center/calculator files.
IMPORTANT: Do NOT delete CTAButton.tsx — it is actively used across all pages.
IMPORTANT: src/hooks/index.ts does NOT export useTilt (already clean) — no change needed there.
</action>
<verify>
<automated>ls src/components/common/SystemPanel.tsx src/components/common/StatusIndicator.tsx src/components/common/MetricDisplay.tsx src/components/common/SectionContainer.tsx src/hooks/useTilt.ts 2>&1 | grep -c "No such file"</automated>
</verify>
<done>All 5 orphaned files deleted. No other files modified or deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Clean barrel exports in common/index.ts</name>
  <files>src/components/common/index.ts</files>
  <action>
Remove the 4 orphaned export lines from `src/components/common/index.ts`:

Remove these exact lines:

```
export { SystemPanel } from './SystemPanel'
export { StatusIndicator } from './StatusIndicator'
export { MetricDisplay } from './MetricDisplay'
export { SectionContainer } from './SectionContainer'
```

Keep all other exports intact, especially:

- `export { CTAButton } from './CTAButton'` — actively used
- `export { GlassCard } from './GlassCard'` — still imported by legacy pages
- All Phase 12 exports (SplineHero, ScrollReveal, ProductMedia)

After editing, run `npx vite build` to verify no broken imports exist. The build must succeed.
</action>
<verify>
<automated>cd "C:/Users/daley/Desktop/Futuremarketingai" && npx vite build 2>&1 | tail -5</automated>
</verify>
<done>Barrel export has no references to deleted components. `vite build` succeeds without import errors.</done>
</task>

</tasks>

<verification>
1. `ls src/components/common/SystemPanel.tsx` returns "No such file"
2. `ls src/components/common/StatusIndicator.tsx` returns "No such file"
3. `ls src/components/common/MetricDisplay.tsx` returns "No such file"
4. `ls src/components/common/SectionContainer.tsx` returns "No such file"
5. `ls src/hooks/useTilt.ts` returns "No such file"
6. `grep -c "SystemPanel\|StatusIndicator\|MetricDisplay\|SectionContainer" src/components/common/index.ts` returns 0
7. `grep "CTAButton" src/components/common/index.ts` still present
8. `npx vite build` exits 0
</verification>

<success_criteria>

- 5 dead code files deleted (4 components + 1 hook)
- Barrel export cleaned (4 lines removed)
- CTAButton and GlassCard preserved
- Vite build passes with zero errors
  </success_criteria>

<output>
After completion, create `.planning/phases/13-dead-code-cleanup-media-fix/13-01-SUMMARY.md`
</output>
