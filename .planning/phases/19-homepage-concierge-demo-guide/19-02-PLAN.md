---
phase: 19-homepage-concierge-demo-guide
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/hooks/useCalendlyBooking.ts
  - src/components/index.ts
  - src/hooks/index.ts
  - src/stores/index.ts
  - src/i18n/config.ts
  - src/App.tsx
  - src/pages/Explorer.tsx
  - src/components/common/StrategicCTA.tsx
autonomous: true
requirements:
  - REQ-ARIA-CLEANUP

must_haves:
  truths:
    - 'No imports reference any file in src/components/ai-assistant/'
    - 'No imports reference old stores (chatStore, journeyStore) from non-deleted files'
    - 'useCalendlyBooking works without journeyStore dependency'
    - 'Explorer page loads without useModuleFollowUp'
    - 'StrategicCTA works without FloatingElementContext'
    - 'Build succeeds with zero errors after all deletions'
    - 'openai package not in package.json (verify or remove)'
    - 'api/chat.ts deleted (old OpenAI proxy)'
  artifacts:
    - path: 'src/hooks/useCalendlyBooking.ts'
      provides: 'Calendly booking without journeyStore dependency'
    - path: 'src/components/index.ts'
      provides: 'Clean barrel without ai-assistant exports'
    - path: 'src/hooks/index.ts'
      provides: 'Clean barrel without ARIA hook exports'
  key_links:
    - from: 'src/pages/Explorer.tsx'
      to: 'src/hooks/useModuleFollowUp.ts'
      via: 'Import must be REMOVED'
      pattern: 'useModuleFollowUp'
    - from: 'src/hooks/useCalendlyBooking.ts'
      to: 'src/stores/journeyStore.ts'
      via: 'Import must be REMOVED and replaced with hardcoded default'
      pattern: 'journeyStore'
---

<objective>
Systematically remove all old ARIA code: refactor shared dependencies first, then delete ARIA components/hooks/stores/configs/utils/API, clean barrel exports, remove i18n namespace, remove FloatingElementContext. Follow safe deletion order to prevent build breakage.

Purpose: Complete clean removal of the legacy ARIA system now that the new persona-driven chatbot is wired in.
Output: Zero ARIA code remaining, clean build, all non-ARIA pages still functional.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-homepage-concierge-demo-guide/19-01-SUMMARY.md
@src/App.tsx
@src/hooks/useCalendlyBooking.ts
@src/components/index.ts
@src/hooks/index.ts
@src/stores/index.ts
@src/pages/Explorer.tsx
@src/components/common/StrategicCTA.tsx
@src/contexts/FloatingElementContext.tsx
@src/i18n/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor shared dependencies before deletion</name>
  <files>src/hooks/useCalendlyBooking.ts, src/pages/Explorer.tsx, src/components/common/StrategicCTA.tsx, src/App.tsx</files>
  <action>
These files import from ARIA modules but are NOT part of ARIA themselves. They must be updated BEFORE deleting ARIA files.

1. **useCalendlyBooking.ts** -- Remove journeyStore dependency:
   - Remove: `import { useJourneyStore } from '../stores/journeyStore'`
   - The hook uses `completedSteps` and `timeOnSite` from journeyStore to compute a recommended Calendly event type via `getCalendlyEventTypeByJourney()`
   - Replace with hardcoded default: `const recommendedEventType = getCalendlyEventTypeByJourney('discovery')` -- this gives the standard discovery call event type
   - Remove the `const { completedSteps, timeOnSite } = useJourneyStore()` destructuring
   - Keep all other logic (personalizationStore usage, ICP scoring, etc.) intact

2. **Explorer.tsx** -- Remove useModuleFollowUp:
   - Remove: `import { useModuleFollowUp } from '../hooks/useModuleFollowUp'`
   - Remove: the `useModuleFollowUp()` call (around line 169)
   - Keep the `window.dispatchEvent(new CustomEvent('module-closed', ...))` calls -- these are harmless no-ops now and can be used later if demo-guide behavior is re-implemented
   - Verify no other ARIA imports exist in Explorer.tsx

3. **StrategicCTA.tsx** -- Remove FloatingElementContext dependency:
   - Remove: `import { useFloatingElement } from '../../contexts/FloatingElementContext'`
   - Remove the `useFloatingElement()` call and any logic that reads from it (e.g., checking if chat panel is open to hide CTA)
   - Replace with: check `chatbotStore` isOpen state if needed for conflict avoidance, OR simply remove the coordination logic (StrategicCTA can show independently of chat)
   - Import chatbotStore: `import { useChatbotStore } from '../../stores/chatbotStore'` if coordination needed

4. **App.tsx** -- Remove FloatingElementProvider:
   - Remove: `import { FloatingElementProvider } from './contexts/FloatingElementContext'`
   - Remove: `<FloatingElementProvider>` wrapper and its closing tag
   - Keep all other providers (HelmetProvider, ToastProvider, ErrorBoundary)

5. Run `npx tsc --noEmit` to verify all refactored files compile before proceeding to deletions.
   </action>
   <verify>`npx tsc --noEmit` passes. Explorer page loads. useCalendlyBooking compiles without journeyStore. StrategicCTA compiles without FloatingElementContext.</verify>
   <done>All non-ARIA files no longer import from ARIA modules. Safe to delete ARIA files.</done>
   </task>

<task type="auto">
  <name>Task 2: Delete all ARIA files, clean barrels, clean i18n</name>
  <files>src/components/index.ts, src/hooks/index.ts, src/stores/index.ts, src/i18n/config.ts</files>
  <action>
Follow this EXACT deletion order. Run `npx tsc --noEmit` after each group.

**Group A: Delete ARIA components (entire directory)**

```
rm -rf src/components/ai-assistant/
```

**Group B: Clean components barrel**
In `src/components/index.ts`:

- Remove: `export { default as AIJourneyAssistant } from './ai-assistant/AIJourneyAssistant'`
- Remove: `export * from './ai-assistant'`
- Remove any comment line referencing "AI Journey Assistant"

Run `npx tsc --noEmit` -- verify no errors.

**Group C: Delete ARIA-only hooks**

```
rm src/hooks/useModuleFollowUp.ts
rm src/hooks/useJourneyNudges.ts
rm src/hooks/useAchievementTracking.ts
```

Clean `src/hooks/index.ts`:

- Remove: `export { useModuleFollowUp } from './useModuleFollowUp'`
- Remove: `export { useAchievementTracking } from './useAchievementTracking'`
- Remove: `export { useJourneyNudges } from './useJourneyNudges'`

Run `npx tsc --noEmit` -- verify no errors.

**Group D: Delete ARIA-only stores**

```
rm src/stores/chatStore.ts
rm src/stores/journeyStore.ts
```

Note: `src/stores/index.ts` only exports personalizationStore (already verified) -- no changes needed.
Note: `personalizationStore.ts` is KEPT (used by Explorer, Calculator, Dashboard, Hero for industry selection).

Run `npx tsc --noEmit` -- verify no errors.

**Group E: Delete ARIA-only utils and services**

```
rm src/services/llmService.ts
rm src/utils/conversationEngine.ts
rm src/utils/intentRecognition.ts
rm src/utils/questionMatcher.ts
rm src/utils/chatNavigationHelpers.ts
```

Check if `src/utils/fallbackResponses.ts` and `src/utils/pageContextI18n.ts` exist -- delete if they do.

Run `npx tsc --noEmit` -- verify no errors.

**Group F: Delete ARIA-only configs**

```
rm src/config/knowledgeBase.json
rm src/config/conversationPersonality.ts
rm src/config/platformKnowledge.ts
rm src/config/assistantJourneys.ts
rm src/config/moduleExplanations.ts
```

Run `npx tsc --noEmit` -- verify no errors.

**Group G: Delete old API endpoint**

```
rm api/chat.ts
```

**Group H: Delete FloatingElementContext**

```
rm src/contexts/FloatingElementContext.tsx
```

Run `npx tsc --noEmit` -- verify no errors.

**Group I: Clean i18n**
In `src/i18n/config.ts`:

- Remove `'ai-assistant'` from the `ns` (namespaces) array

Delete ai-assistant locale files:

```
rm public/locales/en/ai-assistant.json
rm public/locales/nl/ai-assistant.json
rm public/locales/es/ai-assistant.json
```

**Group J: Verify openai package**
Check `package.json` for `openai` in dependencies. If present, run `npm uninstall openai`. If not present, skip (research indicated it may already be absent).

**Group K: Final build**
Run `npx vite build` -- must succeed with zero errors.

**Important:** If ANY `tsc --noEmit` check fails during deletion groups, STOP and fix the import before continuing. The most likely causes are:

- A file outside ARIA importing from a deleted ARIA file (grep for the deleted filename)
- A barrel re-export referencing a deleted file
  </action>
  <verify>

1. `npx tsc --noEmit` -- zero errors
2. `npx vite build` -- build succeeds
3. `ls src/components/ai-assistant/ 2>/dev/null` -- directory does not exist
4. Grep for "ai-assistant" in src/ -- zero matches (except possibly in comments/git history)
5. Grep for "journeyStore" in src/ -- zero matches outside deleted files
6. Grep for "chatStore" in src/stores/ -- only chatbotStore.ts remains (not old chatStore.ts)
7. Grep for "FloatingElementContext" in src/ -- zero matches
   </verify>
   <done>All ARIA code deleted. No dead imports. No orphaned stores. No old API endpoint. i18n namespace cleaned. FloatingElementContext removed. Build succeeds cleanly.</done>
   </task>

</tasks>

<verification>
- `npx vite build` succeeds with no errors or warnings about deleted files
- `npx tsc --noEmit` passes
- No file in src/ imports from src/components/ai-assistant/
- No file in src/ imports journeyStore or old chatStore
- No file in src/ imports FloatingElementContext
- api/chat.ts does not exist
- src/components/ai-assistant/ directory does not exist
- Explorer page loads without errors
- Calculator page loads (useCalendlyBooking works)
- Homepage floating chatbot still works (from Plan 01)
</verification>

<success_criteria>
All old ARIA code removed from codebase. Zero dead imports. useCalendlyBooking refactored to work without journeyStore. StrategicCTA works without FloatingElementContext. Build succeeds. All pages that depended on ARIA hooks/stores continue functioning.
</success_criteria>

<output>
After completion, create `.planning/phases/19-homepage-concierge-demo-guide/19-02-SUMMARY.md`
</output>
