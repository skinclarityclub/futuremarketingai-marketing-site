---
phase: 14-seo-geo-depth-upgrade
plan: 01
type: execute
wave: 1
depends_on:
  - 10-production-integrity-domain-ssot
files_modified:
  - fmai-nextjs/src/lib/seo-config.ts
  - fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx
  - fmai-nextjs/src/components/seo/PersonJsonLd.tsx (new)
  - fmai-nextjs/src/app/[locale]/(marketing)/about/page.tsx
  - fmai-nextjs/src/app/[locale]/(marketing)/case-studies/skinclarity-club/page.tsx
  - fmai-nextjs/src/components/seo/ArticleJsonLd.tsx (author @id ref only — image+publisher stay in 14-03)
autonomous: false
requirements:
  - SEO-GEO-01 (Organization entity completeness)
  - SEO-GEO-02 (Person schema E-E-A-T)
estimated_hours: 4

must_haves:
  truths:
    - 'Organization JSON-LD includes stable @id "https://future-marketing.ai/#org"'
    - 'Organization sameAs contains at least 4 URLs (LinkedIn + Wikidata + Twitter + Crunchbase) once research tasks complete'
    - 'Wikidata item for FutureMarketingAI exists with QID recorded in seo-config.ts (or flagged as deferred in a TODO with a date)'
    - 'PersonJsonLd component exists and emits valid Person schema with worksFor → Organization @id'
    - 'Daley Person schema renders on /about for all 3 locales'
    - 'Sindy Person schema renders on /case-studies/skinclarity-club for all 3 locales'
    - 'hasOfferCatalog lists the 12 current skills (or is removed entirely — no v9 residue)'
    - 'knowsAbout expanded from 4 topics to 10 topics'
    - 'foundingDate matches research answer (Daley-confirmed year)'
    - 'Google Rich Results test detects Organization + Person on /about without errors'
  artifacts:
    - path: 'fmai-nextjs/src/components/seo/PersonJsonLd.tsx'
      provides: 'Reusable Person JSON-LD emitter with sameAs, worksFor, jobTitle, knowsAbout'
    - path: 'fmai-nextjs/src/lib/seo-config.ts'
      provides: 'ORG_ID, DALEY_PERSON_ID, SINDY_PERSON_ID, WIKIDATA_URL, TWITTER_URL, CRUNCHBASE_URL constants'
  key_links:
    - from: 'OrganizationJsonLd.tsx'
      to: 'PersonJsonLd.tsx'
      via: 'founder.@id referencing DALEY_PERSON_ID'
      pattern: 'founder.*@id.*daley'
    - from: 'about/page.tsx'
      to: 'PersonJsonLd.tsx'
      via: 'render with person="daley"'
      pattern: 'PersonJsonLd.*daley'
    - from: 'case-studies/skinclarity-club/page.tsx'
      to: 'PersonJsonLd.tsx'
      via: 'render with person="sindy"'
      pattern: 'PersonJsonLd.*sindy'
---

<objective>
Build the entity foundation for GEO — Organization with full sameAs + stable @id + v10-correct hasOfferCatalog + 10 knowsAbout topics, plus a reusable Person schema component rendered for Daley (founder) and Sindy (SKC operator).

Purpose: Per audit 05 GEO sec "Top 15 GEO Fixes #2 + #9", sameAs-with-Wikidata is the single highest citation multiplier (3.2x AI Mode lift per schema research). Person schema for the founder is the single highest E-E-A-T signal (+30.64% citation correlation). Today the Organization has only 1 sameAs (LinkedIn) and zero Person schemas exist anywhere on the site.

Output: Organization + Person entity graph that ChatGPT / Claude / Perplexity / Google AI can resolve and cite. Foundation for 14-02 (Service + FAQ waves build on Organization @id and Person @id references).
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-seo-geo-depth-upgrade/README.md
@.planning/phases/14-seo-geo-depth-upgrade/RESEARCH.md
@docs/audits/2026-04-24-full-audit/04-seo-technical.md
@docs/audits/2026-04-24-full-audit/05-geo-llm-citation.md
@docs/research-schema-markup-structured-data-seo-geo.md
@docs/research/2026-03-28-geo-llmeo-ai-citation-research.md
@fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx
@fmai-nextjs/src/components/seo/ArticleJsonLd.tsx
@fmai-nextjs/src/lib/seo-config.ts
</context>

<tasks>

<task type="research">
  <name>Task 0: Confirm open decisions from RESEARCH.md sec 10</name>
  <action>
Before writing code, get Daley to confirm:

1. Agency founding year (code says 2025, memory says 2024). Pick one → goes in `foundingDate`.
2. KvK registration status. If registered, get profile URL. If not, skip and add a TODO comment with a 2026-Q3 recheck date.
3. Twitter/X handle `@FutureMarketAI` — claimed or needs claiming? If not claimed, claim in 5 min.
4. Crunchbase profile — create at https://www.crunchbase.com/add-new if not already. 30 min. Free.
5. YouTube channel existence — if yes, URL; if no, skip.
6. Wikidata item creation — OK to proceed under Daley's personal account? (Takes 15 min, fully reversible.)

Defaults (if no response in 24h per README):
- foundingDate = 2024-01-01
- KvK = skip
- Twitter = claim + add
- Crunchbase = create + add
- YouTube = skip
- Wikidata = proceed

Capture decisions in `.planning/phases/14-seo-geo-depth-upgrade/DECISIONS-14-01.md`. Log in Obsidian via `obsidian-capture` skill at `Agency/Website/seo-geo/2026-04-24-phase-14-entity-decisions.md`.
  </action>
  <verify>
    <manual>DECISIONS-14-01.md exists with Daley sign-off OR default timeout passed + decisions logged with reasoning</manual>
  </verify>
  <done>All 6 decisions are documented. External accounts (Twitter, Crunchbase, Wikidata, LinkedIn personal) have canonical URLs ready to paste into seo-config.ts.</done>
</task>

<task type="research">
  <name>Task 1: Create Wikidata items for FutureMarketingAI + Daley (founder)</name>
  <files>external — Wikidata</files>
  <action>
Per RESEARCH.md sec 1:

1. Go to https://www.wikidata.org/wiki/Special:NewItem (log in first at https://www.wikidata.org/wiki/Special:UserLogin).

2. Create FMai item:
   - Label (en): `FutureMarketingAI`
   - Description (en): `Dutch AI marketing automation agency and platform building Clyde, an AI Marketing Employee for marketing agencies`
   - Description (nl): `Nederlands AI-marketing-automatiseringsbureau en platform dat Clyde bouwt, een AI Marketing Medewerker voor marketingbureaus`
   - Add statements:
     - P31 (instance of) → Q4830453 (business)
     - P17 (country) → Q55 (Netherlands)
     - P571 (inception) → {foundingDate from Task 0}
     - P452 (industry) → Q11030 (marketing) AND Q11660 (artificial intelligence)
     - P856 (official website) → https://future-marketing.ai
     - P2002 (Twitter username) → FutureMarketAI (if Task 0 confirmed)
     - P4264 (LinkedIn company ID) → futuremarketingai
   - Add at least 2 references (website URL, LinkedIn URL) to avoid deletion by patrol.

3. Note the assigned QID (format `Q12345678`).

4. Create Daley item (optional, can be Phase 14.5 if time-constrained):
   - Label (en): `Daley Maat` (confirm full name with Daley)
   - Description (en): `Dutch entrepreneur, founder of FutureMarketingAI`
   - Statements:
     - P31 (instance of) → Q5 (human)
     - P27 (country of citizenship) → Q55 (Netherlands)
     - P106 (occupation) → Q131524 (entrepreneur) + Q43845 (businessperson)
     - P108 (employer) → {FMai QID from above}
     - P112 (founder) — reverse: add Daley as founder on FMai item
     - P69 (educated at) → (if relevant + verifiable)
   - Reference: LinkedIn personal URL.

5. Record QIDs in `DECISIONS-14-01.md`.

If Wikidata review flags either item for deletion within 7 days, document the reason and retry with more references. Minor deletions are reversible.
  </action>
  <verify>
    <automated>grep -E "Q[0-9]{7,10}" .planning/phases/14-seo-geo-depth-upgrade/DECISIONS-14-01.md | wc -l</automated>
    <manual>Both Wikidata items are live at https://www.wikidata.org/wiki/{QID} (check 24h after creation — no speedy-delete tag).</manual>
  </verify>
  <done>Two QIDs exist (one for FMai, one for Daley) and are captured in DECISIONS-14-01.md. URLs are ready for seo-config.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Extend seo-config.ts with entity @ids + external profile URLs</name>
  <files>fmai-nextjs/src/lib/seo-config.ts</files>
  <action>
Add the following constants to `src/lib/seo-config.ts` (after the existing LINKEDIN_URL line):

```typescript
// --- Entity Identity (added Phase 14-01) ---

// Stable @id fragments — used as cross-references in JSON-LD graph
// Pattern: ${SITE_URL}/${path}#${entity-fragment}
export const ORG_ID = `${SITE_URL}/#org`
export const WEBSITE_ID = `${SITE_URL}/#website`
export const DALEY_PERSON_ID = `${SITE_URL}/about/#daley`
export const SINDY_PERSON_ID = `${SITE_URL}/case-studies/skinclarity-club/#sindy`

export const pageWebPageId = (locale: string, path: string) =>
  `${SITE_URL}/${locale}${path === '/' ? '' : path}#webpage`

export const skillServiceId = (slug: string) =>
  `${SITE_URL}/skills/${slug}/#service`

// External profiles (sameAs) — confirmed in DECISIONS-14-01.md
// TODO(phase-14-01): swap placeholder QIDs + URLs for the real values once Wikidata items land
export const WIKIDATA_URL = 'https://www.wikidata.org/wiki/Q_PLACEHOLDER_FMAI'
export const WIKIDATA_DALEY_URL = 'https://www.wikidata.org/wiki/Q_PLACEHOLDER_DALEY'
export const TWITTER_URL = 'https://x.com/FutureMarketAI'
export const CRUNCHBASE_URL = 'https://www.crunchbase.com/organization/futuremarketingai'
export const LINKEDIN_DALEY_URL = 'https://www.linkedin.com/in/daleymaat'  // confirm slug
export const LINKEDIN_SINDY_URL = 'https://www.linkedin.com/in/sindy-skinclarity'  // confirm with Sindy
export const KVK_URL: string | null = null  // TODO: set once registered

// YouTube + Instagram are optional — leave null if not yet active
export const YOUTUBE_URL: string | null = null
export const INSTAGRAM_URL: string | null = null

// Topical authority (knowsAbout) — expand from 4 to 10 per research doc sec 2
export const ORG_KNOWS_ABOUT = [
  'AI Marketing Automation',
  'Generative AI',
  'LLM Orchestration',
  'Brand Voice Modeling',
  'Multi-Brand Content Operations',
  'n8n workflow automation',
  'EU AI Act Compliance',
  'AVG / GDPR',
  'Conversational AI for B2B',
  'Marketing Agency Tooling',
]

// foundingDate — confirmed in DECISIONS-14-01.md
export const ORG_FOUNDING_DATE = '2024-01-01'  // TODO: confirm with Daley; code previously said 2025
```

Replace placeholder QIDs with real ones from Task 1. If a URL is not yet available (KvK, YouTube), export `null` and handle in OrganizationJsonLd with conditional spread.

Export each new constant. Do not change existing exports.

Cite research: RESEARCH.md sec 1 (Wikidata), sec 6 (@id pattern).
  </action>
  <verify>
    <automated>grep -E "ORG_ID|DALEY_PERSON_ID|SINDY_PERSON_ID|WIKIDATA_URL|TWITTER_URL|CRUNCHBASE_URL|ORG_KNOWS_ABOUT|ORG_FOUNDING_DATE" fmai-nextjs/src/lib/seo-config.ts | wc -l</automated>
  </verify>
  <done>All new constants exist and compile. TypeScript has no errors. `npm run build` passes (note: the schemas using these constants are updated in Tasks 3-5, so temporary unused-const warnings are expected until then).</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite OrganizationJsonLd.tsx (sameAs, @id, hasOfferCatalog v10, knowsAbout, foundingDate)</name>
  <files>fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx</files>
  <action>
Rewrite the Organization schema per the v10 content model + research sec 2 + audit 04 fix #3 + #8.

New structure:

```typescript
import { JsonLd } from './JsonLd'
import {
  SITE_URL,
  SITE_NAME,
  ORG_EMAIL,
  ORG_ID,
  DALEY_PERSON_ID,
  LINKEDIN_URL,
  WIKIDATA_URL,
  TWITTER_URL,
  CRUNCHBASE_URL,
  KVK_URL,
  YOUTUBE_URL,
  INSTAGRAM_URL,
  ORG_KNOWS_ABOUT,
  ORG_FOUNDING_DATE,
  ENTITY_DESCRIPTION,
} from '@/lib/seo-config'
import { SKILLS_DATA } from '@/lib/skills-data'

// Filter null-valued external profile URLs before emitting sameAs
function buildSameAs(): string[] {
  return [
    LINKEDIN_URL,
    WIKIDATA_URL,
    TWITTER_URL,
    CRUNCHBASE_URL,
    KVK_URL,
    YOUTUBE_URL,
    INSTAGRAM_URL,
  ].filter((u): u is string => typeof u === 'string' && u.length > 0)
}

// hasOfferCatalog — 12 skills as Services, referenced by @id so SkillPage ServiceJsonLd (14-02) agrees
function buildOfferCatalog() {
  return {
    '@type': 'OfferCatalog',
    name: 'Clyde AI Marketing Skills',
    itemListElement: SKILLS_DATA.map((skill) => ({
      '@type': 'Offer',
      itemOffered: {
        '@type': 'Service',
        '@id': `${SITE_URL}/skills/${skill.slug}/#service`,
        name: skill.name,
        description: skill.shortDescription,
      },
      seller: { '@id': ORG_ID },
    })),
  }
}

export function OrganizationJsonLd() {
  const data = {
    '@context': 'https://schema.org',
    '@type': ['Organization', 'ProfessionalService'],
    '@id': ORG_ID,
    name: SITE_NAME,
    url: SITE_URL,
    logo: {
      '@type': 'ImageObject',
      url: `${SITE_URL}/og-image.png`,
      width: 1200,
      height: 630,
    },
    email: ORG_EMAIL,
    description: ENTITY_DESCRIPTION,
    foundingDate: ORG_FOUNDING_DATE,
    founder: {
      '@id': DALEY_PERSON_ID,
    },
    address: {
      '@type': 'PostalAddress',
      addressCountry: 'NL',
      addressRegion: 'Netherlands',
    },
    areaServed: ['NL', 'EU', 'Worldwide'],
    knowsAbout: ORG_KNOWS_ABOUT,
    knowsLanguage: ['en', 'nl', 'es'],
    hasOfferCatalog: buildOfferCatalog(),
    contactPoint: {
      '@type': 'ContactPoint',
      email: ORG_EMAIL,
      contactType: 'sales',
      availableLanguage: ['English', 'Dutch', 'Spanish'],
    },
    sameAs: buildSameAs(),
    priceRange: '€€€',
  }

  return <JsonLd data={data} />
}
```

Key changes vs old version:

1. `@id` added (line 4 of data) — stable URI for cross-references.
2. `founder` is now a `@id` reference to `DALEY_PERSON_ID` — the Person schema (Task 4) provides the full Person entity. Removes the inline `{ '@type': 'Person', name: 'Daley', jobTitle: 'Founder & CEO' }` duplicate.
3. `logo` is now an `ImageObject` with width/height (required by rich results).
4. `hasOfferCatalog` lists 12 skills imported from `skills-data.ts` (replacing v9 "AI Chatbots / Marketing Machine" entries that no longer exist on site). Each skill offer references the skill page's Service `@id` so 14-02's ServiceJsonLd is the same entity.
5. `knowsAbout` expanded from 4 topics to 10 (per research sec 2).
6. `foundingDate` driven by constant.
7. `sameAs` now includes 4-7 URLs (filters out null-valued placeholders for KvK, YouTube, Instagram until ready).

Cite in a JSDoc header comment:
- Schema.org Organization: https://schema.org/Organization
- Audit 04 fix #3 + #8
- Audit 05 GEO Top 15 #2
  </action>
  <verify>
    <automated>cd fmai-nextjs && npm run build 2>&1 | tail -20</automated>
    <manual>Paste rendered HTML JSON-LD from http://localhost:3000/nl into https://validator.schema.org/ — expect Organization with @id + sameAs (4+ URLs) + hasOfferCatalog (12 items) + founder(@id) with zero errors.</manual>
  </verify>
  <done>Build passes. Organization JSON-LD validates. No v9 "Marketing Machine" string remains anywhere in the rendered schema. hasOfferCatalog lists 12 skill names matching the current `/skills/*` routes.</done>
</task>

<task type="auto">
  <name>Task 4: Create PersonJsonLd component (reusable for Daley + Sindy + future authors)</name>
  <files>fmai-nextjs/src/components/seo/PersonJsonLd.tsx (new)</files>
  <action>
Create a new reusable Person JSON-LD component per research sec 1 Tier 1 + research sec 6 (@id pattern).

```typescript
import type { WithContext, Person } from 'schema-dts'
import { JsonLd } from './JsonLd'
import { SITE_URL, ORG_ID } from '@/lib/seo-config'

interface PersonJsonLdProps {
  /** Stable @id for this person — e.g. DALEY_PERSON_ID */
  id: string
  /** Display name — e.g. "Daley Maat" */
  name: string
  /** Role/title — e.g. "Founder & CEO of FutureMarketingAI" */
  jobTitle: string
  /** Short bio — 1-2 sentences, self-contained, quotable for LLMs */
  description: string
  /** External profiles — LinkedIn, X, Wikidata (if notable), etc. Falsy/null entries are filtered. */
  sameAs?: Array<string | null | undefined>
  /** Optional knowsAbout topics for E-E-A-T. 5-10 items. */
  knowsAbout?: string[]
  /** Optional image URL (absolute). 1200x630 recommended. */
  image?: string
  /** Optional worksFor override. Defaults to ORG_ID (our Organization). */
  worksForId?: string
}

export function PersonJsonLd({
  id,
  name,
  jobTitle,
  description,
  sameAs = [],
  knowsAbout,
  image,
  worksForId = ORG_ID,
}: PersonJsonLdProps) {
  const filteredSameAs = sameAs.filter((u): u is string => typeof u === 'string' && u.length > 0)

  const data: WithContext<Person> = {
    '@context': 'https://schema.org',
    '@type': 'Person',
    '@id': id,
    name,
    jobTitle,
    description,
    worksFor: { '@id': worksForId },
    ...(filteredSameAs.length > 0 ? { sameAs: filteredSameAs } : {}),
    ...(knowsAbout && knowsAbout.length > 0 ? { knowsAbout } : {}),
    ...(image ? { image } : {}),
    ...(SITE_URL ? { url: SITE_URL } : {}),
  }

  return <JsonLd data={data} />
}
```

Add a JSDoc header with cites:
- Schema.org Person: https://schema.org/Person
- Research doc sec 1 Tier 1 Person schema
- Audit 04 fix #4 + audit 05 GEO Top 15 #9 (E-E-A-T +30.64% citation correlation per Semrush Jan 2026)
  </action>
  <verify>
    <automated>cd fmai-nextjs && npm run build 2>&1 | grep -E "error|PersonJsonLd" | head -10</automated>
    <automated>test -f fmai-nextjs/src/components/seo/PersonJsonLd.tsx && echo "exists"</automated>
  </verify>
  <done>PersonJsonLd.tsx file exists. Component compiles. Unit-test-style dev-server render (tasks 5 + 6 do the actual page wiring, which triggers the schema render).</done>
</task>

<task type="auto">
  <name>Task 5: Render PersonJsonLd for Daley on /about page (all 3 locales)</name>
  <files>fmai-nextjs/src/app/[locale]/(marketing)/about/page.tsx</files>
  <action>
Import PersonJsonLd + DALEY_PERSON_ID + LINKEDIN_DALEY_URL + WIKIDATA_DALEY_URL + TWITTER_URL into about/page.tsx.

Inside the JSX (after the existing `<WebPageJsonLd>` + `<BreadcrumbJsonLd>` emits), add:

```tsx
<PersonJsonLd
  id={DALEY_PERSON_ID}
  name={t('founder.fullName')}  // new i18n key
  jobTitle={t('founder.role')}  // e.g. "Founder & CEO van FutureMarketingAI"
  description={t('founder.bio')}  // 1-2 sentence summary
  sameAs={[LINKEDIN_DALEY_URL, WIKIDATA_DALEY_URL, TWITTER_URL]}
  knowsAbout={[
    'AI Marketing Automation',
    'LLM Orchestration',
    'n8n workflow automation',
    'Brand Voice Modeling',
    'B2B Marketing Strategy',
    'EU AI Act Compliance',
  ]}
  image={`${SITE_URL}/daley-portrait.jpg`}  // if available, else omit prop
/>
```

Add i18n keys to `messages/nl.json`, `messages/en.json`, `messages/es.json`:

```json
"about": {
  "founder": {
    "fullName": "Daley Maat",  // confirm with Daley
    "role": "Founder & CEO van FutureMarketingAI",
    "bio": "Daley Maat bouwt FutureMarketingAI, een AI Marketing Medewerker voor bureaus. Ex-ondernemer in marketing, founder SkinClarity Club, technisch architect achter Clyde en de vier-laags-memory."  // confirm accuracy
  }
}
```

Keep under 200 chars per field (LLM-quotable). Verify the page already has a founder section to avoid duplicate visible text.

If `daley-portrait.jpg` doesn't exist yet, omit the `image` prop. Do NOT break the build with a 404 reference.
  </action>
  <verify>
    <automated>cd fmai-nextjs && npm run build 2>&1 | tail -15</automated>
    <manual>Visit http://localhost:3000/nl/about — view page source, verify a JSON-LD block with `"@type":"Person"` and `"@id":"https://future-marketing.ai/about/#daley"` is present. Repeat for /en and /es.</manual>
    <manual>Paste URL into https://search.google.com/test/rich-results — expect Person entity detected.</manual>
  </verify>
  <done>Person schema renders on /nl/about, /en/about, /es/about. Rich Results test passes. Founder bio i18n keys added to all 3 locale files with matching structure.</done>
</task>

<task type="auto">
  <name>Task 6: Render PersonJsonLd for Sindy on /case-studies/skinclarity-club (all 3 locales)</name>
  <files>fmai-nextjs/src/app/[locale]/(marketing)/case-studies/skinclarity-club/page.tsx</files>
  <action>
Per audit 05 GEO Top 15 #9 (Sindy testifies on home + case study without a Person entity):

Import PersonJsonLd + SINDY_PERSON_ID + LINKEDIN_SINDY_URL into the SKC case study page.

Add after the existing JSON-LD emits:

```tsx
<PersonJsonLd
  id={SINDY_PERSON_ID}
  name={t('testimonial.author.name')}  // "Sindy ..." — confirm with Sindy
  jobTitle={t('testimonial.author.role')}  // e.g. "Founder & Operator, SkinClarity Club"
  description={t('testimonial.author.bio')}  // 1-sentence: who she is + what she operates with Clyde
  sameAs={[LINKEDIN_SINDY_URL]}
  worksForId={`${SITE_URL}/case-studies/skinclarity-club/#organization-skc`}  // SKC is her employer, not FMai
/>
```

Because Sindy's `worksFor` is SkinClarity Club (not FMai), override `worksForId` to point to a new Organization `@id` for SKC.

If the case study doesn't already emit an Organization schema for SkinClarity Club, add one inline (small — just `@id` + `name` + `url`):

```tsx
import { JsonLd } from '@/components/seo/JsonLd'

<JsonLd
  data={{
    '@context': 'https://schema.org',
    '@type': 'Organization',
    '@id': `${SITE_URL}/case-studies/skinclarity-club/#organization-skc`,
    name: 'SkinClarity Club',
    url: 'https://skinclarityclub.nl',
    description: t('client.description'),
  }}
/>
```

Update `messages/*.json` with:
```json
"case_studies": {
  "skc": {
    "testimonial": {
      "author": {
        "name": "Sindy Maat",  // confirm with Sindy
        "role": "Founder & Operator, SkinClarity Club",
        "bio": "Sindy runt 4 merken onder SkinClarity Club en gebruikt Clyde als AI marketing medewerker — 3 Instagram-accounts, autonome content, merk-isolated memory."
      }
    },
    "client": {
      "description": "Nederlandse skincare brand-portfolio. 4 merken, 3 Instagram-accounts, full content-operatie."
    }
  }
}
```

If keys already exist with slightly different structure, adapt — do not duplicate.
  </action>
  <verify>
    <automated>cd fmai-nextjs && npm run build 2>&1 | tail -15</automated>
    <manual>Visit /nl/case-studies/skinclarity-club — view-source should show Person(@id=#sindy) + Organization(@id=#organization-skc). Validate at https://validator.schema.org/.</manual>
  </verify>
  <done>Sindy Person schema renders on SKC case study for all 3 locales. SKC Organization also emitted. Validator shows both entities with no errors.</done>
</task>

<task type="auto">
  <name>Task 7: Update ArticleJsonLd to use Daley Person @id reference (author)</name>
  <files>fmai-nextjs/src/components/seo/ArticleJsonLd.tsx</files>
  <action>
Minimal change — the full ArticleJsonLd completion (image, publisher.logo, mainEntityOfPage) is in 14-03. Here in 14-01 we only fix the author reference to use the new Daley @id.

Current code (line 33-36):
```typescript
author: {
  '@type': 'Person',
  name: author,
  url: SITE_URL,
},
```

Change to:
```typescript
import { DALEY_PERSON_ID, SITE_URL, SITE_NAME } from '@/lib/seo-config'

// ...
author: author === 'Daley Maat' || author === 'Daley'
  ? { '@id': DALEY_PERSON_ID }
  : {
      '@type': 'Person',
      name: author,
      url: SITE_URL,
    },
```

Rationale: if the blog post is written by Daley (the only current author), the Article schema references the Person @id instead of duplicating data. This lets search engines + LLMs unify the author across /about and blog posts.

For future authors, the fallback path keeps the inline Person. When a second author joins, create a Person @id + add them to the branch.

Note: the full ArticleJsonLd expansion (image, publisher.logo, mainEntityOfPage, about sameAs Wikidata) happens in 14-03 task 3. Keep this change minimal.
  </action>
  <verify>
    <automated>cd fmai-nextjs && npm run build 2>&1 | tail -10</automated>
    <manual>Visit /en/blog/ai-marketing-automation-guide — view-source, confirm Article.author is now `{"@id":"https://future-marketing.ai/about/#daley"}` instead of the old inline Person.</manual>
  </verify>
  <done>Build passes. Blog-post Article schema references Daley by @id. No regression in FAQ / date / inLanguage fields.</done>
</task>

<task type="auto">
  <name>Task 8: Perplexity baseline probe + commit</name>
  <files>.planning/phases/14-seo-geo-depth-upgrade/BASELINE-perplexity-probe-2026-04-24.md (new)</files>
  <action>
Per RESEARCH.md sec 10 — capture baseline BEFORE the schema changes propagate to Google/Perplexity.

Run 5 probes via MCP perplexity_search:

1. "Wat is Clyde van FutureMarketingAI"
2. "What is an AI Marketing Medewerker"
3. "Who is Daley Maat founder FutureMarketingAI"
4. "FutureMarketingAI pricing tiers"
5. "SkinClarity Club AI content automation case study"

For each probe, record:
- Full prompt
- Whether future-marketing.ai appears in `citations[]`
- Whether futuremarketingai.com appears (shows pre-Phase-10 URLs in training data)
- Top 3 citations with URLs
- Any misinformation about FMai (wrong pricing, wrong URLs, wrong product framing)

Save as `.planning/phases/14-seo-geo-depth-upgrade/BASELINE-perplexity-probe-2026-04-24.md`.

Commit all Phase 14-01 changes in one commit:
```
type(scope): message

seo(schema): Phase 14-01 — Organization + Person entity foundation

- Add stable @id (ORG_ID, DALEY_PERSON_ID, SINDY_PERSON_ID) + pageWebPageId/skillServiceId helpers
- Expand sameAs to 4-7 URLs incl. Wikidata {QID}, Crunchbase, Twitter, LinkedIn company + personal
- Rewrite hasOfferCatalog to 12 skills (v10), removing v9 Marketing-Machine residue
- Expand knowsAbout 4 → 10 topics per research doc sec 2
- New PersonJsonLd component rendered for Daley (/about) + Sindy (SKC case)
- ArticleJsonLd now references Daley by @id (unifies author identity)
- Correct foundingDate from 2025 → {confirmed year}

Why: audit 05 GEO 42/100 — sameAs + Person schema are the two highest-leverage lifts
(+3.2x AI Mode citation, +30.64% E-E-A-T citation correlation per Semrush Jan 2026).

Refs: docs/audits/2026-04-24-full-audit/04-seo-technical.md fixes #3 #4 #8
      docs/audits/2026-04-24-full-audit/05-geo-llm-citation.md Top-15 #2 #9
      docs/research-schema-markup-structured-data-seo-geo.md sec 1 Tier 1

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Do NOT push or open a PR — wave-1 peers (14-03, 14-04) commit independently. Phase-level PR at the end.
  </action>
  <verify>
    <automated>git log --oneline | head -5</automated>
    <automated>test -f .planning/phases/14-seo-geo-depth-upgrade/BASELINE-perplexity-probe-2026-04-24.md</automated>
  </verify>
  <done>Baseline probe recorded. Git commit exists on `feature/phase-14-seo-geo-depth` (or equivalent phase branch). Rich Results test passes for Organization + Person on /about.</done>
</task>

</tasks>

<verification>
Run all of these before declaring 14-01 complete:

1. `cd fmai-nextjs && npm run build` — Next.js build passes without errors
2. `cd fmai-nextjs && npm run lint` — no new lint errors introduced
3. Manual: paste https://future-marketing.ai/nl (preview deploy URL) into https://search.google.com/test/rich-results — expect Organization + Person (on /about) + WebSite + WebPage + BreadcrumbList detected, zero errors.
4. Manual: paste the same URL into https://validator.schema.org/ — zero errors, zero warnings on Organization + Person.
5. `grep -r "Marketing Machine" fmai-nextjs/src/components/seo/` — must return 0 results (v9 residue gone).
6. `grep -r "AI Chatbots" fmai-nextjs/src/components/seo/` — must return 0 results.
7. `grep -c "sameAs" fmai-nextjs/src/components/seo/OrganizationJsonLd.tsx` — returns ≥1.
8. View-source on /nl/about contains `"@id":"https://future-marketing.ai/about/#daley"` AND `"@type":"Person"`.
9. BASELINE-perplexity-probe-2026-04-24.md exists with 5 probes documented.

If any verification fails, fix and re-run. Do not mark plan done until all 9 pass.
</verification>

<success_criteria>
- Organization JSON-LD contains `@id`, `sameAs` (≥4 URLs), `hasOfferCatalog` (12 skills), `knowsAbout` (10 topics), correct `foundingDate`, `founder.@id` reference.
- PersonJsonLd.tsx exists, compiles, is rendered on /about (Daley) and /case-studies/skinclarity-club (Sindy) for all 3 locales.
- ArticleJsonLd references Daley by @id for his authored posts.
- Google Rich Results test + schema.org validator both pass with zero errors on the updated pages.
- Baseline Perplexity probe is documented for post-deploy comparison.
- Single git commit lands with "Phase 14-01" in message.
</success_criteria>

<output>
After completion, create `.planning/phases/14-seo-geo-depth-upgrade/14-01-SUMMARY.md` documenting:
- Wikidata QIDs assigned
- External profile URLs added
- Any decisions that deviated from defaults (e.g. Daley chose a different founding year)
- Baseline perplexity probe summary (how many of 5 probes cited us)
- Handoff note for 14-02 (reminding that Organization @id + Service @id cross-reference pattern is now established)
</output>
