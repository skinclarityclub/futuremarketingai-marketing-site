---
phase: 16-design-seo-audit-v2-sota
plan: 13
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/12-security-privacy.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '12-security-privacy.md exists with ≥800 words'
    - 'Doc captures HTTP security headers from production URL including CSP, HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy'
    - 'Doc audits cookies on production via DOM + Set-Cookie observation: name, scope, secure, httpOnly, sameSite, expires, GDPR-classification'
    - 'Doc maps GDPR data-flow for /api/apply, /api/contact, /api/newsletter (input → processor → storage → retention)'
    - 'Doc captures npm audit baseline (vulnerability counts by severity)'
    - 'Findings carry full severity schema'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/12-security-privacy.md'
      provides: 'Security headers + cookies + GDPR data-flow + npm audit'
      contains: 'CSP'
  key_links:
    - from: '12-security-privacy.md'
      to: 'https://future-marketing.ai'
      via: 'HTTP-header probe via curl/WebFetch'
      pattern: 'curl'
---

<objective>
Audit production security and privacy posture: HTTP headers, CSP, cookies, GDPR data-flow per API route, and npm audit baseline. Cross-check Phase 10 + Phase 12 + Phase 11 invariants did not regress.

Purpose: EU AI Act, AVG, EAA, plus EU buyer evaluator persona (16-04). Without a security/privacy audit doc, the buyer evaluator funnel cannot quote concrete posture.
Output: `docs/audits/2026-05-18-v2/12-security-privacy.md` with headers + cookies + data-flow + npm-audit + ≥25 findings.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTTP headers + cookies + CSP audit</name>
  <files>docs/audits/2026-05-18-v2/12-security-privacy.md</files>
  <action>
Probe production URL headers via `curl -sI https://future-marketing.ai/` and `curl -sI https://future-marketing.ai/nl` plus `/en` and `/es`. Capture every response header.

Cross-check via WebFetch against `https://securityheaders.com/?q=future-marketing.ai` and `https://observatory.mozilla.org/analyze/future-marketing.ai` (≤4 WebFetch calls total, log to BUDGET.log).

Sections:

1. `## Executive summary` (top): grade per scanner (A/B/C/D/E/F), top 3 themes, P0/P1 counts.
2. `## HTTP security headers matrix` table: header name, value observed, expected/SOTA value, status (pass/warn/fail), severity. Cover:
   - Content-Security-Policy
   - Strict-Transport-Security
   - X-Frame-Options
   - X-Content-Type-Options
   - Referrer-Policy
   - Permissions-Policy
   - Cross-Origin-Opener-Policy
   - Cross-Origin-Embedder-Policy
   - Cross-Origin-Resource-Policy
   - Server (info-leak risk)
3. `## CSP deep-dive`: parse the CSP value, list directives per source-class (script-src, style-src, img-src, connect-src, frame-src, etc.). Flag unsafe-inline, unsafe-eval, wildcard, missing report-uri.
4. `## Cookie inventory`: walk DOM snapshots in `test-results/audit-v2/dom/` for `document.cookie`-evidence + use production HEAD to capture Set-Cookie. Table: name, scope (domain+path), secure, httpOnly, sameSite, expires/maxAge, purpose (strict/preferences/analytics/marketing), GDPR-classification (necessary / functional / analytics / marketing), CookieConsentBanner alignment.

Rules: geen em-dashes, no production-code edits.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/12-security-privacy.md' -Raw -ErrorAction Stop; if (($c -match '## HTTP security headers matrix') -and ($c -match '## CSP deep-dive') -and ($c -match '## Cookie inventory')) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Headers matrix + CSP deep-dive + cookie inventory present with concrete values from production.</done>
</task>

<task type="auto">
  <name>Task 2: GDPR data-flow + npm audit + findings + commit</name>
  <files>docs/audits/2026-05-18-v2/12-security-privacy.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append `## GDPR data-flow per API route`: for each of /api/apply, /api/contact, /api/newsletter (read-only references to `fmai-nextjs/src/app/api/`):
- Personal-data fields collected
- Lawful basis (consent / contract / legitimate interest)
- Processors (Resend, Supabase, Upstash, Calendly)
- Storage location (Supabase EU region, Resend, Upstash EU region)
- Retention period
- Subject rights handling (access, deletion, portability)
- Hashing (SHA-256 IP per Phase 10-02)
- Data-sharing with third parties (sub-processor list cross-ref DPA, Phase 4-02)
- Cookie/tracker exposure

Append `## GDPR data-flow diagram` (ASCII or mermaid block) per API route.

Append `## npm audit baseline`: run `cd fmai-nextjs && npm audit --production --json` and aggregate by severity. Cross-check against Phase 10-04 baseline (0 high) and Phase 13-03 dep bumps. Flag any new high/critical as P0.

Append `## Third-party tracker review`: GA4, Calendly, Spline, Stripe, Resend telemetry. Each: load mechanism, data sent, GDPR purpose, consent gate.

Append `## Top 25 findings` ranked, full schema.

Update STATE.md, commit:

```bash
git add docs/audits/2026-05-18-v2/12-security-privacy.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-13 Team 11 security headers + privacy + GDPR compliance"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/12-security-privacy.md' -Raw; if (($c -match '## GDPR data-flow') -and ($c -match '## npm audit baseline') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/12-security-privacy.md' -Pattern '^### Finding ').Count -ge 25)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Data-flow + npm audit + tracker review + ≥25 findings + single commit.</done>
</task>

</tasks>

<verification>
- `12-security-privacy.md` ≥800 words; headers + CSP + cookies + GDPR data-flow + npm audit + ≥25 findings.
- Conventional commit landed.
</verification>

<success_criteria>
16-16 fix-plan can rank security/privacy work by EU-buyer-evaluator impact and legal-risk severity.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-13-SUMMARY.md`
</output>
