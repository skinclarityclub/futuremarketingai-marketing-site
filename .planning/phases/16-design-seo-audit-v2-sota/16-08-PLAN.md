---
phase: 16-design-seo-audit-v2-sota
plan: 08
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/07-accessibility.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '07-accessibility.md exists with ≥800 words'
    - 'Doc aggregates axe-core JSON results from 16-02 across 32 routes × 3 locales'
    - 'Doc includes WCAG 2.2 AA scorecard per route with pass/fail summary'
    - 'Keyboard navigation, focus-trap behaviour, skip-link, and aria-landmarks are verified manually via DOM snapshots'
    - 'Readability scores (Flesch-Kincaid) per locale captured for top 10 routes'
    - 'Findings carry full severity schema'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/07-accessibility.md'
      provides: 'WCAG 2.2 AA audit + axe aggregation + readability'
      contains: 'WCAG'
  key_links:
    - from: '07-accessibility.md'
      to: 'fmai-nextjs/test-results/audit-v2/axe/'
      via: 'JSON aggregation'
      pattern: 'axe/'
---

<objective>
Run a deep WCAG 2.2 AA audit against the marketing site by aggregating the axe-core JSON outputs from 16-02 plus manual keyboard + focus + landmark walk-throughs of the DOM snapshots. Score readability per locale.

Purpose: EAA was in force per 2025-06-28. Phase 11 closed the critical-path violations. This plan finds what's left below the surface and feeds 16-16 with a prioritised a11y backlog.
Output: `docs/audits/2026-05-18-v2/07-accessibility.md` with axe aggregation, per-route scorecard, manual findings, readability table.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate axe-core results + build per-route scorecard</name>
  <files>docs/audits/2026-05-18-v2/07-accessibility.md</files>
  <action>
Walk `fmai-nextjs/test-results/audit-v2/axe/` and read every `<route>-<locale>.json` file (96 files expected). Aggregate violations by:

- WCAG rule ID (e.g. color-contrast, landmark-one-main, aria-required-attr)
- Severity (minor / moderate / serious / critical)
- Route distribution (which routes hit each rule)

Sections in `07-accessibility.md`:

1. `## Executive summary` (top): pass-rate per WCAG 2.2 AA principle, critical/serious counts, top 3 themes.
2. `## Methodology`: axe-core tags used (`wcag2a`, `wcag2aa`, `wcag22aa`), viewport, locale coverage, manual checks layered on top.
3. `## Aggregate violation matrix`: table per rule-id with severity, hit-count, route-list, sample evidence link (axe JSON path).
4. `## Per-route scorecard`: 96 rows (route × locale) with critical/serious/moderate/minor counts and pass/fail verdict.
5. `## Notable rule deep-dives`: for the top 5 rules (by hit-count and/or severity), one paragraph of root-cause analysis with code-path references (read-only).

Rules: geen em-dashes, no production-code edits.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/07-accessibility.md' -Raw -ErrorAction Stop; if (($c -match '## Aggregate violation matrix') -and ($c -match '## Per-route scorecard')) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Axe aggregation + scorecard + rule deep-dives present.</done>
</task>

<task type="auto">
  <name>Task 2: Manual checks + readability + findings + commit</name>
  <files>docs/audits/2026-05-18-v2/07-accessibility.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append `## Manual keyboard + focus + landmarks walk-through` — for 8 representative routes (home, /apply, /pricing, /founding-member, /about, /memory, /skills/clyde, /case-studies/skinclarity-club) check via DOM snapshot inspection plus `agent-browser-copilot` on localhost:

- Skip-link is first focusable element and targets `<main id="main">`
- aria-landmarks present (banner, main, contentinfo, complementary, navigation)
- Header mega-menu keyboard pattern (Enter/Space opens, Arrows navigate, Escape closes, focus returns)
- BookingModal focus-trap + return-on-close
- Form first-error focus
- Reduced-motion full coverage (cross-ref 16-09 motion findings)

Append `## Readability per locale` — for the top 10 content-heavy routes, compute Flesch-Kincaid (or Leesindex Mommers for NL) scores per locale on the body copy extracted from DOM snapshots. Render as table: Route, NL score, EN score, ES score, target ≥60.

Append `## Top 25 findings` ranked per severity. Each finding follows the standard schema.

Append `## EAA compliance scorecard`: explicit pass/fail per EAA Annex I obligation relevant to web (perceivable, operable, understandable, robust).

Update STATE.md, commit:

```bash
git add docs/audits/2026-05-18-v2/07-accessibility.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-08 Team 06 accessibility deep-dive WCAG 2.2 AA"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/07-accessibility.md' -Raw; if (($c -match '## Manual keyboard') -and ($c -match '## Readability per locale') -and ($c -match '## EAA compliance scorecard') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/07-accessibility.md' -Pattern '^### Finding ').Count -ge 25)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Manual walk-throughs + readability + EAA scorecard + ≥25 findings + single commit.</done>
</task>

</tasks>

<verification>
- `07-accessibility.md` ≥800 words; axe aggregation + per-route scorecard + manual walk-throughs + readability + EAA scorecard + ≥25 findings.
- Findings reference real axe JSON files and DOM snapshots.
- Conventional commit landed.
</verification>

<success_criteria>
16-16 fix-plan can rank a11y work by severity × route-coverage. The EAA scorecard establishes legal-risk baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-08-SUMMARY.md`
</output>
