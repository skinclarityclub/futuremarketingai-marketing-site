---
phase: 16-design-seo-audit-v2-sota
plan: 09
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/08-performance.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '08-performance.md exists with ≥800 words'
    - 'Lighthouse runs aggregated across ≥10 routes × mobile + desktop form-factor'
    - 'Doc captures LCP-element + LCP-source per route'
    - 'Bundle analysis (First Load JS top 10 routes) is captured from 16-02 build baseline'
    - 'CrUX field data (or PageSpeed Insights API fallback) is captured for the production URL'
    - 'Findings carry full severity schema'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/08-performance.md'
      provides: 'Performance + CWV + bundle audit'
      contains: 'CWV'
  key_links:
    - from: '08-performance.md'
      to: 'fmai-nextjs/test-results/audit-v2/lighthouse/'
      via: 'JSON aggregation'
      pattern: 'lighthouse/'
---

<objective>
Aggregate Lighthouse runs from 16-02, layer CrUX field data on top, identify LCP sources per route, and analyze the bundle composition. Find regressions or remaining bloat after Phase 13's 70 KB cleanup.

Purpose: Phase 13 trimmed 70 KB. This plan finds what remains and re-baselines CWV so 16-16 can target weekly Lighthouse Performance > 90 across critical routes.
Output: `docs/audits/2026-05-18-v2/08-performance.md` with CWV scorecard, LCP analysis, bundle top-10.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate Lighthouse runs + capture CrUX + LCP source per route</name>
  <files>docs/audits/2026-05-18-v2/08-performance.md</files>
  <action>
Walk `fmai-nextjs/test-results/audit-v2/lighthouse/` and aggregate each `<route>-<locale>-<formFactor>.json`. Build:

1. `## Executive summary` (top): median Lighthouse Performance across mobile + desktop, P0/P1/P2 CWV failures, top 3 themes.
2. `## CWV scorecard`: table per route × form-factor with LCP (ms), FID/INP (ms), CLS, TBT (ms), Performance score. Use red/yellow/green per Core Web Vitals thresholds.
3. `## LCP element + source per route`: table with route, LCP element selector, source URL (image, font, hero), size (KB), opportunity hint from Lighthouse.
4. `## CrUX field data`: query PageSpeed Insights API (`https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://future-marketing.ai/<locale><route>&strategy=mobile`) for top 5 routes via WebFetch (≤10 calls total). Capture origin-level + URL-level CrUX where available. Log calls to BUDGET.log.

Rules: geen em-dashes, no production-code edits, no `taskkill`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/08-performance.md' -Raw -ErrorAction Stop; if (($c -match '## CWV scorecard') -and ($c -match '## LCP element')) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>CWV scorecard + LCP analysis + CrUX field data captured.</done>
</task>

<task type="auto">
  <name>Task 2: Bundle analysis + findings + commit</name>
  <files>docs/audits/2026-05-18-v2/08-performance.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append `## Bundle analysis`:

- Pull top-10 routes by First Load JS from `01-baseline-snapshot.md` build output.
- For each route, list dominant chunks (e.g. spline-runtime, framer-motion, ChatWidget, CalendlyModal, BookingModal).
- Identify chunks present that shouldn't be (cross-ref Phase 13-01 ClientIslands gating).
- Identify font payloads (target ≤2 families per Phase 13-03).
- Reference dead-code candidates (orphan components, unused @types/etc).

Append `## Third-party scripts audit`: GA4, Resend, Calendly, Spline, Stripe. Each: load mechanism (sync/async/defer/dynamic), size, network-waterfall position from HAR files in `test-results/audit-v2/har/`.

Append `## Image audit`: count of `<img>` and `next/image` instances per route, formats (PNG/WebP/AVIF), responsive sizes used, lazy-loading attribute presence, hero-image priority hint.

Append `## Top 25 findings` ranked, with `effort` callout for L items.

Append `## KPI baseline + targets for 16-16`: median Perf score, LCP p75, INP p75, CLS p75, bundle size, with proposed targets for the 12-week roadmap.

Update STATE.md, commit:

```bash
git add docs/audits/2026-05-18-v2/08-performance.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-09 Team 07 performance + CWV + bundle analysis"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/08-performance.md' -Raw; if (($c -match '## Bundle analysis') -and ($c -match '## Third-party scripts audit') -and ($c -match '## KPI baseline') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/08-performance.md' -Pattern '^### Finding ').Count -ge 25)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Bundle + third-party + image + ≥25 findings + KPI baseline + single commit.</done>
</task>

</tasks>

<verification>
- `08-performance.md` ≥800 words; CWV scorecard + LCP source + bundle + third-party + image + ≥25 findings.
- Conventional commit landed.
</verification>

<success_criteria>
16-16 roadmap weekly KPI tracker can lift Lighthouse Performance and CWV against measurable baselines.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-09-SUMMARY.md`
</output>
