---
phase: 16-design-seo-audit-v2-sota
plan: 11
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/10-content-copy-i18n.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '10-content-copy-i18n.md exists with ≥800 words'
    - 'Doc verifies i18n parity (key count + structure) across NL/EN/ES'
    - 'Doc enforces glossary across all 3 locales (vaardigheden, merken, Plan een gesprek)'
    - 'Doc captures em-dash sweep across messages/*.json (zero allowed)'
    - 'Flesch-Kincaid (NL: Leesindex Mommers) readability scores captured for top 10 routes per locale'
    - 'Cultural-fit findings for ES locale captured optionally via Gemini grounded check'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/10-content-copy-i18n.md'
      provides: 'Content + copy + i18n cultural adaptation audit'
      contains: 'i18n parity'
  key_links:
    - from: '10-content-copy-i18n.md'
      to: 'fmai-nextjs/messages/nl.json, en.json, es.json'
      via: 'read-only key-by-key comparison'
      pattern: 'messages/'
---

<objective>
Compare i18n key trees across NL/EN/ES for parity, scan for glossary slips, em-dashes, IK/WIJ tone slips, hardcoded English in NL/ES, and run a cultural-fit check on ES via Gemini grounded research.

Purpose: Phase 12 closed a batch of i18n debt. Phase 16 finds what's regressed since and what new debt the content-upgrade introduced.
Output: `docs/audits/2026-05-18-v2/10-content-copy-i18n.md` with parity table, glossary scorecard, em-dash sweep, readability.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n parity + glossary scorecard + em-dash sweep</name>
  <files>docs/audits/2026-05-18-v2/10-content-copy-i18n.md</files>
  <action>
Read `fmai-nextjs/messages/nl.json`, `en.json`, `es.json` (read-only — do NOT edit). Use the standard `jq`-style comparison to build:

1. `## Executive summary` (top): parity-coverage %, glossary-compliance %, em-dash count, top 3 themes.
2. `## i18n parity matrix`:
   - Total top-level namespaces per locale
   - Missing namespaces in EN vs NL
   - Missing namespaces in ES vs NL
   - Missing nested keys per namespace (sample top 10 gaps)
3. `## Glossary scorecard` (cross-ref 16-04 brand audit):
   - `Plan een gesprek` (must be present where booking CTA appears, NL only — EN `Book a call`, ES `Reserva una llamada`)
   - `vaardigheden` (NL, never `features` in user-facing copy)
   - `merken` on conversion pages (NL — never `klanten`)
   - `AI Marketing Medewerker` and `Clyde` consistency across locales
4. `## Em-dash sweep` — count of `—` in NL, EN, ES `messages/*.json` (target: 0). Each occurrence with key path + value excerpt.
5. `## IK/WIJ slips` — first-person voice audit in NL: should use TU/JE (informal) consistently, no "wij" salesy slips on conversion pages.
6. `## Hardcoded English in NL/ES` — scan rendered DOM in `test-results/audit-v2/dom/` for English strings on `/nl/...` and `/es/...` pages that aren't proper nouns (Clyde, FMai). Severity-tag with route + DOM line.

Rules: geen em-dashes (irony noted), no production-code edits, read-only on messages/*.json.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/10-content-copy-i18n.md' -Raw -ErrorAction Stop; if (($c -match '## i18n parity matrix') -and ($c -match '## Em-dash sweep') -and ($c -match '## Glossary scorecard')) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Parity matrix + glossary + em-dash + IK/WIJ + hardcoded-English sections present.</done>
</task>

<task type="auto">
  <name>Task 2: Readability scores + optional ES cultural-fit check + findings + commit</name>
  <files>docs/audits/2026-05-18-v2/10-content-copy-i18n.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append `## Readability per locale` — for top 10 content-heavy routes (home, /about, /how-it-works, /memory, /pricing, /founding-member, /case-studies/skinclarity-club, /skills/clyde, /skills/social-media, /blog), extract body copy from `test-results/audit-v2/dom/` and compute:

- NL: Leesindex Mommers (or Flesch-Kincaid NL adaptation)
- EN: Flesch-Kincaid Reading Ease
- ES: Fernandez-Huerta or Szigriszt-Pazos

Target: ≥60 (plain language). Render as table. Tag routes scoring <50 as P1 readability issues.

Append `## Cultural-fit check (optional)`:

Use Gemini grounded research with one call (count this toward the 100 phase cap, log to BUDGET.log) for the ES locale. Query example: `"Voor een Nederlandse AI marketing platform die Spaanse bureau-eigenaren wil aanspreken, welke culturele en taalkundige aspecten zijn cruciaal in B2B SaaS messaging in 2026? Voorbeelden van native EU-Spaanse SaaS sites."`

Summarise the response and cross-reference findings against the ES copy in `messages/es.json` (read-only).

Append `## Top 25 findings` ranked. Each follows standard schema.

Update STATE.md, commit:

```bash
git add docs/audits/2026-05-18-v2/10-content-copy-i18n.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-11 Team 09 content + copy + i18n cultural adaptation"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/10-content-copy-i18n.md' -Raw; if (($c -match '## Readability per locale') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/10-content-copy-i18n.md' -Pattern '^### Finding ').Count -ge 25)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Readability + ES cultural-fit + ≥25 findings + single commit.</done>
</task>

</tasks>

<verification>
- `10-content-copy-i18n.md` ≥800 words; parity + glossary + em-dash + readability + ≥25 findings.
- Conventional commit landed.
- No edits to messages/*.json.
</verification>

<success_criteria>
16-16 i18n backlog has concrete missing-keys table + glossary fix-list ready for /gsd:plan-phase consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-11-SUMMARY.md`
</output>
