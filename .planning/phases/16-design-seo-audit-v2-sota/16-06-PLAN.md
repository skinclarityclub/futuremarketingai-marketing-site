---
phase: 16-design-seo-audit-v2-sota
plan: 06
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/05-seo-technical.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '05-seo-technical.md exists with ≥800 words'
    - 'Per-route metadata matrix exists with 32 routes × 3 locales rows (96 rows) covering title, description, canonical, og, hreflang, robots'
    - 'JSON-LD entities are validated route-by-route (Organization, Person, Service, Faq, Article, Speakable, BreadcrumbList) with pass/fail per entity'
    - 'sitemap.xml and robots.ts are validated against canonical domain future-marketing.ai'
    - 'Findings carry full severity schema'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/05-seo-technical.md'
      provides: 'SEO technical audit + JSON-LD validation + 96-row metadata matrix'
      contains: 'metadata matrix'
  key_links:
    - from: '05-seo-technical.md'
      to: 'fmai-nextjs/test-results/audit-v2/dom/'
      via: 'rendered head + JSON-LD evidence'
      pattern: 'dom/'
    - from: '05-seo-technical.md'
      to: 'fmai-nextjs/public/llms.txt and llms-full.txt'
      via: 'llms.txt validation references (read-only)'
      pattern: 'llms.txt'
---

<objective>
Validate every SEO-relevant surface across 32 routes × 3 locales: title, description, canonical, hreflang, og, twitter, robots, JSON-LD entities, sitemap.xml, robots.ts, llms.txt. Score against Google rich-results criteria and the SOTA markers from 16-01.

Purpose: Phase 14 lifted GEO 42 → 70+. This plan finds what remains: missing canonicals, hreflang gaps, JSON-LD entities that fail rich-result validation, meta-description truncation, schema-content drift.
Output: `docs/audits/2026-05-18-v2/05-seo-technical.md` with 96-row metadata matrix, JSON-LD validation matrix, sitemap/robots audit.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
@docs/research-schema-markup-structured-data-seo-geo.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 96-row metadata matrix from DOM snapshots</name>
  <files>docs/audits/2026-05-18-v2/05-seo-technical.md</files>
  <action>
For each of 32 routes × 3 locales (96 rows), parse the `<head>` from `fmai-nextjs/test-results/audit-v2/dom/<route>-<locale>.html` and extract:

- `<title>` (length in chars)
- `<meta name="description">` (length in chars, truncation risk if >155)
- `<link rel="canonical">` (URL, must use `future-marketing.ai`)
- `<link rel="alternate" hreflang="nl|en|es|x-default">` (presence, correctness)
- `<meta property="og:title">`, `og:description`, `og:image`, `og:locale`, `og:url`
- `<meta name="twitter:card">`, `twitter:image`
- `<meta name="robots">`

Render as `## Metadata matrix` table (markdown). Use one row per route+locale = 96 rows. For long-description fields, link to the DOM file rather than inlining full text.

Tag fails in a final column `Issues` with bracketed severity tags `[P0-canonical-mismatch]`, `[P1-description-truncated]`, `[P2-hreflang-missing]` etc.

Cross-reference with `fmai-nextjs/public/sitemap.xml` (read-only) and `fmai-nextjs/src/app/robots.ts` (read-only): are all 32 routes present in sitemap, do robots Allow rules cover the 15+ AI crawlers from Phase 14-04?

Rules: geen em-dashes, no production-code edits.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/05-seo-technical.md' -Raw -ErrorAction Stop; if ($c -match '## Metadata matrix') { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>96-row metadata matrix written; sitemap + robots cross-check noted inline.</done>
</task>

<task type="auto">
  <name>Task 2: Validate JSON-LD entities + sitemap + llms.txt + commit</name>
  <files>docs/audits/2026-05-18-v2/05-seo-technical.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append `## JSON-LD validation` — for each route+locale, list every `<script type="application/ld+json">` block in the DOM snapshot. Validate each:

- Schema type (Organization, Person, Service, FAQPage, Article, BreadcrumbList, SpeakableSpecification)
- Required fields per schema.org spec + Google rich-result criteria
- `@id` stability (matches `${SITE_URL}/path/#fragment` pattern from Phase 14-01)
- Cross-references (`Person.worksFor.@id` resolves; `Service.provider.@id` resolves; `Faq` items match visible FAQ word-for-word)
- Speakable selectors actually match elements in the rendered DOM

Render as a `JSON-LD validation matrix` table: Route, Locale, Schema type, Status (pass/warn/fail), Issues.

Run optional Google Rich Results test via WebFetch against `https://search.google.com/test/rich-results?url=<route>` for the 5 highest-impact routes (home, /pricing, /founding-member, /case-studies/skinclarity-club, /skills/clyde × 3 locales = 15 calls). Cap at 15 WebFetch calls; log to BUDGET.log.

Append `## sitemap.xml audit`:
- Count entries, compare to actual rendered routes
- lastmod accuracy (vs git mtime)
- hreflang alternates per entry
- Priority/changefreq sanity

Append `## robots.ts audit`:
- AI-crawler Allow rules count (target ≥15 per Phase 14-04)
- Host line uses `future-marketing.ai`
- Disallow rules don't block sitemap-listed routes

Append `## llms.txt and llms-full.txt audit`:
- llmstxt.org spec compliance
- v10 pricing accuracy
- 12-skill mention
- domain canonical references

Append `## Top 25 findings` ranked, `## Executive summary` at top.

Update STATE.md, commit:

```bash
git add docs/audits/2026-05-18-v2/05-seo-technical.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-06 Team 04 SEO technical + schema.org validation"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/05-seo-technical.md' -Raw; if (($c -match '## JSON-LD validation') -and ($c -match '## sitemap.xml audit') -and ($c -match '## robots.ts audit') -and ($c -match '## llms.txt') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/05-seo-technical.md' -Pattern '^### Finding ').Count -ge 25)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>JSON-LD matrix + sitemap audit + robots audit + llms.txt audit + ≥25 findings + executive summary; single commit.</done>
</task>

</tasks>

<verification>
- `05-seo-technical.md` ≥800 words; 96-row metadata matrix; JSON-LD validation matrix; sitemap + robots + llms.txt audits.
- ≥25 ranked findings with full schema.
- No edits to production code.
- Conventional commit landed.
</verification>

<success_criteria>
16-16 can rank SEO fixes by rich-result eligibility impact. Per-route data is concrete enough that fix-plan can name exact files and line numbers.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-06-SUMMARY.md`
</output>
