---
phase: 16-design-seo-audit-v2-sota
plan: 03
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/02-visual-design.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '02-visual-design.md exists with ≥800 words and contains Top 25 findings'
    - 'Each finding has severity (P0/P1/P2/P3), route(s), viewport(s), locale(s), evidence path, impact-hypothesis, proposed fix, effort (S/M/L), confidence (low/med/high)'
    - 'Doc includes 3 alternate visual directions described textually (no mockups in this phase)'
    - 'Findings reference the 25 SOTA markers from 16-01 by number'
    - 'Zero em-dashes in the doc'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/02-visual-design.md'
      provides: 'Visual design Top 25 findings + 3 alternate directions'
      contains: 'Visual Design'
  key_links:
    - from: '02-visual-design.md'
      to: 'test-results/audit-v2/screenshots/'
      via: 'evidence-path references'
      pattern: 'screenshots/'
    - from: '02-visual-design.md'
      to: '00-competitive-intel.md SOTA markers'
      via: 'marker-number citations'
      pattern: 'marker '
---

<objective>
Audit FMai marketing site through the ui-ux-pro-max + frontend-design lens: typography hierarchy, color use, spacing rhythm, motion patterns, density, component consistency, alignment with the SOTA marker rubric. Produce a Top 25 ranked findings doc plus 3 alternate visual directions described textually.

Purpose: Wave 4 (plan 16-16) cannot build the fix-plan and roadmap without ranked, severity-tagged visual findings. This team owns the visual-design slice end-to-end.
Output: `docs/audits/2026-05-18-v2/02-visual-design.md` (≥800 words, Top 25 findings, 3 alternate directions, references to 16-01 SOTA markers).
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk the screenshot gallery and DOM dir, surface visual-design issues</name>
  <files>docs/audits/2026-05-18-v2/02-visual-design.md</files>
  <action>
Open `fmai-nextjs/test-results/audit-v2/screenshots/index.html` and walk through the home, pricing, founding-member, memory, about, how-it-works, case-studies/skinclarity-club, and the 12 skill pages across 3 locales (NL/EN/ES) and 5 viewports.

For each screen, evaluate against the 25 SOTA markers in `docs/audits/2026-05-18-v2/00-competitive-intel.md` § "SOTA markers rubric". Capture any deviation as a finding with this exact schema (per AUTONOMOUS-PROTOCOL.md):

```
### Finding N: <one-line title>

- **Severity**: P0 | P1 | P2 | P3
- **SOTA marker**: marker <number> from 00-competitive-intel.md (one or more)
- **Routes**: /, /pricing, /skills/clyde (etc.)
- **Viewports**: mobile-s, mobile-l, tablet, desktop, desktop-w (subset)
- **Locales**: nl, en, es (or all)
- **Evidence**: test-results/audit-v2/screenshots/<path>.png (one or more)
- **Code path (if applicable)**: fmai-nextjs/src/<file>:<line> (one or more)
- **Impact hypothesis**: one sentence on which KPI this affects (conversion, trust, perceived quality)
- **Proposed fix**: one paragraph, no code, fix-plan compatible
- **Effort**: S | M | L
- **Confidence**: low | med | high
```

Categories to scan (use these as section headers in the doc):

1. Typography (font-family, size scale, line-height, letter-spacing, heading rhythm, mono-vs-sans pairing, NL/EN/ES glyph fit)
2. Color (palette adherence to `#0a0d14 / #00d4aa / #f5a623 / #e8ecf4 / #8C98AD`, contrast on bg-deep/bg-surface, accent over-use, gradient consistency)
3. Spacing (vertical rhythm, section padding, card padding, mobile-vs-desktop transitions)
4. Component consistency (CTA shapes, GlassCard variants, badge styling, icon sizing, divider use)
5. Motion (Framer Motion patterns, prefers-reduced-motion respect, scroll-trigger timing, blob animations, hover micro-interactions)
6. Density and information hierarchy (above-the-fold density, F-pattern compatibility, hero-CTA dominance, primary-vs-secondary visual weight)
7. Visual proof (logo-lab, hero-Spline, OG image, founder photo, Sindy headshot placeholder)

For each category produce 2-5 findings. Total: at least 25. Reference SOTA markers by number from 16-01.

Rules:
- Geen em-dashes (—). Use komma, punt, of dubbele punt.
- Evidence paths must point at real PNGs from the gallery; if a route was skipped in 16-02, mark `evidence: SKIPPED in 16-02 baseline` and use a code-path-only reasoning instead.
- Code-path references include line numbers wherever possible (e.g. `fmai-nextjs/src/app/[locale]/page.tsx:124`).
- No proposed fix may include a code patch in this plan; this is a research doc.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Test-Path 'docs/audits/2026-05-18-v2/02-visual-design.md') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/02-visual-design.md' -Pattern '^### Finding ').Count -ge 25)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Doc has at least 25 findings, each with full schema fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add 3 alternate visual directions + commit</name>
  <files>docs/audits/2026-05-18-v2/02-visual-design.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append a `## Alternate visual directions` section to the doc. Describe 3 distinct redesign directions textually (no Stitch/Higgsfield mockups in this phase, per CONTEXT § Deferred). Each direction:

```
### Direction A: <name>

- **Hero pattern**: one paragraph
- **Color treatment**: tokens to add/remove, ratios
- **Typography**: type-scale changes, font-family swaps if any
- **Motion**: pattern shift (e.g. "drop scroll-pinned, embrace static cinematic")
- **IA implication**: how nav and section order change
- **Reference SOTA site** (from 16-01): which competitor most closely embodies this direction
- **Risk**: what this loses vs current
- **Effort**: S | M | L
```

Suggested directions (pick 3, or substitute with stronger fits if Wave 0 intel revealed better candidates):
- A: "Minimal editorial" (Vercel/Linear)
- B: "AI-cinematic" (Anthropic/OpenAI hero)
- C: "Conversion-dense SaaS" (Stripe/Intercom)

Append a `## Executive summary` (one paragraph max) at the TOP of the doc summarising P0 + P1 counts, top 3 themes, and recommended direction.

Append a `## Scope adaptations` section if any 16-02 captures were skipped (mention which routes/engines).

Update STATE.md with `16-03: status: completed` + timestamp + SHA placeholder. Then commit:

```bash
git add docs/audits/2026-05-18-v2/02-visual-design.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-03 Team 01 visual design + style catalog evaluation"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/02-visual-design.md' -Raw; if (($c -match '## Alternate visual directions') -and ($c -match '### Direction A') -and ($c -match '### Direction B') -and ($c -match '### Direction C')) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Doc contains executive summary, 25+ findings, and 3 alternate directions; atomic commit landed.</done>
</task>

</tasks>

<verification>
- `02-visual-design.md` exists, ≥800 words, ≥25 schema-compliant findings.
- 3 alternate visual directions described textually with reference sites + risk.
- Evidence paths point at real PNGs or document the 16-02 skip.
- Conventional commit landed on `audit/2026-05-18-v2-sota`.
- No edits to `fmai-nextjs/src/` or `fmai-nextjs/messages/`.
</verification>

<success_criteria>
Plan 16-16 can pull a ranked visual-design backlog from this doc without re-evaluation. The 3 alternate directions feed the strategic roadmap.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-03-SUMMARY.md`
</output>
