---
phase: 16-design-seo-audit-v2-sota
plan: 05
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '04-interactions-forms-microcopy.md exists with ≥800 words'
    - 'Every CTA across 32 routes is catalogued with state matrix (default/hover/focus/active/disabled)'
    - 'ApplicationForm and ContactForm are deep-audited (each field, each error state, each success state)'
    - 'Microcopy compliance with brand glossary verified (Plan een gesprek, no Sign up, no klanten)'
    - 'Findings carry severity, route, viewport, locale, evidence, code-path, impact, fix, effort, confidence'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md'
      provides: 'Every CTA + form state catalog + microcopy findings'
      contains: 'CTA'
  key_links:
    - from: '04-interactions-forms-microcopy.md'
      to: 'fmai-nextjs/src/components/apply/ApplicationForm.tsx and forms/ContactForm.tsx'
      via: 'read-only code-path references'
      pattern: 'ApplicationForm'
---

<objective>
Catalog every CTA, link, form field, and microcopy string across 32 routes. Drive each interaction through default/hover/focus/active/disabled/error/success states using agent-browser-copilot + Playwright DOM snapshots. Score for clarity, brand-glossary compliance, accessibility, friction.

Purpose: Conversion lives in interaction details. Wave 4 fix-plan needs a CTA-by-CTA backlog ranked by leak-cost.
Output: `docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md` with state matrices, form deep-dives, microcopy findings.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog every CTA + link with state matrix</name>
  <files>docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md</files>
  <action>
Open `fmai-nextjs/test-results/audit-v2/dom/` and grep for `<button`, `<a `, `CTAButton`, `role="button"`. Build a master CTA table:

`## CTA inventory` — columns: ID (page + label slug), Route, Label NL, Label EN, Label ES, Variant (primary/secondary/link/icon), Destination URL, Brand-glossary compliance (pass/fail), Notes. Aim for ≥80 entries (32 routes × ~2.5 CTAs avg).

For each unique CTA archetype (not every route), use `agent-browser-copilot` to walk through the 5 states (default/hover/focus/active/disabled) on http://localhost:3000 + the live production URL. Capture observations:

`## State matrix per archetype` — for each archetype: hover behaviour, focus ring visible/absent, focus-visible color, active state distinguishable, disabled state visible, keyboard activation works (Enter/Space).

Findings schema (per AUTONOMOUS-PROTOCOL.md):

```
### Finding N: <title>
- Severity: P0|P1|P2|P3
- Route(s): /, /apply, /pricing (etc.)
- Viewport(s)
- Locale(s)
- Evidence: dom/<file>.html line OR screenshots/<file>.png
- Code path: fmai-nextjs/src/<file>:<line> (read-only reference)
- Impact hypothesis
- Proposed fix (no code)
- Effort: S|M|L
- Confidence: low|med|high
```

Cross-check brand glossary on every label: `Plan een gesprek` (not `Boek`, not `Sign up`), `vaardigheden`, no `klanten` on conversion CTAs.

Rules: geen em-dashes, no production-code edits, do not `taskkill` the dev-server.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md' -Raw -ErrorAction Stop; if (($c -match '## CTA inventory') -and ($c -match '## State matrix per archetype') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md' -Pattern '^### Finding ').Count -ge 15)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>CTA inventory + state matrix per archetype, ≥15 findings present.</done>
</task>

<task type="auto">
  <name>Task 2: Deep-audit ApplicationForm + ContactForm + microcopy + commit</name>
  <files>docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append `## ApplicationForm deep-audit`:

For each field (name, email, organization, role, problem, etc.):
- Label NL/EN/ES, placeholder presence, helper text, required marker
- Validation rule (zod schema reference, read-only)
- Error message NL/EN/ES (per-field aria-describedby wiring after Phase 11-03)
- aria-invalid behaviour
- autoComplete token correctness (per Phase 11-03)
- inputMode correctness
- Tab order
- First-error focus behaviour
- Success state (Phase 15-02 Calendly embed)
- Failure modes (network error, validation error, rate-limit)

Append `## ContactForm deep-audit` — same schema.

Append `## Microcopy findings` table:
- Page, Selector, Current NL/EN/ES, Brand-glossary call (pass/fail), Recommended NL/EN/ES, Severity

Append `## Top 25 findings` ranked across CTA + forms + microcopy.

Append `## Executive summary` (top of doc).

Update STATE.md, commit:

```bash
git add docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-05 Team 03 interactions + forms + microcopy"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md' -Raw; if (($c -match '## ApplicationForm deep-audit') -and ($c -match '## ContactForm deep-audit') -and ($c -match '## Microcopy findings') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/04-interactions-forms-microcopy.md' -Pattern '^### Finding ').Count -ge 25)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Doc has both form deep-audits, microcopy table, ≥25 findings, executive summary, single commit.</done>
</task>

</tasks>

<verification>
- `04-interactions-forms-microcopy.md` ≥800 words, contains CTA inventory + 2 form deep-audits + ≥25 findings.
- Findings reference real DOM/screenshot evidence and read-only code paths.
- Conventional commit landed.
</verification>

<success_criteria>
16-16 can rank CTA + form fixes by leak-cost without re-walking each form. Microcopy table feeds directly into i18n fix work.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-05-SUMMARY.md`
</output>
