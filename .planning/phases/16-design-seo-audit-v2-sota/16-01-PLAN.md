---
phase: 16-design-seo-audit-v2-sota
plan: 01
type: research
wave: 1
depends_on: []
files_modified:
  - docs/audits/2026-05-18-v2/00-competitive-intel.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - 'docs/audits/2026-05-18-v2/00-competitive-intel.md exists and is at least 800 words'
    - 'Doc lists 5 to 7 named competitors with URL, positioning summary, public pricing where available'
    - 'Doc lists 3 reference SOTA sites with key-pattern extraction (hero, IA, schema, motion, color)'
    - 'Doc lists 25 SOTA markers as a rubric reusable by Wave 2 teams'
    - 'All research routed through Gemini 2.5 Flash with Google Search grounding, zero Perplexity calls in this plan'
    - 'Each Gemini call logged to BUDGET.log with timestamp, model, and one-line query summary'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/00-competitive-intel.md'
      provides: 'Wave 0 competitive baseline and SOTA marker rubric'
      contains: 'SOTA markers'
    - path: '.planning/phases/16-design-seo-audit-v2-sota/BUDGET.log'
      provides: 'Append-only research-call telemetry'
      contains: 'gemini-flash-grounded'
  key_links:
    - from: '00-competitive-intel.md'
      to: 'Wave 2 team plans 16-03 through 16-14'
      via: 'SOTA marker rubric and competitor scorecard'
      pattern: 'SOTA markers'
---

<objective>
Establish the Wave 0 competitive and SOTA baseline that anchors all 12 Wave 2 audit teams. Without this baseline the team docs have no external benchmark and the executive summary cannot rank findings on a SOTA-relative scale.

Purpose: Wave 2 teams need (a) named competitors to score against, (b) reference best-in-class patterns to compare FMai against, and (c) a 25-marker SOTA rubric so severity tagging stays consistent across 12 parallel docs.
Output: One audit doc `docs/audits/2026-05-18-v2/00-competitive-intel.md` plus an append-only `BUDGET.log` of research telemetry.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run three Gemini grounded research queries and log each call</name>
  <files>.planning/phases/16-design-seo-audit-v2-sota/BUDGET.log</files>
  <action>
Pre-flight: verify Gemini key with `node -e "console.log(!!process.env.GEMINI_API_KEY ? 'OK' : 'MISSING')"`. Fail with hard-abort note if MISSING (see AUTONOMOUS-PROTOCOL.md § Hard abort). Then run the three queries below verbatim through `node ~/.claude/scripts/gemini-research.mjs "<query>" --json --model flash`. Capture raw JSON output for each to a temp file (e.g. `.planning/phases/16-design-seo-audit-v2-sota/tmp/q1.json`, `q2.json`, `q3.json`); these temp files are gitignored and may be discarded after Task 3.

Queries (run in this order):

1. `"Top AI marketing agencies and AI agent-as-a-service platforms for Dutch and EU B2B SMBs in 2026 — named competitors, pricing models, positioning. Provide URLs and current pricing where public."`
2. `"Best-in-class SaaS marketing websites 2026 — landing page design patterns, conversion optimization, SOTA examples with URLs and what makes them stand out."`
3. `"Current state of GEO and LLMEO best practices 2026 — schemas, llms.txt patterns, AI-citation tactics, real-world case studies measuring AI traffic uplift."`

For every call, append one line to `.planning/phases/16-design-seo-audit-v2-sota/BUDGET.log` in this format (UTC timestamp, ISO 8601):

```
2026-05-18T14:05:23Z gemini-flash-grounded call 1 of 100 (query: "Top AI marketing agencies...")
```

Rules: NO Perplexity calls in this plan (per AUTONOMOUS-PROTOCOL.md research provider lock). On 429: wait 60 s, retry up to 2 times; if still failing, switch `--model flash` to `--model pro` (separate quota) and document the swap in BUDGET.log. Soft-cap awareness: ≤100 grounded calls across the entire phase; this plan should consume ≤6.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Get-Content '.planning/phases/16-design-seo-audit-v2-sota/BUDGET.log' -ErrorAction Stop | Select-String 'gemini-flash-grounded').Count -ge 3) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>BUDGET.log contains at least three gemini-flash-grounded entries; raw JSON outputs available in tmp/ for Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Scrape three reference SOTA sites with firecrawl</name>
  <files>.planning/phases/16-design-seo-audit-v2-sota/BUDGET.log</files>
  <action>
From the Gemini query 2 output (best-in-class SaaS marketing websites 2026) pick the 3 most-cited reference sites. If fewer than 3 named URLs return, supplement with reasonable defaults from the SOTA marketing-site canon (e.g. linear.app, vercel.com, stripe.com, framer.com, anthropic.com, openai.com) and document the substitution in the doc Task 3 writes.

For each of the 3 URLs run `firecrawl-scrape` (use the `firecrawl-scrape` skill) to extract markdown of the homepage. Capture: hero pattern, primary CTA, IA depth (top-nav items), color palette, motion patterns, schema.org types detected, llms.txt presence (try `<root>/llms.txt`), key UVP phrasing.

Log each firecrawl call to BUDGET.log:

```
2026-05-18T14:12:44Z firecrawl-scrape call 1 of 80 (url: https://linear.app)
```

Rules: ≤3 firecrawl calls for this task. If a site blocks scraping or returns <500 words, document the gap and pick the next candidate. Total Phase 16 firecrawl budget is 80 calls.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Get-Content '.planning/phases/16-design-seo-audit-v2-sota/BUDGET.log' -ErrorAction Stop | Select-String 'firecrawl-scrape').Count -ge 3) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>BUDGET.log shows 3 firecrawl-scrape calls; extracted markdown captured for use in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Synthesize competitive-intel.md with 5-7 competitors, 3 SOTA references, 25 SOTA markers</name>
  <files>docs/audits/2026-05-18-v2/00-competitive-intel.md</files>
  <action>
Create the file `docs/audits/2026-05-18-v2/00-competitive-intel.md` (≥800 words). Use these sections, in order:

1. `## Scope` — one paragraph stating this doc anchors Wave 2 teams. No production code changes. Research provider: Gemini 2.5 Flash with Google Search grounding only.
2. `## Competitors (5-7 named)` — table with columns: Name, URL, Region, Positioning summary (one sentence), Public pricing (or "ondoorzichtig"), Differentiator vs FMai, Threat level (low/med/high). Source from Gemini query 1 output. If <5 strong competitors return, mark gaps and add nearest substitutes (e.g. generic marketing automation SaaS).
3. `## Reference SOTA sites (3)` — for each site from Task 2: hero pattern, IA top-nav, color palette tokens, motion patterns observed, schema.org types detected, llms.txt presence, UVP phrasing extracted, one screenshot link (place at `test-results/audit-v2/screenshots/refs/<slug>.png` if captured; otherwise mark TODO).
4. `## SOTA markers rubric (25 markers)` — numbered list of 25 binary markers Wave 2 teams use to score FMai pages. Group into 5 categories (Visual 1-5, IA 6-10, SEO/GEO 11-15, Performance 16-20, Conversion 21-25). Each marker is a one-line testable statement, e.g. `1. Hero has one dominant primary CTA (no split-attention secondary at equal visual weight)`. Markers are reused as scoring axis across 02 through 13.
5. `## GEO/LLMEO state of practice 2026` — summary of Gemini query 3 output. Include current SOTA llms.txt patterns, key schema types (Article, Service, FAQ, Speakable, Person, Organization), AI-crawler allowlist conventions, and ≥1 case study reference for AI-traffic uplift measurement (cite source URL).
6. `## Open questions for Wave 2` — bullet list of 5-10 unresolved questions Wave 2 teams should answer (e.g. "Does competitor X publish a public roadmap?").
7. `## Sources` — every URL cited above with one-line context.

Formatting rules (per CLAUDE.md + AUTONOMOUS-PROTOCOL.md):

- Geen em-dashes (—) anywhere in this doc. Use komma, punt, of dubbele punt.
- Nederlands voor user-facing samenvattingen waar relevant, Engels voor technische termen.
- Severity tags op findings: `P0` / `P1` / `P2` / `P3` (Wave 2 teams use the same scheme).
- Geen `taskkill`/`killall`/`pkill` ever.
- Canonical domain only: `future-marketing.ai`.

After writing, update `.planning/phases/16-design-seo-audit-v2-sota/STATE.md` with `16-01: status: completed`, finished timestamp, and the commit SHA placeholder. Discard the temp/ folder.

Final commit:

```bash
git add docs/audits/2026-05-18-v2/00-competitive-intel.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log .planning/phases/16-design-seo-audit-v2-sota/STATE.md
git commit -m "docs(audit): 16-01 Wave 0 pre-audit intelligence — competitors + SOTA markers"
```

No `--no-verify`. Conventional commits enforced.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Test-Path 'docs/audits/2026-05-18-v2/00-competitive-intel.md') -and ((Get-Content 'docs/audits/2026-05-18-v2/00-competitive-intel.md' | Measure-Object -Word).Words -ge 800)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>00-competitive-intel.md exists, ≥800 words, contains all 7 sections + 25 SOTA markers, committed atomically on the audit branch.</done>
</task>

</tasks>

<verification>
- `docs/audits/2026-05-18-v2/00-competitive-intel.md` exists with ≥800 words and 7 sections.
- 25 SOTA markers numbered and categorized.
- BUDGET.log shows ≥3 Gemini calls and ≥3 Firecrawl calls.
- No Perplexity calls anywhere in this plan.
- Single atomic commit `docs(audit): 16-01 ...` on `audit/2026-05-18-v2-sota`.
</verification>

<success_criteria>
Wave 2 teams have a defendable competitive baseline + 25-marker rubric to score every finding against. Without this plan, Wave 2 produces incoherent severity calls.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-01-SUMMARY.md`
</output>
