---
phase: 16-design-seo-audit-v2-sota
plan: 04
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/03-brand-narrative-ia.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '03-brand-narrative-ia.md exists with ≥800 words'
    - 'Doc maps the IA (sitemap, top-nav, footer) across 32 routes and identifies orphan and dead-end pages'
    - 'Doc grades 4 user-journey maps (Bureau-owner researcher, EU buyer evaluator, GTM bench-marker, returning prospect)'
    - 'Doc validates glossary compliance: vaardigheden, merken, Plan een gesprek, no klanten on conversion pages'
    - 'Doc surfaces narrative weaknesses with severity tags (P0/P1/P2/P3) and route references'
    - 'Findings reference SOTA markers from 16-01 where applicable'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/03-brand-narrative-ia.md'
      provides: 'Brand voice, narrative arc, IA, and user-journey audit'
      contains: 'Brand'
  key_links:
    - from: '03-brand-narrative-ia.md'
      to: 'fmai-nextjs/messages/*.json'
      via: 'glossary-key references (no edits)'
      pattern: 'messages/'
    - from: '03-brand-narrative-ia.md'
      to: '00-competitive-intel.md competitor IA patterns'
      via: 'competitor cross-references'
      pattern: 'competitor'
---

<objective>
Audit FMai brand voice (tone, vocabulary, glossary compliance), narrative arc (problem → product → proof → action), information architecture (sitemap, top-nav, footer, breadcrumbs), and 4 user-journey maps. Find narrative breaks, IA dead-ends, and glossary slips that erode positioning.

Purpose: Phase 16 ranks visual and conversion findings, but without a brand+narrative+IA audit the fix-plan misses the meta-layer that makes individual pages cohere. This team owns the meta-layer.
Output: `docs/audits/2026-05-18-v2/03-brand-narrative-ia.md` with sitemap diagram, 4 journey maps, glossary scorecard, and severity-tagged findings.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map IA + grade glossary across 3 locales</name>
  <files>docs/audits/2026-05-18-v2/03-brand-narrative-ia.md</files>
  <action>
Walk the DOM snapshots in `fmai-nextjs/test-results/audit-v2/dom/` for all 32 routes × 3 locales. Build the IA map:

1. `## Sitemap (actual rendered)` — table with: URL, parent (top-nav vs footer vs nested), depth from home, internal-links-out-count, internal-links-in-count, orphan-status (yes/no), dead-end (no outbound links other than nav/footer).
2. `## Top-nav comparison NL/EN/ES` — sub-tables showing every top-nav item per locale. Highlight i18n-divergent items.
3. `## Footer comparison NL/EN/ES` — same.
4. `## Breadcrumb coverage` — which routes render breadcrumbs, which do not, and where breadcrumb absence breaks the wayfinding promise.

Then grade brand-glossary compliance against `fmai-nextjs/CLAUDE.md` § "Key-phrase glossary":

5. `## Glossary scorecard` — table with: Term (`vaardigheden`, `merken`, `Plan een gesprek`, `AI Marketing Medewerker`, `Clyde`), expected, observed offences with route + line, severity. Mark `klanten` on conversion pages as P0. Mark `Boek een gesprek` as P1. Mark any em-dash in body copy as P2 (cross-checks 16-11 i18n team).

Use `Grep` style scans against `test-results/audit-v2/dom/` (read-only) to find offences; do not run scripts that would write to messages/ or src/.

Schema for every finding (same as 16-03):

```
### Finding N: <title>
- Severity: P0|P1|P2|P3
- Routes/Viewports/Locales
- Evidence: dom/<file>.html line excerpt OR screenshots/<file>.png
- Code path: messages/<locale>.json key (read-only reference)
- Impact hypothesis
- Proposed fix (no code)
- Effort: S|M|L
- Confidence: low|med|high
```

Rules: geen em-dashes, no production-code edits.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/03-brand-narrative-ia.md' -Raw -ErrorAction Stop; if (($c -match '## Sitemap') -and ($c -match '## Glossary scorecard') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/03-brand-narrative-ia.md' -Pattern '^### Finding ').Count -ge 12)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Sitemap, top-nav/footer comparison, breadcrumb coverage, glossary scorecard, and ≥12 findings present.</done>
</task>

<task type="auto">
  <name>Task 2: Build 4 user-journey maps + narrative arc audit + commit</name>
  <files>docs/audits/2026-05-18-v2/03-brand-narrative-ia.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append to the doc:

`## User-journey maps (4 personas)` — for each persona walk the most plausible click path, score each step for narrative continuity (does the next page promise pick up where the previous one left off?), CTA visibility, friction.

Personas:
1. Bureau-owner researcher (lands on home, reads about, scans pricing, leaves without applying)
2. EU buyer evaluator (compliance-heavy, lands on legal/privacy → memory → founding-member)
3. GTM bench-marker (compares competitors, lands on case-studies/skinclarity-club → pricing → apply)
4. Returning prospect (lands on home → /apply, abandons, returns via /skills/clyde or /memory)

Each map has 5-7 steps with `narrative_break: yes/no`, `CTA_visible: yes/no`, `proposed fix` per break.

`## Narrative arc audit` — for each of these key pages, score arc: home, about, how-it-works, /skills/clyde, /memory, /case-studies/skinclarity-club, /pricing, /founding-member, /apply, /contact. Use rubric:

- Hook (why care, problem framing)
- Reveal (what FMai does, Clyde introduction)
- Proof (SKC case, metrics, founding-member counter)
- Action (one dominant CTA, clear next step)
- Reassurance (privacy, EU-native, GDPR, founding spot scarcity)

Severity-tag missing or broken arc steps.

`## Top 25 findings` — collect findings from Task 1 + Task 2 into a ranked list (P0 first, then P1, etc.). Reference SOTA markers from 16-01.

`## Executive summary` (top of doc, after Task 2 completes) — one paragraph: P0/P1 counts, top 3 themes, one-line recommendation feeding 16-16.

Update STATE.md and commit:

```bash
git add docs/audits/2026-05-18-v2/03-brand-narrative-ia.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-04 Team 02 brand narrative + information architecture"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/03-brand-narrative-ia.md' -Raw; if (($c -match '## User-journey maps') -and ($c -match '## Narrative arc audit') -and ((Select-String -Path 'docs/audits/2026-05-18-v2/03-brand-narrative-ia.md' -Pattern '^### Finding ').Count -ge 25)) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Doc has 4 journey maps, narrative arc audit, ≥25 ranked findings, executive summary, single commit.</done>
</task>

</tasks>

<verification>
- `03-brand-narrative-ia.md` exists, ≥800 words, 4 journeys, ≥25 findings, glossary scorecard.
- Sitemap and top-nav/footer comparison tables present.
- No edits to `fmai-nextjs/src/` or `messages/`.
- Conventional commit landed.
</verification>

<success_criteria>
The fix-plan in 16-16 can prioritize narrative and IA work without re-reading every page. The journey maps surface meta-friction that single-page audits miss.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-04-SUMMARY.md`
</output>
