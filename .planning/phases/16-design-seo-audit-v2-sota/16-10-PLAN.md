---
phase: 16-design-seo-audit-v2-sota
plan: 10
type: research
wave: 2
depends_on:
  - 16-02
files_modified:
  - docs/audits/2026-05-18-v2/09-cross-browser.md
  - .planning/phases/16-design-seo-audit-v2-sota/STATE.md
  - .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
autonomous: true
requirements:
  - AUDIT-V2-SOTA

must_haves:
  truths:
    - '09-cross-browser.md exists with ≥800 words'
    - 'Doc diffs Chromium vs WebKit vs Firefox screenshots for top 10 routes × desktop + mobile-l'
    - 'Doc identifies engine-specific regressions with severity tags'
    - 'Findings carry full severity schema'
    - 'WebKit-on-Windows skips are documented per AUTONOMOUS-PROTOCOL.md Rule 2'
  artifacts:
    - path: 'docs/audits/2026-05-18-v2/09-cross-browser.md'
      provides: 'Cross-browser compatibility diff matrix'
      contains: 'WebKit'
  key_links:
    - from: '09-cross-browser.md'
      to: 'fmai-nextjs/test-results/audit-v2/screenshots-webkit/ and screenshots-firefox/'
      via: 'side-by-side image references'
      pattern: 'screenshots-'
---

<objective>
Diff Chromium, WebKit, and Firefox screenshots from 16-02 over top 10 routes × desktop + mobile-l. Find engine-specific regressions (font rendering, motion, layout, image decoding).

Purpose: Phase 16 success requires a 3-engine baseline. Without this team's output, the cross-browser column on the executive summary stays empty.
Output: `docs/audits/2026-05-18-v2/09-cross-browser.md` with diff matrix + severity-tagged findings.
</objective>

<execution_context>
@C:/Users/daley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/daley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-design-seo-audit-v2-sota/README.md
@.planning/phases/16-design-seo-audit-v2-sota/PRD.md
@.planning/phases/16-design-seo-audit-v2-sota/AUTONOMOUS-PROTOCOL.md
@.planning/phases/16-design-seo-audit-v2-sota/16-CONTEXT.md
@fmai-nextjs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build diff matrix for top 10 routes × 3 engines</name>
  <files>docs/audits/2026-05-18-v2/09-cross-browser.md</files>
  <action>
For these 10 routes: home, /about, /how-it-works, /pricing, /founding-member, /memory, /apply, /contact, /skills/clyde, /case-studies/skinclarity-club:

Open the three engine-specific screenshot dirs in `fmai-nextjs/test-results/audit-v2/`:
- `screenshots/` (Chromium)
- `screenshots-webkit/`
- `screenshots-firefox/`

For each route × locale (NL primary, EN spot-check) × viewport (desktop + mobile-l), compare the three PNGs visually. Note differences:
- Font rendering (subpixel, weight, kerning)
- Color rendering (P3 vs sRGB, gradient banding)
- Layout (flexbox/grid/CSS Container Query support)
- Motion (Framer Motion, blob animations, scroll-pinned sections)
- Image decoding (next/image, AVIF/WebP fallback)
- Form rendering (date/email inputs, native vs styled)
- Backdrop-filter / mask-image / clip-path (WebKit-specific)

Sections:
1. `## Executive summary` (top): engine-specific regression count by severity, top 3 themes.
2. `## Methodology` + WebKit-on-Windows known issues (cross-ref AUTONOMOUS-PROTOCOL.md Rule 2 soft-warn).
3. `## Diff matrix` table: Route × Viewport × Engine columns with status (match / minor-diff / major-diff / blocker / skipped).
4. `## Engine-specific regressions deep-dive` for each issue found, with image links to all three engine shots.
5. `## Skipped engines + reasons` (if any).

Rules: geen em-dashes, no production-code edits.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'docs/audits/2026-05-18-v2/09-cross-browser.md' -Raw -ErrorAction Stop; if (($c -match '## Diff matrix') -and ($c -match '## Engine-specific regressions')) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>Diff matrix + engine-specific regression deep-dives complete.</done>
</task>

<task type="auto">
  <name>Task 2: Findings + commit</name>
  <files>docs/audits/2026-05-18-v2/09-cross-browser.md, .planning/phases/16-design-seo-audit-v2-sota/STATE.md</files>
  <action>
Append `## Top 15 findings` ranked. Each finding follows standard schema. Severity guide:
- Engine renders unusable content (overlap, hidden buttons): P0
- Engine renders unbranded content (wrong color, missing motion): P1
- Engine renders minor cosmetic deviation (font weight, gradient banding): P2

Append `## Recommended browserlist updates` if any (read-only reference to `fmai-nextjs/package.json`).

Append `## Executive summary` at top.

Update STATE.md, commit:

```bash
git add docs/audits/2026-05-18-v2/09-cross-browser.md .planning/phases/16-design-seo-audit-v2-sota/STATE.md .planning/phases/16-design-seo-audit-v2-sota/BUDGET.log
git commit -m "docs(audit): 16-10 Team 08 cross-browser + device compatibility"
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Select-String -Path 'docs/audits/2026-05-18-v2/09-cross-browser.md' -Pattern '^### Finding ').Count -ge 15) { Write-Host 'PASS' } else { Write-Host 'FAIL' }"</automated>
  </verify>
  <done>≥15 findings, executive summary, single commit.</done>
</task>

</tasks>

<verification>
- `09-cross-browser.md` ≥800 words; diff matrix + regression deep-dives + ≥15 findings.
- Conventional commit landed.
</verification>

<success_criteria>
16-16 roadmap can size cross-browser fixes by leak-cost (e.g. Safari is 20-30 percent of EU mobile traffic).
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-seo-audit-v2-sota/16-10-SUMMARY.md`
</output>
